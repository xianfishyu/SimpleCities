# V4-07 共线重叠验证

2026-09-12，GitHub #8，基线 `f63d180a1c23cbced4a21fbcefc26418345c1237`。核心判定和Godot反馈并行开发，整合后串行运行构建与实机契约。

## 交付范围

- `PlanBuild` 先验证来源和输入，再查找草稿与所有已发布折线段的正长度共线覆盖。冲突以沿本次草稿归一化的不可变区间返回，合并相邻区间；同异类型、正反方向、部分或完全重复均整笔拒绝，无目标计划、ID消耗或内容改变。
- 当前米字主格点约束下，使用最多321项临时轴向格差分数组，复杂度O(已有链点数+草稿格数)；不改变规范道路边存储粒度。未来允许格心输入时需扩展对应位置分辨率，当前不声称已交付格心或路口。
- 预览在后台调用同一PlanBuild，只保留一个worker及一个可替换的最新请求。结果尚未确认时为灰色；可建为绿色；有冲突时仅冲突跨度红色，其余草稿为灰色，提示“与已有道路重叠，整笔不可建造”。按下草稿时清除已有悬停提亮，不修改已有道路mesh或样式。
- 取消、切换地图、加载和来源失效阻止旧预览发布。鼠标松开独立重新规划，不使用预览结果绕过最终校验，也不偷偷建造不重叠的剩余部分。
- 端点接触不属于重叠，可正常续建。几何点交叉、路面宽度交叠不产生重叠区间；当前点交叉可因尚未接入路口而被其他原因拒绝，#9继续交付该功能。schema仍为3，没有格式迁移。

## 核心与构建

- 最小red：重复建造原本返回开放端点/闭环理由，不含重叠判定；新增测试1/1失败。核心实现后RoadOverlapTests 24/24通过。
- 新增公开行为覆盖八方向、四格长和四类profile、倒向/包含覆盖、合并折线内不连续区间、类型边界相邻区间合并、重载、非法输入、取消和来源过期。拒绝前后token、水位与codec字节保持一致。
- `dotnet test SimpleCities.sln --no-restore --verbosity minimal`：核心112/112，应用968/968，无跳过。
- Debug与ExportRelease的solution build均0警告0错误；Roslyn加载5个项目，compiler/analyzer诊断为空。新运行脚本Godot LSP诊断为空。
- 预览工作gate复用现有Debug-only V4OperationWorkProbe；BeforePreviewWork在Debug程序集存在，在ExportRelease不存在。新脚本已从QA导出资源排除。

## 真实Forward+/Vulkan

- Godot 4.7 Mono、Vulkan/Forward+、Dummy音频，`v4_overlap_runtime_contract.gd`。预览入口缺失时先退出1（runtime-red）；最终退出0、明确PASS，stderr为空。
- 四类草稿从(-200,0)到(600,0)覆盖旧路(0,0)到(400,0)：绘制数据均为三段，仅中段红色，原因明确。按住期间以及松开后原state/profile保持不变，重新保存的payload字节一致；快速松开、不等待预览完成的反向重复也拒绝。
- 端点接触预览Ready并成功续建。受控gate验证Pending颜色为中性、连续鼠标变化只应用最终请求，以及换地图后旧预览不再出现；pending_neutral/latest_only/stale_rejected全true。
- `overlap.png`在frame_post_draw后截图并检查：红色只覆盖中间冲突区间，非冲突草稿灰色，灰色背景保持原样。
- 旧单道路和异步操作运行契约在当前实现下均退出0并PASS，stderr为空，证明预览接入未破坏四类建造/重载、八方向、取消门禁、目标绘制计时及退出安全。

## 编辑器与限制

MCP确认V4MapTest并reload场景，实际启动日志通过DAP捕获，编辑器错误为空。一次MCP exec交互读取超时，未把该调用记为通过；完整交互结论来自上述独立Vulkan进程，而非猜测编辑器状态。游戏已停止，原编辑器保留。

## Standards

0条硬违规，0条可行动设计建议。

## Spec

初评与最终日志复核均0条剩余问题。没有将尚未实现的路口交叉误记为本票完成。

## 清理

所有本次启动的独立运行进程均退出，MCP测试游戏停止；临时存档由正常删除入口清理，saves-v4仅剩框架根锁文件。工作gate在worker完成后释放等待资源，未修改V3存档或历史截图。

# 2026-07-20 会话整理：OpenCode 工具链与会话记录 Skill

> 作用域：SimpleCities 项目会话记录
> 存放位置：`docs/session-notes/`
> Git 状态：当前可被 Git 跟踪，尚未暂存或提交

## 会话目标

完善 SimpleCities 的 OpenCode 项目环境，明确 Godot MCP、C# LSP 和项目 Skill 的职责；为 Skill 与 MCP/LSP 编写项目文档；新增会话记录 Skill；并整理 `.opencode/` 与 `docs/session-notes/` 的版本控制策略。

## 已完成

### MCP 与 C# LSP

- 在 `opencode.json` 中保留两个项目本地 MCP：
  - `godot`：`@satelliteoflove/godot-mcp`；
  - `godot-minimal`：固定版本 `@ryanmazzolini/minimal-godot-mcp@0.1.6`。
- Godot MCP addon 版本为 `4.1.0`，项目插件路径为 `addons/godot_mcp/`，编辑器桥接地址为 `127.0.0.1:6550`。
- `godot-minimal` 使用 `GODOT_WORKSPACE_PATH` 绑定当前项目，主要提供 GDScript 诊断和运行中游戏的 DAP console。
- 安装并验证 `csharp-ls 0.26.0`。
- 明确当前 C# 诊断调用链不是 OpenCode 原生 LSP，而是：

  ```text
  Oh My OpenAgent lsp-daemon MCP -> csharp-ls
  ```

- 区分了 OpenCode 原生顶层 `lsp` 配置、名为 `lsp` 的 MCP 服务，以及被禁用的独立 `Lsp*` 工具名。

### 项目 Skill

- 新增 `.opencode/skills/godot-csharp-qa/SKILL.md`：
  - Tier 1：`csharp-ls` 聚焦诊断与 `dotnet build SimpleCities.sln`；
  - Tier 2：增加 Godot 编辑器、场景、资源和项目设置检查；
  - Tier 3：增加真实运行场景、输入、确定性时间、运行态、编辑器日志与 DAP console。
- 新增 `.opencode/skills/session-recorder/SKILL.md`：
  - 用户要求整理、保存或交接会话时触发；
  - 输出到 `docs/session-notes/`；
  - 区分已完成、已验证、未完成、阻塞和既有状态；
  - 不复制完整 transcript、密钥、个人信息或大段原始日志；
  - 不授权 Git 暂存、提交、推送、重置或清理。
- 保留并校准既有项目 Skill：
  - `bugfix-recorder` 按 owning system 写入 `docs/bugfix/<system>.md`；
  - `todo-manager` 按 owning system 维护 `docs/todo/<system>.md`；
  - `git-master` 维护项目 Git 调查与提交规则。

### 工具链文档

- 编写并整理 `docs/opencode-tooling/skills.md`：统一说明项目 Skill、触发条件、职责边界、QA Tier 和组合流程。
- 编写 `docs/opencode-tooling/opencode-mcp-lsp.md`：说明 MCP/LSP 架构、`csharp-ls` 调用链、原生 LSP 与 MCP LSP 的区别，以及新会话排障步骤。
- QA 独立说明已合并到 `skills.md`，避免同一规则维护两份副本。
- 后续文档目录重组将 OpenCode 工具文档从 `docs/manuals/opencode-tooling/` 移到独立的 `docs/opencode-tooling/`。

### Bugfix 与 Todo 文档拆分

- 将 Bugfix 按 owning system 整理为：
  - `docs/bugfix/road-graph.md`；
  - `docs/bugfix/save-system.md`（当时为 `persistence.md`，后续迁移）；
  - `docs/bugfix/grid-rendering.md`；
  - `docs/bugfix/README.md` 负责导航和旧编号映射。
- 将 Todo 按 owning system 整理为：
  - `docs/todo/road-graph.md`；
  - `docs/todo/save-system.md`（当时为 `persistence.md`，后续迁移）；
  - `docs/todo/grid-rendering.md`；
  - `docs/todo/tool-input.md`；
  - `docs/todo/traffic-simulation.md`；
  - `docs/todo/README.md` 负责导航和旧编号映射。
- 迁移覆盖 10 个历史 Bug 和 86 条 Todo 源记录。
- 保留 45 个 Todo 状态：29 个活动项、10 个延期项、6 个已完成基线。
- 87 个目标引用完成解析和去重检查。
- 旧 Todo `5.3` 当时拆为 `persistence:5.3` 与 `grid-rendering:5.3`，由 `persistence` 负责集成验收；后续命名迁移已将其统一改为 `save-system:5.3`。
- 迁移前的字节级备份、SHA-256 清单、计划和验证证据保存在：

  ```text
  .omo/backups/system-doc-split/
  .omo/plans/split-system-docs.md
  .omo/evidence/split-system-docs/
  ```

### `.opencode/` 版本控制策略

- 移除根 `.gitignore` 中过宽的 `.opencode/` 忽略规则。
- 根 `.gitignore` 现在只针对安装产物忽略 `.opencode/node_modules/`。
- `.opencode/.gitignore` 保留：

  ```gitignore
  node_modules/
  bun.lock
  ```

- 以下内容现在允许 Git 跟踪：
  - `.opencode/skills/`；
  - `.opencode/package.json`；
  - `.opencode/package-lock.json`；
  - `.opencode/.gitignore`。
- `package.json` 声明 OpenCode 插件环境依赖；`package-lock.json` 锁定直接和传递依赖；`node_modules/` 可由 lockfile 重建，因此不跟踪。

### 会话记录目录

- 会话记录从 `.omo/session-notes/` 迁移到 `docs/session-notes/`。
- 当前最新决策允许 `docs/session-notes/` 进入版本控制；根 `.gitignore` 不再忽略该目录。
- 已有记录：
  - `docs/session-notes/2026-07-20-文档目录重组.md`；
  - 本文件。

### 历史提交

本会话早期曾完成以下提交：

- `c3b253a`：Godot MCP 集成；
- `15e4582`：更新 AI 指南至 RoadGraph 当前架构；
- `3caebec`：补记 RoadType 存档和重复铺路修复；
- `dec56e3`：将道路 Todo 清单迁入 `docs/todo`。

本会话后续的 Skill、工具链文档、会话记录和版本控制规则尚未提交。

## 已验证

- 对 `Scripts/Road/RoadGraph.cs` 执行真实 `lsp_diagnostics`，返回 Roslyn `CS8019` 未使用 using 警告，证明 `csharp-ls` 已加载项目并响应。
- LSP 状态曾显示 `csharp: alive`，项目根目录解析正确。
- `opencode debug config` 曾显示启用的 Oh My OpenAgent `lsp-daemon` MCP，并成功发现 `session-recorder` Skill。
- `opencode debug config` 的最终合并配置证明 `opencode.json` 只声明项目本地 Godot MCP；LSP MCP 来自会话合并配置。
- Skill 与 MCP/LSP 文档的相对 Markdown 链接和关键术语检查通过。
- Bugfix/Todo 迁移检查覆盖编号、状态、目标引用、Markdown 链接与锚点；迁移 Oracle 最终判定 PASS。
- 迁移源文档与 `.omo/backups/system-doc-split/` 中备份的 SHA-256 一致。
- `.opencode/node_modules/` 当前被忽略；Skill、`package.json`、`package-lock.json` 和 `.opencode/.gitignore` 当前不被忽略。
- 当前 `git check-ignore` 不匹配 `docs/session-notes/`，会话记录可以进入 Git。
- 本次存档前对相关配置和文档执行 `git diff --check`，没有发现空白错误。
- 本轮主要是 OpenCode 配置和文档工作，没有运行 `dotnet build` 或 Godot 场景 QA；不能据此声明当前游戏构建或运行时行为已验证。

## 重要决策

- 项目 Skill 属于团队开发规则，应随仓库分发，不应因忽略整个 `.opencode/` 而只在当前机器生效。
- `.opencode/node_modules/` 是生成的安装产物，不进入 Git；依赖清单和 lockfile进入 Git以支持可重建环境。
- `csharp-ls` 不是项目 MCP，也不是 QA Skill 本身；它是由会话中的 `lsp-daemon` MCP 启动的语言服务器，QA Skill只负责规定何时调用它。
- Skill 的执行源文件保留在 `.opencode/skills/<name>/SKILL.md`，人类可读说明集中在 `docs/opencode-tooling/skills.md`。
- `docs/session-notes/` 当前采用可跟踪策略；这覆盖了会话早期“保持 Git 忽略”的旧方案。
- 会话记录只保存连续性上下文，不替代 Bugfix、Todo、设计文档或源码事实。

## 未完成与阻塞

- 当前会话的 Skill 列表在 `session-recorder` 创建前已加载，因此本轮调用 `skill("session-recorder")` 返回未找到；磁盘 Skill 和最终配置均已存在，新 OpenCode 会话应重新发现它。
- 没有执行 Git 暂存、提交或推送。
- `.opencode` 新文件、OpenCode 工具文档和会话记录仍处于未跟踪状态，需在用户明确要求提交时选择性暂存。
- `docs/opencode-tooling/skills.md` 中仍存在两处过时措辞：一处要求确认 `.omo/` 被忽略，另一处声称 `.opencode/` 整体被忽略；它们与当前实际策略不一致，后续应校准。
- 本文件已合并并取代 7 月 19 日的阶段性 MCP/Skill 记录，早期“会话记录被 Git 忽略”的方案只作为历史决策保留，不再是当前策略。

## 既有工作区状态

- 工作树还包含本轮 OpenCode 工具工作以外的 C#、Godot 文档、Todo、`opencode.json` 和 `saves/` 改动。
- 文档重组在 Git 状态中表现为旧路径删除与新路径未跟踪；暂存后 Git 才可能识别为重命名。
- 本轮会话记录没有回退、清理或归因这些既有修改。

## 相关文件

- `opencode.json`：项目本地 `godot` 与 `godot-minimal` MCP 配置。
- `.opencode/skills/godot-csharp-qa/SKILL.md`：Godot C# QA 权威规则。
- `.opencode/skills/session-recorder/SKILL.md`：会话记录权威规则。
- `.opencode/package.json`：OpenCode 插件环境直接依赖。
- `.opencode/package-lock.json`：OpenCode 插件环境锁定依赖图。
- `.opencode/.gitignore`：`.opencode` 内生成文件忽略规则。
- `docs/opencode-tooling/skills.md`：项目 Skill 人类可读说明。
- `docs/opencode-tooling/opencode-mcp-lsp.md`：MCP/LSP 架构与排障说明。
- `docs/session-notes/`：项目会话连续性记录。

## 后续建议

1. 新开 OpenCode 会话，确认 `session-recorder` 出现在可用 Skill 列表，并执行一次最小记录测试。
2. 校准 `docs/opencode-tooling/skills.md` 中两处过时的 `.omo/` 和 `.opencode/` 忽略说明。
3. 提交前审查完整 Git diff，选择性纳入 `.opencode` 规则、工具链文档、会话记录和 `.gitignore`，排除无关源码、存档与用户既有改动。

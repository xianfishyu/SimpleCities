# 2026-07-20 会话整理：待办计划中文化

> 作用域：SimpleCities 项目本地会话记录
> 存放位置：`docs/session-notes/`
> Git 状态：`docs/session-notes/` 未被忽略，可由 Git 跟踪；本次未执行暂存或提交。

## 会话目标

将项目的持久化待办管理规则和 `docs/todo/` 系统路线图中面向人的英文内容统一为简体中文，同时保持机器标识、代码术语和引用关系不变。

## 已完成

- 将 `.opencode/skills/todo-manager/SKILL.md` 的说明和规则正文翻译为中文。
- 在 `todo-manager` 写作规则中明确要求：面向人的标题、说明、状态、原因和验收内容必须使用中文；代码标识符、系统 key、路径和外部专有名称保持准确原文。
- 将 `docs/todo/README.md` 的标题、元数据、章节、表头、组合说明、备注和总体依赖说明翻译为中文。
- 将以下系统待办文档的面向人内容统一为中文：
  - `docs/todo/road-graph.md`
- `docs/todo/save-system.md`
  - `docs/todo/grid-rendering.md`
  - `docs/todo/tool-input.md`
  - `docs/todo/traffic-simulation.md`
- 保留了系统 key、todo ID、来源 key 的值、HTML anchor、链接目标、文件路径、代码/API 标识符、复选框状态、验收要求和既有日期。

## 已验证

- `git diff --check -- "docs/todo" ".opencode/skills/todo-manager/SKILL.md"`：通过，未发现空白或补丁格式问题。
- 对 `docs/todo/*.md` 检索原有英文结构标签：未发现 `Status Summary`、`Execution Order`、`Source key`、`Related refs`、`Product Phase` 等残留的面向人英文结构文本。
- 复核了六份 `docs/todo/` Markdown 中的 HTML anchor 和系统引用；稳定 ID、链接目标与复选框状态未作语义性修改。
- 本次仅修改文档和 Skill，不涉及 C#、Godot 场景、资源、项目设置或运行时行为，因此未执行 `dotnet build` 或 Godot 运行测试。

## 重要决策

- 中文化只针对面向人的内容；`RoadGraph`、`RoadType`、`SaveManager`、`TrafficGraph` 等代码名不翻译。
- 本次中文化发生时，`road-graph`、`persistence`、`grid-rendering`、`tool-input` 和 `traffic-simulation` 等系统 key 保持不变，以免破坏跨文档引用；后续命名迁移已将 `persistence` 统一改为 `save-system`。
- `summary_table_row`、`todo_checkbox_heading` 等映射类型值保持不变，因为它们属于稳定机器数据，而不是展示文案。
- 本记录只存档本次会话的修改事实，不改变 `docs/todo/` 中任何工作项的开放、延期或完成状态。

## 未完成与阻塞

- 未创建 Git 提交；如果需要形成仓库历史记录，必须由用户另行明确授权。
- 未检查 `docs/todo/` 之外的项目文档是否仍包含同类英文结构，因为本次范围仅为待办管理 Skill 和系统待办目录。

## 相关文件

- `.opencode/skills/todo-manager/SKILL.md`：系统待办的管理规则；存档系统现使用 `save-system` key。
- `docs/todo/README.md`：系统待办导航和遗留来源映射。
- `docs/todo/road-graph.md`：路网图系统路线图。
- `docs/todo/save-system.md`：存档系统路线图。
- `docs/todo/grid-rendering.md`：网格渲染系统路线图。
- `docs/todo/tool-input.md`：工具输入系统路线图。
- `docs/todo/traffic-simulation.md`：交通模拟系统路线图。

## 后续建议

1. 后续维护 `docs/todo/` 时继续遵循中文展示内容与稳定英文技术标识分离的规则。
2. 如需提交本次文档修改，提交前再次复核工作树，确保只暂存本记录所列文件。

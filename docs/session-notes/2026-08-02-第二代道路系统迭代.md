# 2026-08-02 会话整理：第二代道路系统迭代

> 作用域：SimpleCities 项目本地会话记录
> 存放位置：`docs/session-notes/`
> Git 状态：本记录与所述实现、测试及规范文档在同一提交中跟踪

## 会话目标

阅读 `docs/manuals/road-system-v2-gen.md`，以后附录 D 的第二代最终范围为准，按 `docs/todo/road-graph.md` 的执行顺序逐步实现、验证、记录并提交 RoadGraph 工作。

## 已完成

- 完成 `road-graph:0.6`：强化正交/对角交叉、waypoint 拆分、单边删除和整组删除回归，显式校验节点邻接、Group 归属、空间命中和删除不创建替代 Edge。
- 完成 `road-graph:0.7`：`FindClosestNode` 与 `GetOrCreateNode` 共用 `FindClosestIndexedNode`；节点身份半径固定为包含边界的 `0.5f`，多候选选择最近节点，近似等距时选择较小 Node ID。
- 完成 `road-graph:1.1`：补充相同 RoadType、不同 RoadGroup 的隔离测试，确认独立玩家提交不会因共线而合并；RoadType 移除后的复核归属 `road-graph:2.7`。
- 完成 `road-graph:1.3`：使用统一距离平方 epsilon 排除近似共享端点，避免伪内部交叉拆除并重建既有 Edge，同时保留真实四连接交叉。
- 完成 `road-graph:2.2`：删除未调用且依赖 `DirectionUtil` 的 `RoadGraph.IsPathValid`，使 RoadGraph 数据层不再引用方向枚举、输入网格或 `CellSize`。
- 完成 `road-graph:2.1`：新增 `SubmitPolyline`、`RoadPathSubmissionResult` 和结构化错误枚举，在写图前拒绝非有限坐标、几何退化、节点身份塌缩、重复点和自交/折返；旧 `AddRoad` 复用同一链路。
- 推进 `road-graph:2.4` 折线基础：公共结构化折线入口已建立；原生曲线写入与未知几何类型拒绝等待后续集成。
- 推进 `road-graph:2.4` 提交契约：新增不可变 `RoadPath` 原生段请求快照；扩展提交结果为创建/删除 Node、Edge、Group 的确定排序摘要，现有折线交叉拆分和合并也返回完整实体差异。
- 推进 `road-graph:2.5`（2026-08-03）：新增原生几何段统一参数域、查询和拆分契约，并完成直线段实现；曲线族、序列化及 GraphEdge 集成仍保持开放。
- 推进 `road-graph:2.5` cubic Bézier：原生保留四个控制点，完成解析位置/切线、精确极值包围盒、误差受控弧长和 De Casteljau 拆分。
- 推进 `road-graph:2.5` 几何序列化：新增带显式版本和稳定类型判别的纯数据契约，完成直线与 cubic Bézier 的 JSON/DTO 往返，并在构造运行时对象前结构化拒绝畸形或不一致数据。
- 推进 `road-graph:2.5` GraphEdge 集成：GraphEdge 改以只读连续原生几何段作为长度和中间锚点的事实来源；现有折线写入、合并、拆分及旧 waypoint 恢复统一转换为 line 段，直接持有 cubic Bézier 时保留控制参数。
- 推进 `road-graph:2.5` 样条：新增 cubic Hermite 原生几何段，保留端点与两端导数向量，完成查询、包围盒、长度、Hermite 类型拆分和版本化序列化。
- 推进 `road-graph:2.5` 圆弧：新增以圆心、半径、起始角和有符号扫角表达的原生圆弧，完成解析长度、方向切线、轴向极值包围盒、同类型拆分和版本化序列化。
- 推进 `road-graph:2.5` 回旋线：新增弧长参数化 clothoid，保存起点、起始航向、起终曲率和弧长，完成线性曲率、误差受控位置积分、保守包围盒、同类型拆分和版本化序列化。
- 推进 `road-graph:2.5` 一般圆锥曲线：新增正权重 rational quadratic Bézier，保存三个控制点和三个齐次权重，完成有理位置/切线、解析极值包围盒、误差受控长度、齐次拆分和版本化序列化。
- 完成 `save-system:0.5`：RoadGraph 恢复改为在临时 Node/Edge/Group 字典中全量校验，只有 schema、全局 ID、引用、成员关系、原生几何、端点和 `nextID` 全部有效时才一次提交；失败保持活动图和 ID 分配状态不变。
- 完成 `save-system:5.3`：新道路 JSON 使用 `schemaVersion`、`nodes`、`edges`、`groups` 及对应 ID 字段，直接保存六类原生几何段的类型和控制参数，不迁移旧 Junction/Segment/Road payload。
- 推进 `save-system:0.3`、`0.4`：新道路 schema 已移除 RoadType、旧类型回退和旧版本兼容，并精确拒绝缺失/旧版/未来版/错误类型的 RoadGraph 版本；RoadType 运行时契约和 manifest 版本校验仍待后续完成。
- 推进 `road-graph:2.4` 原生提交：新增 `SubmitPath(RoadPath)`，六类原生几何和连续复合路径可从非 UI 调用方直接写入 GraphEdge；非法请求在任何写图前返回结构化错误，成功返回完整实体变更摘要并发出一致的 Edge 事件。
- 原生路径端点解析支持近节点复用：line、cubic Bézier、cubic Hermite 和 rational quadratic 按类型重建端点/控制参数；circular arc 与 clothoid 只接受保持原生参数语义的整体平移，其他情况以 `UnsupportedEndpointSnap` 无副作用拒绝。
- 推进 `road-graph:2.6` 最近点基础：`RoadGeometrySegment` 新增统一最近点结果与查询 API，line、circular arc 使用解析解，其余四类曲线使用原生包围盒和同类型拆分的分支限界搜索。
- 完成 `road-graph:1.2`：`FindClosestEdge` 只由空间索引收集候选，最终逐 Edge 的全部原生几何段执行权威最近点查询；等距稳定选择较小 Edge ID，不再以端点、waypoint 或显示采样决定结果。
- 推进 `road-graph:3.2` 原生几何空间占据：每个 `RoadGeometrySegment` 以保守 `Bounds` 覆盖全部 bucket，圆查询使用原生最近点过滤；恢复可重建、删除可完整清理曲线引用。
- 推进 `road-graph:2.6` 点上判定：`TryFindPointOnGeometry` 在显式距离容差内返回权威参数、位置、距离和端点/内部分类，为后续按曲线参数拆分提供统一输入。
- 推进 `road-graph:2.6` 直线交点：直线-直线使用解析参数并区分交叉、端点接触和重叠；直线与其余原生曲线使用包围盒剪枝和同类型细分，返回双方稳定参数及交叉/相切分类。
- 更新 `docs/todo/road-graph.md` 的状态总览、完成证据与后续责任边界。
- 将节点身份和近共享端点缺陷分别记录为 `docs/bugfix/road-graph.md` 中的 `road-graph:BUG-15`、`road-graph:BUG-16`。
- 将曲线内部相切被重复报告的缺陷记录为 `docs/bugfix/road-graph.md` 中的 `road-graph:BUG-17`。
- 将损坏 RoadGraph 存档在校验前清空活动图的缺陷记录为 `docs/bugfix/save-system.md` 中的 `save-system:BUG-3`。

## 已验证

- `road-graph:0.6`：`RoadGraphRegressionTests` 聚焦测试 17/17 通过；当时解决方案测试 52/52 通过。
- `road-graph:0.7`：新增测试在修复前 7 项中 3 项失败；修复后 `RoadGraphNodeIdentityTests` 7/7、当时解决方案测试 59/59 通过。
- `road-graph:1.1`：`RoadGraphRegressionTests` 18/18 通过；最终 `dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore` 为 60 通过、0 失败、0 跳过。
- `road-graph:1.3`：修复前聚焦回归 21 项中 1 项失败；修复后 21/21 通过，解决方案测试 63/63 通过。
- `road-graph:2.2`：`RoadGraphContinuousSpaceTests` 3/3 通过；RoadGraph 禁用依赖扫描无命中。
- `road-graph:2.1`：`RoadGraphPathSubmissionTests` 19/19 通过，覆盖结构化失败、完整状态原子性、Edge 事件静默、任意合法折线和旧 API 兼容。
- `road-graph:2.4` 变更摘要：路径提交与拓扑聚焦测试 47/47 通过，覆盖请求防御性复制、成功创建实体、交叉替换 Edge、排序稳定性及失败空摘要。
- `road-graph:2.5` 直线几何基础：`RoadGeometrySegmentTests` 14/14 通过，覆盖有限构造、参数域、位置、单位切线、包围盒和拆分重组。
- `road-graph:2.5` cubic Bézier：`RoadGeometrySegmentTests` 24/24 通过，新增控制点语义、导数切线、线性/二次导数极值包围盒、直线退化、拆分参数映射和弧长重组覆盖。
- `road-graph:2.5` 几何序列化：`RoadGeometrySerializationTests` 17/17 通过；与几何段测试合并聚焦为 41/41，覆盖 line/cubic Bézier 语义往返和所有结构化拒绝类别。
- `road-graph:2.5` GraphEdge 集成：`GraphEdgeGeometryTests` 5/5 通过；连同拓扑与覆盖回归的聚焦测试 29/29 通过。
- `road-graph:2.5` cubic Hermite：相关几何与序列化聚焦测试 56/56 通过，覆盖原生参数、Hermite 基函数位置、解析极值包围盒、端点切线、拆分参数映射、无效数据拒绝和 JSON 往返。
- `road-graph:2.5` 圆弧：全部几何聚焦测试 76/76 通过，覆盖正反扫角、整圆、轴向极值、参数域、拆分重组、无效半径/扫角和 JSON 往返。
- `road-graph:2.5` clothoid：全部几何聚焦测试 89/89 通过，覆盖线性曲率、直线/常曲率解析退化、长距离微小曲率变化、拆分参数映射、参数拒绝和 JSON 往返。
- `road-graph:2.5` rational quadratic：全部几何聚焦测试 103/103 通过，覆盖四分之一圆、普通二次退化、权重整体缩放不变性、齐次拆分、无效权重和 JSON 往返。
- V2 道路存档聚焦测试：`RoadGraphPersistenceV2Tests`、`RoadGraphNodeIdentityTests`、`GraphEdgeGeometryTests` 合计 24/24 通过，覆盖新字段、六类原生几何双重往返、版本拒绝、全局 ID、悬空引用、Group 成员关系、非法几何、连续性、端点、非有限数值、孤立节点、`nextID` 和失败状态原子性。
- `godot --headless --path . --log-file .godot/qa-roadgraph-v2-persistence.log --script tests/godot/pause_menu_runtime_contract.gd`：授权 `user://` 写入后输出 `PASS pause menu runtime contract`，主场景完成两轮 autosave 保存加载；两条未挂载 `ToolManager` 的 HUD 警告属于契约脚本既有隔离场景。
- `dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：192 通过、0 失败、0 跳过。
- `dotnet test SimpleCities.sln --configuration Debug --no-restore`：本批最终 204 通过、0 失败、0 跳过。
- `RoadGraphNativePathSubmissionTests`：12/12 通过，覆盖六类原生几何、复合路径、近节点吸附、未知类型、断裂/塌缩/重复锚点、吸附拒绝、重复覆盖、曲线与弦区分、变更摘要、事件和存档往返。
- 原生提交批次路径聚焦测试：34/34 通过；`dotnet test SimpleCities.sln --configuration Debug --no-restore` 更新为 216 通过、0 失败、0 跳过。
- 原生提交批次 `dotnet build SimpleCities.sln --configuration Debug --no-restore`：顺序重跑为 0 警告、0 错误；并行测试/构建曾产生一次 DLL 文件占用重试警告，不属于源码告警。
- 原生提交批次 Godot 暂停菜单运行时契约：输出 `PASS pause menu runtime contract`，主场景两轮 autosave 保存加载通过。
- `RoadGeometryClosestPointTests`：13/13 通过，覆盖 line 投影/端点、圆弧径向投影/扫角端点/等距决胜、四类一般曲线的曲线上收敛、曲线中段与非法参数。
- 最近点批次 `dotnet build SimpleCities.sln --configuration Debug --no-restore`：0 警告、0 错误；`dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：229 通过、0 失败、0 跳过。
- `RoadGraphCurveSpatialQueryTests`：12/12 通过，覆盖六类原生几何中段、高曲率 Bézier 远离端点弦、真实最近曲线、包含半径边界且拒绝半径外、等距 Edge ID 决胜、存档恢复重建和删除清理。
- 曲线空间查询批次 `dotnet build SimpleCities.sln --configuration Debug --no-restore`：0 警告、0 错误；`dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：241 通过、0 失败、0 跳过。
- 曲线空间查询批次 Godot 暂停菜单运行时契约：授权 `user://` 写入后输出 `PASS pause menu runtime contract`，主场景完成两轮 autosave 保存加载；两条未挂载 `ToolManager` 的警告属于既有隔离场景。
- `RoadGeometryPointLocationTests`：15/15 通过，覆盖六类原生几何内部点、起点/内部/终点分类、包含距离边界、端点参数容差和非法参数拒绝。
- 点上判定批次 `dotnet build SimpleCities.sln --configuration Debug --no-restore`：0 警告、0 错误；`dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：256 通过、0 失败、0 跳过。
- `RoadGeometryLineIntersectionTests`：15/15 通过，覆盖解析直线交叉、端点接触、重叠、平行分离、Bézier 多交点/内部相切、圆弧双交点、一般曲线已知参数交点及非法容差拒绝。
- 直线交点批次 `dotnet build SimpleCities.sln --configuration Debug --no-restore`：0 警告、0 错误；`dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：271 通过、0 失败、0 跳过。
- `dotnet build SimpleCities.sln --configuration Debug --no-restore`：修改前与修改后均为 0 警告、0 错误。
- `godot --headless --editor --path . --quit`：退出码 0 并为新 C# 文件生成 UID；同时出现 imgui GDExtension 临时 DLL、系统证书、MCP 端口占用和编辑器设置写入错误，因此不作为编辑器门禁通过证据。扫描误删的已跟踪临时 DLL 已用同哈希原始 DLL 恢复，未留下该副作用。
- `git diff --check`：本批提交前通过。

## 重要决策

- 第二代范围以后附录 D 为权威；指南正文末尾的 Phase 2 分区系统不是当前 RoadGraph V2 的下一步。
- `SnapRadius = 0.5f` 是连续空间数据层的节点身份容差，不与 `CellSize` 或 UI 网格吸附绑定。
- `GeometryEpsilon = 1e-4f` 在位置比较中作为距离平方阈值；交点端点判定不再使用 `Vector2 ==`，且不与节点身份半径混用。
- RoadGroup 表示一次用户提交，是自动合并的硬边界；RoadType 不作为第二代最终契约，移除工作由 `road-graph:2.7` 承担。
- `road-graph:2.2` 只负责解除 RoadGraph 对方向和网格概念的依赖；折线路径的结构化校验由 `2.1` 完成，原生曲线提交与完整结果契约继续由 `2.4` 验收。
- `SubmitPolyline` 是 `2.4` 的折线兼容入口，`SubmitPath` 是原生几何入口；两者共享最终 GraphEdge 写入方法。`2.4` 在曲线与既有道路的交叉、重叠和拆分事件验收完成前保持开放。
- `RoadGraphChangeSummary` 以提交前后 Node、Edge、Group 权威 ID 集合差异生成，不尝试从事件顺序推断结果；因此交叉拆边、合并和实体替换都能稳定报告。
- 原生几何段统一采用 `[0, 1]` 参数域；显示采样不进入 `RoadGeometrySegment` 权威契约，未来渲染采样只能作为派生数据。
- cubic Bézier 弧长使用曲线弦长下界与控制多边形上界递归收敛，不依赖显示采样；包围盒使用导数内部极值而非控制点包围盒。
- 原生几何序列化以 `version = 1` 和区分大小写的稳定字符串 `line` / `cubicBezier` 判别类型；当前版本拒绝未知字段和类型专属字段混用，后续格式演进必须提升版本并显式映射。
- GraphEdge 的 `GeometrySegments` 是运行时权威几何；`Points` 暂作为现有渲染和折线拓扑算法的防御性兼容视图，仅表示段边界，不得成为曲线真相。
- 本批不向旧 `SegmentData` schema 添加临时曲线字段；新 V2 Node/Edge/Group 命名、严格版本拒绝、完整校验和曲线参数存档由 `save-system:0.4`、`0.5`、`5.3` 一次完成。
- 附录 D 未限定样条族；第二代采用 cubic Hermite 作为明确的原生样条语义，以端点导数表达连续性。其与 cubic Bézier 解析等价，但使用独立 `cubicHermite` 判别并在拆分后保留 Hermite 参数。
- 单个原生圆弧的有符号扫角限制在一周以内；正负号保留逆时针/顺时针语义，较长复合路径由多个连续圆弧段表达。
- clothoid 以弧长为权威长度，曲率在起终值之间线性变化；一般位置通过自适应 Simpson 积分获得，保守包围盒由路径弧长严格限定而非显示采样。空间索引阶段可收紧包围范围，但不得改变这些原生参数。
- 一般圆锥曲线采用三个正权重的 rational quadratic Bézier 表达；三组齐次权重全部保留，使任意参数拆分后仍属于同一类型，而不是强制把子段端点权重归一化后丢失参数语义。
- 新 RoadGraph 道路 schema 自身版本从 `schemaVersion = 1` 起步，字段名严格区分大小写；旧 `version = 2` 表示旧私有 payload，不继承为新 schema 的版本号，也不提供迁移。
- RoadGraph 恢复以“完整临时状态 -> 一次提交”为事务边界；损坏 payload 通过 `JsonException` 返回诊断，失败时不清空图、不改变 `_nextID`、不发出 `GraphCleared`。
- RoadType 仍暂留运行时类以避免把 `road-graph:2.7` 混入本批，但不再是第二代道路 JSON 的字段或兼容回退来源；临时构造值不具备持久化语义。
- `RoadPath` 的每个原生几何段当前形成一条 GraphEdge，相邻段共享 GraphNode；这保留控制参数并为后续按曲线参数拆分提供明确拓扑边界。
- 原生路径覆盖判定不能复用把 Edge 端点连成折线的旧 `IsPathCovered`：line 只由现有 line 几何覆盖，曲线按版本化类型与参数精确比较，避免把曲线弦误判为已有道路。
- 一般曲线最近点不采用固定显示采样：搜索队列以每个原生子曲线 `Bounds` 到查询点的距离作为严格下界，以当前最佳距离与最小未探索下界的差值作为停止条件；返回参数始终映射回原段 `[0, 1]`。
- 空间索引保存的是可重建的 `EdgeGeometryRef` 候选服务：当前以每个原生段的保守包围盒覆盖 bucket，以权威最近点执行圆相交过滤；整段包围盒带来的候选放大留给 `road-graph:3.1`～`3.3` 测量和优化，不以显示采样换取较小候选集。
- 点上判定将空间命中容差与端点参数分类容差分开：前者决定查询点是否属于几何，后者只决定命中应作为起点、内部还是终点处理；非法坐标和非有限/越界容差直接拒绝。
- 直线-曲线交点不把固定采样作为权威：子曲线 `Bounds` 对直线的法向区间和切向投影区间提供剪枝，收敛叶节点映射回原曲线参数；相切点附近的连续容差命中按参数簇归并并保留残差最小候选。
- 本对话只维护本文件作为会话档案，不新增并行的会话记录。

## 未完成与阻塞

- `road-graph:2.4` 的原生曲线校验、无交互写入、未知类型拒绝、变更摘要和事件一致性已完成；曲线与既有道路的交叉、重叠和拆分仍依赖 `road-graph:2.6`，因此本项保持开放。
- `road-graph:2.5` 要求的运行时几何族、独立序列化映射和新 V2 GraphEdge 捕获/恢复已完成；仍缺 `road-graph:2.6` 对原生曲线的交叉、拆分和重建集成，因此 2.5 暂不关闭。
- `road-graph:2.6` 已有统一最近点、点上判定、直线-直线/曲线交点并接入 `RoadGraph.FindClosestEdge`，仍缺曲线-曲线交点、曲线重叠、按交点参数拆分和 RoadGraph 写入链集成。
- `save-system:0.3` 仍等待 `road-graph:2.7` 从图实体和兼容 API 移除 RoadType；`save-system:0.4` 仍缺 manifest 精确版本拒绝，`save-system:0.11` 仍缺整槽预检。
- `road-graph:3.2` 已完成六类原生几何的保守空间占据、查询、重建与删除功能；不同 bucket size 的曲线候选规模及 10k/100k 性能尚未测量，因此保持开放。
- 当前会话未提供 `csharp-ls` MCP，无法执行逐文件 C# LSP 诊断；编译器、xUnit 分析器和解决方案测试均已通过。
- Godot headless 扫描存在 imgui GDExtension、系统证书、端口占用和编辑器设置写入错误；本轮不涉及场景或资源集成，未将该额外扫描视为 Tier 2 通过。
- 本批改动属于 RoadGraph 运行时查询与存档恢复后的派生索引行为，按 Tier 3 执行完整自动化及 Godot 主场景存档运行时契约。

## 相关文件

- `Scripts/Road/RoadGraph.cs`：RoadGraph 权威写入链、折线路径前置校验、节点身份和 Group 边界。
- `Scripts/Road/SpatialIndex.cs`：原生几何空间引用、包围盒 bucket 占据和圆查询过滤。
- `Scripts/Road/RoadGraph.Persistence.cs`：严格 V2 Node/Edge/Group DTO、原生几何捕获、全量预检与事务式恢复。
- `Scripts/Road/RoadGraph.PathSubmission.cs`：原生 RoadPath 校验、端点解析、覆盖判定和权威 Edge 写入。
- `Scripts/Road/GraphEdge.cs`：只读连续原生几何段、派生长度和折线兼容视图。
- `Scripts/Road/RoadPathSubmissionResult.cs`：路径提交状态、完整变更摘要和结构化失败原因。
- `Scripts/Road/RoadPath.cs`：不可变原生几何段请求快照。
- `Scripts/Road/Geometry/RoadGeometrySegment.cs`：原生几何类型、统一参数域和拆分契约。
- `Scripts/Road/Geometry/LineRoadGeometrySegment.cs`：权威直线几何实现。
- `Scripts/Road/Geometry/CubicBezierRoadGeometrySegment.cs`：权威 cubic Bézier 控制点和几何查询实现。
- `Scripts/Road/Geometry/CubicHermiteRoadGeometrySegment.cs`：端点导数样条及保留类型的解析拆分。
- `Scripts/Road/Geometry/CircularArcRoadGeometrySegment.cs`：有符号圆弧参数、解析查询和同类型拆分。
- `Scripts/Road/Geometry/ClothoidRoadGeometrySegment.cs`：弧长参数化线性曲率缓和曲线和误差受控积分。
- `Scripts/Road/Geometry/RationalQuadraticRoadGeometrySegment.cs`：正权重一般圆锥曲线、解析极值和齐次拆分。
- `Scripts/Road/Geometry/RoadGeometrySerialization.cs`：原生几何版本化 DTO、JSON 映射和结构化拒绝结果。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphRegressionTests.cs`：拓扑、删除和 Group 边界回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphNodeIdentityTests.cs`：节点吸附半径、多候选和存档恢复回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphContinuousSpaceTests.cs`：任意 R² 折线路径和数据层分层契约。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphPathSubmissionTests.cs`：结构化路径校验、失败原子性和兼容入口回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadPathSubmissionChangeSummaryTests.cs`：请求不可变性、实体差异和失败空摘要回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGeometrySegmentTests.cs`：直线几何查询、校验和无损拆分契约。
- `tests/SimpleCities.RoadGraph.Tests/RoadGeometrySerializationTests.cs`：直线/cubic Bézier 语义往返及无部分对象失败契约。
- `tests/SimpleCities.RoadGraph.Tests/GraphEdgeGeometryTests.cs`：GraphEdge line 段转换、cubic 控制参数保留、只读边界和恢复回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphPersistenceV2Tests.cs`：schema 字段、六类原生几何往返、损坏内容拒绝和失败原子性回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphNativePathSubmissionTests.cs`：六类原生路径提交、吸附、失败原子性、事件、覆盖语义和存档联动回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGeometryClosestPointTests.cs`：六类几何最近点解析/数值收敛、端点和非法参数回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGeometryPointLocationTests.cs`：六类几何点上判定、端点分类和容差回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGeometryLineIntersectionTests.cs`：直线与原生几何的交叉、接触、相切、重叠和参数回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphCurveSpatialQueryTests.cs`：六类原生几何的最近 Edge、空间索引重建和删除回归。
- `tests/SimpleCities.RoadGraph.Tests/CubicHermiteRoadGeometrySegmentTests.cs`：Hermite 参数、查询、拆分、拒绝和序列化回归。
- `tests/SimpleCities.RoadGraph.Tests/CircularArcRoadGeometrySegmentTests.cs`：圆弧方向、整圆、包围盒、参数域、拆分和序列化回归。
- `tests/SimpleCities.RoadGraph.Tests/ClothoidRoadGeometrySegmentTests.cs`：曲率、退化解析、拆分、积分边界和序列化回归。
- `tests/SimpleCities.RoadGraph.Tests/RationalQuadraticRoadGeometrySegmentTests.cs`：圆锥曲线、权重不变性、极值、拆分和序列化回归。
- `docs/todo/road-graph.md`：第二代 RoadGraph 执行顺序和完成证据。
- `docs/bugfix/road-graph.md`：`road-graph:BUG-15`～`road-graph:BUG-17` 的规范修复记录。
- `docs/bugfix/save-system.md`：`save-system:BUG-3` 的事务式恢复修复记录。
- `docs/todo/save-system.md`：第二代道路存档 schema、版本策略和后续槽位工作。

## 后续建议

1. 实现 `road-graph:2.6` 的直线-曲线与曲线-曲线交点参数求解，并区分端点接触、内部交叉、相切和重叠。
2. 使用稳定交点参数拆分原生子曲线，再接入 `SubmitPath` 的既有道路交叉处理。

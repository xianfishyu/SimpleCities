# 2026-08-02 会话整理：第二代道路系统迭代

> 作用域：SimpleCities 项目本地会话记录
> 存放位置：`docs/session-notes/`
> Git 状态：本记录与所述实现、测试及规范文档在同一提交中跟踪

## 会话目标

阅读 `docs/manuals/road-system-v2-gen.md`，以后附录 D 的第二代最终范围为准，按 `docs/todo/road-graph.md` 的执行顺序逐步实现、验证、记录并提交 RoadGraph 工作。

## 已完成

- 完成 `road-graph:0.6`：强化正交/对角交叉、waypoint 拆分、单边删除和整组删除回归，显式校验节点邻接、Group 归属、空间命中和删除不创建替代 Edge。
- 完成 `road-graph:0.7`：`FindClosestNode` 与 `GetOrCreateNode` 共用 `FindClosestIndexedNode`；节点身份半径固定为包含边界的 `0.5f`，多候选选择最近节点，近似等距时选择较小 Node ID。
- 完成 `road-graph:1.1`：确认 RoadGroup 是自动合并的唯一硬边界；不同 Group 的独立玩家提交不会因共线而合并。
- 完成 `road-graph:1.3`：使用统一距离平方 epsilon 排除近似共享端点，避免伪内部交叉拆除并重建既有 Edge，同时保留真实四连接交叉。
- 完成 `road-graph:2.2`：删除未调用且依赖 `DirectionUtil` 的 `RoadGraph.IsPathValid`，使 RoadGraph 数据层不再引用方向枚举、输入网格或 `CellSize`。
- 完成 `road-graph:2.1`：新增 `SubmitPolyline`、`RoadPathSubmissionResult` 和结构化错误枚举，在写图前拒绝非有限坐标、几何退化、节点身份塌缩、重复点和自交/折返；旧 `AddRoad` 复用同一链路。
- 推进 `road-graph:2.4` 折线基础：公共结构化折线入口已建立；原生曲线写入与未知几何类型拒绝等待后续集成。
- 推进 `road-graph:2.4` 提交契约：新增不可变 `RoadPath` 原生段请求快照；扩展提交结果为创建/删除 Node、Edge、Group 的确定排序摘要，现有折线交叉拆分和合并也返回完整实体差异。
- 推进 `road-graph:2.5`（2026-08-03）：新增原生几何段统一参数域、查询和拆分契约，并完成直线段实现；曲线族、序列化及 GraphEdge 集成仍保持开放。
- 推进 `road-graph:2.5` cubic Bézier：原生保留四个控制点，完成解析位置/切线、精确极值包围盒、误差受控弧长和 De Casteljau 拆分。
- 推进 `road-graph:2.5` 几何序列化：新增带显式版本和稳定类型判别的纯数据契约，完成直线与 cubic Bézier 的 JSON/DTO 往返，并在构造运行时对象前结构化拒绝畸形或不一致数据。
- 推进 `road-graph:2.5` GraphEdge 集成：GraphEdge 改以只读连续原生几何段作为长度和中间锚点的事实来源；现有折线写入、合并、拆分及旧 waypoint 恢复统一转换为 line 段，直接持有 cubic Bézier 时保留控制参数。
- 推进 `road-graph:2.5` 样条：新增 cubic Hermite 原生几何段，保留端点与两端导数向量，完成查询、包围盒、长度、Hermite 类型拆分和版本化序列化。
- 推进 `road-graph:2.5` 圆弧：新增以圆心、半径、起始角和有符号扫角表达的原生圆弧，完成解析长度、方向切线、轴向极值包围盒、同类型拆分和版本化序列化。
- 推进 `road-graph:2.5` 回旋线：新增弧长参数化 clothoid，保存起点、起始航向、起终曲率和弧长，完成线性曲率、误差受控位置积分、保守包围盒、同类型拆分和版本化序列化。
- 推进 `road-graph:2.5` 一般圆锥曲线：新增正权重 rational quadratic Bézier，保存三个控制点和三个齐次权重，完成有理位置/切线、解析极值包围盒、误差受控长度、齐次拆分和版本化序列化。
- 完成 `save-system:0.5`：RoadGraph 恢复改为在临时 Node/Edge/Group 字典中全量校验，只有 schema、全局 ID、引用、成员关系、原生几何、端点和 `nextID` 全部有效时才一次提交；失败保持活动图和 ID 分配状态不变。
- 完成 `save-system:5.3`：新道路 JSON 使用 `schemaVersion`、`nodes`、`edges`、`groups` 及对应 ID 字段，直接保存六类原生几何段的类型和控制参数，不迁移旧 Junction/Segment/Road payload。
- 完成 `save-system:0.3`、`0.4`：新道路 schema 已移除 RoadType、旧类型回退和旧版本兼容；RoadGraph 与 manifest 均精确拒绝缺失、旧版、未来版和错误类型版本。
- 推进 `road-graph:2.4` 原生提交：新增 `SubmitPath(RoadPath)`，六类原生几何和连续复合路径可从非 UI 调用方直接写入 GraphEdge；非法请求在任何写图前返回结构化错误，成功返回完整实体变更摘要并发出一致的 Edge 事件。
- 原生路径端点解析支持近节点复用：line、cubic Bézier、cubic Hermite 和 rational quadratic 按类型重建端点/控制参数；circular arc 与 clothoid 只接受保持原生参数语义的整体平移，其他情况以 `UnsupportedEndpointSnap` 无副作用拒绝。
- 推进 `road-graph:2.6` 最近点基础：`RoadGeometrySegment` 新增统一最近点结果与查询 API，line、circular arc 使用解析解，其余四类曲线使用原生包围盒和同类型拆分的分支限界搜索。
- 完成 `road-graph:1.2`：`FindClosestEdge` 只由空间索引收集候选，最终逐 Edge 的全部原生几何段执行权威最近点查询；等距稳定选择较小 Edge ID，不再以端点、waypoint 或显示采样决定结果。
- 推进 `road-graph:3.2` 原生几何空间占据：每个 `RoadGeometrySegment` 以保守 `Bounds` 覆盖全部 bucket，圆查询使用原生最近点过滤；恢复可重建、删除可完整清理曲线引用。
- 推进 `road-graph:2.6` 点上判定：`TryFindPointOnGeometry` 在显式距离容差内返回权威参数、位置、距离和端点/内部分类，为后续按曲线参数拆分提供统一输入。
- 推进 `road-graph:2.6` 直线交点：直线-直线使用解析参数并区分交叉、端点接触和重叠；直线与其余原生曲线使用包围盒剪枝和同类型细分，返回双方稳定参数及交叉/相切分类。
- 推进 `road-graph:2.6` 曲线-曲线交点：统一 `FindIntersections` 对双方原生子曲线执行二维参数分支限界，覆盖单/多交点、内部相切、圆弧双交点、端点接触和无交点；完全相同且同参数方向的曲线直接报告 overlap。
- 推进 `road-graph:2.6` 曲线重叠：新增双方参数区间结果，以第二参数升/降序表达同向/反向；通过端点权威参数对应和递归全区间验证识别五类非直线几何及直线的整段/部分重叠。
- 推进 `road-graph:2.6` 多参数拆分：`RoadGeometrySubdivision.SplitAtParameters` 将无序、容差去重后的原几何参数逐次映射到剩余子曲线，返回保持原生类型的子段及各自全局参数范围。
- 推进 `road-graph:2.6` Edge 级原生拆分：新增内部 `SplitEdgeAtGeometryParameters`，把段索引和局部参数归一化为整条 Edge 的有序边界，一次替换原 Edge，并保持六类原生几何、端点 Node ID、Group、双向 `EdgeRef`、空间索引和 Edge 事件。
- `SplitEdgeAtPosition` 改用各原生段的 `TryFindPointOnGeometry` 参数并复用 Edge 级拆分入口，旧折线提交的交叉和 waypoint 拆分不再把原生曲线降级为 line 段。
- 推进 `road-graph:2.4`、`2.6` 原生离散交点提交：`SubmitPath` 在任何写图前查询新旧原生段交点，分别汇总既有 Edge 与新路径参数；每条既有 Edge 只替换一次，新路径直接以同类型子段建边。
- 六类直线-曲线交叉、Bézier 多交点、内部相切和端点接触现会创建共享 GraphNode；`RoadGraphChangeSummary` 同时报告被替换旧 Edge 与双方新 Edge，原 Group 和新提交 Group 各自保持成员关系。
- 完成 `road-graph:2.4`、`2.5`、`2.6`：`SubmitPath` 将 `RoadGeometryOverlap` 的双方区间边界纳入拆分计划，完全覆盖在写图前返回 `FullyCovered`，部分同向/反向覆盖只创建未覆盖原生子段，复合路径仍以覆盖边界拆分旧 Edge 并连接新几何。
- 重叠与离散交点来自不同查询分支但落在同一空间节点时，旧 Edge 与新路径均按参数或空间身份合并边界，避免近似参数差造成极短 Edge。
- 完成 `road-graph:2.7`：删除 `RoadType` 源码与 UID，并从 `GraphEdge`、`RoadGroup`、公共/兼容提交 API、Edge 创建、拆分、合并、恢复和 `RoadBuilder` 中移除类型状态与分支。
- 完成 `save-system:0.3`：第二代运行时与 JSON 均不再保存或恢复 RoadType，同时删除 `SaveData.cs` 中无人引用的旧道路 DTO 和类型回退字段。
- 推进 `road-graph:3.1`：新增独立 Release 性能入口和固定 1k/10k/100k 数据集，记录七类操作的平均/P95、分配量、空间候选、全表扫描与访问 Edge 数；完整结果写入 `docs/performance/road-graph-v2-baseline.md`。
- 完成 `road-graph:3.1`～`3.3`：新增矩形 bucket 查询，将折线与原生几何的覆盖、交点、锚点和拆分候选局部化；10k 全场景达到 16.67 ms 门槛，100k 压力结果已记录。
- 完成 `road-graph:4.1`：新增 RoadGraph 内部不变式断言，统一验证拓扑、Group、无孤立节点及空间引用的实体对应和精确 bucket 覆盖；新增五类删除事务回归入口。
- 完成 `road-graph:4.2`：三个公共遍历入口返回调用时稳定快照，Node 邻接与 Group 成员不再暴露可变底层集合，损坏 Edge 的完整路径读取改为显式失败。
- 完成 `road-graph:4.3`：统一仅 detach Edge 的内部原语，公开删除在原语之后清理；原生拆分与合并复用该原语，合并不再删除并复活远端节点。
- 更新 `docs/todo/road-graph.md` 的状态总览、完成证据与后续责任边界。
- 完成 `road-graph:4.4`：单删、整组删、原生拆分与合并统一收集受影响 Node/Group，在一次提交中清理并验证，随后按稳定顺序发布 Edge 事件；删除 `suppressMerge` 参数及死分支。
- 完成 `road-graph:6.1`：将指南第 10～11 节的迁移提案改为带当前处置状态的历史记录，明确阶段 A/B 已完成、旧 RoadType 和旧存档兼容提案已取代、TrafficGraph/A* 延期，并移除已失效的 Phase 2 执行时间线。
- 完成 `road-graph:6.2`：按当前源码同步设计指南和类参考中的原生几何、提交 API、RoadType 边界、删除事务、事件顺序、空间命中与复杂度、严格 v2 存档字段和失败原子性。
- 完成 `save-system:0.4`：manifest 改为大小写敏感的精确 `schemaVersion = 1` 门禁，在任何系统恢复前拒绝缺失、旧版、未来版和错误类型；保存入口显式写入当前版本。
- 推进 `save-system:0.9`：提取纯 .NET `SaveSlotStore`，让隔离临时目录测试覆盖真实文件保存、加载、manifest、缺失文件、版本拒绝、捕获失败和非空槽递归删除；多槽列举、内部槽 ID 与整槽原子提交仍待后续。
- 推进 `save-system:0.10`：manifest 从混合语义的 `slotName` 改为独立 `slotId/displayName`；`SaveAs` 生成 `manual-<GUID>`，`CurrentSlotName` 改为 `CurrentSlotID`。玩家名称允许中文、空格、重复值和路径字符，但永不参与路径计算；内部 ID、系统文件名、根目录直接子项和目录链接均有独立防护。只读 ACL 与真实 Windows 导出包仍待验证，因此路线图保持部分完成。
- 完成 `save-system:1.1`：manifest 增加人口、资金和缩略图引用占位字段，`ListSlots` 返回含有效/损坏状态的摘要；列表不加载 RoadGraph，有效槽按 UTC 时间和内部 ID 稳定排序，损坏槽与缺失缩略图不会阻断其他摘要。
- 完成 `save-system:0.11`：新增 `IPreparedSaveable`，整槽先校验 manifest 与必需文件、读取并解析全部 JSON、准备 RoadGraph/相机临时状态，再统一提交；损坏后续文件不再留下前序系统的部分恢复结果。
- 完成 `save-system:0.9`：保存先全量捕获/序列化，再通过 staging/backup 整槽发布；覆盖失败恢复旧槽，读写删与列举入口恢复崩溃残留，内部事务目录不进入玩家槽列表。
- 完成 `save-system:1.4`：SaveManager 保留 RoadGraph、相机和未来系统的注册扩展机制，但 V2 配置只选择 `road_network`；新 manifest 不再列相机，加载不改变相机状态，未来配置新增独立 JSON 不修改道路 schema。
- 完成 `save-system:1.2`：暂停菜单新增命名槽管理视图；玩家可另存为同名但 ID 独立的手动槽，并在显示目标摘要后确认或取消覆盖、加载和删除。损坏槽显示诊断，禁用覆盖/加载并保留确认删除入口；保存视图在 435×480 和 1600×900 下均保持面板边界。
- 将节点身份和近共享端点缺陷分别记录为 `docs/bugfix/road-graph.md` 中的 `road-graph:BUG-15`、`road-graph:BUG-16`。
- 将曲线内部相切被重复报告的缺陷记录为 `docs/bugfix/road-graph.md` 中的 `road-graph:BUG-17`。
- 将严格端点断言误报合法原生拆分残差的缺陷记录为 `docs/bugfix/road-graph.md` 中的 `road-graph:BUG-18`。
- 将损坏 RoadGraph 存档在校验前清空活动图的缺陷记录为 `docs/bugfix/save-system.md` 中的 `save-system:BUG-3`。
- 将缺失 manifest 版本被 DTO 默认值误接受的缺陷记录为 `docs/bugfix/save-system.md` 中的 `save-system:BUG-4`。

## 已验证

- `road-graph:0.6`：`RoadGraphRegressionTests` 聚焦测试 17/17 通过；当时解决方案测试 52/52 通过。
- `road-graph:0.7`：新增测试在修复前 7 项中 3 项失败；修复后 `RoadGraphNodeIdentityTests` 7/7、当时解决方案测试 59/59 通过。
- `road-graph:1.1`：`RoadGraphRegressionTests` 18/18 通过；最终 `dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore` 为 60 通过、0 失败、0 跳过。
- `road-graph:1.3`：修复前聚焦回归 21 项中 1 项失败；修复后 21/21 通过，解决方案测试 63/63 通过。
- `road-graph:2.2`：`RoadGraphContinuousSpaceTests` 3/3 通过；RoadGraph 禁用依赖扫描无命中。
- `road-graph:2.1`：`RoadGraphPathSubmissionTests` 19/19 通过，覆盖结构化失败、完整状态原子性、Edge 事件静默、任意合法折线和旧 API 兼容。
- `road-graph:2.4` 变更摘要：路径提交与拓扑聚焦测试 47/47 通过，覆盖请求防御性复制、成功创建实体、交叉替换 Edge、排序稳定性及失败空摘要。
- `road-graph:2.5` 直线几何基础：`RoadGeometrySegmentTests` 14/14 通过，覆盖有限构造、参数域、位置、单位切线、包围盒和拆分重组。
- `road-graph:2.5` cubic Bézier：`RoadGeometrySegmentTests` 24/24 通过，新增控制点语义、导数切线、线性/二次导数极值包围盒、直线退化、拆分参数映射和弧长重组覆盖。
- `road-graph:2.5` 几何序列化：`RoadGeometrySerializationTests` 17/17 通过；与几何段测试合并聚焦为 41/41，覆盖 line/cubic Bézier 语义往返和所有结构化拒绝类别。
- `road-graph:2.5` GraphEdge 集成：`GraphEdgeGeometryTests` 5/5 通过；连同拓扑与覆盖回归的聚焦测试 29/29 通过。
- `road-graph:2.5` cubic Hermite：相关几何与序列化聚焦测试 56/56 通过，覆盖原生参数、Hermite 基函数位置、解析极值包围盒、端点切线、拆分参数映射、无效数据拒绝和 JSON 往返。
- `road-graph:2.5` 圆弧：全部几何聚焦测试 76/76 通过，覆盖正反扫角、整圆、轴向极值、参数域、拆分重组、无效半径/扫角和 JSON 往返。
- `road-graph:2.5` clothoid：全部几何聚焦测试 89/89 通过，覆盖线性曲率、直线/常曲率解析退化、长距离微小曲率变化、拆分参数映射、参数拒绝和 JSON 往返。
- `road-graph:2.5` rational quadratic：全部几何聚焦测试 103/103 通过，覆盖四分之一圆、普通二次退化、权重整体缩放不变性、齐次拆分、无效权重和 JSON 往返。
- V2 道路存档聚焦测试：`RoadGraphPersistenceV2Tests`、`RoadGraphNodeIdentityTests`、`GraphEdgeGeometryTests` 合计 24/24 通过，覆盖新字段、六类原生几何双重往返、版本拒绝、全局 ID、悬空引用、Group 成员关系、非法几何、连续性、端点、非有限数值、孤立节点、`nextID` 和失败状态原子性。
- `godot --headless --path . --log-file .godot/qa-roadgraph-v2-persistence.log --script tests/godot/pause_menu_runtime_contract.gd`：授权 `user://` 写入后输出 `PASS pause menu runtime contract`，主场景完成两轮 autosave 保存加载；两条未挂载 `ToolManager` 的 HUD 警告属于契约脚本既有隔离场景。
- `dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：192 通过、0 失败、0 跳过。
- `dotnet test SimpleCities.sln --configuration Debug --no-restore`：本批最终 204 通过、0 失败、0 跳过。
- `RoadGraphNativePathSubmissionTests`：12/12 通过，覆盖六类原生几何、复合路径、近节点吸附、未知类型、断裂/塌缩/重复锚点、吸附拒绝、重复覆盖、曲线与弦区分、变更摘要、事件和存档往返。
- 原生提交批次路径聚焦测试：34/34 通过；`dotnet test SimpleCities.sln --configuration Debug --no-restore` 更新为 216 通过、0 失败、0 跳过。
- 原生提交批次 `dotnet build SimpleCities.sln --configuration Debug --no-restore`：顺序重跑为 0 警告、0 错误；并行测试/构建曾产生一次 DLL 文件占用重试警告，不属于源码告警。
- 原生提交批次 Godot 暂停菜单运行时契约：输出 `PASS pause menu runtime contract`，主场景两轮 autosave 保存加载通过。
- `RoadGeometryClosestPointTests`：13/13 通过，覆盖 line 投影/端点、圆弧径向投影/扫角端点/等距决胜、四类一般曲线的曲线上收敛、曲线中段与非法参数。
- 最近点批次 `dotnet build SimpleCities.sln --configuration Debug --no-restore`：0 警告、0 错误；`dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：229 通过、0 失败、0 跳过。
- `RoadGraphCurveSpatialQueryTests`：12/12 通过，覆盖六类原生几何中段、高曲率 Bézier 远离端点弦、真实最近曲线、包含半径边界且拒绝半径外、等距 Edge ID 决胜、存档恢复重建和删除清理。
- 曲线空间查询批次 `dotnet build SimpleCities.sln --configuration Debug --no-restore`：0 警告、0 错误；`dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：241 通过、0 失败、0 跳过。
- 曲线空间查询批次 Godot 暂停菜单运行时契约：授权 `user://` 写入后输出 `PASS pause menu runtime contract`，主场景完成两轮 autosave 保存加载；两条未挂载 `ToolManager` 的警告属于既有隔离场景。
- `RoadGeometryPointLocationTests`：15/15 通过，覆盖六类原生几何内部点、起点/内部/终点分类、包含距离边界、端点参数容差和非法参数拒绝。
- 点上判定批次 `dotnet build SimpleCities.sln --configuration Debug --no-restore`：0 警告、0 错误；`dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：256 通过、0 失败、0 跳过。
- `RoadGeometryLineIntersectionTests`：15/15 通过，覆盖解析直线交叉、端点接触、重叠、平行分离、Bézier 多交点/内部相切、圆弧双交点、一般曲线已知参数交点及非法容差拒绝。
- 直线交点批次 `dotnet build SimpleCities.sln --configuration Debug --no-restore`：0 警告、0 错误；`dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：271 通过、0 失败、0 跳过。
- `RoadGeometryCurveIntersectionTests`：12/12 通过，覆盖 Bézier-Bézier 单/多交点和内部相切、圆弧双交点、五类曲线已知参数交点、端点接触、分离、完全相同曲线 overlap 及第二参数为直线时的参数顺序。
- 曲线-曲线交点批次 `dotnet build SimpleCities.sln --configuration Debug --no-restore`：0 警告、0 错误；`dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：283 通过、0 失败、0 跳过。
- 重叠参数区间批次交点聚焦测试：44/44 通过；新增覆盖 Bézier、Hermite、圆弧、clothoid、rational quadratic 的整段反向以及同向/反向部分重叠，验证直线重叠双方参数，并覆盖同向/反向整圆。
- 重叠参数区间批次 `dotnet build SimpleCities.sln --configuration Debug --no-restore`：0 警告、0 错误；`dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：300 通过、0 失败、0 跳过。
- `RoadGeometrySubdivisionTests`：14/14 通过，覆盖六类原生几何在 `[0.2, 0.5, 0.8]` 多参数拆分后的类型、端点、局部到全局参数映射，以及无序输入、端点忽略、容差去重、空输入和非法参数拒绝。
- 多参数拆分批次 `dotnet build SimpleCities.sln --configuration Debug --no-restore`：0 警告、0 错误；`dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：314 通过、0 失败、0 跳过。
- `RoadGraphNativeEdgeSubdivisionTests`：10/10 通过，覆盖六类单段原生几何、无序多参数、容差去重、Edge 端点忽略、多原生段内部拆分、共享段边界去重、原 Node ID 与 Group 保持、双向 `EdgeRef`、空间索引、事件计数和 V2 存档往返。
- Edge 级拆分批次相关拓扑回归：57/57 通过；`dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：324 通过、0 失败、0 跳过。
- Edge 级拆分批次 `dotnet build SimpleCities.sln --configuration Debug --no-restore`：0 警告、0 错误。
- Edge 级拆分批次 Godot 主场景运行契约：首次受限运行因 `user://` 无法写入导致绑定断言失败；授权写入后重跑输出 `PASS pause menu runtime contract`，两轮 autosave 保存加载通过，两条缺少 `ToolManager` 的警告属于隔离脚本既有状态。
- `RoadGraphNativePathIntersectionSubmissionTests`：9/9 通过，覆盖六类直线-曲线内部交叉、Bézier 三交点、内部相切、双方端点接触、原生类型、四连接节点、Group、事件、变更摘要和 V2 存档往返。
- 原生离散交点提交批次相关回归：59/59 通过；`dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：333 通过、0 失败、0 跳过。
- 原生离散交点提交批次 `dotnet build SimpleCities.sln --configuration Debug --no-restore`：0 警告、0 错误；Godot 主场景运行契约输出 `PASS pause menu runtime contract`，两轮 autosave 保存加载通过。
- `RoadGraphNativePathOverlapSubmissionTests`：26/26 通过，覆盖六类几何的同向/反向完整覆盖、部分覆盖、区间并集完整覆盖、复合路径覆盖边界锚定、Group、事件、变更摘要和 V2 存档往返。
- 原生重叠提交批次相关回归：105/105 通过；`dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：359 通过、0 失败、0 跳过。
- 原生重叠提交批次 `dotnet build SimpleCities.sln --configuration Debug --no-restore`：0 警告、0 错误；Godot 主场景运行契约输出 `PASS pause menu runtime contract`，两轮 autosave 保存加载通过。
- RoadType 移除批次聚焦测试：98/98 通过；源码边界测试确认 `Scripts/Road/*.cs`、`Scripts/Core/SaveData.cs` 不含 `RoadType`，且 `Scripts/Road/RoadType.cs` 不存在。
- RoadType 移除批次 `dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：359 通过、0 失败、0 跳过；`dotnet build SimpleCities.sln --configuration Debug --no-restore`：0 警告、0 错误。
- RoadType 移除批次 Godot 主场景运行契约：输出 `PASS pause menu runtime contract`，两轮 autosave 保存加载通过。
- `godot --headless --editor --path . --quit --log-file .godot/qa-roadgraph-remove-roadtype-editor.log`：退出码 0；扫描仍报告既有根证书、缓存 UID、MCP 6550 端口占用和编辑器设置写入错误，因此编辑器日志门禁受限。扫描删除的 imgui 临时 DLL 已用 SHA1 相同的原始 DLL 恢复，两份无关测试 UID 已移除。
- RoadGraph 优化前性能诊断契约聚焦测试：`RoadGraphContinuousSpaceTests` 5/5 通过，确认最近边命中只记录空间候选，空白原生提交准确记录两次全表扫描和访问 Edge 数。
- RoadGraph Release 基线：10k 下短路、完整覆盖、最近边和删除通过 16.67 ms 门槛；长路 P95 18.911 ms、原生曲线 42.769 ms、多交叉 68.910 ms 未通过。100k 多交叉为 2,528.012 ms，按范围只记录不阻塞。
- RoadGraph 优化复测：`dotnet run --project tests/SimpleCities.RoadGraph.Performance/SimpleCities.RoadGraph.Performance.csproj --configuration Release --no-restore -- --enforce-budget` 退出码 0；10k 长路、原生曲线、多交叉 P95 分别为 1.162 ms、1.179 ms、5.113 ms，七类场景全表扫描和访问 Edge 数均为 0；100k 多交叉为 15.841 ms。
- 空间查询局部化批次 Godot 主场景运行契约：受限运行因 `user://` 无法写入而失败；授权重跑输出 `PASS pause menu runtime contract`，两轮 autosave 保存加载通过，两条缺少 `ToolManager` 的警告属于既有隔离场景。
- `godot --headless --editor --path . --quit --log-file .godot/qa-roadgraph-spatial-localization-editor.log`：退出码 0；既有根证书、缓存 UID、MCP 6550 端口占用和编辑器设置写入错误仍使编辑器日志门禁受限，扫描副作用已清理。
- 空间查询局部化批次最终验证：解决方案测试 360/360，主解决方案 Debug 与性能项目 Release 构建均为 0 警告、0 错误。
- 删除不变式批次聚焦测试：`RoadGraphInvariantTests` 5/5 通过，覆盖单删、整组删、原生曲线拆边、同 Group 共线合并、非法路径和完全覆盖路径拒绝后的提交状态。
- 删除不变式批次最终验证：`dotnet test SimpleCities.sln --configuration Debug --no-restore` 为 365/365；`dotnet build SimpleCities.sln --configuration Debug --no-restore` 为 0 警告、0 错误。当前会话未提供 `csharp-ls` MCP，逐文件 LSP 诊断仍阻塞；本批不改变场景、资源或用户可见运行行为，按 Tier 1 完成验证。
- 状态封装批次聚焦测试：`RoadGraphEncapsulationTests` 3/3 通过，覆盖调用后再修改图的稳定枚举、Node/Group 集合强制转换修改和缺失端点明确失败；既有 `GraphEdgePoints_CannotMutateGraphStateOutsideRoadGraphApi` 继续通过。
- 状态封装批次最终验证：`dotnet test SimpleCities.sln --configuration Debug --no-restore` 为 368/368；`dotnet build SimpleCities.sln --configuration Debug --no-restore` 为 0 警告、0 错误。逐文件 `csharp-ls` 诊断因 MCP 未提供而阻塞；本批按 Tier 1 验证，不涉及场景或资源门禁。
- 底层 detach 批次聚焦回归：`RoadGraphInvariantTests` 与 `RoadGraphRegressionTests` 合计 26/26；新测试确认 detach 保留孤立 Node 和空 Group、移除空间命中且不发事件。
- 底层 detach 批次最终验证：`dotnet test SimpleCities.sln --configuration Debug --no-restore` 为 369/369；`dotnet build SimpleCities.sln --configuration Debug --no-restore` 为 0 警告、0 错误。逐文件 `csharp-ls` 诊断仍因 MCP 未提供而阻塞；内部重构按 Tier 1 验证。
- 性能基线批次 `dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：360 通过、0 失败、0 跳过；主解决方案 Debug 与性能项目 Release 构建均为 0 警告、0 错误。
- `godot --headless --editor --path . --quit --log-file .godot/qa-roadgraph-performance-baseline-editor.log`：退出码 0，并为 `RoadGraph.Diagnostics.cs` 生成 UID；既有根证书、缓存 UID、MCP 6550 端口占用和编辑器设置写入错误仍使编辑器日志门禁受限，扫描副作用已清理。
- `dotnet build SimpleCities.sln --configuration Debug --no-restore`：修改前与修改后均为 0 警告、0 错误。
- `godot --headless --editor --path . --quit`：退出码 0 并为新 C# 文件生成 UID；同时出现 imgui GDExtension 临时 DLL、系统证书、MCP 端口占用和编辑器设置写入错误，因此不作为编辑器门禁通过证据。扫描误删的已跟踪临时 DLL 已用同哈希原始 DLL 恢复，未留下该副作用。
- `git diff --check`：本批提交前通过。
- `RoadGraphMutationEventTests`：3/3 通过；整组删除、原生拆分与合并的每个事件处理器均能对最终图执行 `AssertInvariants`，合并事件顺序为移除、移除、新增。
- `dotnet test SimpleCities.sln --configuration Debug --no-restore`：372 通过、0 失败、0 跳过。
- `dotnet build SimpleCities.sln --configuration Debug --no-restore`：0 警告、0 错误。
- `dotnet run --project tests/SimpleCities.RoadGraph.Performance/SimpleCities.RoadGraph.Performance.csproj --configuration Release --no-restore -- --enforce-budget`：10k 全部场景满足 16.67 ms P95 硬门槛；单边删除 P95 为 0.038 ms。
- `godot --headless --path . --log-file .godot/qa-roadgraph-mutation-transaction.log --script tests/godot/pause_menu_runtime_contract.gd`：首次受限运行因 `user://` 写入失败不能计为通过；授权重跑输出 `PASS pause menu runtime contract`，两轮 autosave 保存/加载通过。两条 `ConstructionDock` 缺少 `ToolManager.Instance` 警告来自既有隔离场景。
- `SaveManagerManifestVersionTests`：修复前缺失版本和错误大小写字段两项被 DTO 默认值误接受；修复后当前/缺失/旧版/未来版/错误类型/错误大小写 6/6 通过，完整解决方案测试增至 378/378。
- `godot --headless --path . --log-file .godot/qa-save-manifest-version.log --script tests/godot/pause_menu_runtime_contract.gd`：输出 `PASS pause menu runtime contract`，两轮 autosave 保存/加载通过；两条 `ConstructionDock` 警告为既有隔离场景行为。
- `SaveManagerSlotContractTests` 与 `SaveManagerManifestVersionTests`：聚焦 12/12 通过；完整解决方案测试 384/384，Debug 构建 0 警告、0 错误。
- `godot --headless --path . --log-file .godot/qa-save-slot-store.log --script tests/godot/pause_menu_runtime_contract.gd`：`SaveSlotStore` 提取后仍输出 `PASS pause menu runtime contract`，两轮 autosave 保存/加载通过。
- `dotnet test tests/SimpleCities.RoadGraph.Tests/SimpleCities.RoadGraph.Tests.csproj --filter "FullyQualifiedName~SaveManagerSlotContractTests|FullyQualifiedName~SaveManagerManifestVersionTests"`：内部槽位 ID、自由显示名和路径边界聚焦测试 28/28 通过。
- `dotnet test SimpleCities.sln --configuration Debug --no-restore`：槽位身份批次后 400/400 通过。
- `dotnet build SimpleCities.sln --configuration Debug --no-restore`：槽位身份批次后 0 警告、0 错误。
- `godot --headless --path . --log-file .godot/qa-save-slot-identity.log --script tests/godot/pause_menu_runtime_contract.gd`：新 manifest 下两轮 autosave 保存/加载通过并输出 `PASS pause menu runtime contract`；既有隔离场景 `ToolManager.Instance` 警告未变化。
- `dotnet test tests/SimpleCities.RoadGraph.Tests/SimpleCities.RoadGraph.Tests.csproj --filter "FullyQualifiedName~SaveManagerSlotContractTests|FullyQualifiedName~SaveManagerManifestVersionTests"`：可列举摘要与元数据批次聚焦测试 33/33 通过。
- `dotnet test SimpleCities.sln --configuration Debug --no-restore`：可列举摘要批次后 405/405 通过。
- `dotnet build SimpleCities.sln --configuration Debug --no-restore`：可列举摘要批次后 0 警告、0 错误。
- `godot --headless --path . --log-file .godot/qa-save-slot-listing.log --script tests/godot/pause_menu_runtime_contract.gd`：新增元数据下两轮 autosave 保存/加载通过并输出 `PASS pause menu runtime contract`；既有隔离场景警告未变化。
- `dotnet test tests/SimpleCities.RoadGraph.Tests/SimpleCities.RoadGraph.Tests.csproj --filter "FullyQualifiedName~SaveManagerSlotContractTests|FullyQualifiedName~SaveManagerManifestVersionTests|FullyQualifiedName~RoadGraphPersistenceV2Tests"`：整槽预检批次 55/55 通过。
- `dotnet test SimpleCities.sln --configuration Debug --no-restore`：整槽预检批次后 415/415 通过。
- `dotnet build SimpleCities.sln --configuration Debug --no-restore`：整槽预检批次后 0 警告、0 错误。
- `godot --headless --path . --log-file .godot/qa-save-slot-prevalidation-final.log --script tests/godot/pause_menu_runtime_contract.gd`：主动破坏 autosave RoadGraph 后出现预期加载错误，`CurrentSlotID` 保持手动槽；清理临时槽、重建 autosave 后输出 `PASS pause menu runtime contract`。
- `dotnet test tests/SimpleCities.RoadGraph.Tests/SimpleCities.RoadGraph.Tests.csproj --filter "FullyQualifiedName~SaveManagerSlotContractTests|FullyQualifiedName~SaveManagerManifestVersionTests"`：整槽原子发布批次 48/48 通过。
- `dotnet test SimpleCities.sln --configuration Debug --no-restore`：整槽原子发布批次后 420/420 通过。
- `dotnet build SimpleCities.sln --configuration Debug --no-restore`：整槽原子发布批次后 0 警告、0 错误。
- `godot --headless --path . --log-file .godot/qa-save-slot-atomic-publish.log --script tests/godot/pause_menu_runtime_contract.gd`：三条预期失败均保持手动 `CurrentSlotID`；清理并重建 autosave 后输出 `PASS pause menu runtime contract`。
- `dotnet test tests/SimpleCities.RoadGraph.Tests/SimpleCities.RoadGraph.Tests.csproj --filter "FullyQualifiedName~SaveManagerSlotContractTests|FullyQualifiedName~SaveManagerManifestVersionTests"`：V2 RoadGraph-only 配置批次 51/51 通过。
- `dotnet test SimpleCities.sln --configuration Debug --no-restore`：V2 RoadGraph-only 批次后 423/423 通过。
- `dotnet build SimpleCities.sln --configuration Debug --no-restore`：V2 RoadGraph-only 批次后 0 警告、0 错误。
- `godot --headless --path . --log-file .godot/qa-save-v2-roadgraph-only-final.log --script tests/godot/pause_menu_runtime_contract.gd`：autosave manifest 只有道路文件、相机 JSON 不存在且加载不改相机；日志准确显示 1 个文件并输出 `PASS pause menu runtime contract`。
- `dotnet test tests/SimpleCities.RoadGraph.Tests/SimpleCities.RoadGraph.Tests.csproj --configuration Debug --no-build --no-restore --filter "FullyQualifiedName~PauseMenuContractTests|FullyQualifiedName~GameHUDCompositionContractTests|FullyQualifiedName~SaveManagerSlotContractTests|FullyQualifiedName~SaveManagerManifestVersionTests"`：命名槽 UI 与后端聚焦测试 58/58 通过。
- `dotnet test SimpleCities.sln --configuration Debug --no-build --no-restore`：命名槽管理批次后 423/423 通过。
- `dotnet build SimpleCities.sln --configuration Debug --no-restore`：命名槽管理批次后 0 警告、0 错误。
- `godot --headless --path . --log-file .godot/qa-save-management-final.log --script tests/godot/pause_menu_runtime_contract.gd`：键盘 `ui_accept` 打开管理视图并确认操作；标准 Button/ItemList 控件信号覆盖同名另存为、选中、覆盖、加载、删除及各取消路径；损坏槽只允许删除，435×480 布局在视口内，全部临时手动槽完成清理并输出 `PASS pause menu runtime contract`。损坏槽警告与不存在槽、非法 ID、损坏 RoadGraph 的三条错误是契约脚本主动触发的预期诊断。

## 重要决策

- 第二代范围以后附录 D 为权威；指南正文末尾的 Phase 2 分区系统不是当前 RoadGraph V2 的下一步。
- `SnapRadius = 0.5f` 是连续空间数据层的节点身份容差，不与 `CellSize` 或 UI 网格吸附绑定。
- `GeometryEpsilon = 1e-4f` 在位置比较中作为距离平方阈值；交点端点判定不再使用 `Vector2 ==`，且不与节点身份半径混用。
- RoadGroup 表示一次用户提交，是第二代自动合并的唯一硬边界；RoadType 已从第二代运行时、API 和存档契约移除，第三代必须通过新契约和 schema 版本重新引入。
- `road-graph:2.2` 只负责解除 RoadGraph 对方向和网格概念的依赖；折线路径的结构化校验由 `2.1` 完成，原生曲线提交与完整结果契约继续由 `2.4` 验收。
- `SubmitPolyline` 是折线兼容入口，`SubmitPath` 是原生几何入口；两者共享最终 GraphEdge 写入方法。原生交叉、重叠和拆分事件已完成验收，`road-graph:2.4` 随本批关闭。
- `RoadGraphChangeSummary` 以提交前后 Node、Edge、Group 权威 ID 集合差异生成，不尝试从事件顺序推断结果；因此交叉拆边、合并和实体替换都能稳定报告。
- 原生几何段统一采用 `[0, 1]` 参数域；显示采样不进入 `RoadGeometrySegment` 权威契约，未来渲染采样只能作为派生数据。
- cubic Bézier 弧长使用曲线弦长下界与控制多边形上界递归收敛，不依赖显示采样；包围盒使用导数内部极值而非控制点包围盒。
- 原生几何序列化以 `version = 1` 和区分大小写的稳定字符串 `line` / `cubicBezier` 判别类型；当前版本拒绝未知字段和类型专属字段混用，后续格式演进必须提升版本并显式映射。
- GraphEdge 的 `GeometrySegments` 是运行时权威几何；`Points` 暂作为现有渲染和折线拓扑算法的防御性兼容视图，仅表示段边界，不得成为曲线真相。
- 本批不向旧 `SegmentData` schema 添加临时曲线字段；新 V2 Node/Edge/Group 命名、严格版本拒绝、完整校验和曲线参数存档由 `save-system:0.4`、`0.5`、`5.3` 一次完成。
- 附录 D 未限定样条族；第二代采用 cubic Hermite 作为明确的原生样条语义，以端点导数表达连续性。其与 cubic Bézier 解析等价，但使用独立 `cubicHermite` 判别并在拆分后保留 Hermite 参数。
- 单个原生圆弧的有符号扫角限制在一周以内；正负号保留逆时针/顺时针语义，较长复合路径由多个连续圆弧段表达。
- clothoid 以弧长为权威长度，曲率在起终值之间线性变化；一般位置通过自适应 Simpson 积分获得，保守包围盒由路径弧长严格限定而非显示采样。空间索引阶段可收紧包围范围，但不得改变这些原生参数。
- 一般圆锥曲线采用三个正权重的 rational quadratic Bézier 表达；三组齐次权重全部保留，使任意参数拆分后仍属于同一类型，而不是强制把子段端点权重归一化后丢失参数语义。
- 新 RoadGraph 道路 schema 自身版本从 `schemaVersion = 1` 起步，字段名严格区分大小写；旧 `version = 2` 表示旧私有 payload，不继承为新 schema 的版本号，也不提供迁移。
- RoadGraph 恢复以“完整临时状态 -> 一次提交”为事务边界；损坏 payload 通过 `JsonException` 返回诊断，失败时不清空图、不改变 `_nextID`、不发出 `GraphCleared`。
- 第二代道路生产源码与存档 DTO 不保留任何 RoadType 兼容层；旧类型字段不会静默映射为默认道路分级。
- `RoadPath` 的每个原生几何段当前形成一条 GraphEdge，相邻段共享 GraphNode；这保留控制参数并为后续按曲线参数拆分提供明确拓扑边界。
- 原生路径覆盖判定不能复用把 Edge 端点连成折线的旧 `IsPathCovered`：line 只由现有 line 几何覆盖，曲线按版本化类型与参数精确比较，避免把曲线弦误判为已有道路。
- 一般曲线最近点不采用固定显示采样：搜索队列以每个原生子曲线 `Bounds` 到查询点的距离作为严格下界，以当前最佳距离与最小未探索下界的差值作为停止条件；返回参数始终映射回原段 `[0, 1]`。
- 空间索引保存的是可重建的 `EdgeGeometryRef` 候选服务：当前以每个原生段的保守包围盒覆盖 bucket，以权威最近点执行圆相交过滤；整段包围盒带来的候选放大留给 `road-graph:3.1`～`3.3` 测量和优化，不以显示采样换取较小候选集。
- 点上判定将空间命中容差与端点参数分类容差分开：前者决定查询点是否属于几何，后者只决定命中应作为起点、内部还是终点处理；非法坐标和非有限/越界容差直接拒绝。
- 直线-曲线交点不把固定采样作为权威：子曲线 `Bounds` 对直线的法向区间和切向投影区间提供剪枝，收敛叶节点映射回原曲线参数；相切点附近的连续容差命中按参数簇归并并保留残差最小候选。
- 曲线-曲线离散交点使用双方子曲线包围盒的二维参数搜索，每步拆分空间范围较大的一侧；候选必须满足双方真实位置残差，并在双方参数维度组成连通簇，避免把同一相切根重复返回或把空间相近但参数独立的根混为一体。
- `RoadGeometryOverlap` 的第一参数区间始终升序，第二参数保存与第一起止点对应的参数；第二参数降序即反向重叠。交换查询双方时必须同步重排对应端点，不能只交换四个数值字段。
- 重叠候选来自双方端点的权威点上参数，不由离散命中数量推断；候选区间还必须递归验证位置残差、切线共线和方向一致性，避免两条多次相交但不共轨的曲线被误判为重叠。
- 完全相同的版本化原生参数是封闭曲线同向重叠的无歧义快路径；整圆起终位置相同，不能仅依赖端点最近参数恢复完整同向区间，反向整圆仍由递归映射验证。
- 多参数拆分始终以原几何全局参数排序，随后用 `(parameter - previous) / (1 - previous)` 映射到当前剩余子曲线；直接在每次拆分后继续使用原参数会产生错误边界。每个结果显式保留原参数范围，供 Edge 级拓扑映射使用。
- Edge 级拆分参数使用 `geometrySegmentIndex + segmentParameter` 形成只用于排序的全局边界；相邻段的 `(i, 1)` 与 `(i + 1, 0)` 表示同一拓扑位置，只创建一个节点和一次切分。整条 Edge 的起终参数是无操作，不创建空 Edge。
- 拆分替换保留原 Edge 两端 Node 实体；拆分节点只在几何精确位置容差内复用，避免旧 `RemoveEdge` 先清理孤立端点再分配新 Node ID。底层删除事务的全面整理仍留给阶段 4。
- 原生提交交点采用“完整查询计划 -> 拆既有 Edge -> 创建新路径子 Edge”的顺序；交点查询阶段不修改图，Edge 拆分顺序按旧 Edge ID 固定，避免同一 Edge 因多个新路径段交点被重复替换。
- 数值交点即使参数略离开端点容差，只要真实位置仍属于整条 Edge 的空间端点身份，就不得创建极短子 Edge；既有 Edge 和新路径都使用 `GeometryEpsilon` 空间过滤后再执行参数拆分。
- `RoadGeometryIntersectionResult.Intersections` 与 `Overlaps` 分流处理：离散命中创建共享拓扑节点；连续重叠以双方区间边界拆分，并把新路径对应区间标记为已覆盖，不将连续集合伪装成离散节点。
- overlap 计划同时保存旧 Edge 边界、新路径边界和新路径覆盖区间；新路径先细分再以原参数中点分类，因此重叠边界对齐后每个候选子段只可能是完整覆盖或完整未覆盖，不依赖序列化近似判断。
- 当多个 overlap 区间并集覆盖整条新路径时，必须在拆分旧 Edge 前返回 `FullyCovered`；覆盖请求不得仅为补拓扑边界而改变既有图。
- 图不变式使用内部 `RoadGraph.AssertInvariants` 作为单一诊断入口；空间一致性不再只以“查询能命中”为证据，而是核对登记引用集合、实体 ID、几何对象身份和每个引用的精确 bucket 覆盖。
- 公共图遍历的“快照”在方法调用时立即固定成员，而不是首次枚举时延迟复制；实体引用本身通过只读包装、防御性复制和内部 setter 阻止外部绕过 RoadGraph 写入链。
- `DetachEdge` 的职责止于移除 Edge 及其派生/反向引用；孤立 Node、空 Group、事件和不变式提交由顶层事务负责。该原语允许暂时不满足提交后不变式，不能作为公共删除 API。
- 复合 Edge 变更不建立全局事件队列；顶层操作在最终图完成清理和 Debug 不变式验证后发布事件。所有移除事件先于所有新增事件，整组删除按 Edge ID 稳定排序，事件处理器始终查询到提交后的完整一致图。
- 图几何端点不变式遵循 `GeometryEpsilon` 位置契约；圆弧和回旋线解析拆分产生的约 `1e-6` 合法残差不再由严格 `Vector2 ==` 误报。
- 存档管理交互由 `PauseMenu` 持有，`GameHUD` 只通过 `ConfigureSaveManager` 注入后端并继续协调暂停/场景切换；主菜单保存与读档不再隐式覆盖固定 `autosave`。
- 覆盖、加载和删除共用确认视图并保存目标内部 ID；取消时返回存档管理视图。有效槽可执行三类操作，损坏槽只允许删除，避免把损坏 manifest 当作可覆盖或可加载目标。
- 本对话只维护本文件作为会话档案，不新增并行的会话记录。

## 未完成与阻塞

- `road-graph:2.4`～`4.4`、`6.1`～`6.2` 与 `save-system:0.3`～`0.5` 已完成；RoadGraph 文档校准阶段和两层 schema 版本门禁已经闭合。
- `save-system:0.4`、`save-system:0.9`、`save-system:0.11`、`save-system:1.1`、`save-system:1.2` 与 `save-system:1.4` 已完成；`save-system:0.10` 核心实现已通过，仍缺只读 ACL 与真实 Windows 导出包证据；`save-system:1.3` 的独立自动存档策略和 `6.3` 的存档参考文档重写尚未开始。
- `road-graph:4.1`～`4.4` 已完成；P4 删除事务和 P5 最小图不变式基线已经闭合。
- 当前会话未提供 `csharp-ls` MCP，无法执行逐文件 C# LSP 诊断；编译器、xUnit 分析器和解决方案测试均已通过。
- Godot headless 扫描存在 imgui GDExtension、系统证书、端口占用和编辑器设置写入错误；本轮不涉及场景或资源集成，未将该额外扫描视为 Tier 2 通过。
- RoadType 移除批次属于 RoadGraph 与存档运行时契约变更，按 Tier 3 执行完整自动化及 Godot 主场景存档运行时契约。

## 相关文件

- `Scripts/Road/RoadGraph.cs`：RoadGraph 权威写入链、折线路径前置校验、节点身份和 Group 边界。
- `Scripts/Road/SpatialIndex.cs`：原生几何空间引用、包围盒 bucket 占据和圆查询过滤。
- `Scripts/Road/RoadGraph.Diagnostics.cs`：性能计数与提交后图不变式断言。
- `Scripts/Road/GraphNode.cs`、`Scripts/Road/RoadGroup.cs`、`Scripts/Road/GraphEdge.cs`：只读实体集合、防御性快照和损坏端点失败契约。
- `Scripts/Road/RoadGraph.Persistence.cs`：严格 V2 Node/Edge/Group DTO、原生几何捕获、全量预检与事务式恢复。
- `Scripts/Core/SaveManager.cs`、`Scripts/Core/SaveSlotStore.cs`、`Scripts/Core/SaveData.cs`：内部槽位 ID、玩家显示名、manifest 和存储路径边界。
- `Scripts/UI/PauseMenu.cs`、`Scenes/UI/PauseMenu.tscn`：命名槽列表、摘要、另存为、确认操作、损坏槽约束和键盘焦点。
- `Scripts/UI/GameHUD.cs`：向暂停菜单注入 `SaveManager` 并协调暂停与场景切换。
- `tests/SimpleCities.RoadGraph.Tests/SaveManagerSlotContractTests.cs`：自由显示名、重复名称、路径穿越、不可写位置和槽位文件契约。
- `tests/SimpleCities.RoadGraph.Tests/PauseMenuContractTests.cs`、`tests/godot/pause_menu_runtime_contract.gd`：存档管理源码边界、确认/取消路径、键盘和标准控件交互及小视口运行时回归。
- `Scripts/Road/RoadGraph.PathSubmission.cs`：原生 RoadPath 校验、端点解析、覆盖判定和权威 Edge 写入。
- `Scripts/Road/GraphEdge.cs`：只读连续原生几何段、派生长度和折线兼容视图。
- `Scripts/Road/RoadPathSubmissionResult.cs`：路径提交状态、完整变更摘要和结构化失败原因。
- `Scripts/Road/RoadPath.cs`：不可变原生几何段请求快照。
- `Scripts/Road/Geometry/RoadGeometrySegment.cs`：原生几何类型、统一参数域和拆分契约。
- `Scripts/Road/Geometry/LineRoadGeometrySegment.cs`：权威直线几何实现。
- `Scripts/Road/Geometry/CubicBezierRoadGeometrySegment.cs`：权威 cubic Bézier 控制点和几何查询实现。
- `Scripts/Road/Geometry/CubicHermiteRoadGeometrySegment.cs`：端点导数样条及保留类型的解析拆分。
- `Scripts/Road/Geometry/CircularArcRoadGeometrySegment.cs`：有符号圆弧参数、解析查询和同类型拆分。
- `Scripts/Road/Geometry/ClothoidRoadGeometrySegment.cs`：弧长参数化线性曲率缓和曲线和误差受控积分。
- `Scripts/Road/Geometry/RationalQuadraticRoadGeometrySegment.cs`：正权重一般圆锥曲线、解析极值和齐次拆分。
- `Scripts/Road/Geometry/RoadGeometrySerialization.cs`：原生几何版本化 DTO、JSON 映射和结构化拒绝结果。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphRegressionTests.cs`：拓扑、删除和 Group 边界回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphInvariantTests.cs`：单删、整组删、拆边、合并和拒绝路径的不变式回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphEncapsulationTests.cs`：稳定遍历快照、实体集合封装和损坏 Edge 路径回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphNodeIdentityTests.cs`：节点吸附半径、多候选和存档恢复回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphContinuousSpaceTests.cs`：任意 R² 折线路径和数据层分层契约。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphPathSubmissionTests.cs`：结构化路径校验、失败原子性和兼容入口回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadPathSubmissionChangeSummaryTests.cs`：请求不可变性、实体差异和失败空摘要回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGeometrySegmentTests.cs`：直线几何查询、校验和无损拆分契约。
- `tests/SimpleCities.RoadGraph.Tests/RoadGeometrySerializationTests.cs`：直线/cubic Bézier 语义往返及无部分对象失败契约。
- `tests/SimpleCities.RoadGraph.Tests/GraphEdgeGeometryTests.cs`：GraphEdge line 段转换、cubic 控制参数保留、只读边界和恢复回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphPersistenceV2Tests.cs`：schema 字段、六类原生几何往返、损坏内容拒绝和失败原子性回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphNativePathSubmissionTests.cs`：六类原生路径提交、吸附、失败原子性、事件、覆盖语义和存档联动回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGeometryClosestPointTests.cs`：六类几何最近点解析/数值收敛、端点和非法参数回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGeometryPointLocationTests.cs`：六类几何点上判定、端点分类和容差回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGeometryLineIntersectionTests.cs`：直线与原生几何的交叉、接触、相切、重叠和参数回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGeometryCurveIntersectionTests.cs`：曲线-曲线单/多交点、相切、接触、分离、重叠和参数顺序回归。
- `Scripts/Road/Geometry/RoadGeometrySubdivision.cs`：原生几何多参数拆分和全局参数范围结果。
- `tests/SimpleCities.RoadGraph.Tests/RoadGeometrySubdivisionTests.cs`：六类几何多参数拆分、类型保持和参数映射回归。
- `Scripts/Road/RoadGraph.NativeSubdivision.cs`：Edge 级拆分参数归一化、原生子段重组和拓扑替换。
- `Scripts/Road/EdgeGeometrySplitPoint.cs`：内部段索引与局部参数拆分请求。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphNativeEdgeSubdivisionTests.cs`：六类原生 Edge、多参数、多段、拓扑、索引、事件和存档回归。
- `Scripts/Road/RoadGraph.NativePathIntersections.cs`：原生路径与既有 Edge 的离散交点预计算及双方参数计划。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphNativePathIntersectionSubmissionTests.cs`：六类曲线交叉、多交点、相切、端点、事件、摘要和存档集成回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphNativePathOverlapSubmissionTests.cs`：六类同向/反向重叠、完全覆盖、部分覆盖和复合锚定回归。
- `tests/SimpleCities.RoadGraph.Tests/RoadGraphCurveSpatialQueryTests.cs`：六类原生几何的最近 Edge、空间索引重建和删除回归。
- `tests/SimpleCities.RoadGraph.Tests/CubicHermiteRoadGeometrySegmentTests.cs`：Hermite 参数、查询、拆分、拒绝和序列化回归。
- `tests/SimpleCities.RoadGraph.Tests/CircularArcRoadGeometrySegmentTests.cs`：圆弧方向、整圆、包围盒、参数域、拆分和序列化回归。
- `tests/SimpleCities.RoadGraph.Tests/ClothoidRoadGeometrySegmentTests.cs`：曲率、退化解析、拆分、积分边界和序列化回归。
- `tests/SimpleCities.RoadGraph.Tests/RationalQuadraticRoadGeometrySegmentTests.cs`：圆锥曲线、权重不变性、极值、拆分和序列化回归。
- `docs/todo/road-graph.md`：第二代 RoadGraph 执行顺序和完成证据。
- `docs/bugfix/road-graph.md`：`road-graph:BUG-15`～`road-graph:BUG-18` 的规范修复记录。
- `docs/bugfix/save-system.md`：`save-system:BUG-3` 的事务式恢复修复记录。
- `docs/todo/save-system.md`：第二代道路存档 schema、版本策略和后续槽位工作。

## 后续建议

1. 推进 `save-system:1.3` 的独立自动存档策略，定义周期触发、保留槽标识及与手动命名槽的隔离契约。
2. 在具备导出模板时补做 `save-system:0.10` 的真实 Windows 导出包与只读 ACL 验证；随后完成 `save-system:6.3` 的存档参考文档重写。

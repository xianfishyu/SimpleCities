# 2026-08-13 会话整理：第三代道路系统设计与路线图

> 作用域：SimpleCities 项目本地会话记录
> 存放位置：`docs/session-notes/`
> Git 状态：`docs/session-notes/` 已被 Git 跟踪；本记录与第三代道路指南、路线图及 V2/V3 接线文档联合提交
> 工作分支：V2 边界校准已提交到 `feat/road-network-v2`；V3 文档位于 `codex/road-system-v3`，两个分支均未配置 upstream

## 会话目标

审阅当前道路系统、连续道路存储和存档实现，形成第三代道路系统迭代指南；将一条道路从单位长度碎片重构为结构边界之间的最大连续 Edge，覆盖拐弯、自环、独立环、平行 Edge、自交路径、存档迁移、异步保存/加载和编辑历史；最后把 V2 与 V3 的长期工作项完全分开。

## 已完成

- 在 `docs/manuals/road-system-v3-gen.md` 编写第三代道路系统迭代指南，覆盖 canonical Edge、endpoint-role incidence、self-loop/parallel Edge、固定 loop seam、RoadGroup 移除、RoadType、渲染、输入、存档 schema 2、迁移、发布恢复、Load 原子接管、性能边界和 Phase 0～8。
- 在 `docs/todo/v3/` 建立独立 V3 路线图索引和五份系统路线图：`v3-road-graph`、`v3-save-system`、`v3-grid-rendering`、`v3-tool-input`、`v3-ui`，共 23 个活动工作项。
- 将根层 D5.1～D5.3、原问题 9 的平行边策略和 P6.5 改为无复选框的“已取代”历史指针；所有可执行 V3 道路工作只存在于 `docs/todo/v3/`。
- 保留根层 `traffic-simulation:P6.1`～`P6.4`，并明确它们属于 V3 完成后的交通模拟，不计入 V3 道路里程碑。
- 修复 `docs/README.md` 对已删除 `docs/todo/ui.md` 的引用，改为导航 V3 独立路线图索引。
- 在 `feat/road-network-v2` 创建提交 `b09a084`（`修订：明确第二代道路系统后续范围`），只包含 `docs/manuals/road-system-v2-gen.md` 的五处 V2 边界校准；随后从该提交创建 `codex/road-system-v3` 分支。

## 已验证

- 本次变更文档的本地链接和片段链接全部有效；V3 的 23 个状态表行、23 个活动项和精确锚点一一对应。
- 205 次直接 `v3-*:<id>` 引用及 55 个范围终点均能解析到真实工作项；V3 指南与路线图中不存在应带而未带 `v3-` 前缀的引用。
- `docs/` 下 60 份 Markdown 通过严格 UTF-8、无 BOM 检查；第三代指南的 2 个 JSON 示例可解析，20 个代码围栏成对闭合。
- `git diff --check` 通过，仅报告四份根层 todo 的 CRLF 到 LF 提示，没有 whitespace error。
- V2 暂存提交前检查确认只有 `docs/manuals/road-system-v2-gen.md`，且不引用尚未存在于该分支的 V3 文件。
- 本次只修改文档，未运行 Godot/C# QA，也未宣称任何 V3 运行时代码、构建或场景验收完成。

## 重要决策

- `GraphEdge` 表示两个结构边界之间的最大连续道路；转弯和原生几何段边界不自动产生 Node，局部查询通过不持久化的半开 query fragment 保持有界。
- self-loop 使用固定 topology seam 根化；禁止跨 seam 合并或旋转 geometry 数组。line 仅在公共点 bit 相同、精确 `Orient2D == 0` 且 `DotSign > 0` 时合并；full-turn 只接受精确 binary32 `+/-Tau`。
- 图邻接以 endpoint-role incidence 表达，自环贡献两个 incidence；平行 Edge 和 self-loop 是合法领域拓扑，模拟层只能定义映射，不能反向禁止。
- `RoadGraphRevision` 不可变并结构共享；局部编辑不得深拷贝远端全图。撤销/重做保存有界可逆 delta，不保存完整 JSON 副本。
- Load 在 Preflight 预建 graph、tool、隐藏 Mesh/RID、surface 和 hit index，并在一次 non-yield commit 中联合交换；关键表现失败只能发生在提交前，提交后只有普通 observer warning。
- `PublishV2`、`ResolveLegacyState` 和 `Load` 是三个独立事务、token 与结果。Resolve 只改磁盘，不加载、不改 `CurrentSlotID`；Load 只读磁盘，不移动、归档、升级或回写槽。
- V2 与 V3 Git 历史按用户要求分开：V2 分支只保存 V2 边界事实；V3 指南、路线图、根层迁移指针、交通模拟接线和本会话记录在新 V3 分支提交。

## 未完成与阻挡

- V3 的 23 个路线图工作项均尚未实现；本会话只完成设计、分解、所有权和验收契约。
- 未执行任何 V3 C#、Godot 场景、运行时、性能或存档迁移验证；这些验证必须在对应工作项实现后按路线图执行。
- 仓库全量文档检查仍发现一条既有失效片段：`docs/reference/class-reference.md` 第 21 行指向 `#11-兼容性-dto-词汇说明`，实际标题锚点是 `#11-道路存档词汇说明`。该文件本次未修改，问题不属于 V2/V3 拆分范围。

## 相关文件

- `docs/manuals/road-system-v3-gen.md`：第三代架构、算法、存储、存档和分阶段验收主指南。
- `docs/todo/v3/README.md`：V3 唯一路线图索引与全局 Phase 依赖。
- `docs/todo/v3/road-graph.md`：连续 Edge、环路、RoadType、事务与最终集成。
- `docs/todo/v3/save-system.md`：schema 2、V2 迁移、发布恢复与聚合 Load。
- `docs/todo/v3/grid-rendering.md`：canonical Edge、环路、分级表面和表现接管。
- `docs/todo/v3/tool-input.md`：闭合输入、道路改造、可逆历史和 full reset。
- `docs/todo/v3/ui.md`：RoadType、改造工具、诊断与保存/加载 UI。
- `docs/manuals/road-system-v2-gen.md`：第二代已完成边界及第三代之后的明确排除项。

## 后续建议

1. 从 `v3-road-graph:8.0` 开始建立数值、容量、确定性和可复用测试基础设施，再按 V3 索引的 Phase 依赖推进。
2. 在进入 schema 2 实现前冻结合法 V2 schema 1 byte fixture、损坏样例和迁移拒绝矩阵，避免迁移代码静默修复非法历史数据。
3. 实现阶段继续保持 V2 生产路径完整，直到 schema、renderer、save/tool provider、UI 和旧 API 消费者全部准备好后一次 cutover。

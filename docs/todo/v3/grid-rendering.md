# 第三代网格渲染系统待办清单

> 系统 key：`v3-grid-rendering`
> 整理日期：2026-08-23
> 证据：当前工作区 `Scripts/Road/RoadGeometryDisplaySampler.cs`、`RoadRenderer.cs`、`RoadBuilder.cs`、`RoadConfig.cs`，V2 显示与性能契约，`docs/performance/road-rendering-v2-baseline.md`、`docs/manuals/road-system-v2-gen.md` 附录 D 及 `docs/manuals/road-system-v3-gen.md`。
> 主导原则：负责第三代 canonical Edge、self-loop、平行 Edge 和 RoadType 的确定性可视化，生成与实际 mesh 同源的道路表面命中，并为普通 mutation 与 Load 提供各自正确的表现接管协议；视觉样式和派生表面不是 RoadGraph 的事实来源。

## 状态总览

| ID | 发现 | 当前状态 | 处置方式 |
|---|---|---|---|
| 2.0 | V2 renderer 不理解 self-loop incidence 与固定 loop seam | 已完成 | closed ribbon、复杂环路与 seam 重定位，以及平行 Edge 独立命中、拆除、改造、高亮、缩放、同槽重建和完整点选/框选回放矩阵均已验证 |
| 2.1 | `RoadConfig` 没有完整且可验证的四类 RoadType 样式 | 已完成 | 四类 `RoadTypeStyle`、唯一覆盖、严格查询和运行时校验已验证 |
| 2.2 | 单一全局样式的开放 ribbon 不能形成可命中的混合宽度完整路面 | 开放（部分实现） | 分级批次、六分量 token、同代四类 surface、canonical `RoadLocation`、基础空间索引、Junction Patch、普通 mutation stalled/retry 与 stalled-observer 隔离、现有道路事务 admission、deferred continuation 代际失效、真实 `RoadGraph`/slot-target commit-boundary generation、`RoadRenderer` admission、直接 aggregate RoadGraph/renderer/tool/slot-target plans 与真实四参与者 `StartLoad()` RoadGraph/renderer/tool/slot-target commit-boundary generation 失配、ToolManager scene-generation 取消、fake participant observer/cleanup 组合、真实 `RoadGraph` observer/fake companion cleanup 组合、真实 renderer observer warning 与 graph/tool/presentation/slot cleanup 再接管、预提交资源清理、四个已定位 Resource 所有权点、普通更新 token Resource Preflight、真实 `CreateNodeBatch`/`CreateRoadMesh` 工厂故障、`RoadSurfaceSnapshot` 构造故障与 pre-commit `RenderRequestID`/`RoadStyleRevision`/`SceneGeneration`/`GraphFacadeGeneration`/`ChangeSequence`/`GraphFacadeID` 六分量取代、真实 aggregate Load 的 renderer admission 样式快照、参数校验、reservation 后构造前故障、admission 构造后发布前故障、admission 发布并返回后、participant capture 后、`Prepare` phase 推进后、worker-entry、slot preparation 后、graph-state lookup 后、worker-Prepare、renderer worker preparation 后、prepared work 返回主线程后、renderer Resource Preflight、reserved render token 创建故障、`RoadSurfaceSnapshot`/`RoadRendererLoadCommitPlan` 构造故障与 `CreateRoadMesh`/`CreateNodeBatch` 工厂故障、renderer plan 加入后与 slot plan 加入后的后续 aggregate Preflight fallback disposal，以及 aggregate ownership 后的 pre-commit disposal 已建立；其余更新 token renderer 故障与真实 aggregate 组合矩阵仍开放 |
| 2.3 | 混合类型、full reset 和批量改造没有 V3 性能与视觉门禁 | 开放（部分实现） | 10k 连续帧与离散改造门禁、Load 分段、普通 mutation 表现分段、non-aggregate full-reset barrier 基线、`RenderRequestID`/`RoadStyleRevision`/`SceneGeneration`/`GraphFacadeID`/`GraphFacadeGeneration`/`ChangeSequence` 六分量分级性能扰动、四个关键 Resource 所有权点、普通更新 token Resource Preflight 与 stalled-observer 隔离、真实 `CreateNodeBatch`/`CreateRoadMesh` 工厂故障、`RoadSurfaceSnapshot` 构造故障与 pre-commit `RenderRequestID`/`RoadStyleRevision`/`SceneGeneration`/`GraphFacadeGeneration`/`ChangeSequence`/`GraphFacadeID` 六分量取代、真实 aggregate Load 的 renderer admission 样式快照、参数校验、reservation 后构造前故障、admission 构造后发布前故障、admission 发布并返回后、participant capture 后、`Prepare` phase 推进后、worker-entry、slot preparation 后、graph-state lookup 后、worker-Prepare、renderer worker preparation 后、prepared work 返回主线程后、renderer Resource Preflight、reserved render token 创建故障、`RoadSurfaceSnapshot`/`RoadRendererLoadCommitPlan` 构造故障与 `CreateRoadMesh`/`CreateNodeBatch` 工厂故障、renderer plan 后续与 slot plan 后续 fallback disposal、aggregate ownership 后的 pre-commit disposal，以及真实 RoadGraph/renderer/tool/slot-target plans 直接 aggregate 与真实四参与者 `StartLoad()` RoadGraph/renderer/tool/slot-target commit-boundary generation 失配已建立；六分量 token 性能扰动已完成，继续完成其余 renderer/真实 aggregate 组合故障注入 |

### 设计覆盖矩阵

| 设计范围 | 当前事实 | 关联待办 |
|---|---|---|
| canonical Edge、self-loop 与平行 Edge | 普通与 Load mesh 已按 `NodeA == NodeB` 生成 closed ribbon，并以 A/B incidence 隐藏纯 loop seam；两路口环、八字形和支路删除后 seam 重定位已有回归，独立 payload 已验证平行 Edge 的不同 surface owner、拆除/改造、高亮、缩放、同槽重建及点选/框选 undo/redo 回放均保持 owner 独立 | 2.0、`v3-road-graph:8.1`～`8.3` |
| RoadType 与完整道路表面 | 普通 rebuild 与 Load preparer 已从同一不可变 `RoadTypeStyleSnapshot` 为每条 Edge 写入宽度和 vertex color，并生成同源 `EdgeRibbon`、degree-1 `TerminalCap`、degree-2 `SemanticJoin` 与 degree≥3 `JunctionPatch`。四类 primitive 共用不可变 AABB 索引并返回 canonical `RoadLocation`；Junction Patch 使用固定量化、确定 incidence sector 与 Clipper2 union/triangulation | 2.1～2.2、`v3-road-graph:8.4`～`8.5` |
| 表现事务与 Load 原子接管 | 普通 mutation 复用 Load preparer，只为 created/updated Edge 重采样，并在完整 mesh/node batch/四类 surface/index 交换后推进 matching presented token；失败时保留上一代完整表现、以同一 desired token 进入可诊断 stalled 并允许重试，provider 在 pending/stalled 时拒绝 hit。Load worker 预建 `RoadSurfaceSnapshot.PreparedData`，Preflight 创建 `ArrayMesh`/`MultiMesh` 并只绑定 reserved token；未转交资源会在创建异常、Preflight 失败或 plan 放弃时确定性释放，renderer plan 已加入但 aggregate 尚未接管时的后续异常由 orchestration fallback 释放已有 plans，aggregate commit 后则由表现层持有。placement、拆除与改造会话均冻结并复核 provider token，undo/redo 在执行前即时复核同一门禁。普通 mutation 的 deferred 批次重建捕获独立 continuation generation，同步 flush/reset 后旧 callable 失效；fake participant 的 observer/cleanup 组合、真实 `RoadGraph` observer/fake companion cleanup 组合、真实 renderer observer warning 后的 graph/tool/presentation/slot cleanup 再接管、真实 RoadGraph/renderer/tool/slot-target plans 与各自 tracking/fake companions 的直接 commit-boundary generation 原子拒绝，以及真实四参与者 `StartLoad()` 的 RoadGraph/renderer/tool/slot-target 同边界原子拒绝均已验证 | 2.2、`v3-save-system:2.3`、`v3-tool-input:2.4` |
| V2 显示与规模基线 | 六类原生几何已有统一只读显示采样；统一样式的 10k Edge 已通过 60 FPS 门槛并记录 100k 压测 | `grid-rendering:1.1`～`1.2`（V2 已完成）、2.3 |

## 执行顺序

### 阶段 2：第三代道路表面、分级表现与接管门禁

<a id="v3-grid-rendering2.0"></a>

- [x] **2.0 正确渲染 canonical Edge、self-loop 与平行 Edge**
  - 完成前问题：基础 closed ribbon、纯 seam 标记、两路口环、八字形和支路删除后的 seam 重定位需要与平行 Edge 的独立命中和工具接管同时成立；早期证据还缺完整点选/框选回放矩阵。
  - 修改：消费 `EdgeIncidence.Endpoint`；self-loop 使用循环相邻方向生成无裂缝 closed ribbon，seam 不绘制 endpoint/junction 标记；degree 1 绘制 endpoint，degree 大于等于 3 的最终表面由 2.2 junction patch 负责，degree 2 的 seam/semantic boundary 不画伪节点。高亮、矩形选择和中心线命中不得按端点对去重平行 Edge。
  - 依赖：`v3-road-graph:8.1`～`8.3`。
  - 集成负责人：`v3-grid-rendering`；端到端完成判定由 `v3-road-graph:8.6` 负责。
  - 验证：非共线多段 Edge、简单环、全圆原生弧、棒棒糖、两路口环、八字形、支路删除后的 seam 重定位、平行 Edge 独立命中/拆除/改造/高亮，以及缩放/重建截图。
  - 验收：闭环首尾无裂缝、端帽或伪节点；junction/endpoint 数与 incidence 拓扑一致；同端点平行 Edge 均可见且可独立命中。
  - 阶段进展（2026-08-14）：`AppendRoadRibbon` 现在显式消费 Edge 的闭合拓扑，保留首尾重复的显示点列供中心线/高亮使用，但 mesh 只为唯一逻辑点生成顶点；首点用循环前后方向计算 miter，最后一段索引回连首点。普通 mutation 与 `RoadRendererLoadPreparer` 共用该实现；只有同一 self-loop Edge 的 A/B 两个 incidence 才视为纯 seam 并隐藏，self-loop 加支路仍是 junction。
  - 当前证据（2026-08-15）：`RoadRendererLoadPrepareTests` 继续覆盖方形 rooted loop、`+Tau` 全圆弧、棒棒糖、两路口环、八字形、删除支路后的 seam 重定位、开放 ribbon 布局和 worker/direct 确定性；`RoadGraphClosedPathV3Tests.RemoveEdge_TwoJunctionLoopRelocatesSeamToRemainingJunction` 直接锁定领域 seam 重定位。完整自动化为 813/813。真实 `MapTest` 的普通提交和 aggregate Load 都得到 `1 Edge / 8 mesh vertices / 0 node markers`；两路口环在普通与 Load 中均为 `4 Edge / 38 vertices / 2 markers / 20 surface primitives`，删除 seam 侧支路后为 `2 Edge / 21 vertices / 1 marker / 14 surface primitives`。`road_input_strategy_runtime_contract.gd` 与 `road_closed_ribbon_runtime_contract.gd` 均输出 PASS；截图显示原 seam 无伪标记，degree≥3 结点由 patch 覆盖而不再绘制圆形 marker。
  - 验收结果：闭环首尾无裂缝且不产生伪节点；junction/endpoint 数与 incidence 拓扑一致；同端点平行 Edge 均可见、可独立命中，并在点选/框选拆除与 RoadUpgrade 后通过 undo/redo 保持各自 owner 与 RoadType。
  - 阶段进展（2026-08-21）：`RoadRenderer` 将静态 `_roadBatchLayer` 放到父级 `_Draw()` overlay 之下，并提供 `SetHoveredEdgeID/ClearHoveredEdgeID` 绑定入口。真实 Vulkan 平行 Edge 契约先复现 base/first/second 高亮图区域差值全为 `0`，修复后 first/second 选中区域差值分别为 `62.125491/36.211765`，交叉区域均为 `0`；`camera.zoom = 0.25/4.0/1.0` 不改变两条 Edge 的显示点列、mesh vertex count 或 surface owner，同槽 Load 重建后仍保持相同 geometry/owner，并写出 `.godot/qa-road-parallel-edge-visual.png`。契约输出 `PASS`，唯一 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`；该切片只收敛高亮、缩放和重建视觉缺口，不关闭完整工具矩阵。
  - 平行 Edge 完整工具矩阵（2026-08-21）：隔离 Vulkan `road_parallel_edge_runtime_contract.gd` 在同一端点对的两条非重叠多段 Edge 上，依次验证中心线命中、点选/框选拆除、undo/redo、点选/框选 RoadUpgrade、保存/undo/redo RoadType 往返，以及高亮、缩放和同槽 Load 重建后的 geometry/owner 稳定性；契约输出 `PASS`，只保留既有 `ConstructionDock: ToolManager.Instance is missing` warning。该证据关闭 2.0 的平行 Edge 工具矩阵，不替代 2.2/2.3 的资源故障、observer/cleanup 和完整性能矩阵。

<a id="v3-grid-rendering2.1"></a>

- [x] **2.1 建立四类 `RoadTypeStyle` 资源与校验**
  - 当前问题：`RoadConfig` 只有全局道路颜色和宽度，无法为稳定 RoadType 提供单一、可检查的展示映射。
  - 修改：增加以 RoadType 为显式 key 的 `RoadTypeStyle`，V3 首版只含展示名称、颜色和正有限宽度；`RoadConfig` 必须恰好覆盖 `Dirt`、`Street`、`Arterial`、`Highway` 且无重复。虚线、纹理、路肩和中央分隔带延期。
  - 依赖：`v3-road-graph:8.4`。
  - 集成负责人：`v3-grid-rendering`；端到端完成判定由 `v3-road-graph:8.6` 负责。
  - 验证：缺失、重复、空名称、非有限颜色、非正宽度、`.tres` 往返和场景启动诊断测试。
  - 验收：所有合法 Edge 类型都能确定查到唯一样式；无效资源不会静默产生不可见道路或修改图数据。
  - 完成实现（2026-08-14）：新增 `[GlobalClass] RoadTypeStyle`，只导出 `RoadType`、`DisplayName`、`Color` 与 `Width`。`RoadConfig` 默认及 `Scenes/road_config.tres` 均恰好配置 `Dirt / 土路 / #8A6652 / 14`、`Street / 街道 / #60727C / 20`、`Arterial / 主干道 / #D7A928 / 26`、`Highway / 高速道路 / #C84B3A / 32`；校验拒绝空集合、空引用、数量不为四、重复/未定义类型、空名称、非有限或透明颜色及非正/非有限宽度。`GetRoadTypeStyle` 对非法映射或非法目标严格抛错，不提供静默 fallback；`RoadRenderer._Ready()` 会报告非法样式资源，但本切片不提前改动 renderer 的全局宽度/颜色消费。
  - 完成证据（2026-08-14）：`RoadTypeStyleTests` 聚焦组合 23/23、完整自动化 749/749；Debug 与 `ExportRelease` build 均为 0 警告/0 错误，Roslyn compiler/analyzer 与新增 GDScript 契约均为 0 diagnostics。`road_type_style_runtime_contract.gd` 完成生产 `.tres` 的四类值校验、全部非法变体拒绝及 `ResourceSaver`/`ResourceLoader` 往返，并在真实 Vulkan `MapTest` 中确认 builder/renderer 共用生产配置、四类查询稳定、静态 renderer 节点仍为 2；editor error 与 DAP `stderr` 为空。

<a id="v3-grid-rendering2.2"></a>

- [ ] **2.2 在单道路批次中渲染并原子刷新差异化表面**
  - 当前问题：`RoadRenderer` 的普通 rebuild 与 Load participant 已按 Edge 消费四类宽度/颜色，并以六分量 desired/presented token 区分 scene、facade、full reset、graph change、样式和 render request；当前会生成同源 `EdgeRibbon`、degree-1 `TerminalCap`、degree-2 `SemanticJoin` 与 degree≥3 `JunctionPatch`，四类 primitive 均返回 canonical `RoadLocation`，点/矩形查询也已使用构造期不可变 AABB 层级限制局部候选。degree≥3 静态圆形 marker 已移除，混合宽度 T/X/锐角、self-loop 加支路和平行 Edge 使用同一确定 patch 输入。普通 mutation 已提供可观测 stalled/retry 与 provider 级 hit 门禁，placement、拆除、RoadUpgrade 和 undo/redo 已覆盖当前玩家道路事务入口；deferred 批次重建也已在同步 flush/reset 后按 continuation generation 失效；fake participant 的 observer/cleanup 组合、真实 `RoadGraph` observer/fake companion cleanup 组合、真实 renderer observer warning 后的 graph/tool/presentation/slot cleanup 再接管、真实 RoadGraph/renderer/tool/slot-target plans 与各自 tracking/fake companions 的直接 commit-boundary generation 原子拒绝，以及真实四参与者 `StartLoad()` 的 RoadGraph/renderer/tool/slot-target 同边界原子拒绝均已有契约。完整 renderer/Load 故障与性能矩阵仍未完成，因此本项保持开放。
  - 修改：保持一个道路 mesh，按 Edge 样式生成顶点位置和 vertex color；缓存点列仍只来自权威几何。Edge ribbon 在 Node incidence 的切线/half-width 截面以 butt cut 终止；degree 1 用稳定端帽，degree 2 semantic boundary 生成无洞宽度/颜色过渡，degree 大于等于 3 构造 junction patch。incidence 以 outward direction 的 exact half-plane/cross comparator 排序，self-loop A/B 都参与；在 `RoadNumericPolicy` 约束的整数坐标上用固定版本的 Clipper2（或经同等审计的成熟库）完成 offset/union，并用固定 triangulator 处理 canonical ring，固定量化误差、miter limit、bevel/round fallback、RoadType sector 优先级和 Edge ID 最终同值规则，不能依赖存储反向或字典遍历。mesh 构建同步产出不可变 `RoadSurfaceSnapshot`，覆盖 ribbon、cap、semantic join、junction patch 的稳定 owner，统一提供带完整 token、owner kind、Node/Edge/Endpoint、surface/centerline distance 和 canonical location 的 `RoadSurfaceHit` provider。
  - 接管协议：消费一次 `GraphChanged`：created/updated 只标记对应显示路径失效；removed 不得在目标构建成功前破坏上一代缓存，目标 prepare 从该事务的不可变 revision 生成新字典并在成功交换时自然移除；full reset 重建全部显示路径。`RoadRenderToken` 至少含 `SceneGeneration + GraphFacadeID + GraphFacadeGeneration + ChangeSequence + RoadStyleRevision + RenderRequestID`；后台结果只有完全等于 `DesiredToken` 才能在主线程一次交换 mesh、surface index 和 `PresentedRenderToken`。普通 mutation 可异步构建；desired/presented 不同时进入 `RoadPresentationStalled` 门禁并由 provider 拒绝 hit，失败保留上一份完整表现且允许诊断和重试。Load participant 必须在 Preflight 预建隐藏 Mesh/RID、surface snapshot、hit index 和不可抛交换 plan；任何关键创建或 generation 失败只在 commit 前返回，尚未转交的 Godot Resource 必须由创建路径或 plan 的幂等 cleanup 确定性释放。成功时由 aggregate non-yield commit 将 graph、empty tool/overlay root、Mesh/RID、surface/hit index、desired/presented token 与 `CurrentSlotID` 一次联合交换，提交后只允许普通 observer 产生 warning，不存在关键表现失败、表现重试或 `CommittedPresentationFailed` 分支。2.2 只负责 provider/participant，不把真实 RoadUpgrade 或四工具接线当作本项完成条件；纯类型更新不得反向改写几何。
  - 依赖：`v3-grid-rendering:2.0`～`2.1`、`v3-road-graph:8.4`～`8.5`、`v3-save-system:2.3`。
  - 集成负责人：`v3-grid-rendering`；Load 的工具接管由 `v3-tool-input:2.4` 协作，最终组合验收由 `v3-road-graph:8.6` 负责。
  - 验证：四类直线与六类曲线；相同/混合宽度的 T/X/锐角、近共线、self-loop 加支路、平行 Edge 和 semantic boundary；量化边界/坐标上限、miter fallback、canonical ring、三角形方向/NaN/洞；反转 Edge/扰动枚举后的像素与 owner 相同，按一一 ID 重命名构造等价图后像素相同且 owner 按同一映射等价；fake hit consumer 覆盖 cap/miter/junction/宽路边缘、重叠宽窄路、sector 平局和矩形 primitive；分别改变六个 token 分量；普通领域 commit 后接管成功/失败/持续门禁/重试；Load hidden resource 创建失败、generation 失配，以及 fake aggregate 将 graph/tool/mesh/surface/token/`CurrentSlotID` 一次交换后的 observer warning。
  - 验收：颜色/宽度可辨识且不改变几何 JSON；合法路面无洞、尖刺、翻转三角形、伪节点或端帽叠层，遍历/反向不改变像素或 owner，ID 重命名后的 owner 等价；provider 只返回与已呈现 mesh 同 token 的 hit，平行 Edge 不去重；一次图事务最多安排一次目标接管，六个 token 分量任一过期都不发布，mesh/surface/presented token 不混代。普通构建失败保持上一份完整表现并持续门禁，可在同一 desired token 或更新 token 上重试；Load 关键资源失败只发生在 Preflight 且活动 graph/tool/mesh/surface/token/`CurrentSlotID` 逐值不变，成功 plan 只执行一次联合引用交换，提交后无关键表现失败分支；静态节点数不随 Edge 数线性增长。
  - 阶段进展（2026-08-14）：`RoadRendererLoadPreparer` 已在后台从 prepared `RoadGraphRevision` 采样 Edge 点列、基础 open/closed ribbon 顶点/索引和节点 marker；`PreflightPreparedLoad` 在主线程创建未发布的 `ArrayMesh`/`MultiMesh`。`PreparedAggregateLoad` 只在全部 participant generation 与 render reservation 有效时，将这些资源、matching desired/presented `RoadRenderToken`、graph、空工具状态和槽目标一起交换，并在完成后发布 `PresentationReady(RoadRenderToken)`。移除 renderer 后执行 Load 的运行时回归会在 commit 前失败，且 `CurrentSlotID`、活动图 payload、undo/redo 与未完成 placement 均保持不变。
  - 当前证据（2026-08-14）：`RoadRendererLoadPrepareTests`、`PreparedAggregateLoadTests` 及完整 727/727 自动化通过；`road_renderer_lifecycle_runtime_contract.gd` 和 `road_closed_ribbon_runtime_contract.gd` 分别覆盖提交前 participant 失败与闭环基础 mesh 成功联合接管，后者还覆盖两路口环从 `4 Edge / 20 vertices / 4 markers` 到支路删除后 `2 Edge / 12 vertices / 2 markers` 的同代刷新。最新统一样式 Vulkan 数据记录在 2.3；这些证据只证明既有批次与基础 Load participant，没有证明 mixed-width surface 的 `2.2`/`2.3` 完成。
  - 分级 ribbon 进展（2026-08-14）：`RoadConfig.CaptureRoadTypeStyleSnapshot()` 在主线程严格校验并复制四类值；普通 `RebuildStaticBatches` 与后台 `RoadRendererLoadPreparer` 都按 `GraphEdge.RoadType` 解析同一值快照，以对应 half-width 和 vertex color 写入一个 `ArrayMesh`，道路层保持白色 modulate。Load 的 prepared payload 同步携带 `RoadColors`，Preflight 创建的隐藏 mesh 与普通 mutation 重建因而使用相同批次格式；样式资源不会进入 worker，也不会写回图或 JSON。
  - 分级 ribbon 证据（2026-08-14）：`RoadRendererLoadPrepareTests` 聚焦 9/9，直接覆盖四类型宽度/颜色、worker/direct 确定性、closed/open ribbon 数组等长；完整自动化 750/750，Debug 与 `ExportRelease` build 均为 0 警告/0 错误，Roslyn compiler/analyzer 和新 GDScript 契约为 0 diagnostics。真实 Vulkan `road_type_mesh_style_runtime_contract.gd` 先经 aggregate Load 发布 14/20/26/32 宽的四类单 mesh ribbon，再以普通 mutation 触发同批次重建，两条路径均 PASS；截图人工确认四类颜色/宽度可辨识。Godot MCP 主场景 smoke 的 editor error 与 DAP `stderr` 为空。
  - 表现 token 进展（2026-08-14）：新增严格六分量 `RoadRenderToken` 与纯 CLR `RoadPresentationTokenTracker`；每个 `RoadGraph` 分配进程内稳定且不回绕的 `FacadeID`。普通 graph change/full reset、renderer 改绑、scene generation 和显式 `RefreshRoadStyles()` 分别推进所拥有的身份；普通 rebuild 只在 desired 未过期时一次交换 mesh/node batch 后提交 presented。Load admission 只接受 current presentation，预留下一次 facade generation/request，并在 aggregate commit 中同时发布 matching desired/presented token；随后同一 full-reset `GraphChanged` 由 graph token 精确消重。
  - 表现 token 证据（2026-08-14）：`RoadRenderTokenTests` 覆盖六分量相等性、非法身份、普通/full-reset/改绑/scene/style 推进、Load 预留/失效/原子提交及 facade ID 稳定唯一；完整自动化为 763/763，Debug 与 `ExportRelease` build 均为 0 警告/0 错误，Roslyn compiler/analyzer 与 `road_render_token_runtime_contract.gd` 均为 0 diagnostics。真实 `MapTest` 通过 Godot MCP 逐步得到 `initial=(1,1,1,0,1,1)`、`mutation=(1,1,1,1,1,2)`、`style=(1,1,1,1,2,3)`、`Load=(1,1,2,2,2,4)`，每步 desired 等于 presented；Save、Load、删除测试槽和场景清理均成功。独立 GDScript runner 未执行，DAP 读取因 `Max client limits reached` 受阻。
  - Edge ribbon surface 进展（2026-08-14）：新增不可变 `RoadSurfaceSnapshot`、稳定 `RoadSurfaceOwner`/`RoadSurfaceHit` 数据契约，以及点命中和矩形 owner 查询。`AppendRoadRibbon` 每写入一个真实 mesh triangle 就同步写入一个同顶点、同顺序的 `EdgeRibbon` primitive；普通 rebuild 一次交换 mesh、surface snapshot 与 matching presented token，Load preparer 则在 worker 生成同一 surface 的 `PreparedData`，aggregate commit 再与 graph/tool/mesh/slot/token 联合交换。provider 在 desired/presented/snapshot token 任一不一致时拒绝查询；点命中按 surface distance、centerline distance、sector、Edge ID 和稳定 primitive 顺序破同值，矩形查询返回排序去重的 Edge ID。
  - Edge ribbon surface 证据（2026-08-14）：`RoadSurfaceSnapshotTests` 覆盖真实 triangle 内外距离、边界包含、稳定破同值、矩形接触、defensive copy 与非法输入；`RoadRendererLoadPrepareTests` 逐 triangle 对照 mesh index 和 owner，完整自动化为 774/774。Debug 与 `ExportRelease` build、Roslyn compiler/analyzer 和 `road_render_token_runtime_contract.gd` 均为 0 diagnostics。真实 `MapTest` 验证普通 mutation、样式刷新和 aggregate Load 后 `surfacePrimitiveCount=2`，点命中均为 `EdgeRibbon`、`surfaceDistance=0` 且 hit token 等于 desired/presented；Load 前新增道路的旧 surface 已消失，矩形查询只返回已加载 Edge。临时存档槽已删除，editor 无新增错误，DAP `stderr` 为空。
  - Canonical location 进展（2026-08-14）：`RoadGeometryDisplaySampler` 在保持公开点列 API 不变的同时，为内部 `RoadGeometryDisplayPath` 的每个可见区间记录 `GeometryIndex + ParameterStart/ParameterEnd`；普通 cache 与 Load prepared payload 携带同一 provenance。`RoadSurfaceTriangle` 对内部显示细分点和 geometry join 使用左闭右开的 location 所有权，只有开放 Edge 最后一段拥有 `t = 1`，self-loop 最后一段不拥有 seam 终点并由第一 geometry 的 `t = 0` 唯一表示；完全同值的 primitive 优先返回拥有 location 的候选。
  - Canonical location 证据（2026-08-14）：`RoadGeometryDisplaySamplerTests`、`RoadSurfaceSnapshotTests` 与 `RoadRendererLoadPrepareTests` 覆盖曲线显示 span 参数、location 插值、geometry join、开放 B 端、self-loop seam，以及普通/Load triangle provenance；完整自动化为 779/779。Debug 与 `ExportRelease` build 均为 0 警告/0 错误，Roslyn compiler/analyzer 与修改后的 GDScript 为 0 diagnostics。隔离用户目录的 `road_render_token_runtime_contract.gd` 输出 PASS；Godot MCP 逐步验证普通 mutation、样式刷新和 aggregate Load 后的 hit 均返回 `Edge 2 / geometry 0 / parameter 0.5`，且 render token 与 desired/presented 完全匹配。Save、Load、删除测试槽和临时用户目录均已清理，editor 无新增错误，DAP `stderr` 为空。
  - Surface 空间索引进展（2026-08-14）：`RoadSurfaceSnapshot` 为 defensive-copy triangle/disc 构建统一稳定 AABB 层级，叶容量为 8；点/矩形查询先裁剪 node 与 primitive bounds，再按 kind 执行精确 triangle/circle 判定和确定性破同值。查询使用有界 `stackalloc` 遍历栈，不分配候选数组。`RoadSurfaceSnapshot.PreparedData` 以分离数组保存两类 primitive 且不暴露可变状态；普通构造入口继续执行 defensive copy，Load worker 则在 `RoadRendererLoadPreparer.Prepare()` 内完成复制与建树，主线程 Preflight 只将 reserved token 绑定到 prepared 数据。
  - Surface 空间索引证据（2026-08-14）：`RoadSurfaceSnapshotTests` 与 `RoadRendererLoadPrepareTests` 合计 27/27；1,024 个远隔 quad 的点/矩形查询和 128 条 Load prepared 道路均把 primitive 候选限制为 2～8 个、精确测试限制为 2 个。完整自动化 782/782，Debug/`ExportRelease` 0 警告、0 错误，Roslyn/GDScript 0 diagnostics。隔离用户目录的 token 契约与 Godot MCP aggregate Load 均验证 matching token、`surfacePrimitiveCount=2` 和 `Edge 2 / geometry 0 / parameter 0.5`；editor 无新增错误且 DAP `stderr` 为空。测试槽、动态探针、临时用户目录和日志已清理；本切片未重跑 10k/100k Vulkan 性能契约。
  - Terminal cap 进展（2026-08-14）：degree-1 Node 现在从唯一 incidence 解析相邻 Edge 与端点切线，按 `RoadTypeStyle.Width / 2` 生成解析 `RoadSurfaceDisc`，并用相同样式宽度/颜色生成 marker；端点高亮也使用该样式半宽。`RoadSurfaceOwner.TerminalCap` 携带 Edge/Node/Endpoint，A/B 分别绑定 geometry `0 / t=0` 和最后 geometry `/ t=1`。普通 rebuild 与 Load worker 走同一 `CreateTerminalCapSurface`，junction marker 和纯 loop seam 行为不变。
  - Terminal cap 证据（2026-08-14）：`RoadSurfaceSnapshotTests` 与 `RoadRendererLoadPrepareTests` 聚焦组合 34/34，覆盖精确圆内外距离、矩形圆相交、非法半径/bounds、ribbon 同值优先、样式直径/颜色、A/B canonical location、普通/Load primitive 一致与 defensive copy；完整自动化 786/786。Debug/`ExportRelease` 0 警告、0 错误，Roslyn/GDScript 0 diagnostics。隔离运行时契约覆盖 mutation/style/Load；Godot MCP 验证单路 `surfacePrimitiveCount=4`、A/B `TerminalCap` 参数 `0/1` 及 matching hit/desired/presented token，editor 无新增错误且 DAP `stderr` 为空。1k Vulkan camera/preview/highlight P95 为 0.393/0.378/0.339 ms、renderer rebuild 135.918 ms、静态节点为 2，并输出 PASS；本轮未重跑 10k/100k。
  - Semantic join 进展（2026-08-14）：两条不同 Edge、不同 RoadType 的 degree-2 Node 现在读取实际显示 ribbon 的端点截面。非共线方向按 exact orientation 排序，并把外侧 bevel 缺口沿外边中点切成两个纯色三角形；同向方向按固定 RoadType 顺序和最大半宽生成三角 fallback；精确对向方向由 butt 截面直接闭合，不写零面积 primitive。每个 `SemanticJoin` triangle 携带稳定 Edge/Node/Endpoint、sector order 和固定 canonical endpoint `RoadLocation`，degree-2 marker 被移除。普通 rebuild 与 Load worker 共用 helper，mesh/surface triangle、颜色、索引、AABB 索引和 token 同代。
  - Semantic join 证据（2026-08-14）：`RoadSurfaceSnapshotTests` 与 `RoadRendererLoadPrepareTests` 聚焦组合 41/41，覆盖固定 location 与区间互斥、两侧 owner/矩形命中、Edge ID 重命名视觉不变、同向 fallback、精确对向无额外 primitive、普通/Load 结构一致及 degree-2 marker 移除；完整自动化 793/793。Debug/`ExportRelease` 0 警告、0 错误，Roslyn/GDScript 0 diagnostics。隔离 `road_render_token_runtime_contract.gd` 的混合边界 Load 发布 `4 ribbon + 2 cap + 2 SemanticJoin = 8` 个 primitive，只保留两个远端 cap marker，并验证 join hit 的 matching token、稳定 owner 和 canonical endpoint location。Godot MCP 的单路普通 mutation 保持 `surfacePrimitiveCount=4` 且 ribbon/cap 命中正常，editor 无新增错误且 DAP `stderr` 为空。本轮未重跑 10k/100k。
  - Junction Patch 进展（2026-08-15）：新增固定版本 Clipper2 2.0.0 的 `RoadJunctionTessellator`。合法坐标在固定 `1024` 比例上量化，单坐标误差上限为 `0.5 / 1024`；显示宽度严格限制在 `4 / 1024` 到 `1_000_000`。incidence 以 exact half-plane/orientation 排序，self-loop A/B 分别参与；同向 incidence 以 RoadType 降序、Edge ID 最终破同值。轮廓先以 Clipper2 integer union 规范化再三角化，miter 最长为最大 half-width 的 4 倍，锐角超限时使用有界 fallback。owner sector 由相邻边界弧长中点划分，每个 patch triangle 携带稳定 Node/Edge/Endpoint、sector 与 canonical A/B endpoint location；centerline 从量化后的 NodePosition 指向所属 incidence。普通 rebuild 与 Load worker 共用 `AppendJunctionPatch`，mesh、颜色、surface owner、AABB index 与 token 同代发布，并移除 degree≥3 静态圆形 marker。
  - Junction Patch 证据（2026-08-15）：`RoadJunctionTessellatorTests`、`RoadRendererLoadPrepareTests` 与生命周期契约覆盖 T/X、锐角、同向、self-loop、平行 Edge、反向存储、ID 重命名、量化边界、miter fallback、owner/location 和普通/Load 一致性；完整自动化 813/813，Debug/`ExportRelease` build 0 警告、0 错误，solution 与 9 个变更文件的 Roslyn compiler/analyzer、GDScript workspace scan 均为 0 diagnostics。隔离的两个运行时契约均 PASS；混合锐角 Load 发布 `10 ribbon + 2 SemanticJoin + 4 JunctionPatch + 5 cap = 21` 个 primitive、`38 vertices / 5 markers`，patch hit 的 token/owner/location 正确。Godot MCP 的普通 T junction 为 `3 Edge / 21 vertices / 3 markers / 12 primitives` 且 desired/presented 相等；截图无洞、尖刺或旧圆形 junction marker，最终 editor 与 DAP 错误通道为空。
  - 普通 mutation stalled/retry 进展（2026-08-15）：`RoadPresentationTokenTracker` 为当前 desired token 记录 attempt 与完整 `RoadPresentationFailure`，新请求会清除旧失败并拒绝迟到结果。普通 rebuild 复用 `RoadRendererLoadPreparer`；created/updated Edge 才重新采样，未变化点列与 provenance 数组按引用复用。只有 prepare、Mesh/MultiMesh 和 surface snapshot 全部成功且 graph/token 仍匹配时才一次交换缓存与表现；失败保留上一代 mesh、surface 和显示缓存，公开 `phase = stalled`、失败类型/消息与 attempt，并由 provider 拒绝查询。`RetryRoadPresentation()` 在同一 desired token 上重试，成功后才推进 presented。
  - 普通 mutation stalled/retry 证据（2026-08-15）：`RoadRenderTokenTests`、`RoadRendererLoadPrepareTests`、renderer 生命周期与 display sampler 契约覆盖 attempt、同 token 重试、新请求取代迟到失败、未失效点列复用和成功前零交换；当前完整自动化为 833/833。Debug 与 `ExportRelease` build 均为 0 错误，各有 1 条既有 `NU1900`（当前环境无法访问 NuGet 漏洞数据源）；Roslyn compiler/analyzer、变更 GDScript 与 workspace scan 均为 0 diagnostics。隔离用户目录的 `road_render_token_runtime_contract.gd` 通过真实非法样式依次验证 attempt 1 stalled、attempt 2 同 token 失败、修复后 attempt 3 同 token 成功，失败期间旧 edge/mesh/surface 数量保持但 surface 查询为空；测试槽、隔离目录和日志均已清理，editor 无新增错误且 DAP `stderr`/`console` 为空。headless 的 Windows root certificate store error 与缺少 `ToolManager.Instance` warning 为既有环境/夹具输出。
  - 拆除 provider 接管进展（2026-08-15）：新增内部 `IRoadSurfaceSelectionProvider`。`RoadBuilder` 的 hover、连续轨迹和 Shift 矩形框选只查询 current `RoadSurfaceSnapshot`；`RoadRemovalSession` 冻结完整 `RoadRenderToken`，expected token 不再同时匹配 desired/presented/snapshot 时清空稳定 Edge ID 集。`ConfirmRemove()` 在提交前另校验当前 graph 的 `FacadeID` 和 `ChangeSequence`；pending/stalled/superseded 状态不显示旧 hover/选择 overlay，也不产生 history。
  - 拆除 provider 接管证据（2026-08-15）：拆除/history/surface/renderer 聚焦组合 51/51，完整自动化 833/833，双配置 build 均为 0 错误且各有 1 条既有 `NU1900`，Roslyn/GDScript diagnostics 为 0。隔离的 `road_render_token_runtime_contract.gd` 与 `road_input_strategy_runtime_contract.gd` 均 PASS；Godot MCP 验证当前 surface 可选择、样式刷新后旧确认返回 `false` 且 undo 不变、当前 token 删除成功，以及 mutation pending 期间完整旧 edge/cache/mesh/surface 保留、两帧后空图一次发布。测试进程、槽、隔离目录与日志已清理，editor 与 DAP 错误通道为空。
  - 类型化建造接入证据（2026-08-15）：`v3-tool-input:2.1` 已完成 `SelectedRoadType`、会话冻结和显式提交；类型聚焦组合 60/60、当前完整自动化 833/833。隔离输入契约验证四类型保存/full-reset Load，Godot MCP 验证非默认类型提交后的 matching 分级 mesh；这项证据不等于 pending/stalled 全命令 admission。
  - RoadUpgrade 接管证据（2026-08-15）：独立改造会话冻结目标类型和完整 `RoadRenderToken`，连续/Shift 矩形选择只消费 current presented surface，确认前再次校验 graph facade/change sequence；旧 token、NoChanges、取消与 full reset 均清空改造选择和独立 overlay。RoadUpgrade 聚焦组合 17/17、完整自动化 833/833；隔离输入契约及 Godot MCP 已覆盖连续/矩形改造、semantic boundary 合并、单次 undo/redo、样式刷新拒绝旧 token，以及 active upgrade 上的真实 Load。
  - Placement 表现门禁证据（2026-08-20）：`RoadBuilder` 在开始 placement 时冻结完整 presented token，后续草稿操作和确认均复核 provider current 与 graph facade/change sequence；pending、stalled 或新样式请求取代 token 时清空 session/preview 且不修改图/history。`RoadInputStrategyTests` 20/20、完整自动化 840/840、双配置 build 0 警告/0 错误；隔离的 `road_render_token_runtime_contract.gd` 与 `road_input_strategy_runtime_contract.gd` 均 PASS。当前会话未暴露 Roslyn/Godot MCP 与 DAP，未把对应诊断、editor 或 DAP 门禁记为本轮通过。
  - 历史回放门禁证据（2026-08-20）：undo/redo 的可用性查询与实际执行均要求 provider desired/presented/surface token 和 graph facade/change sequence current；pending/stalled 时两栈与图保持不变，undo/redo 各自产生的普通 mutation pending 窗口会拒绝相反方向回放，matching presentation 发布后恢复。`RoadInputStrategyTests` 21/21、完整自动化 841/841、双配置 build 0 警告/0 错误；两个隔离道路运行时契约均 PASS。当前会话未暴露 Roslyn/Godot MCP 与 DAP，未把对应诊断、editor 或 DAP 门禁记为本轮通过。
  - Deferred continuation 进展（2026-08-20）：`ScheduleStaticBatchRebuild()` 为实际排队的普通表现回调捕获 `_staticBatchRebuildContinuationGeneration`；同帧图事件仍只保留一项工作，而 Load admission 同步 flush、普通 full reset、`SetGraph()`、`_ExitTree()` 与 aggregate commit 都推进 generation。旧 callable 即使稍后抵达，也不能清除或执行新 generation 的重建。
  - Deferred continuation 证据（2026-08-20）：`RoadGeometryDisplaySamplerTests + RoadRendererLifecycleContractTests` 16/16、完整自动化 842/842、双配置 build 0 警告/0 错误；隔离 token 契约在 pending mutation 后同栈启动 Load，验证 admission 先同步发布当前代，Load 后额外两帧仍保持新 lineage 的 matching mesh/surface/token。输入契约继续 PASS；隔离目录已清理，原有 Godot 进程未受影响。当前会话未暴露 Roslyn/Godot MCP 与 DAP，未刷新对应通道。
  - 连续 Load 证据（2026-08-20）：隔离 token 契约在同一真实场景和槽上背靠背完成两次 aggregate Load；第二次 graph facade generation、change sequence 与 render request ID 各精确推进一次，四类 surface primitive 的 hit token 全部等于第二次 desired/presented token。完整自动化保持 843/843、双配置 build 0 警告/0 错误；隔离目录和日志已清理，原有 Godot 进程未受影响。
  - 预提交资源生命周期证据（2026-08-20）：普通 rebuild 只在 mesh/node batch 都挂载后转移所有权，过期或异常路径由 `finally` 清理；资源工厂在构造异常时自清理；Load Preflight 在 plan 接管前失败会回收隐藏资源，未提交 plan 的幂等 `Dispose()` 负责资源与 admission，成功 commit 后不释放表现层资源。renderer 生命周期契约 10/10、完整自动化 846/846、双配置 build 0 警告/0 错误，两个隔离道路运行时契约均 PASS；见 `save-system:BUG-14`。当前会话未暴露 Roslyn/Godot MCP 与 DAP，生命周期故障注入脚本的既有 disposed-object 输出未计作干净 console 门禁。
  - 真实 generation 失配证据（2026-08-20）：`PreparedAggregateLoadTests` 9/9 通过受控 commit boundary 使真实 `RoadGraph` participant 的 admission 失效，确认 `LoadPreflightInvalidException` 在任何 reference swap 前返回，graph revision 与辅助计划状态不变，未提交计划清理一次且 admission 释放后 graph 可继续 mutation；完整自动化为 847/847，Debug 与 `ExportRelease` build 均为 0 警告、0 错误。xUnit 宿主无法初始化 Godot native Resource，本证据不覆盖真实 `ArrayMesh`/`MultiMesh` 生命周期。
  - Observer/cleanup 组合证据（2026-08-20）：三个 fake participant 同时触发 observer 与两个 cleanup 异常，`PreparedAggregateLoadTests` 10/10 验证所有引用先完成交换、三个 warning 按顺序聚合且后续 cleanup 不被阻断；完整自动化为 848/848，Debug 与 `ExportRelease` build 0 警告、0 错误。该证据只覆盖 CLR fake participant，真实 renderer/tool/slot participant 的 observer/cleanup 组合、逐关键 Load Preflight Resource 故障和 generation 失配仍未验证。
  - 真实 RoadGraph observer/cleanup 组合证据（2026-08-20）：`RealRoadGraphObserverFailure_IsolatedAlongsideOtherParticipantCleanupFailures` 让真实 `RoadGraph` observer 与 fake `presentation`/`slot` cleanup 同时失败，`PreparedAggregateLoadTests` 达到 11/11；graph、presentation、slot reference swap 先全部完成，graph observer、presentation cleanup、slot cleanup 三个 warning 按顺序聚合，两个 companion cleanup 仍继续执行，新的 graph root 保持可查询且 Edge 类型为 `Highway`。完整自动化为 849/849，Debug build 0 警告、0 错误。该证据只覆盖真实 RoadGraph 的 CLR observer 路径与 fake companion cleanup，不证明真实 renderer/tool/slot participant 的 observer/cleanup 组合、逐关键 Load Preflight Resource 故障或原生 `ArrayMesh`/`MultiMesh` 生命周期。
  - 全参与者代际失配补强（2026-08-21）：`AllParticipantGenerationMismatch_RejectsBeforeAnyReferenceSwap` 让 graph、tool、presentation、slot 四个 fake participant 中的 slot generation 失配；`PreparedAggregateLoad` 在任何 `CommitReferences()` 或 notification 前返回 `LoadPreflightInvalidException`，四方状态仍为旧值且事件序列为空。聚焦 `PreparedAggregateLoadTests` 为 12/12，完整自动化为 864/864；该切片只扩大 CLR aggregate 的原子拒绝证据，不替代真实 renderer/tool/slot generation 失配、原生 Resource 故障或 Vulkan 运行时矩阵。
  - 提交边界逐参与者失配补强（2026-08-21）：同一聚焦组合依次在 `CrossCommitBoundary` 内使 graph、tool、presentation、slot 任一 fake participant 失效；aggregate 对每个位置均在引用交换和通知前拒绝，聚焦 `PreparedAggregateLoadTests` 为 13/13，完整自动化为 865/865。该证据仍只属于 CLR aggregate 控制流，真实 renderer/tool/slot generation、原生 Resource 故障和 Vulkan 矩阵保持开放。
  - 真实 renderer admission 失配证据（2026-08-21）：`road_load_generation_runtime_contract.gd` 以 10k Edge worker Prepare 固定注入窗口，在真实 `RoadRenderer` admission 建立后将 renderer 移出场景树但保留实例；`_ExitTree()` 使 reservation/admission 失效，Load 在 renderer Preflight 返回 `Failed` 且 `committed=false`。旧 desired/presented token、retained surface primitive、道路 mesh 顶点、marker、graph payload、placement/history 与 `CurrentSlotID` 均保持，renderer 重新入树后可再次保存旧图。契约在两个独立 APPDATA 下连续 PASS；GDScript diagnostics、`--check-only`、Roslyn、Debug/`ExportRelease`、完整 865/865 自动化、editor error 和 DAP stderr/console 门均通过。该切片证明真实 renderer 的 Preflight 拒绝与未转交状态保持，不直接覆盖 slot-target commit-boundary、真实 observer/cleanup 或逐关键 Resource 故障。
  - 真实 slot-target 提交边界证据（2026-08-21）：`SaveManager.SlotTargetLoadCommitPlan` 同时冻结 scene 与 `_currentSlotGeneration`；直接契约在 `CrossCommitBoundary` 内只推进槽目标代际，aggregate 在所有 reference swap 和 notification 前拒绝，旧 graph/tool/presentation 与 `CurrentSlotID` 保持。`PreparedAggregateLoadTests` 14/14、完整自动化 866/866、双配置 build 与 Roslyn diagnostics 为 0，隔离 APPDATA 的真实 Load 契约和 `MapTest` smoke 通过。该 CLR 计划级证据不替代真实 renderer observer/cleanup 或 Resource 故障。
  - 真实工具状态快照可观测性补强（2026-08-21）：C# `Rect2?` 的 removal/upgrade selection bounds 不通过 GDScript `get()` 暴露，`RoadRenderer` 新增 `HasRemovalSelectionBounds()`/`GetRemovalSelectionBounds()` 与对应 upgrade getter；真实 Load 代际契约使用这些接口逐值比较矩形 bounds，并同时保留 preview、hover、mesh、surface 与 token。该 API 只增加测试/运行时状态观察，不改变渲染资源或提交语义。
  - 真实 renderer observer/cleanup 再接管证据（2026-08-22）：Debug-only `RoadLoadObserverFailureProbe` 为真实 `RoadRenderer.PresentationReady` 安装一次性抛错 observer；第一次 aggregate Load 仍以 `SucceededWithWarnings` 联合提交 graph/tool/presentation/slot，warning 精确归属 renderer，placement/history 被清空、presentation ready 且 matching、`CurrentSlotID` 指向目标槽。工具随后可切换，第二次 Load 无 warning 成功并恢复 1 Edge matching presentation，结构化结果为 `warning_result_kind=1`、`clean_result_kind=0`、`probe_trigger_count=1`、`rendered_edges=1`、`current_tool=2`。源码契约固定 renderer 逐 observer 隔离、tool notification 为空且完成 builder/admission cleanup、slot notification/cleanup 为 no-op。真实 Vulkan Forward+ 契约 PASS；生命周期聚焦 13/13、完整自动化 871/871、双配置 build、Roslyn compiler 与 GDScript diagnostics 为 0，两个 Debug-only probe 均不进入 `ExportRelease` 且被 QA export 排除。唯一 CLI warning 为既有 `ConstructionDock` 缺少 `ToolManager.Instance`；编辑器冻结 smoke ready/matching、静态节点 2，DAP stderr/console 与 editor 增量日志为空。
  - `RoadSurfaceSnapshot` Preflight 故障证据（2026-08-22）：Debug-only `RoadLoadPreflightResourceFailureProbe` 让真实 `PreflightPreparedLoad()` 在 4 个 road vertices 与 2 个 node markers 已分别生成 `ArrayMesh`/`MultiMesh` 后，以 null `PreparedData` 精确触发 `RoadSurfaceSnapshot` 构造期 `ArgumentNullException`。源码契约固定两个资源工厂、snapshot 与 plan 的先后顺序；真实 Vulkan Forward+ 契约中 `ObjectResourceCount` 为 `77 -> 77`，旧 road mesh/node batch/surface 引用、matching token、surface hit、RoadGraph payload、`CurrentTool`、placement/history 和 `CurrentSlotID` 逐值不变，随后真实 Load 返回 `Succeeded` 并联合接管空图。完整自动化 871/871，生命周期与 QA export 聚焦组合 14/14，双配置 build、Roslyn compiler 与目标 GDScript diagnostics 为 0；三个 Debug-only probe 均不进入 `ExportRelease` 且被 QA export 排除。编辑器冻结 smoke 为 ready/matching/2 static nodes，DAP stderr/console 与 editor 增量 error 为空；唯一 CLI warning 仍为既有 `ConstructionDock` 缺少 `ToolManager.Instance`。
  - 未提交 Load plan disposal 证据（2026-08-22）：同一 Debug-only probe 用有效 4-vertex road mesh、2-marker node batch 与 surface snapshot 创建真实 `RoadRendererLoadCommitPlan`，确认 plan current 后连续执行两次 `Dispose()`；plan 变为 stale，`ObjectResourceCount` 为 `77 -> 77`，旧 mesh/node batch/surface 引用及 graph/tool/token/hit/history/slot 状态逐值保持，随后真实 Load 返回 `Succeeded`。完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn compiler 与目标 GDScript diagnostics 为 0；probe 继续只进入 Debug 并被 QA export 排除。Vulkan Forward+ 契约 PASS，唯一 CLI warning 为既有 `ConstructionDock` 缺少 `ToolManager.Instance`；编辑器冻结 smoke 为 ready/matching/2 static nodes，editor 游标 700 后无新增 error。DAP 因已有客户端达到 8 个上限而未接通；该门禁不计作通过。
  - `CreateNodeBatch` 工厂故障证据（2026-08-22）：同一 Debug-only probe 直接调用真实工厂，传入 `Count=1`、索引读取固定抛出 `InvalidOperationException` 的测试列表；故障发生在 `MultiMesh` 已完成 transform/color 配置、marker mesh 绑定和实例数设置后的 marker loop。factory catch 释放 batch，真实 Vulkan Forward+ 的 `ObjectResourceCount` 为 `77 -> 77`；旧 presentation 引用及 graph/tool/token/hit/history/slot 状态保持，随后真实 Load 返回 `Succeeded`。完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn compiler 与目标 GDScript diagnostics 为 0；probe 继续只进入 Debug 并被 QA export 排除。唯一 CLI warning 为既有 `ConstructionDock` 缺少 `ToolManager.Instance`，editor 游标 700 后无新增 error；DAP 仍受 8-client 上限阻塞。
  - `CreateRoadMesh` 所有权边界证据（2026-08-22）：两个 factory 现以无捕获 static lambda 共用私有 `InitializeOwnedResource()`；源码契约固定 `new ArrayMesh()` / `new MultiMesh()` 接入该 helper，初始化抛错时由唯一 catch Dispose。Debug-only probe 对真实 `ArrayMesh` 注入进入初始化器后的 `InvalidOperationException`，Vulkan Forward+ 的 `ObjectResourceCount` 为 `77 -> 77`，旧 presentation 与 graph/tool/token/hit/history/slot 状态保持，随后真实 Load 返回 `Succeeded`；其余三个资源点继续通过。该证据不声称 Godot 引擎 error 一定转换成托管异常。完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn compiler 与目标 GDScript diagnostics 为 0；probe 仅进入 Debug 且被 QA export 排除。10k grid Load 为 `682.988 ms`，camera/preview/highlight P95 为 `0.508/1.679/0.822 ms`，静态节点 2，契约 PASS；唯一 CLI warning 为既有 `ConstructionDock` 缺少 `ToolManager.Instance`。编辑器冻结 smoke ready/matching/2 static nodes，DAP stderr/console 与 editor 游标 700 后增量 error 均为空，临时目录已清理。
  - 普通更新 token 纯 `Prepare` 故障证据（2026-08-22）：Debug-only `RoadRendererUpdateTokenFailureProbe` 在样式/revision snapshot 完成后、`RoadRendererLoadPreparer.Prepare(...)` 与任何 Godot Resource 创建前，为下一枚真实 graph mutation token 注入一次性 `InvalidOperationException`；probe 与普通 Resource Preflight、`CreateRoadMesh`、`CreateNodeBatch`、`RoadSurfaceSnapshot` 及 pre-commit supersession 五类探针互斥。真实 Vulkan Forward+ 的 attempt 1 推进 desired/stalled token，保留旧 presented/Edge/mesh/marker/surface/cache，provider 拒绝旧与未呈现 surface hit，且 `ObjectResourceCount` 为 `77 -> 77`；attempt 2 以同一 desired token 原子发布新增 Edge/surface。结构化结果为 `UPDATE_TOKEN_PREPARE_FAILURE_RESULT resource_before=77 resource_after=77 trigger_count=1 stalled_attempt=1 recovered_attempt=2`。`RoadRendererLifecycleContractTests` 39/39、生命周期与 QA export 聚焦组合 40/40、完整自动化 897/897，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics 及隔离 `--check-only` 均通过；正式 Vulkan 契约退出码为 0 并 PASS。Godot MCP 确认正确项目、addon/server 4.1.0 匹配与 `MapTest` 未运行，minimal stderr/console 为空且 editor 游标 1989 后无新增 error；Debug DLL 包含 probe/API，`ExportRelease` 不包含。本证据将普通更新相邻构建故障点由四个扩为五个；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。
  - 普通更新 token Resource Preflight 故障证据（2026-08-22）：Debug-only `RoadRendererUpdateTokenFailureProbe` 在 ready presentation 上布置一次性故障，partial hook 位于 `CreateRoadMesh()` / `CreateNodeBatch()` 完成后、surface 构造和表现提交前；`ExportRelease` 不包含 probe 实现、类型或注入消息。真实 Vulkan graph mutation 的 attempt 1 进入 stalled，desired 推进而 presented 保持旧 token，旧 mesh/node batch/surface/cache 引用均未交换，provider 拒绝旧 surface hit；未转交资源释放后 `ObjectResourceCount` 回到 `77`。attempt 2 以同一 desired token 重试并原子发布新 Edge/surface，结构化结果为 `resource_before=77 resource_after=77 trigger_count=1 stalled_attempt=1 recovered_attempt=2`。完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn compiler 与目标 GDScript diagnostics 为 0；编辑器冻结状态 ready/matching/2 static nodes，DAP stderr/console 与游标 710 后的 editor 增量 error 均为空。
  - 普通更新 token `CreateNodeBatch` 工厂故障证据（2026-08-22）：Debug-only `RoadRendererUpdateTokenFailureProbe` 为下一枚新 token 把 marker 输入替换为 `Count=1` 的抛错列表；真实 ordinary rebuild 已创建 road mesh，`CreateNodeBatch()` 再分配并配置 `MultiMesh`，marker loop 首次索引读取才抛出 `InvalidOperationException`。factory helper 释放 node batch，外层 `finally` 释放 road mesh。真实 Vulkan Forward+ 的 attempt 1 推进 desired、保留旧 presented/Edge/mesh/marker/surface 并进入 stalled，provider 拒绝旧 hit，`ObjectResourceCount` 为 `77 -> 77`；attempt 2 以同一 desired token 原子发布新增 Edge/surface。结构化结果为 `trigger_count=1`、`marker_read_count=1`、`stalled_attempt=1`、`recovered_attempt=2`。生命周期与 QA export 聚焦组合 21/21、完整自动化 878/878，双配置 build、Roslyn compiler/analyzer 与目标 GDScript diagnostics 均为 0；Debug DLL 包含 probe 类型、arming API、marker wrapper 与注入消息，`ExportRelease` 均不包含。Godot MCP 8 帧 smoke 读到道路系统与两类静态表现节点，minimal stderr/console 为空，editor 游标 767 后无新增消息。隔离 QA 根已清理，唯一 CLI warning 为既有 `ConstructionDock` 缺少 `ToolManager.Instance`。
  - 普通更新 token `CreateRoadMesh` 工厂故障证据（2026-08-22）：同一 Debug-only probe 为下一枚新 token 把 road index 输入替换为一次性抛错的 `IReadOnlyCollection<int>`；真实 `CreateRoadMesh()` 先分配 `ArrayMesh`，再在 `InitializeOwnedResource()` 内首次枚举 indices 时抛出 `InvalidOperationException`，helper 立即释放 road mesh，node batch 尚未创建。真实 Vulkan 1.4.341 Forward+ 的 attempt 1 推进 desired、保留旧 presented/Edge/mesh/marker/surface 并进入 stalled，provider 拒绝旧 hit，`ObjectResourceCount` 为 `77 -> 77`；attempt 2 以同一 desired token 原子发布新增 Edge/surface。结构化结果为 `UPDATE_TOKEN_ROAD_MESH_FAILURE_RESULT resource_before=77 resource_after=77 trigger_count=1 index_enumeration_count=1 stalled_attempt=1 recovered_attempt=2`。`RoadRendererLifecycleContractTests` 21/21、生命周期与 QA export 聚焦组合 22/22、完整自动化 879/879，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Vulkan 契约输出三个 update-token 结果并 PASS。Debug DLL 包含 probe 类型、road-mesh arming API、indices wrapper 与注入消息，`ExportRelease` 均不包含。Godot MCP 8 帧 smoke 读到道路系统与两类静态表现节点，minimal stderr/console 为空，editor 游标 767 后无新增消息；隔离 QA 根已清理，唯一 CLI warning 为既有 `ConstructionDock` 缺少 `ToolManager.Instance`。
  - 真实 aggregate Load renderer Resource Preflight 故障证据（2026-08-22）：同一 Debug-only probe 在真实 `StartLoad()` 的 renderer Preflight 中，于 `CreateRoadMesh()` / `CreateNodeBatch()` 完成后、render token/surface/plan 创建前注入一次性异常。operation 返回 `Failed`、`committed=false` 与精确错误，`ObjectResourceCount` 为 `77 -> 77`；活动 graph/tool/presentation/surface/token/hit/slot 逐值不变。随后同一源槽 Load 返回 `Succeeded` 并联合接管空 graph/tool/presentation/slot，证明所有 admission 与未提交 plan 均已释放。完整自动化 871/871、聚焦组合 14/14、双配置 build、Roslyn compiler/analyzer 与 GDScript diagnostics 为 0；Vulkan Forward+ 契约 PASS，Debug 包含 probe 实现而 `ExportRelease` 不含 hook 实现、probe 类型或注入消息，QA export 继续排除其源码；partial hook 声明名称不作为排除断言。唯一 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`；编辑器 smoke ready/matching/2 static nodes，DAP stderr/console 为空，游标 721 后无新增 error。
  - renderer plan 后续 aggregate Preflight fallback disposal 证据（2026-08-22）：同一 probe 通过 `SaveManager` partial hook 在真实 `StartLoad()` 已把 renderer commit plan 加入 `preflightPlans` 后、slot plan 创建与 aggregate ownership 之前注入一次性异常。operation 返回 `Failed`、`committed=false`；`RunLoadAsync()` 的 `finally` fallback 逐个 `Dispose()` 已有 graph/tool/renderer plan，释放 renderer admission 和未转交资源，活动 graph/tool/presentation/surface/token/hit/slot 逐值不变。结构化结果为 `post_renderer_failure_result_kind=2`、`post_renderer_failure_committed=false`、`post_renderer_failure_trigger_count=1`、`post_renderer_resource_count_before=77`、`post_renderer_resource_count_after=77`；随后同槽 Save 与最终来源槽 Load 均成功。完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn production/test compiler/analyzer 与目标 GDScript diagnostics 为 0；Vulkan Forward+ 契约 PASS，`ExportRelease` 无 hook 实现、probe 类型或注入消息，QA export 继续排除 probe 源码。唯一 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`；editor 游标 721 后无新增 error，DAP stderr/console 为空。该证据只关闭 renderer plan 加入后的 fallback disposal 子场景，2.2 保持开放。
  - slot plan 后续 aggregate Preflight fallback disposal 证据（2026-08-22）：同一 probe 的第二个 `SaveManager` partial hook 位于 `SlotTargetLoadCommitPlan` 加入 `preflightPlans` 后、aggregate ownership 之前。operation 返回 `Failed`、`committed=false`；`RunLoadAsync()` 的 `finally` fallback 逐个 `Dispose()` 已有 graph/tool/renderer/slot plan，释放 renderer admission 和未转交资源，活动 graph/tool/presentation/surface/token/hit/slot 逐值不变。结构化结果为 `post_slot_failure_result_kind=2`、`post_slot_failure_committed=false`、`post_slot_failure_trigger_count=1`、`post_slot_resource_count_before=77`、`post_slot_resource_count_after=77`、最终 `load_result_kind=0`；随后同槽 Save 与最终来源槽 Load 均成功。完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn production/test compiler/analyzer 与目标 GDScript diagnostics 为 0；Vulkan Forward+ 契约以退出码 0 输出 PASS，`ExportRelease` 无 hook 实现、probe 类型或注入消息，QA export 继续排除 probe 源码。唯一 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`；editor 游标 721 后无新增 error，DAP stderr/console 为空。该证据只关闭 slot plan 加入后的 fallback disposal 子场景，2.2 保持开放。
  - aggregate ownership 后的 pre-commit disposal 证据（2026-08-22）：同一 probe 的第三个 `SaveManager` partial hook 位于 `PreparedAggregateLoad` 已取得 graph/tool/renderer/slot plans 所有权后、`Commit()` 前。operation 返回 `Failed`、`committed=false`；aggregate 离开 `using` scope 时自行 `Dispose()` 全部 owned plans，`aggregateOwnsPlans=true` 阻止 orchestration fallback 重复清理。活动 graph/tool/presentation/surface/token/hit/slot 逐值不变。结构化结果为 `post_ownership_failure_result_kind=2`、`post_ownership_failure_committed=false`、`post_ownership_failure_trigger_count=1`、`post_ownership_resource_count_before=77`、`post_ownership_resource_count_after=77`、最终 `load_result_kind=0`；随后同槽 Save 与最终来源槽 Load 均成功。完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn production/test compiler/analyzer 与目标 GDScript diagnostics 为 0；Vulkan 1.4 Forward+ 契约以退出码 0 输出 PASS，`ExportRelease` 不编译 probe 源码，release DLL 不含 probe 类型或注入消息。唯一 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`；editor 游标 721 后无新增 error，DAP stderr/console 为空。该证据只关闭 ownership transfer 后、commit 前的 aggregate disposal 子场景，2.2 保持开放。
  - 真实 renderer commit-boundary generation 失配证据（2026-08-22）：Debug-only `RoadLoadPreflightResourceFailureProbe` 以真实 renderer admission/preparer/Preflight 构造有效 `RoadRendererLoadCommitPlan`，再与 graph/tool/slot 三个 tracking plans 组成 `PreparedAggregateLoad`。受控 lease 在 `CrossCommitBoundary` 内释放 admission，第二次 generation 复核抛出 `LoadPreflightInvalidException`；四方引用交换与 `MarkCommitted()` 均为 0，aggregate disposal 释放 renderer 隐藏资源，三个 tracking plans 各清理一次。旧 mesh/node batch/surface 与 desired/presented token 保持，`ObjectResourceCount` 为 `77 -> 77`，renderer admission 可重新取得。结构化结果为 `renderer_boundary_count=1`、`renderer_boundary_mark_committed_count=0`；`RoadRendererLifecycleContractTests` 14/14、生命周期与 QA export 聚焦 15/15、完整自动化 872/872，双配置 build、Roslyn production/test compiler/analyzer 与目标 GDScript diagnostics 均为 0，Vulkan 1.4 Forward+ 契约 PASS，editor/DAP 错误通道为空。`ExportRelease` DLL 不含 probe 类型、方法、lease 或注入 token。该证据只覆盖真实 renderer plan 与 tracking companions 的直接 aggregate，不冒充真实 `StartLoad()` 四参与者完整 operation，2.2 保持开放。
  - 仍缺（保持开放）：已定位的 Load Preflight Resource 所有权点、普通更新 token Resource Preflight 与真实 `CreateNodeBatch`/`CreateRoadMesh` 工厂故障、真实 aggregate Load 的 renderer Resource Preflight、reserved render token 创建故障、`RoadSurfaceSnapshot`/`RoadRendererLoadCommitPlan` 构造故障与 `CreateRoadMesh`/`CreateNodeBatch` 工厂故障、renderer plan 后续与 slot plan 后续 fallback disposal、aggregate ownership 后的 pre-commit disposal，以及真实 renderer plan 的 commit-boundary generation 失配均有真实 Godot 证据；其余更新 token renderer 故障、其他真实 aggregate 组合与完整性能矩阵仍需协作验证。

  - 普通更新 token `RoadSurfaceSnapshot` 构造故障证据（2026-08-22）：同一 Debug-only `RoadRendererUpdateTokenFailureProbe` 为下一枚真实 graph mutation token 把 snapshot `PreparedData` 替换为 null；hook 位于 road mesh、node batch 与统一 Resource Preflight 之后，真实 `RoadSurfaceSnapshot` 构造函数抛出 `ArgumentNullException("prepared")`。此时两个 Resource 均已创建但未转交，ordinary rebuild 的 `finally` 同步释放二者。真实 Vulkan 1.4.341 Forward+ 的 attempt 1 推进 desired、保留旧 presented/Edge/mesh/marker/surface 并进入 stalled，provider 拒绝旧 hit，`ObjectResourceCount` 为 `77 -> 77`；attempt 2 以同一 desired token 原子发布新增 Edge/surface。结构化结果为 `UPDATE_TOKEN_SURFACE_SNAPSHOT_FAILURE_RESULT resource_before=77 resource_after=77 trigger_count=1 stalled_attempt=1 recovered_attempt=2`。`RoadRendererLifecycleContractTests` 22/22、生命周期与 QA export 聚焦组合 23/23、完整自动化 880/880，双配置 build、Roslyn production/test compiler/analyzer、目标与 LSP workspace scan 可见的 2 个 GDScript diagnostics 均为 0；Vulkan 契约输出四个 update-token 结果并 PASS。Debug DLL 包含 probe 类型与 snapshot arming/probe API，`ExportRelease` 均不包含。Godot MCP 分步推进 1+7 帧，运行树包含道路系统与两类静态表现节点，minimal stderr/console 为空，editor 游标 767 后无新增消息；隔离 QA 根已移入回收站，唯一 CLI warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。2.2 仍因其余 renderer 故障和真实 aggregate 组合矩阵保持开放。

  - 普通更新 token pre-commit 取代证据（2026-08-22）：同一 Debug-only `RoadRendererUpdateTokenFailureProbe` 在 snapshot 构造完成、两个 Resource 尚未转交时，通过正式 `RequestRebuild()` 只推进 `RenderRequestID`。原 target attempt 1 命中现有 desired-token 复核后正常丢弃，不产生异常或 stalled；外层 `finally` 释放 road mesh 与 node batch，旧 presentation 保持但 provider 进入 pending 门禁，`ObjectResourceCount` 为 `77 -> 77`。replacement 与原 target 除 `RenderRequestID + 1` 外完全一致，明确驱动后以自己的 attempt 1 发布新增 Edge/surface。结构化结果为 `UPDATE_TOKEN_PRECOMMIT_SUPERSESSION_RESULT resource_before=77 resource_after=77 trigger_count=1 superseded_attempt=1 replacement_attempt=1`。`RoadRendererLifecycleContractTests` 23/23、生命周期与 QA export 聚焦组合 24/24、完整自动化 881/881，双配置 build、Roslyn production/test compiler/analyzer、目标与 LSP workspace scan 可见的 2 个 GDScript diagnostics 均为 0；Vulkan 契约输出四个故障结果与一个 supersession 结果并 PASS。Debug DLL 包含 supersession API，`ExportRelease` 不包含。Godot MCP 首个单帧回执超时，随后 7+1 帧完成，运行树与 minimal/editor 错误通道正常；隔离 QA 根已移入回收站，唯一 CLI warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。本证据只覆盖 `RenderRequestID`，2.2 仍因其他 token 分量的真实运行时取代、其余 renderer 故障和真实 aggregate 组合矩阵保持开放。

  - 普通更新 token pre-commit `RoadStyleRevision` 取代证据（2026-08-22）：同一 Debug-only probe 复用 snapshot 后、引用交换前的 hook，以正式 `RequestStyleRefresh()` 在真实 graph mutation 的 target attempt 1 内推进样式修订。target 因 desired 已被 replacement 取代而正常返回 false，不产生异常或 stalled；`finally` 释放两个未转交 Resource，旧 presentation 保持且 provider 在 pending 时拒绝旧 hit，`ObjectResourceCount` 为 `77 -> 77`。replacement 与 target 的 scene/facade/facade generation/change sequence 相同，`RoadStyleRevision` 和每次请求必然推进的 `RenderRequestID` 各加一；replacement 以自己的 attempt 1 发布新增 Edge/surface。结构化结果为 `UPDATE_TOKEN_PRECOMMIT_STYLE_SUPERSESSION_RESULT resource_before=77 resource_after=77 trigger_count=1 superseded_attempt=1 replacement_attempt=1`。`RoadRendererLifecycleContractTests` 24/24、生命周期与 QA export 聚焦组合 25/25、完整自动化 882/882，双配置 build、Roslyn production/test compiler/analyzer、目标与 LSP workspace scan 可见的 2 个 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约输出两种 supersession 并 PASS，唯一 warning 为既有 `ConstructionDock` warning。Godot MCP 8 帧 smoke 的运行树与 minimal/editor 错误通道正常；Debug DLL 包含样式取代 API，`ExportRelease` 不包含，隔离 QA 根已移入回收站。本证据将六分量真实 pre-commit 取代覆盖推进到 `RenderRequestID` 与 `RoadStyleRevision`；其余四个分量、其余 renderer 故障和真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 普通更新 token pre-commit `SceneGeneration` 取代证据（2026-08-22）：Debug-only probe 将取代模式收敛为三值枚举，并在连续 Load 断言之后通过正式 `SetSceneGeneration()` 为真实 graph mutation 的 target attempt 1 生成 replacement；注入状态只存活到随后测试槽和 `MapTest` 清理，不被后续 Save/Load 消费。target 因 desired 已被取代而正常返回 false，不产生异常或 stalled；`finally` 释放两个未转交 Resource，旧 presentation 保持且 provider 在 pending 时拒绝旧 hit，`ObjectResourceCount` 为 `77 -> 77`。replacement 与 target 的 facade/facade generation/change sequence/style 相同，`SceneGeneration` 和 `RenderRequestID` 各加一，并以自己的 attempt 1 发布新增 Edge/surface。结构化结果为 `UPDATE_TOKEN_PRECOMMIT_SCENE_SUPERSESSION_RESULT resource_before=77 resource_after=77 trigger_count=1 superseded_attempt=1 replacement_attempt=1`。`RoadRendererLifecycleContractTests` 25/25、生命周期与 QA export 聚焦组合 26/26、完整自动化 883/883，双配置 build、Roslyn production/test compiler/analyzer、目标与 LSP workspace scan 可见的 2 个 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约输出三种 supersession 并 PASS，唯一 CLI warning 为既有 `ConstructionDock` warning。Godot MCP 8 帧 smoke 运行树正常，minimal stderr/console 为空；editor 仅新增序号 771～772 的既有 MCP 单连接拒绝 warning。Debug DLL 包含 scene arming/helper，`ExportRelease` 不包含，隔离 QA 根已移入回收站。六分量真实 pre-commit 取代现覆盖三项；`GraphFacadeID`、`GraphFacadeGeneration`、`ChangeSequence` 及其余故障矩阵仍未完成，因此 2.2 保持开放。

  - 普通更新 token pre-commit `GraphFacadeGeneration` 取代证据（2026-08-22）：同一 Debug-only probe 在 snapshot、road mesh 与 node batch 准备完成后，先校验当前 `RoadGraph.FacadeID` 和 `CurrentStateToken.ChangeSequence` 与 target 一致，再通过正式 `RequestGraphChange(targetToken.ChangeSequence, isFullReset: true)` 生成 replacement。target 因 desired 已被取代而正常返回 false，不产生异常或 stalled；`finally` 释放两个未转交 Resource，旧 presentation 保持且 provider 在 pending 时拒绝旧 hit，`ObjectResourceCount` 为 `77 -> 77`。replacement 与 target 的 scene/facade ID/change sequence/style 相同，仅 `GraphFacadeGeneration` 和 `RenderRequestID` 各加一，并以自己的 attempt 1 发布新增 Edge/surface。结构化结果为 `UPDATE_TOKEN_PRECOMMIT_GRAPH_GENERATION_SUPERSESSION_RESULT resource_before=77 resource_after=77 trigger_count=1 superseded_attempt=1 replacement_attempt=1`。`RoadRendererLifecycleContractTests` 26/26、生命周期与 QA export 聚焦组合 27/27、完整自动化 884/884，双配置 build、Roslyn production/test compiler/analyzer、目标与 LSP workspace scan 可见的 2 个 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约输出四种 supersession 并 PASS，唯一 CLI warning 为既有 `ConstructionDock` warning。Godot MCP 冻结 `MapTest` 推进 8 帧，运行树正常，minimal stderr/console 为空；editor 仅新增序号 774～781 的既有 MCP 单连接拒绝 warning，项目已停止。Debug DLL 包含 graph-generation arming/helper，`ExportRelease` 不包含，隔离 QA 根已移入回收站。六分量真实 pre-commit 取代现覆盖四项；`GraphFacadeID`、`ChangeSequence` 及其余故障矩阵仍未完成，因此 2.2 保持开放。

  - 普通更新 token pre-commit `ChangeSequence` 取代证据（2026-08-22）：同一 Debug-only probe 在 target 的 snapshot、road mesh 与 node batch 准备完成后，通过真实 `RoadGraph.SubmitPolyline(RoadType.Street, ...)` 提交第二次隔离 mutation；其 `GraphChanged` 由正式 renderer 路径生成 replacement，probe 仅失效自动 deferred continuation 以保留 pending 观察窗。target 因 desired 与 graph revision 已过期而正常返回 false，不产生异常或 stalled；`finally` 释放两个未转交 Resource，旧 presentation 保持且两条未呈现道路均不可命中，`ObjectResourceCount` 为 `77 -> 77`。replacement 与 target 的 scene/facade ID/facade generation/style 相同，仅 `ChangeSequence` 与 `RenderRequestID` 各加一，并以自己的 attempt 1 将两次 mutation 一次发布。结构化结果为 `UPDATE_TOKEN_PRECOMMIT_CHANGE_SEQUENCE_SUPERSESSION_RESULT resource_before=77 resource_after=77 trigger_count=1 superseded_attempt=1 replacement_attempt=1`。`RoadRendererLifecycleContractTests` 27/27、生命周期与 QA export 聚焦组合 28/28、完整自动化 885/885，双配置 build、Roslyn production/test compiler/analyzer、目标与 LSP workspace scan 可见的 2 个 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约输出五种 supersession 并 PASS，唯一 CLI warning 为既有 `ConstructionDock` warning。Godot MCP 冻结 `MapTest` 推进 8 帧且运行树正常；minimal DAP 因既有 8-client 上限未接通，editor 仅新增序号 783 的同源 warning，项目已停止。Debug DLL 包含 change-sequence arming/helper，`ExportRelease` 不包含，隔离 QA 根已移入回收站。六分量真实 pre-commit 取代现覆盖五项；`GraphFacadeID` 及其余故障矩阵仍未完成，因此 2.2 保持开放。

  - 普通更新 token pre-commit `GraphFacadeID` 取代证据（2026-08-22）：同一 Debug-only probe 在 target 的 snapshot、road mesh 与 node batch 准备完成后，捕获当前 graph 快照并创建新 `RoadGraph`，通过真实 `CommitPreparedLoad()` 将新 facade 的 `ChangeSequence` 对齐 target，再调用正式 `RoadRenderer.SetGraph()`。`SetGraph()` 在同一调用栈同步发布 replacement；外层 target 因 graph 引用和 desired token 已变化而正常返回 false，`finally` 释放两个未转交 Resource，旧 presentation 未被外层 attempt 覆盖，`ObjectResourceCount` 保持 `77 -> 77`。replacement 的 `GraphFacadeID` 变为新值，`GraphFacadeGeneration` 与 `RenderRequestID` 各加一，scene/change sequence/style 保持不变，并以自己的 attempt 1 同步 ready；仍绑定旧 graph 的 `RoadBuilder` 因 facade ID 不匹配拒绝新命令。结构化结果为 `UPDATE_TOKEN_PRECOMMIT_GRAPH_FACADE_ID_SUPERSESSION_RESULT resource_before=77 resource_after=77 trigger_count=1 superseded_attempt=1 replacement_attempt=1`。`RoadRendererLifecycleContractTests` 28/28、生命周期与 QA export 聚焦组合 29/29、完整自动化 886/886，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区可见的 2 个 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约 PASS，Godot MCP 8 帧 smoke 的运行树与 editor 错误通道正常。minimal DAP 因既有 8-client 状态未接入，不能声明 console 通过。Debug DLL 包含新 probe/API/helper，`ExportRelease` 不包含，隔离 QA 根已移入回收站，项目已停止。至此六分量真实 pre-commit 取代全部覆盖；其余 renderer 故障和真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 普通更新 `PresentationStalled` observer 隔离证据（2026-08-22）：扩展后的 Debug-only probe 在真实 Resource Preflight 故障前依次订阅一次性抛错 observer 与后续计数 observer。attempt 1 的构建异常仍成为 desired token 的 stalled 事实；首个 observer 抛出的 `InvalidOperationException` 被逐订阅者捕获并转为预期 warning，后续 observer 仍执行一次。旧 presented/mesh/marker/surface 保持但 provider 拒绝 hit，未转交 Resource 同步释放，`ObjectResourceCount` 为 `77 -> 77`；同一 desired token 在 attempt 2 成功发布。结构化结果为 `UPDATE_TOKEN_STALLED_OBSERVER_FAILURE_RESULT resource_before=77 resource_after=77 failure_trigger_count=1 throwing_observer_count=1 continuing_observer_count=1 stalled_attempt=1 recovered_attempt=2`。`RoadRendererLifecycleContractTests` 29/29、生命周期与 QA export 聚焦组合 30/30、完整自动化 887/887，双配置 build、Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约与 Godot MCP 8 帧 smoke PASS。minimal DAP 返回 `No active debug session`；Debug DLL 包含 probe API/handler/message，`ExportRelease` 不包含，隔离 QA 根已移入回收站，项目已停止。本证据关闭普通 stalled observer 逐订阅者隔离子场景；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load `CreateNodeBatch` 工厂故障证据（2026-08-22）：Debug-only `RoadLoadPreflightResourceFailureProbe` 在含 1 Edge 的真实 `StartLoad()` renderer Preflight 中，于 road mesh 创建后把 node marker 输入替换为 `Count=1` 的抛错列表。`CreateNodeBatch()` 已分配并配置 `MultiMesh` 后在首个 marker 索引读取抛出 `InvalidOperationException`；factory helper 释放 node batch，外层 Preflight catch 释放 road mesh。operation 返回 `Failed`、`committed=false` 且无 warning，活动 graph/tool/placement/history/presentation/surface/token/hit/slot 逐值不变，`ObjectResourceCount` 为 `77 -> 77`；后续 Preflight 与最终无故障 Load 均可重新接管。结构化结果为 `aggregate_node_batch_failure_result_kind=2 aggregate_node_batch_failure_committed=false aggregate_node_batch_failure_trigger_count=1 aggregate_node_batch_marker_read_count=1 aggregate_node_batch_resource_count_before=77 aggregate_node_batch_resource_count_after=77 load_result_kind=0`。`RoadRendererLifecycleContractTests` 30/30、生命周期与 QA export 聚焦组合 31/31、完整自动化 888/888，双配置 build、Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Godot 4.7 Vulkan Forward+ 契约与 Godot MCP 8 帧 smoke PASS，editor 游标 783 后无新增消息。minimal DAP 返回 `No active debug session`；Debug DLL 包含 probe 类型/API/wrapper/message，`ExportRelease` 不包含，隔离 QA 根已移入回收站，项目已停止。本证据关闭真实 aggregate node-batch factory 子场景；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load `CreateRoadMesh` 工厂故障证据（2026-08-22）：同一 Debug-only probe 在含 1 Edge 的真实 `StartLoad()` renderer Preflight 中，把 `prepared.RoadIndices` 替换为 `Count=1` 的抛错集合。`CreateRoadMesh()` 完成 vertex/UV/color 数组准备并分配 `ArrayMesh` 后，在首次枚举 indices 时抛出 `InvalidOperationException`，尚未写入 surface；factory ownership helper 释放唯一已建 Resource，node batch 尚未创建。operation 返回 `Failed`、`committed=false` 且无 warning，活动 graph/tool/placement/history/presentation/surface/token/hit/slot 逐值不变，`ObjectResourceCount` 为 `77 -> 77`；后续全部 Preflight 场景与最终无故障 Load 均可重新接管。结构化结果为 `aggregate_road_mesh_failure_result_kind=2 aggregate_road_mesh_failure_committed=false aggregate_road_mesh_failure_trigger_count=1 aggregate_road_mesh_index_enumeration_count=1 aggregate_road_mesh_resource_count_before=77 aggregate_road_mesh_resource_count_after=77 load_result_kind=0`。`RoadRendererLifecycleContractTests` 31/31、生命周期与 QA export 聚焦组合 32/32、完整自动化 889/889，双配置 build、Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Godot 4.7 Vulkan 1.4.341 Forward+ 契约与 Godot MCP 8 帧 smoke PASS，editor 游标 783 后无新增消息。minimal DAP 返回 `No active debug session`；Debug DLL 包含 probe 类型/API/indices wrapper/message，`ExportRelease` 不包含，隔离 QA 根已移入回收站，项目已停止。本证据关闭真实 aggregate road-mesh factory 子场景；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load `RoadSurfaceSnapshot` 构造故障证据（2026-08-22）：同一 Debug-only probe 在含 1 Edge 的真实 `StartLoad()` renderer Preflight 中，于 road mesh、node batch、统一 Resource Preflight 与 reserved render token 完成后把 snapshot `PreparedData` 替换为 null。真实 `RoadSurfaceSnapshot` 构造函数抛出 `ArgumentNullException("prepared")`，外层 Preflight catch 释放两个已创建但未转交的 Resource。operation 返回 `Failed`、`committed=false` 且无 warning，活动 graph/tool/placement/history/presentation/surface/token/hit/slot 逐值不变，`ObjectResourceCount` 为 `77 -> 77`；后续全部 Preflight 场景与最终无故障 Load 均可重新接管。结构化结果为 `aggregate_snapshot_failure_result_kind=2 aggregate_snapshot_failure_committed=false aggregate_snapshot_failure_trigger_count=1 aggregate_snapshot_resource_count_before=77 aggregate_snapshot_resource_count_after=77 load_result_kind=0`。`RoadRendererLifecycleContractTests` 32/32、生命周期与 QA export 聚焦组合 33/33、完整自动化 890/890，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Godot 4.7 Vulkan 1.4.341 Forward+ 契约与 Godot MCP 8 帧 smoke PASS，editor 游标 783 后无新增消息。minimal DAP 返回 `No active debug session`；Debug DLL 包含 probe 类型与 snapshot arming/probe API，`ExportRelease` 不包含，隔离 QA 根已移入回收站，项目已停止。本证据关闭真实 aggregate snapshot 构造子场景；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load reserved render token 创建故障证据（2026-08-22）：同一 Debug-only probe 在含 1 Edge 的真实 `StartLoad()` renderer Preflight 中，于 road mesh、node batch 与统一 Resource Preflight 完成后只把本次局部 `RoadRenderLoadReservation` 副本置为 default。真实 `_presentationTokens.CreateReservedLoadToken()` 抛出 `InvalidOperationException("Road render load reservation is stale.")`，tracker 与 admission reservation 保持不变，外层 Preflight catch 释放两个已创建但未转交的 Resource。operation 返回 `Failed`、`committed=false` 且无 warning，活动 graph/tool/placement/history/presentation/surface/token/hit/slot 逐值不变，`ObjectResourceCount` 为 `77 -> 77`；后续全部 Preflight 场景与最终无故障 Load 均可重新接管。结构化结果为 `aggregate_reserved_token_failure_result_kind=2 aggregate_reserved_token_failure_committed=false aggregate_reserved_token_failure_trigger_count=1 aggregate_reserved_token_resource_count_before=77 aggregate_reserved_token_resource_count_after=77 load_result_kind=0`。`RoadRendererLifecycleContractTests` 33/33、生命周期与 QA export 聚焦组合 34/34、完整自动化 891/891，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Godot 4.7 Vulkan 1.4.341 Forward+ 契约以退出码 0 PASS，Godot MCP 冻结 `MapTest` 8 帧后运行树含两类静态表现节点，editor 游标 783 后无新增消息。minimal DAP 返回 `No active debug session`；Debug DLL 包含 probe 类型与 reserved-token arming/query API，`ExportRelease` 不包含，两次隔离 QA 根已移入回收站，项目已停止。唯一 CLI warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。本证据关闭真实 aggregate reserved render token 创建子场景；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load `RoadRendererLoadCommitPlan` 构造故障证据（2026-08-22）：同一 Debug-only probe 在含 1 Edge 的真实 `StartLoad()` renderer Preflight 中，于 road mesh、node batch、reserved render token 与 `RoadSurfaceSnapshot` 完成后，在 plan constructor 已捕获全部字段但尚未返回 caller 时抛出一次性 `InvalidOperationException`。constructor 未完成，plan 所有权没有转交，外层 Preflight catch 同步释放两个 Godot Resource。operation 返回 `Failed`、`committed=false` 且无 warning，活动 graph/tool/placement/history/presentation/surface/token/hit/slot 逐值不变，`ObjectResourceCount` 为 `77 -> 77`；后续全部 Preflight 场景与最终无故障 Load 均可重新接管。结构化结果为 `aggregate_commit_plan_failure_result_kind=2 aggregate_commit_plan_failure_committed=false aggregate_commit_plan_failure_trigger_count=1 aggregate_commit_plan_resource_count_before=77 aggregate_commit_plan_resource_count_after=77 load_result_kind=0`。`RoadRendererLifecycleContractTests` 34/34、生命周期与 QA export 聚焦组合 35/35、完整自动化 892/892，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Godot 4.7 Vulkan 1.4.341 Forward+ 契约以退出码 0 PASS。Godot MCP 冻结 `MapTest` 8 帧后运行树含两类静态表现节点，minimal stderr/console 为空；editor 增量窗口的最新 100 条均为既有桥接基础设施噪声，停止后游标 1918 无新增消息。Debug DLL 包含 probe 类型与 commit-plan arming API，`ExportRelease` 不包含，隔离 QA 根已移入回收站，项目已停止。唯一 CLI warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。本证据关闭真实 aggregate commit-plan constructor 子场景；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实四参与者 `StartLoad()` renderer commit-boundary generation 失配证据（2026-08-22）：既有直接 probe 仍以真实 `RoadRendererLoadCommitPlan` 搭配三个 `TrackingLoadCommitPlan`；新增 Debug-only `SaveManager` partial 则在 aggregate ownership 后、`Commit()` 前包装真实 storage lease，并在其 `CrossCommitBoundary()` 回调内释放 renderer admission。第二次 generation 复核在任何 graph/tool/presentation/slot `CommitReferences()` 前拒绝，operation 返回 `Failed`、`committed=false` 且无 warning，boundary 为 1、storage `MarkCommitted()` 为 0；四方引用零交换，aggregate disposal 释放隐藏 renderer 资源，`ObjectResourceCount` 为 `77 -> 77`，最终无故障 Load 可重新接管。结构化结果为 `renderer_boundary_operation_failure_result_kind=2 renderer_boundary_operation_failure_committed=false renderer_boundary_operation_failure_trigger_count=1 renderer_boundary_operation_count=1 renderer_boundary_operation_mark_committed_count=0 load_result_kind=0`。`RoadRendererLifecycleContractTests` 35/35、生命周期与 QA export 聚焦组合 36/36、完整自动化 893/893，双配置 build、Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约 PASS。Godot MCP 的项目、版本、`MapTest`、Vulkan、minimal console 与 editor 增量门通过，但运行树相关 RPC 持续超时，故不声明该门通过；`ExportRelease` 不含 partial 实现、probe、wrapper 或测试 API。该证据关闭完整真实 operation 的这一子场景；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实四参与者 `StartLoad()` ToolManager commit-boundary generation 失配证据（2026-08-22）：既有直接 probe 仍以真实 `ToolLoadCommitPlan` 搭配三个 `ToolTrackingLoadCommitPlan`；新增 Debug-only `SaveManager` partial 在 renderer operation probe 后、`Commit()` 前再次包装真实 storage lease，并在其 `CrossCommitBoundary()` 回调内释放当前 `ToolLoadAdmission` 及 builder admission。第二次 generation 复核在任何 graph/tool/presentation/slot `CommitReferences()` 前拒绝，operation 返回 `Failed`、`committed=false` 且无 warning，boundary 为 1、storage `MarkCommitted()` 为 0；活动 graph payload、`CurrentTool=Road`、`SelectedRoadType=Arterial`、placement/history、presentation/surface/token/hit/slot 逐值不变，同槽 Save 后 payload 逐字节相等，aggregate disposal 释放隐藏 renderer 资源，`ObjectResourceCount` 为 `77 -> 77`，最终无故障 Load 可重新接管。结构化结果为 `tool_boundary_operation_failure_result_kind=2 tool_boundary_operation_failure_committed=false tool_boundary_operation_failure_trigger_count=1 tool_boundary_operation_count=1 tool_boundary_operation_mark_committed_count=0 tool_boundary_operation_resource_count_before=77 tool_boundary_operation_resource_count_after=77 load_result_kind=0`。`RoadRendererLifecycleContractTests` 36/36、生命周期与 QA export 聚焦组合 37/37、完整自动化 894/894，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区可见的 3 个 GDScript diagnostics 均为 0；隔离 `APPDATA` 的 Vulkan 1.4.341 Forward+ 契约 PASS，唯一 CLI warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。Godot MCP 确认正确项目、版本、`MapTest`、Vulkan 与空 stderr/console/editor 增量错误，但 `godot_game_time` 与 runtime-state RPC 超时，故不声明运行树门通过；Debug DLL 包含 probe、wrapper 与测试 API，`ExportRelease` 不包含。测试槽、隔离 QA 根和临时日志均已清理，项目已停止。该证据关闭完整真实 operation 的 ToolManager 子场景；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - `RenderRequestID` 分级性能扰动证据（2026-08-22）：`road_rendering_performance_contract.gd --measure-token-perturbation=render-request` 在 target attempt 1 完成全部准备但尚未交换引用时调用正式 `RequestRebuild()`；1k/10k/25k/50k/100k 均证明 superseded attempt 正常丢弃、未转交 Resource 同步释放、旧 mesh/marker/surface 保持但 mixed-token hit 被拒绝，replacement 与 superseded 仅 `RenderRequestID: 3 -> 4`，并由 replacement attempt 1 完整发布 `N+1` Edge。五档资源计数均为 `77 -> 77`，Vulkan 契约均 PASS。该证据只关闭六分量性能扰动矩阵中的 `RenderRequestID`；其余五分量、其余 renderer 故障与真实 aggregate 组合仍开放，因此 2.2 保持开放。

  - `RoadStyleRevision` 分级性能扰动证据（2026-08-22）：同一契约新增 `--measure-token-perturbation=road-style`，在 snapshot、road mesh、node batch 与 surface 已准备但尚未交换引用时调用正式 `RequestStyleRefresh()`；1k/10k/25k/50k/100k 均证明 target attempt 1 正常丢弃且不进入 stalled，replacement 保持 scene/facade/facade generation/change sequence，仅将 `RoadStyleRevision: 1 -> 2` 与必然伴随新请求的 `RenderRequestID: 3 -> 4` 各推进一位。两个未转交 Resource 同步释放，旧 presentation 保持但 mixed-token hit 被拒绝，replacement attempt 1 完整发布 `N+1` Edge，资源计数均为 `77 -> 77`。五档 Vulkan 契约均 PASS。六分量性能扰动矩阵现完成 `RenderRequestID` 与 `RoadStyleRevision`；其余四分量、其余 renderer 故障与真实 aggregate 组合仍开放，因此 2.2 保持开放。

  - `SceneGeneration` 分级性能扰动证据（2026-08-22）：同一契约新增 `--measure-token-perturbation=scene-generation` 并逐项核对六分量；retained 到 superseded 只推进 `ChangeSequence + 1` 与 `RenderRequestID + 1`，提交前 scene replacement 再保持 facade/facade generation/change sequence/style 不变，仅将 `SceneGeneration: 1 -> 2` 与 `RenderRequestID: 3 -> 4` 各推进一位。1k/10k/25k/50k/100k 均证明 target attempt 1 正常丢弃且不进入 stalled，两个未转交 Resource 同步释放，旧 presentation 保持但 mixed-token hit 被拒绝，replacement attempt 1 完整发布 `N+1` Edge，资源计数均为 `77 -> 77`。五档 Vulkan 契约与两条既有模式的 1k 兼容回归均 PASS。六分量性能扰动矩阵现完成 `RenderRequestID`、`RoadStyleRevision` 与 `SceneGeneration`；其余三分量、其余 renderer 故障与真实 aggregate 组合仍开放，因此 2.2 保持开放。

  - `GraphFacadeID` 分级性能扰动证据（2026-08-22）：同一契约新增必须搭配单一 `--dataset-size` 的 `--measure-token-perturbation=graph-facade-id`；target attempt 1 到达提交前边界后，正式 `SetGraph()` 在同一调用栈内同步发布新 facade 的完整 replacement，因此该分支输出 `replacement_mode=synchronous`、`recovery_ms=0`。replacement 保持 scene/change sequence/style，令 `GraphFacadeID` 从当前稳定 ID 切换为不同正值，并将 `GraphFacadeGeneration: 2 -> 3`、`RenderRequestID: 3 -> 4` 各推进一位；原 target 正常拒绝，两个未转交 Resource 释放，新 facade 的 matching mesh/surface/hit 已完整 ready。1k/10k/25k/50k/100k 均保持资源 `77 -> 77`、Edge `N -> N+1`、两个 attempt 为 1，Vulkan 契约与前三种模式的 1k 兼容回归均 PASS。六分量性能扰动矩阵现完成四项；其余 `GraphFacadeGeneration`、`ChangeSequence`、其余 renderer 故障与真实 aggregate 组合仍开放，因此 2.2 保持开放。

  - `GraphFacadeGeneration` 分级性能扰动证据（2026-08-22）：同一契约新增 `--measure-token-perturbation=graph-facade-generation`，在提交前边界调用正式 full-reset token 路径；1k/10k/25k/50k/100k 均证明 target attempt 1 正常丢弃且不进入 stalled，replacement 保持 scene/facade/change sequence/style，仅将 `GraphFacadeGeneration: 2 -> 3` 与必然伴随新请求的 `RenderRequestID: 3 -> 4` 各推进一位。两个未转交 Resource 同步释放，旧 presentation 保持但 mixed-token hit 被拒绝，replacement attempt 1 完整发布 `N+1` Edge，资源计数均为 `77 -> 77`。五档 Vulkan 契约与相邻 `graph-facade-id` 1k 兼容回归均 PASS。六分量性能扰动矩阵现完成五项；只余 `ChangeSequence`、其余 renderer 故障与真实 aggregate 组合仍开放，因此 2.2 保持开放。

  - `ChangeSequence` 分级性能扰动证据（2026-08-22）：同一契约新增 `--measure-token-perturbation=change-sequence`，由第一条真实 builder mutation 生成 superseded token，再在其提交前边界直接提交第二条不重叠的真实 graph mutation。retained 到 superseded、superseded 到 replacement 均只推进 `ChangeSequence + 1` 与 `RenderRequestID + 1`，其他四个 token 分量稳定；target attempt 1 丢弃两个未转交 Resource，pending 期两条道路均不可命中，replacement attempt 1 一次发布 `N+2` Edge 且两处 hit 均绑定 replacement token。1k/10k/25k/50k/100k 均保持资源 `77 -> 77` 并在独立 Vulkan 进程中 PASS，相邻 `graph-facade-generation` 1k 兼容回归继续 PASS。六分量性能扰动矩阵已完成；其余 renderer 故障与真实 aggregate 组合仍开放，因此 2.2 保持开放。

  - 真实 aggregate Load renderer worker-`Prepare` 故障证据（2026-08-23）：Debug-only `SaveManager` partial hook 位于真实 `StartLoad()` 的 `Task.Run` 内，在 `PreparedSaveSlot` 已产出并取得 `RoadGraphRevision` 后、`RoadRendererLoadPreparer.Prepare(...)` 与任何 renderer Preflight/Resource 创建前触发一次性 `InvalidOperationException`。operation 返回 `Failed`、`committed=false` 且无 warning；活动 graph payload、`CurrentTool=Road`、`SelectedRoadType=Arterial`、placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 全部零交换，`ObjectResourceCount` 为 `77 -> 77`。probe 解除后，紧随其后的无故障 Load 成功取得全部 admission 并联合接管空源槽。结构化结果为 `renderer_worker_prepare_failure_result_kind=2 renderer_worker_prepare_failure_committed=false renderer_worker_prepare_failure_trigger_count=1 renderer_worker_prepare_resource_count_before=77 renderer_worker_prepare_resource_count_after=77 load_result_kind=0`。`RoadRendererLifecycleContractTests` 40/40、生命周期与 QA export 聚焦组合 41/41、完整自动化 898/898，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离 `--check-only` 及正式 Vulkan 1.4.341 Forward+ 契约均通过；MCP 项目/addon/server、未运行状态与游标 1989 后的 editor/minimal 错误通道也已复核。Debug DLL 包含 probe/API，`ExportRelease` 不包含，隔离 QA 根已清理。本证据把真实 aggregate 相邻失败子场景由九个扩为十个；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load renderer admission 样式快照故障证据（2026-08-23）：真实 `RoadRenderer.BeginLoadAdmission()` 在推进 generation 后调用 `Config.CaptureRoadTypeStyleSnapshot()`，只有快照合法时才 reserve render token、构造并发布 renderer admission。Vulkan 契约暂时移除共享生产 `RoadConfig` 的最后一种样式，使真实校验在 worker 启动与任何 Godot Resource 分配前返回 `Failed`、`committed=false` 且无 warning；此时已经取得的 graph/tool admission 由 `RunLoadAsync()` 的 `finally` 释放，活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，资源 `77 -> 77`。恢复配置后，紧邻的 worker-Prepare probe 能进入 worker 并触发一次，最终无故障 Load 成功联合接管，证明早期失败没有滞留 admission。结构化新增结果为 `renderer_admission_style_snapshot_failure_result_kind=2 renderer_admission_style_snapshot_failure_committed=false renderer_admission_style_snapshot_resource_count_before=77 renderer_admission_style_snapshot_resource_count_after=77`。`RoadRendererLifecycleContractTests` 41/41、生命周期与 QA export 聚焦组合 42/42、完整自动化 899/899，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、构建后的隔离 `--check-only` 及正式 Vulkan 1.4.341 Forward+ 契约均通过；MCP 项目/addon/server、未运行状态与游标 1989 后的 editor/minimal 错误通道也已复核。本证据使用生产校验而未新增生产 probe，把真实 aggregate 相邻失败子场景由十个扩为十一个；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load renderer admission 参数校验故障证据（2026-08-23）：真实 `RoadRenderer.BeginLoadAdmission()` 在取得合法样式快照后，以生产 `CurveDisplayTolerance` 构造 `RoadRendererLoadSettings` 并调用 `settings.Validate()`；只有参数合法时才会 reserve render token、构造并发布 renderer admission。Vulkan 契约暂时把共享生产配置的容差设为 `0.0`，真实校验以 `RoadRenderer curve tolerance is invalid.` 在 worker 启动与任何 Godot Resource 分配前返回 `Failed`、`committed=false` 且无 warning；已有 graph/tool admission 由 `RunLoadAsync()` 的 `finally` 释放，活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，资源 `77 -> 77`。恢复配置后，紧邻的 worker-Prepare probe 能进入 worker 并触发一次，最终无故障 Load 成功联合接管，证明参数校验失败没有滞留 admission。结构化新增结果为 `renderer_admission_settings_failure_result_kind=2 renderer_admission_settings_failure_committed=false renderer_admission_settings_resource_count_before=77 renderer_admission_settings_resource_count_after=77`。`RoadRendererLifecycleContractTests` 42/42、生命周期与 QA export 聚焦组合 43/43、完整自动化 900/900，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、构建后的隔离 `--check-only` 及正式 Vulkan 1.4.341 Forward+ 契约均通过；MCP 项目/addon/server、未运行状态与游标 1989 后的 editor/minimal 错误通道也已复核。本证据使用生产参数校验而未新增生产 probe，把真实 aggregate 相邻失败子场景由十一个扩为十二个；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load renderer admission 构造前故障证据（2026-08-23）：真实 `RoadRenderer.BeginLoadAdmission()` 已取得合法 `RoadTypeStyleSnapshot`、通过 `RoadRendererLoadSettings.Validate()`，并由 `_presentationTokens.ReserveLoad()` 取得 `RoadRenderLoadReservation`；随后 Debug-only partial probe 在 `new RoadRendererLoadAdmission(...)`、`_loadAdmission` 发布与 worker 启动前一次性抛出 `InvalidOperationException("Injected aggregate Load renderer admission construction failure.")`。operation 返回 `Failed`、`committed=false` 且无 warning，已有 graph/tool admission 由 `RunLoadAsync()` 的 `finally` 释放；render reservation 只推进 tracker 内部请求身份，没有交换 desired/presented，活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，`ObjectResourceCount` 为 `77 -> 77`。紧随其后的 worker-Prepare probe 仍能取得新 reservation/admission 并触发一次，最终无故障 Load 成功联合接管，证明构造前失败没有留下阻塞状态。结构化新增结果为 `renderer_admission_construction_failure_result_kind=2 renderer_admission_construction_failure_committed=false renderer_admission_construction_failure_trigger_count=1 renderer_admission_construction_resource_count_before=77 renderer_admission_construction_resource_count_after=77`。`RoadRendererLifecycleContractTests` 43/43、生命周期与 QA export 聚焦组合 44/44、完整自动化 901/901，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离目标脚本 `--check-only` 及正式 Vulkan 1.4.341 Forward+ 契约均通过；Debug 反射可见 9 个相关 probe 入口，`ExportRelease` 中 probe 类型不存在且匹配入口为 0，MCP 项目/addon/server、未运行状态与游标 1989 后的 editor/minimal 错误通道也已复核。本证据把真实 aggregate 相邻失败子场景由十二个扩为十三个；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load renderer admission 发布前故障证据（2026-08-23）：真实 `RoadRenderer.BeginLoadAdmission()` 已成功构造局部 `RoadRendererLoadAdmission`，其构造函数只捕获 owner、generation、graph、reservation 与 pure CLR preparer；随后 Debug-only partial probe 在 `_loadAdmission = admission` 与 worker 启动前一次性抛出 `InvalidOperationException("Injected aggregate Load renderer admission publication failure.")`。operation 返回 `Failed`、`committed=false` 且无 warning，orchestration 没有接到 renderer admission 返回值，`_loadAdmission` 保持 null；已有 graph/tool admission 由 `RunLoadAsync()` 的 `finally` 释放，render reservation 未交换 desired/presented。活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，保存后的 payload 不变，`ObjectResourceCount` 为 `77 -> 77`。紧随其后的 worker-Prepare probe 仍能取得并发布新 admission、进入 worker 且触发一次，最终无故障 Load 成功联合接管。结构化新增结果为 `renderer_admission_publication_failure_result_kind=2 renderer_admission_publication_failure_committed=false renderer_admission_publication_failure_trigger_count=1 renderer_admission_publication_resource_count_before=77 renderer_admission_publication_resource_count_after=77`。`RoadRendererLifecycleContractTests` 44/44、生命周期与 QA export 聚焦组合 45/45、完整自动化 902/902，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离目标脚本 `--check-only` 及正式 Vulkan 1.4.341 Forward+ 契约均通过；Debug 反射可见 10 个相关 probe 入口，`ExportRelease` 中 probe 类型不存在且匹配入口为 0，MCP 项目/addon/server、未运行状态及 editor/minimal 错误通道也已复核。两个隔离 QA 根均已清理且原路径不存在，其中运行时根已移入回收站。本证据把真实 aggregate 相邻失败子场景由十三个扩为十四个；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load renderer admission 返回后故障证据（2026-08-23）：真实 `StartLoad()` 已从 `RoadRenderer.BeginLoadAdmission()` 取得并保存已发布的 renderer admission；随后 Debug-only partial probe 在 `SaveSlotStore.CaptureLoadParticipants(...)`、`lease.AdvanceTo(Prepare)` 与 worker `Task.Run` 之前一次性抛出 `InvalidOperationException("Injected aggregate Load failure after renderer admission publication.")`。operation 返回 `Failed`、`committed=false` 且无 warning，`RunLoadAsync()` 的 `finally` 按 renderer、tool、graph 顺序释放 admission。活动 graph/tool/placement/history/presentation/surface/token/hit/slot 零交换，保存后的活动 payload 逐字节不变，`ObjectResourceCount` 为 `77 -> 77`；紧邻的 worker-Prepare probe 仍进入 worker 并触发一次，最终无故障 Load 成功联合接管。结构化新增结果为 `post_renderer_admission_failure_result_kind=2 post_renderer_admission_failure_committed=false post_renderer_admission_failure_trigger_count=1 post_renderer_admission_resource_count_before=77 post_renderer_admission_resource_count_after=77`。`RoadRendererLifecycleContractTests` 45/45、生命周期与 QA export 聚焦组合 46/46、完整自动化 903/903，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离目标脚本 `--check-only` 及正式 Vulkan 1.4.341 Forward+ 契约均通过；Debug 反射可见新 probe 的 10 个相关入口，`ExportRelease` 中 probe 类型不存在且匹配入口为 0，MCP 项目/addon/server、未运行状态及 editor/minimal 错误通道也已复核。两个隔离 QA 根均已清理且原路径不存在，其中运行时根已移入回收站。本证据把真实 aggregate 相邻失败子场景由十四个扩为十五个；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load participant capture 后故障证据（2026-08-23）：真实 `StartLoad()` 已从 `RoadRenderer.BeginLoadAdmission()` 取得已发布的 admission，并由 `SaveSlotStore.CaptureLoadParticipants(GetRequiredSaveables())` 完成 participant 定义校验与唯一正式 payload 的 load reader 捕获；随后 Debug-only partial probe 记录一个 `CapturedLoadParticipant`，再于 `lease.AdvanceTo(Prepare)` 与 worker `Task.Run` 前一次性抛出 `InvalidOperationException("Injected aggregate Load failure after participant capture.")`。operation 返回 `Failed`、`committed=false` 且无 warning，`RunLoadAsync()` 的 `finally` 按 renderer、tool、graph 顺序释放 admission。活动 graph/tool/placement/history/presentation/surface/token/hit/slot 零交换，保存后的活动 payload 逐字节不变，`ObjectResourceCount` 为 `77 -> 77`；紧邻的 worker-Prepare probe 仍进入 worker 并触发一次，最终无故障 Load 成功联合接管。结构化新增结果为 `post_participant_capture_failure_result_kind=2 post_participant_capture_failure_committed=false post_participant_capture_failure_trigger_count=1 post_participant_capture_participant_count=1 post_participant_capture_resource_count_before=77 post_participant_capture_resource_count_after=77`。`RoadRendererLifecycleContractTests` 46/46、生命周期与 QA export 聚焦组合 47/47、完整自动化 904/904，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离目标脚本 `--check-only` 及正式 Vulkan 1.4.341 Forward+ 契约均通过；Debug 反射可见新 probe 的 10 个相关方法，`ExportRelease` 中 probe 类型不存在且匹配方法为 0，MCP 项目/addon/server、未运行状态及 editor/minimal 错误通道也已复核。隔离项目根与 `APPDATA` profile 根均已移入回收站且原路径不存在。本证据把真实 aggregate 相邻失败子场景由十五个扩为十六个；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load `Prepare` phase 推进后故障证据（2026-08-23）：真实 `StartLoad()` 已完成 participant capture，并由 `lease.AdvanceTo(SaveOperationPhase.Prepare)` 把 operation phase 发布为 `3`；随后 Debug-only partial probe 记录 observed phase `3`，再于 worker `Task.Run` 创建前一次性抛出 `InvalidOperationException("Injected aggregate Load failure after entering the Prepare phase.")`。operation 以 `finalPhase=3`、`Failed`、`committed=false` 且无 warning 返回，`RunLoadAsync()` 的 `finally` 按 renderer、tool、graph 顺序释放 admission。活动 graph/tool/placement/history/presentation/surface/token/hit/slot 零交换，保存后的活动 payload 逐字节不变；关闭隔离副本中与契约无关的 MCP/ImGui tooling autoload 后，`ObjectResourceCount` 为 `65 -> 65`。紧邻的 worker-Prepare probe 仍进入 worker 并触发一次，最终无故障 Load 成功联合接管。结构化新增结果为 `post_prepare_phase_failure_result_kind=2 post_prepare_phase_failure_final_phase=3 post_prepare_phase_failure_committed=false post_prepare_phase_failure_trigger_count=1 post_prepare_phase_observed_phase=3 post_prepare_phase_resource_count_before=65 post_prepare_phase_resource_count_after=65`。`RoadRendererLifecycleContractTests` 47/47、生命周期与 QA export 聚焦组合 48/48、完整自动化 905/905，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离目标脚本 `--check-only` 及正式 Vulkan 1.4.341 Forward+ 契约均通过；Debug 反射可见新 probe 的 10 个相关方法，`ExportRelease` 中 probe 类型不存在且匹配方法为 0，MCP 项目/addon/server、未运行状态及 editor/minimal 错误通道也已复核。所有隔离根均已清理，5 个 profile 进入回收站，长路径项目根已永久删除。本证据把真实 aggregate 相邻失败子场景由十六个扩为十七个；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load worker-entry 故障证据（2026-08-23）：Debug-only `SaveManager` partial hook 位于真实 `StartLoad()` 的 `Task.Run` worker 内，在 worker 计时起点后、`CreateSlotStore().PrepareLoad(...)` 与任何 slot/renderer preparation 前触发一次性 `InvalidOperationException("Injected aggregate Load failure after entering the preparation worker.")`。probe 记录触发一次且不在主线程运行；operation 以 `finalPhase=3`、`Failed`、`committed=false` 且无 warning 返回。活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，保存后的 payload 逐字节不变，`ObjectResourceCount` 为 `79 -> 79`。结构化结果为 `worker_entry_failure_result_kind=2 worker_entry_failure_final_phase=3 worker_entry_failure_committed=false worker_entry_failure_trigger_count=1 worker_entry_failure_off_main_thread=true worker_entry_resource_count_before=79 worker_entry_resource_count_after=79`；既有 renderer worker-Prepare probe 仍触发一次，最终无故障 Load 成功联合接管。`RoadRendererLifecycleContractTests` 48/48、生命周期与 QA export 聚焦组合 49/49、完整自动化 906/906，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离 `--check-only` 与正式 Vulkan 1.4.341 Forward+ 契约均通过；MCP 项目/addon/server、未运行状态与 editor/minimal 错误通道也已复核。测试清理冲刷托管终结器后，CLI 在 PASS 后以退出码 0 结束且不再触发 `Godot.Collections.Array.Finalize()` 访问冲突；七个隔离 QA 根已移入回收站。本证据把真实 aggregate 相邻失败子场景由十七个扩为十八个；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load slot preparation 后故障证据（2026-08-23）：Debug-only `SaveManager` partial hook 位于真实 `StartLoad()` 的 `Task.Run` worker 内，`CreateSlotStore().PrepareLoad(...)` 已完成存储读取、完整性校验与 `PreparedSaveSlot` 构造，随后在 `slot.GetPreparedState(context.Graph)` 前一次性抛出 `InvalidOperationException("Injected aggregate Load failure after slot preparation.")`。probe 记录非主线程执行、source slot ID、participant 数 `1` 且只触发一次；graph-state lookup、renderer pure CLR `Prepare`、Godot Resource Preflight 和 commit 均未开始。operation 以 `finalPhase=3`、`Failed`、`committed=false` 且无 warning 返回，活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，保存后的 payload 逐字节不变，`ObjectResourceCount` 为 `77 -> 77`。结构化结果为 `post_slot_preparation_failure_result_kind=2 post_slot_preparation_failure_final_phase=3 post_slot_preparation_failure_committed=false post_slot_preparation_failure_trigger_count=1 post_slot_preparation_failure_off_main_thread=true post_slot_preparation_participant_count=1 post_slot_preparation_resource_count_before=77 post_slot_preparation_resource_count_after=77`，observed slot ID 与 source slot 一致；既有 renderer worker-Prepare probe 仍触发一次，最终无故障 Load 以 `load_result_kind=0` 成功联合接管。`RoadRendererLifecycleContractTests` 49/49、生命周期与 QA export 聚焦组合 50/50、完整自动化 907/907，双配置 build、Roslyn compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、目标脚本 `--check-only` 与正式 Vulkan 1.4.341 Forward+ 契约均通过；Debug binary 包含新 probe/hook，`ExportRelease` 不包含。MCP 项目/addon/server、未运行状态、editor/minimal 错误通道及 CLI 正常退出均已复核，两个隔离 `APPDATA` profile 已移入回收站。本证据把真实 aggregate 相邻失败子场景由十八个扩为十九个、早期边界由九个扩为十个；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load graph-state lookup 后故障证据（2026-08-23）：Debug-only `SaveManager` partial hook 位于真实 `StartLoad()` 的 `Task.Run` worker 内；`CreateSlotStore().PrepareLoad(...)` 已产出 source slot 对应、含 1 个 participant 的 `PreparedSaveSlot`，`slot.GetPreparedState(context.Graph)` 也已返回 `IPreparedSaveState`，随后在生产 `graphState as RoadGraphRevision` 类型确认前一次性抛出 `InvalidOperationException("Injected aggregate Load failure after graph-state lookup.")`。probe 记录非主线程执行、返回状态类型 `RoadGraphRevision` 且只触发一次；revision 验证、renderer pure CLR `Prepare`、Godot Resource Preflight 和 commit 均未开始。operation 以 `finalPhase=3`、`Failed`、`committed=false` 且无 warning 返回，活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，保存后的 payload 逐字节不变，`ObjectResourceCount` 为 `77 -> 77`。结构化结果为 `post_graph_state_lookup_failure_result_kind=2 post_graph_state_lookup_failure_final_phase=3 post_graph_state_lookup_failure_committed=false post_graph_state_lookup_failure_trigger_count=1 post_graph_state_lookup_failure_off_main_thread=true post_graph_state_lookup_observed_state_type=RoadGraphRevision post_graph_state_lookup_resource_count_before=77 post_graph_state_lookup_resource_count_after=77`；既有 renderer worker-Prepare probe 仍触发一次，最终无故障 Load 以 `load_result_kind=0` 成功联合接管。`RoadRendererLifecycleContractTests` 50/50、生命周期与 QA export 聚焦组合 51/51、完整自动化 908/908，双配置 build、Roslyn compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、目标脚本 `--check-only` 与正式 Vulkan 1.4.341 Forward+ 契约均通过；Debug binary 包含新 probe/hook，`ExportRelease` 不包含。MCP 项目/addon/server、未运行状态、editor/minimal 错误通道及 CLI 正常退出均已复核，本轮隔离 `APPDATA` profile 已移入回收站。本证据把真实 aggregate 相邻失败子场景由十九个扩为二十个、早期边界由十个扩为十一个；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load renderer worker preparation 后故障证据（2026-08-23）：Debug-only `SaveManager` partial hook 位于真实 `StartLoad()` 的 `Task.Run` worker 内；`rendererAdmission.Preparer.Prepare(graphRevision)` 已完成纯 CLR presentation 准备并返回 `RoadRendererPreparedLoad`，随后在 `PreparedLoadWork` 构造返回与任何主线程 Godot Resource Preflight 前一次性抛出 `InvalidOperationException("Injected aggregate Load failure after renderer worker preparation.")`。probe 记录非主线程执行、只触发一次，并确认空 source slot 的 prepared presentation 为 `0 road vertex / 0 surface primitive / 0 node marker`。operation 以 `finalPhase=3`、`Failed`、`committed=false` 且无 warning 返回，活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，保存后的 payload 逐字节不变，`ObjectResourceCount` 为 `77 -> 77`。结构化结果为 `post_renderer_worker_preparation_failure_result_kind=2 post_renderer_worker_preparation_failure_final_phase=3 post_renderer_worker_preparation_failure_committed=false post_renderer_worker_preparation_failure_trigger_count=1 post_renderer_worker_preparation_failure_off_main_thread=true post_renderer_worker_preparation_road_vertex_count=0 post_renderer_worker_preparation_surface_primitive_count=0 post_renderer_worker_preparation_node_marker_count=0 post_renderer_worker_preparation_resource_count_before=77 post_renderer_worker_preparation_resource_count_after=77`；最终无故障 Load 以 `load_result_kind=0` 成功联合接管。`RoadRendererLifecycleContractTests` 51/51、生命周期与 QA export 聚焦组合 52/52、完整自动化 909/909，双配置 build、Roslyn compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、目标脚本 `--check-only` 与正式 Vulkan 1.4.341 Forward+ 契约均通过；Debug binary 包含新 probe/hook，`ExportRelease` 不包含。MCP/editor/minimal 错误通道及 CLI 正常退出均已复核，本轮隔离 `APPDATA` profile 已移入回收站。本证据把真实 aggregate 相邻失败子场景由二十个扩为二十一个、早期边界由十一个扩为十二个；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

  - 真实 aggregate Load prepared work 返回后故障证据（2026-08-23）：Debug-only `SaveManager` partial hook 紧接真实 `StartLoad()` 的 `await Task.Run(...)`；worker 已完成 storage、graph-state lookup 与纯 CLR renderer preparation，并把完整 `PreparedLoadWork` 返回主线程，随后在 `EnsureSceneRequestCurrent(sceneRequest)`、cancellation 复核与任何主线程 Godot Resource Preflight 前一次性抛出 `InvalidOperationException("Injected aggregate Load failure after the preparation worker returned.")`。probe 记录主线程执行、只触发一次、source slot ID、participant 数 `1`，并确认空 source slot 的 prepared presentation 为 `0 road vertex / 0 surface primitive / 0 node marker`。operation 以 `finalPhase=3`、`Failed`、`committed=false` 且无 warning 返回，活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，保存后的 payload 逐字节不变，`ObjectResourceCount` 为 `77 -> 77`。结构化结果为 `post_prepared_work_return_failure_result_kind=2 post_prepared_work_return_failure_final_phase=3 post_prepared_work_return_failure_committed=false post_prepared_work_return_failure_trigger_count=1 post_prepared_work_return_failure_on_main_thread=true post_prepared_work_return_participant_count=1 post_prepared_work_return_road_vertex_count=0 post_prepared_work_return_surface_primitive_count=0 post_prepared_work_return_node_marker_count=0 post_prepared_work_return_resource_count_before=77 post_prepared_work_return_resource_count_after=77`，observed slot ID 与 source slot 一致；最终无故障 Load 以 `load_result_kind=0` 成功联合接管。`RoadRendererLifecycleContractTests` 52/52、生命周期与 QA export 聚焦组合 53/53、完整自动化 910/910，双配置 build、Roslyn compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、目标脚本 `--check-only` 与正式 Vulkan 1.4.341 Forward+ 契约均通过；Debug binary 包含新 probe/hook，`ExportRelease` 不包含。MCP/editor/minimal 错误通道及 CLI 正常退出均已复核，本轮隔离 `APPDATA` profile 已移入回收站。本证据把真实 aggregate 相邻失败子场景由二十一个扩为二十二个、早期边界由十二个扩为十三个；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.2 保持开放。

<a id="v3-grid-rendering2.3"></a>

- [ ] **2.3 建立混合类型视觉、接管与性能门禁**
  - 当前问题：V2 基线只含统一样式，没有类型切换到 mesh 可见的离散延迟，也无法证明 per-edge 颜色/宽度和 junction patch 仍满足规模目标。现有全图 mesh 重建基线约为 10k 159 ms、100k 1170 ms，不能把后台总耗时误报为主线程无卡顿，也不能用普通 mutation 的最终重试成功掩盖 Load 的提交后关键失败。
  - 修改：同时使用 junction-dense 10k Edge 和 geometry-dense 长 Edge 数据集，记录 Node/Edge/geometry/query fragment/self-loop/parallel Edge/junction patch/surface primitive 数；分别测量主线程 render snapshot capture、mesh+surface presentation commit、full-reset barrier 总时长、Load hidden resource Preflight 与联合 commit、后台或分帧 polygon/tessellation/index 总耗时，及镜头、闭环/类型化建造预览、各 owner kind 命中、拆除/改造预览、1/100/1000 Edge 改造与撤销重做、draw calls、objects、primitives、子节点和分配量。扰动完整 `RoadRenderToken` 的每个维度验证过期构建丢弃；普通 mutation 接管前旧 mesh 完整可见且所有 `RoadSurfaceHit`/道路命令被锁，失败进入可观测 stalled 状态并可重试；Load 在 Preflight 关键失败时不提交，在成功时联合交换且不经过 stalled/retry。100k 使用相同口径压力测试。先记录同机 V2 对照并固定主线程离散门槛，再决定是否需要分块 mesh。
  - 依赖：`v3-grid-rendering:2.2`、`v3-tool-input:2.2`、`v3-tool-input:2.4`。
  - 集成负责人：`v3-grid-rendering`；最终组合验收由 `v3-road-graph:8.6` 负责。
  - 验证：真实 `MapTest` / Vulkan Forward+ 自动化；四类 T/X/锐角、semantic boundary、简单环/棒棒糖/两路口环可辨识截图及像素差；性能与 sequence 接管文档；普通 mutation 的成功/失败/重试时序，以及 Load 每个关键 Preflight 故障点、一次联合交换、普通 observer warning 和无提交后关键表现失败断言。
  - 验收：10k 连续交互 P95 不超过 16.67 ms，静态道路节点和 draw call 不随 Edge 数线性增长；snapshot capture、普通 presentation commit、full-reset barrier、Load Preflight/联合 commit 和离散改造满足 Phase 0 固定门槛；旧/新 mesh、surface index 和 token 不混代，过期任务不覆盖新图，barrier 内无道路交互；Load 成功后 graph/tool/mesh/surface/token/`CurrentSlotID` 同代可见，失败只保留旧会话，observer warning 不回滚成功提交；100k 结果完整记录但不阻塞 V3。
  - 当前基线（2026-08-14）：统一样式 Vulkan 完整运行中，10k camera/preview/highlight P95 为 0.657/0.779/0.672 ms，Load 与 renderer rebuild 为 608.025 ms；首轮 100k 为 13.517/0.722/0.699 ms、重建 4108.956 ms，独立 100k 复跑为 0.616/0.661/0.637 ms、重建 4492.629 ms，两轮均输出 PASS，静态 renderer 节点为 2。首轮 100k camera 的 13.517 ms 尾延迟与复跑值同时保留，不能只报告热复跑。
  - 分级 ribbon 基线（2026-08-14）：加入 per-edge vertex color 后，独立真实 Vulkan 10k camera/preview/highlight P95 为 0.564/0.827/0.833 ms、Load 与 renderer rebuild 为 665.452 ms；随后独立 100k 为 8.674/0.977/0.737 ms、重建 4719.153 ms。两档均输出 PASS，draw call 为 4/5/4，静态 renderer 节点为 2；100k 仍只记录压力结果，不取代 10k 硬门槛。
  - Terminal cap 增量基线（2026-08-14）：本轮只执行 1k Vulkan，camera/preview/highlight P95 为 0.393/0.378/0.339 ms、renderer rebuild 135.918 ms、静态 renderer 节点为 2，并输出 PASS。该结果不刷新既有 10k/100k 基线，也不满足 2.3 的规模门禁。
  - Load 线性恢复复验（2026-08-17）：独立直线数据集的 12k/20k/40k/60k/80k/100k Load 与 renderer rebuild 为 779.503/1193.439/2428.374/3431.815/4463.697/5129.679 ms，100k camera/preview/highlight P95 为 0.660/0.658/0.675 ms；独立 10k `--enforce-budget` 为 0.461/0.482/0.491 ms、重建 648.221 ms。各级均 PASS，draw call 为 4/5/4、静态 renderer 节点为 2；该数据证明 RoadGraph Load 二次退化未回归，但不含 junction-dense/geometry-dense、四类 owner、类型改造或完整 token 扰动，不能替代本项的完整性能矩阵。
  - 当前 CLI/Godot 收口复验（2026-08-20）：完整自动化 849/849、Debug/`ExportRelease` build 0 警告/0 错误；Release C# `--enforce-budget` 10k/100k 多交叉 P95 为 7.575/6.339 ms。隔离 `APPDATA` 的 Vulkan `road_rendering_performance_contract.gd --enforce-budget` 输出 `PASS`，10k camera/preview/highlight P95 为 0.549/0.541/0.572 ms、Load/renderer rebuild 718.363 ms，100k 为 0.710/0.682/0.689 ms、Load/renderer rebuild 4617.638 ms，静态 renderer 节点为 2。该复验刷新独立直线 fixture 的当前结果，不含 junction-dense/geometry-dense、四类 owner、类型改造或完整 token 扰动；Roslyn/Godot MCP/DAP 未暴露，headless editor 的 UID/6550 环境错误和直接脚本的既有 `ToolManager.Instance` warning 不计作代码通过。
  - 完整 token 扰动进展（2026-08-20）：`RoadRenderTokenTests.AnyTokenDimensionPerturbationRejectsLateBuildFailure` 逐一扰动 `SceneGeneration`、`GraphFacadeID`、`GraphFacadeGeneration`、`ChangeSequence`、`RoadStyleRevision`、`RenderRequestID` 六个维度；每个迟到失败均被拒绝，旧 presented、当前 desired 和 stalled 状态保持不变。聚焦与完整自动化分别为 21/21、855/855，Debug 与 `ExportRelease` build 均为 0 警告/0 错误。该切片只覆盖纯 CLR 接管门禁，不替代真实 renderer/Load 故障注入或规模性能矩阵。
  - Junction-dense Vulkan 进展（2026-08-20）：`road_rendering_performance_contract.gd --dataset-kind=junction-dense --dataset-size=10000/100000 --enforce-budget` 在真实 `MapTest`、Vulkan Forward+、隔离 `APPDATA` 下均输出 PASS。10k 为 camera/preview/highlight P95 `0.559/2.056/0.640 ms`、Load/renderer rebuild `723.610 ms`；100k 为 `0.720/0.630/0.695 ms`、`5415.270 ms`。两档 draw call 为 `4/5/4`、objects 为 `4/56/4`、静态 renderer 节点均为 `2`，10k 三类连续帧均低于 `16.67 ms` 硬门槛；数据集每四条 Edge 共享一个四向 junction 并轮换四类 `RoadType`。该切片覆盖真实 junction patch、混合颜色、Load 与三类交互，但 geometry-dense、四类 owner 命中、离散类型改造时延、完整 token 扰动性能矩阵和逐关键故障注入仍开放。
  - Geometry-dense Vulkan 进展（2026-08-20）：`road_rendering_performance_contract.gd --dataset-kind=geometry-dense --dataset-size=10000/100000 --enforce-budget` 在真实 `MapTest`、Vulkan Forward+、隔离 `APPDATA` 下均输出 PASS；每条独立 Edge 使用 8 个 line geometry segment，100k 总计 800,000 段，低于 `RoadGraphCapacity.MaximumGeometrySegments = 4,000,000`。10k camera/preview/highlight P95 为 `0.482/0.540/0.447 ms`、Load/renderer rebuild `2425.009 ms`；100k 为 `0.681/0.702/0.647 ms`、`23934.401 ms`，draw call 为 `4/5/4`、objects 为 `4/56/4`、静态 renderer 节点为 `2`，primitives 为 `2,000,004/2,000,056/2,000,004`。10k 三类连续帧均低于 `16.67 ms`；100k 只作为压力记录。该切片覆盖 geometry-dense Load、混合类型 mesh 与三类交互，但四类 owner 命中、离散类型改造时延、完整 token 扰动性能矩阵和逐关键故障注入仍开放。
  - 离散类型改造基线（2026-08-20）：`road_rendering_performance_contract.gd --dataset-kind=grid --measure-type-change --dataset-size=10000/100000 --enforce-budget` 通过真实 `RoadBuilder` 矩形选择、`ConfirmUpgrade()`、`UndoLastEdit()` 与 `RedoLastEdit()` 测量调用开始到 matching presentation 的总时延；每个批次都断言精确选择 1/100/1000 Edge、每次 `ChangeSequence + 1`、undo/redo 栈只移动一个边界、rendered Edge 数不变。10k 的 upgrade 为 `196.606/221.815/229.789 ms`、undo 为 `121.617/131.934/99.196 ms`、redo 为 `119.818/129.936/95.386 ms`；100k 分别为 `1395.043/1328.287/1281.294 ms`、`951.968/906.888/925.009 ms`、`922.485/912.903/918.522 ms`。两档均输出 PASS，静态 renderer 节点为 2；这些是一次性全表现接管离散基线，不套用 `16.67 ms` 连续帧门槛。主线程 snapshot/commit 分段、四类 owner 命中性能、完整 token 扰动性能与逐关键故障注入仍开放。
  - 固定离散改造门禁（2026-08-21）：`road_rendering_performance_contract.gd` 将 `--enforce-budget` 的 10k `grid` 档固定为 1/100/1000 Edge 的 upgrade/undo/redo 三元门槛，分别为 `350/250/250 ms`、`400/300/300 ms`、`500/400/400 ms`；门槛只约束一次性 10k 接管，不替代 `16.67 ms` 连续帧门。真实 Vulkan Forward+ 的 10k camera/preview/highlight P95 为 `0.566/0.552/0.542 ms`，Load/renderer rebuild 为 `686.841 ms`；三档实际 upgrade/undo/redo 分别为 `231.976/117.160/118.457 ms`、`181.826/119.210/112.865 ms`、`230.531/152.095/156.811 ms`，均输出 `PASS`。100k 同口径压力为 camera/preview/highlight `0.694/0.721/0.699 ms`、Load/renderer rebuild `5402.321 ms`，三档为 `1619.649/999.881/1050.825 ms`、`1462.646/994.870/978.441 ms`、`1462.799/994.955/987.573 ms`；因不套用 10k 门槛仍输出 `PASS`。唯一 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`，headless 运行提前结束未作为通过证据。
  - 平行 Edge 工具切片（2026-08-21）：新增 `road_parallel_edge_runtime_contract.gd`，以独立 V3 payload 构造同一端点对、两条非重叠多段 Edge；真实 Vulkan aggregate Load 后，两处中心线查询均返回 `EdgeRibbon` 且 Edge ID 不同。点选和矩形框选拆除的确认、undo/redo 均只减少/恢复上方 Edge；点选和矩形框选 RoadUpgrade 的确认、undo/redo 均只作用于下方 Edge，存档往返断言 `street/arterial`、`street/highway`、`street/arterial`，最终两个 surface owner 仍可独立命中。契约输出 `PASS`，唯一 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`；此前 closed-ribbon 旧夹具在第二支路提交处失败，未作为本切片证据。
  - 点选回放补强（2026-08-21）：`road_parallel_edge_runtime_contract.gd` 继续覆盖点选拆除的 `RedoLastEdit()` owner 保持，以及点选 RoadUpgrade 的保存、undo、redo 三段 RoadType 往返（`street/highway -> street/arterial -> street/highway -> street/arterial`），并在后续矩形工具前重新发布原始 fixture。磁盘脚本 `godot --headless --check-only` 通过，完整 .NET 自动化为 `863/863`，Debug build 为 0 警告/0 错误；本轮真实 Godot Vulkan 重跑在保存后续文件接管处无响应，未记录 PASS 或新增运行时证据，故 2.0 仍保持开放。
  - 同类型平行边界规范化（2026-08-21）：`RoadGraph.Diagnostics` 拒绝同类型 degree-2 boundary，`RoadGraph.Canonicalization.TryMergeAtNode()` 按 `RoadType` 合并两条同类型 Edge；因此矩形 RoadUpgrade 回归把下方 `highway` 改为异类型 `arterial`，验证工具只改变目标 owner 且不触发语义边界消失。该约束与第三代指南 6.2 的 merge key 规则一致，不把同类型合并误报为平行 Edge 工具缺陷。
  - 四类 owner 契约提交（2026-08-20）：`b42188d` 为 `road_rendering_performance_contract.gd` 增加 `owner-dense` fixture 与 `--measure-owner-hits`；每个单元固定 8 Edge/13 Node，包含三条东/浅东/反向西锐角 junction Edge、两条异类型 `SemanticJoin` Edge 和三条独立 Edge，并对 `EdgeRibbon`、`TerminalCap`、`SemanticJoin`、`JunctionPatch` 各执行 20 批 × 1,000 次查询，断言 matching render token、surface distance 为 0、canonical location 和 owner 全命中。提交前 `dotnet test SimpleCities.sln --no-restore` 为 855/855，Debug/`ExportRelease` build 均为 0 警告/0 错误；本会话的 Godot CLI 受现有编辑器 PID `74652`/场景初始化环境门阻塞，未记录 owner 10k/100k 运行时指标，不能据此关闭性能门。
  - 纯 CLR 空间索引规模证据（2026-08-20）：`2e5ba65` 在 `RoadSurfaceSnapshotTests.PointQueryKeepsExactTriangleWorkLocalAtPerformanceContractScale` 增加 10,000 与 100,000 Edge 两档，每条 Edge 生成两个 ribbon triangle，并查询中间 Edge。聚焦 `RoadSurfaceSnapshotTests` 通过 21/21；两档均断言命中目标 Edge、索引节点访问 `1..64`、primitive 候选 `2..8`、exact triangle tests 精确为 `2` 且候选数小于总 primitive 数；完整自动化为 857/857，Debug/`ExportRelease` build 均为 0 警告/0 错误。该证据只覆盖纯 CLR AABB 索引局部性，不替代真实 Godot 四类 owner 命中、snapshot/commit 时延或逐关键故障矩阵；Godot owner-dense 运行时门仍保持阻塞。
  - Owner-dense Vulkan 收口复验（2026-08-21，修复提交 38afbbd）：真实 Vulkan Forward+、隔离 APPDATA 的 road_rendering_performance_contract.gd --dataset-kind=owner-dense --dataset-size=10000/100000 --measure-owner-hits --enforce-budget 均输出 PASS。10k camera/preview/highlight P95 为 0.482/0.467/0.468 ms、Load/renderer rebuild 为 736.276 ms；100k 为 0.573/0.625/0.561 ms、Load/renderer rebuild 为 6135.119 ms。两档 draw call 为 4/5/4、objects 为 4/56/4、静态 renderer 节点为 2；10k 连续帧均低于 16.67 ms。四类 EdgeRibbon、TerminalCap、SemanticJoin、JunctionPatch 均完成 20 批 × 1,000 次查询，JunctionPatch 查询 P95 为 0.065123/0.040361 ms，surface primitives 为 41,250/412,500。Roslyn compiler/analyzer、GDScript diagnostics 与 Godot editor error 均为 0；唯一 warning 为既有 ConstructionDock 缺少 ToolManager.Instance。该切片关闭 owner-dense 运行时缺口，但完整 token 扰动性能、snapshot/commit 分段、逐关键故障注入和平行 Edge 工具矩阵仍开放。
  - 当前 100k grid Vulkan Load 复验（2026-08-21）：隔离 APPDATA 的 `road_rendering_performance_contract.gd --dataset-kind=grid --dataset-size=100000 --enforce-budget` 退出码为 0 并输出 `PASS`；camera/preview/highlight P95 为 `0.635/0.664/0.669 ms`，Load/renderer rebuild 为 `4980.748 ms`，draw call 为 `4/5/4`、objects 为 `4/56/4`、静态 renderer 节点为 `2`。stderr 仅有既有 `ConstructionDock: ToolManager.Instance is missing` warning。该次复验尚未记录 snapshot/commit 分段，且当时 Roslyn CodeLens 与 Godot MCP/DAP 未暴露；下条切片补齐 Load 分段口径，但完整 token 性能扰动、逐关键故障注入与完整性能矩阵仍开放，因此 2.3 不关闭。
  - Load 分段性能可观测性（2026-08-21）：`SaveManager` 在成功 Load 后保存带 operation token/target slot 的 worker Prepare、主线程 hidden-resource Preflight、reference commit、aggregate commit 与端到端快照；`PreparedAggregateLoad` 的 reference 指标只包围 generation 复核与全部引用交换。`road_rendering_performance_contract.gd` 直接等待真实 `StartLoad()` 并输出 `LOAD_PHASE_RESULT`。隔离 APPDATA 的 Vulkan grid 1k/10k/100k 分段分别为 `113.216/9.839/0.713/1.949/126.841 ms`、`622.155/24.648/1.769/2.997/651.217 ms`、`4770.893/151.645/4.168/5.621/4929.850 ms`，外部观测 Load 为 `132.327/657.366/4938.626 ms`；100k camera/preview/highlight P95 为 `0.656/0.642/0.683 ms`，draw call `4/5/4`、objects `4/56/4`、静态节点 `2`。三档均 PASS，唯一 warning 为既有 `ConstructionDock` 缺少 `ToolManager.Instance`。聚焦 aggregate 16/16、完整自动化 868/868、双配置 build、Roslyn compiler 与目标 GDScript diagnostics 为 0，editor/DAP 错误通道为空；analyzer 未获信任授权，未运行。该切片只建立 Load 分段口径，尚未基于多轮冷/热样本固定门槛。
  - 普通 mutation 分段性能可观测性（2026-08-21）：`RoadRenderer.GetLastPresentationPerformanceMetrics()` 仅在 matching `RoadRenderToken` 完成 presentation commit 后发布不可变快照，区分 style/root snapshot capture、纯 CLR Prepare、Godot Resource Preflight、presentation commit、rebuild total 与 graph event 到 ready，并携带 attempt 和 full-reset 分类。性能契约对 1/100/1000 Edge 的 upgrade/undo/redo 每次匹配表现都拒绝旧 token、full-reset 误分类、负值和不满足 `phase sum <= rebuild total <= request-to-ready` 的数据，再输出 `PRESENTATION_PHASE_RESULT`。真实 Vulkan grid 10k 的 snapshot/Prepare/Resource/commit/rebuild/request-to-ready 范围为 `0.033～0.043/42.916～67.472/3.368～4.845/0.023～0.035/47.465～72.010/48.042～72.638 ms`；100k 为 `0.030～0.090/498.725～561.087/36.216～44.828/0.040～0.052/537.191～606.009/539.680～606.375 ms`。两档各九次 matching mutation 均 PASS，10k 固定离散门槛继续通过；1k smoke、10k、100k 的 camera/preview/highlight P95 分别为 `0.426/0.490/0.438`、`0.464/0.477/0.470`、`0.648/0.662/0.659 ms`。生命周期聚焦 11/11、完整自动化 869/869、双配置 build、Roslyn compiler 与目标 GDScript diagnostics 为 0；编辑器保持 `MapTest` 且未运行，DAP stderr/console 为空，editor error 仅有既有 LSP 重复打开文件。唯一 Vulkan warning 仍为既有 `ConstructionDock` 缺少 `ToolManager.Instance`。本切片不基于单轮样本新增阈值。
  - Non-aggregate full-reset barrier 基线（2026-08-22）：Debug-only `RoadFullResetPerformanceProbe` 以 O(1) `CaptureSnapshot()` 驱动既有单参与者 `RoadGraph.CommitPreparedLoad()`，`road_rendering_performance_contract.gd --measure-full-reset` 在同一同步调用返回前要求 renderer ready、desired/presented/metrics token 完全一致，graph lineage 更新、`ChangeSequence + 1`，render token 只推进 facade generation/change sequence/request ID，并保持 scene/facade/style、Edge 数、mesh 顶点、marker 与 surface primitive 不变。隔离 APPDATA 的 Vulkan Forward+ grid 1k/10k/100k 均以退出码 0 输出 `FULL_RESET_BARRIER_RESULT` 与 `PASS`；barrier 总时长分别为 `7.036/84.139/733.651 ms`，snapshot/Prepare/Resource/commit/rebuild/request-to-ready 分别为 `0.031/4.679/0.625/0.019/5.417/5.493 ms`、`0.035/59.112/11.336/0.118/71.270/71.468 ms`、`0.038/605.915/41.139/0.035/647.307/647.488 ms`。10k camera/preview/highlight P95 为 `0.532/0.488/0.442 ms`，100k 为 `0.664/0.705/0.657 ms`；静态节点均为 2。生命周期聚焦 12/12、完整自动化 870/870、Debug/`ExportRelease` build 与 Roslyn compiler/GDScript diagnostics 为 0；MSBuild 证明 probe 不进入 `ExportRelease`，QA export 也显式排除源文件和 UID。单轮样本不固定新阈值，2.3 保持开放。
  - 关键 Resource 故障矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已用真实 Godot Resource 关闭 `RoadSurfaceSnapshot` 构造点、未提交 `RoadRendererLoadCommitPlan.Dispose()`、`CreateNodeBatch` 工厂内部异常、`CreateRoadMesh` 所有权边界、普通更新 token Resource Preflight、普通更新 token 的真实 `CreateNodeBatch` 与 `CreateRoadMesh` 工厂故障、真实 aggregate Load 的 renderer Resource Preflight、reserved render token 创建故障、`RoadSurfaceSnapshot`/`RoadRendererLoadCommitPlan` 构造故障与 `CreateRoadMesh`/`CreateNodeBatch` 工厂故障、renderer plan 加入后与 slot plan 加入后的后续 aggregate Preflight fallback disposal，以及 aggregate ownership 后的 pre-commit disposal，分别证明 plan 接管前失败、plan 放弃、两个 factory 初始化中途失败、普通表现提交前失败、普通 node-batch factory 内失败、普通 road-mesh factory 内失败、renderer plan 构造前失败、真实 `StartLoad()` 中 road-mesh/node-batch factory 内、reserved token 创建期、snapshot 构造期与 commit-plan constructor 返回前失败、renderer plan 已收集但 slot plan 尚未创建时失败、slot plan 也已收集但 aggregate 尚未接管时失败，以及 aggregate 接管后但 commit 尚未开始时失败；各边界都由唯一责任方同步释放已建资源，renderer admission 与后续 Load 可继续。
  - 仍缺（保持开放）：Junction Patch 已通过聚焦功能、Load 和普通 T junction 视觉验证，普通 mutation stalled/retry、现有玩家道路事务 admission、deferred continuation 代际失效及 token 绑定的 snapshot/Prepare/Resource/commit/rebuild/request-to-ready 分段已有契约；junction-dense、geometry-dense、四类 owner 命中和离散类型改造的 10k/100k 总时延基线已经建立，10k 离散改造固定门槛已由真实 Vulkan 运行时执行，Load worker Prepare/Preflight/reference/aggregate/total 分段口径、non-aggregate full-reset barrier 基线、四个已定位 Resource 所有权点、普通更新 token Resource Preflight、普通更新 token 的真实 `CreateNodeBatch` 与 `CreateRoadMesh` 工厂故障、九个真实 aggregate Preflight 故障子场景、真实 RoadGraph/renderer/tool/slot-target plans 与各自 tracking/fake companions 的直接 commit-boundary generation 失配，以及真实四参与者 `StartLoad()` 的 RoadGraph/renderer/tool/slot-target 同边界失配也已建立。完整 token 扰动性能矩阵、其余 renderer 故障和其他真实 aggregate 组合注入仍未完成。现有聚焦证据、四条分级直线截图、ribbon hit、历史 1k cap PASS、patch 小图和小规模 placement/拆除/改造/回放/重试都不能替代完整接管与规模门禁。

  - 普通更新 snapshot 构造矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 新增真实 ordinary rebuild 的 `RoadSurfaceSnapshot` 构造故障点；两个工厂完成后的 `ArgumentNullException("prepared")` 由外层唯一责任方释放 road mesh 与 node batch，资源计数保持 `77 -> 77`，同一 desired token 在 attempt 2 成功。完整自动化 880/880、生命周期与 QA export 聚焦 23/23、双配置 build、Roslyn/GDScript diagnostics 与 Vulkan/MCP 运行时门均通过。该证据只补齐一个普通更新资源边界，不替代完整 token 性能扰动、其余 renderer 故障或其他真实 aggregate 组合注入，因此 2.3 保持开放。

  - Pre-commit token 取代矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已在两个 Resource 和 snapshot 准备完成后，以真实 `RenderRequestID` 取代证明过期 attempt 不交换表现引用且同步释放资源，replacement attempt 1 可继续发布；资源计数保持 `77 -> 77`。完整自动化 881/881、生命周期与 QA export 聚焦 24/24、双配置 build、Roslyn/GDScript diagnostics 与 Vulkan/MCP 运行时门均通过。该证据不是六分量真实运行时取代或完整性能扰动矩阵，因此 2.3 保持开放。

  - Pre-commit 样式 token 取代矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已在相同资源边界增加真实 `RoadStyleRevision + 1`、`RenderRequestID + 1` 的 replacement，证明样式目标过期同样不会交换混代 mesh/surface/token，资源计数保持 `77 -> 77`，replacement attempt 1 可继续发布。完整自动化 882/882、生命周期与 QA export 聚焦 25/25、双配置 build、Roslyn/GDScript diagnostics 与 Vulkan/MCP 运行时门均通过。当前只覆盖六分量中的 `RenderRequestID` 与 `RoadStyleRevision`，尚不能替代其余四分量或完整性能扰动矩阵，因此 2.3 保持开放。

  - Pre-commit scene token 取代矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已在连续 Load 之后以正式 `SetSceneGeneration()` 增加 `SceneGeneration + 1`、`RenderRequestID + 1` 的 replacement，证明 scene 目标过期同样不交换混代表现，资源计数保持 `77 -> 77`，replacement attempt 1 可继续发布。完整自动化 883/883、生命周期与 QA export 聚焦 26/26、双配置 build、Roslyn/GDScript diagnostics 与 Vulkan/MCP 运行时门均通过。当前六分量覆盖为三项，尚不能替代剩余 facade ID、facade generation、change sequence 或完整性能扰动矩阵，因此 2.3 保持开放。

  - Pre-commit graph facade generation 取代矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已通过正式 `RequestGraphChange(..., isFullReset: true)` 增加 `GraphFacadeGeneration + 1`、`RenderRequestID + 1` 的 replacement，并保持 scene、facade ID、change sequence 与 style 不变，证明 full-reset 表现代际目标过期同样不交换混代 mesh/surface/token，资源计数保持 `77 -> 77`，replacement attempt 1 可继续发布。完整自动化 884/884、生命周期与 QA export 聚焦 27/27、双配置 build、Roslyn/GDScript diagnostics 与 Vulkan/MCP 运行时门均通过。当前六分量覆盖为四项，尚不能替代剩余 `GraphFacadeID`、`ChangeSequence` 或完整性能扰动矩阵，因此 2.3 保持开放。

  - Pre-commit change sequence 取代矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已由第二次真实 `RoadGraph.SubmitPolyline()` 通过 `GraphChanged` 增加 `ChangeSequence + 1`、`RenderRequestID + 1` 的 replacement，并保持 scene、facade ID、facade generation 与 style 不变；target 资源释放且两次 mutation 只由 replacement attempt 1 联合呈现，资源计数保持 `77 -> 77`。完整自动化 885/885、生命周期与 QA export 聚焦 28/28、双配置 build、Roslyn/GDScript diagnostics 与 Vulkan/MCP 运行时门均通过；minimal DAP 因既有 8-client 上限未接通。当前六分量覆盖为五项，尚不能替代剩余 `GraphFacadeID` 或完整性能扰动矩阵，因此 2.3 保持开放。

  - Pre-commit graph facade ID 取代矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已通过真实 `RoadRenderer.SetGraph()` 切换到新 `RoadGraph`，让 replacement 更换 `GraphFacadeID`、增加 `GraphFacadeGeneration + 1` 与 `RenderRequestID + 1`，同时保持 scene、change sequence 与 style 不变；同步嵌套重建以 attempt 1 发布 replacement，外层 target 不交换引用并释放资源，旧 graph builder 也由 facade ID 门禁拒绝提交。资源计数保持 `77 -> 77`。完整自动化 886/886、生命周期与 QA export 聚焦 29/29、双配置 build、Roslyn/GDScript diagnostics 与 Vulkan/MCP 运行时门均通过；minimal DAP 因既有 8-client 状态未接入。至此六分量真实 pre-commit 取代全部覆盖，但尚不能替代完整 token 性能扰动、其余 renderer 故障或其他真实 aggregate 组合注入，因此 2.3 保持开放。

  - 普通 stalled observer 故障矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已在真实 ordinary Resource Preflight 故障上叠加 `PresentationStalled` observer 异常，证明首个 observer 失败不会阻断后续 observer，也不会替换原 stalled 事实、泄漏未转交 Resource 或阻止同 token attempt 2 恢复。资源计数保持 `77 -> 77`，完整自动化 887/887、生命周期与 QA export 聚焦 30/30、双配置 build、Roslyn/GDScript diagnostics 与 Vulkan/MCP 运行时门均通过。该证据不替代完整 token 性能扰动、其余 renderer 故障或其他真实 aggregate 组合注入，因此 2.3 保持开放。

  - 真实 aggregate node-batch factory 故障矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已通过含 1 Edge 的真实 `StartLoad()` 在 renderer Preflight 内触发 `CreateNodeBatch` marker loop 异常，证明 factory 与外层 Preflight 分别释放 node batch 和已创建 road mesh，四方状态零提交且后续 Load 可重新接管。资源计数保持 `77 -> 77`，完整自动化 888/888、生命周期与 QA export 聚焦 31/31、双配置 build、Roslyn/GDScript diagnostics 与 Vulkan/MCP 运行时门均通过；minimal DAP 仍返回 `No active debug session`。该证据不替代完整 token 性能扰动、其余 renderer 故障或其他真实 aggregate 组合注入，因此 2.3 保持开放。

  - 真实 aggregate road-mesh factory 故障矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已通过含 1 Edge 的真实 `StartLoad()` 在 renderer Preflight 内触发 `CreateRoadMesh` indices 枚举异常，证明 factory ownership helper 释放已分配但未写入 surface 的 `ArrayMesh`，node batch 不会提前创建，四方状态零提交且后续 Load 可重新接管。资源计数保持 `77 -> 77`，完整自动化 889/889、生命周期与 QA export 聚焦 32/32、双配置 build、Roslyn/GDScript diagnostics 与 Vulkan/MCP 运行时门均通过；minimal DAP 仍返回 `No active debug session`。该证据不替代完整 token 性能扰动、其余 renderer 故障或其他真实 aggregate 组合注入，因此 2.3 保持开放。

  - 真实 aggregate snapshot 构造故障矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已通过含 1 Edge 的真实 `StartLoad()` 在两个 Resource、统一 Resource Preflight 与 reserved render token 完成后触发 `RoadSurfaceSnapshot` 的 `ArgumentNullException("prepared")`，证明外层 Preflight catch 同步释放 road mesh 与 node batch，四方状态零提交且后续 Load 可重新接管。资源计数保持 `77 -> 77`，完整自动化 890/890、生命周期与 QA export 聚焦 33/33、双配置 build、Roslyn/GDScript diagnostics 与 Vulkan/MCP 运行时门均通过；minimal DAP 仍返回 `No active debug session`。该证据不替代完整 token 性能扰动、其余 renderer 故障或其他真实 aggregate 组合注入，因此 2.3 保持开放。

  - 真实 aggregate reserved render token 创建故障矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已通过含 1 Edge 的真实 `StartLoad()` 在两个 Resource 与统一 Resource Preflight 完成后让局部 reservation 副本失效，触发真实 `CreateReservedLoadToken()` 的 stale-reservation 异常，证明 tracker 与 admission reservation 不变、外层 Preflight catch 同步释放 road mesh 与 node batch，四方状态零提交且后续 Load 可重新接管。资源计数保持 `77 -> 77`，完整自动化 891/891、生命周期与 QA export 聚焦 34/34、双配置 build、Roslyn/GDScript diagnostics 与 Vulkan/MCP 运行时门均通过；minimal DAP 仍返回 `No active debug session`。该证据不替代完整 token 性能扰动、其余 renderer 故障或其他真实 aggregate 组合注入，因此 2.3 保持开放。

  - 真实 aggregate commit-plan 构造故障矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已通过含 1 Edge 的真实 `StartLoad()` 在 snapshot 完成后、`RoadRendererLoadCommitPlan` constructor 返回前触发一次性异常，证明 plan 未取得所有权时外层 Preflight catch 仍同步释放 road mesh 与 node batch，四方状态零提交且后续 Load 可重新接管。资源计数保持 `77 -> 77`，完整自动化 892/892、生命周期与 QA export 聚焦 35/35、双配置 build、Roslyn/GDScript diagnostics 与 Vulkan 运行时门均通过；Godot MCP 运行树与 minimal console 通过，editor 门仅观察到既有桥接基础设施噪声。该证据不替代完整 token 性能扰动、其余 renderer 故障或其他真实 aggregate 组合注入，因此 2.3 保持开放。

  - 真实四参与者 commit-boundary generation 失配矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已把 renderer generation 失配从“真实 renderer plan + 三个 tracking companions”的直接 probe 扩展到真实 graph/tool/presentation/slot `StartLoad()`；storage `CrossCommitBoundary()` 内释放 renderer admission 后，第二次复核以 `Failed`、`committed=false` 在四方零交换和 `MarkCommitted()=0` 时拒绝，隐藏资源清理后计数保持 `77 -> 77`，最终 Load 可重新接管。完整自动化 893/893、生命周期与 QA export 聚焦 36/36、双配置 build、Roslyn/GDScript diagnostics 与 Vulkan 运行时门均通过；Godot MCP 的项目与 console 门通过，但运行树 RPC 超时，未计作通过。该证据不替代完整 token 性能扰动、其余 renderer 故障或其他真实 aggregate 组合注入，因此 2.3 保持开放。

  - 真实四参与者 ToolManager commit-boundary generation 失配矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已把 tool generation 失配从“真实 tool plan + 三个 tracking companions”的直接 probe 扩展到真实 graph/tool/presentation/slot `StartLoad()`；storage `CrossCommitBoundary()` 内释放 tool/builder admissions 后，第二次复核以 `Failed`、`committed=false` 在四方零交换和 `MarkCommitted()=0` 时拒绝，隐藏资源清理后计数保持 `77 -> 77`，活动 RoadType/placement/history 不变，最终 Load 可重新接管。完整自动化 894/894、生命周期与 QA export 聚焦 37/37、双配置 build、Roslyn/GDScript diagnostics 与隔离 Vulkan 运行时门均通过；Godot MCP 的项目与 console 门通过，但分帧和 runtime-state RPC 超时，未计作运行树通过。该证据不替代完整 token 性能扰动、其余 renderer 故障或其他真实 aggregate 组合注入，因此 2.3 保持开放。

  - 真实四参与者 RoadGraph commit-boundary generation 失配矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已把 graph generation 失配从“真实 graph plan + presentation/slot 两个 tracking companions”的直接 probe 扩展到真实 graph/tool/presentation/slot `StartLoad()`；storage `CrossCommitBoundary()` 内释放 `RoadGraphLoadAdmission` 后，第二次复核以 `Failed`、`committed=false` 在四方零交换和 `MarkCommitted()=0` 时拒绝，隐藏 renderer 资源清理后计数保持 `77 -> 77`，活动 RoadType/placement/history 与 graph payload 逐值不变，最终 Load 可重新接管。完整自动化 895/895、生命周期与 QA export 聚焦 38/38、双配置 build、Roslyn/GDScript diagnostics 与隔离 Vulkan 运行时门均通过；Godot MCP 的项目与 console 门通过，但分帧 RPC 超时，未计作运行树通过。该证据不替代完整 token 性能扰动、其余 renderer 故障或其他真实 aggregate 组合注入，因此 2.3 保持开放。

  - 真实四参与者 slot-target commit-boundary generation 失配矩阵进展（2026-08-22）：`v3-save-system:2.3` 已把 slot generation 失配从“真实 `SlotTargetLoadCommitPlan` + graph/tool/presentation fake companions”的计划级契约扩展到真实 graph/tool/presentation/slot `StartLoad()`；storage `CrossCommitBoundary()` 内以 `NextGeneration()` 推进 `_currentSlotGeneration` 后，第二次复核以 `Failed`、`committed=false` 在四方零交换和 `MarkCommitted()=0` 时拒绝，隐藏 renderer 资源清理后计数保持 `77 -> 77`，活动 RoadType/placement/history、graph payload 与 `CurrentSlotID` 不变，最终 Load 可重新接管。完整自动化 896/896、生命周期与 QA export 聚焦 39/39、双配置 build、Roslyn/GDScript diagnostics、隔离 GDScript `--check-only` 与 Vulkan 运行时门均通过；Godot MCP 项目/编辑器门、minimal console 与 editor 增量错误门通过，Debug DLL 包含 probe 而 `ExportRelease` 不包含，测试槽与 QA 根已清理。该证据不替代完整 token 性能扰动、其余 renderer 故障或其他真实 aggregate 组合注入，因此 2.2、2.3 均保持开放。

  - `RenderRequestID` 分级性能矩阵进展（2026-08-22）：真实 Vulkan grid 1k/10k/25k/50k/100k 的提交前拒绝/恢复/合计分别为 `45.680/9.888/55.568 ms`、`232.832/59.794/292.626 ms`、`532.356/155.228/687.584 ms`、`787.032/294.867/1081.899 ms`、`1612.186/579.119/2191.305 ms`；10k camera/preview/highlight P95 为 `0.485/0.538/1.743 ms`，100k 为 `0.730/0.709/0.667 ms`。五档均保持资源 `77 -> 77`、Edge `N -> N+1`，且 superseded/replacement 仅 `RenderRequestID: 3 -> 4`。完整自动化 896/896、双配置 build、Roslyn/GDScript diagnostics 与隔离 `--check-only` 均通过；MCP 的项目/Vulkan/game bridge/minimal console 门通过，但 game-time、运行树与 runtime-state RPC 超时，未计作通过。该证据只完成六分量性能矩阵的一个子项，其余五分量及 renderer/aggregate 故障矩阵仍开放，因此 2.3 不关闭。

  - `RoadStyleRevision` 分级性能矩阵进展（2026-08-22）：真实 Vulkan grid 1k/10k/25k/50k/100k 的提交前拒绝/恢复/合计分别为 `46.001/9.939/55.940 ms`、`221.569/58.811/280.380 ms`、`572.255/175.426/747.681 ms`、`830.529/298.035/1128.564 ms`、`1568.166/568.939/2137.105 ms`；camera/preview/highlight P95 依次为 `0.415/0.405/0.401 ms`、`0.531/0.753/1.544 ms`、`0.555/0.573/0.535 ms`、`0.812/0.817/0.807 ms`、`0.866/0.717/0.681 ms`。五档均保持资源 `77 -> 77`、Edge `N -> N+1`，superseded/replacement 的 `RoadStyleRevision` 为 `1 -> 2`、`RenderRequestID` 为 `3 -> 4`，其他四个 token 分量稳定，两个 attempt 均为 1；原 `render-request` 1k 兼容回归也继续 PASS。完整自动化 896/896，双配置 build、Roslyn compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离 `--check-only` 均通过；MCP 的项目/Vulkan/game bridge/minimal console 门通过，但 game-time 与 runtime-state RPC 超时，未计作通过。六分量性能矩阵现完成两个子项，其余四分量及 renderer/aggregate 故障矩阵仍开放，因此 2.3 不关闭。

  - `SceneGeneration` 分级性能矩阵进展（2026-08-22）：真实 Vulkan grid 1k/10k/25k/50k/100k 的提交前拒绝/恢复/合计分别为 `47.371/12.581/59.952 ms`、`233.495/63.975/297.470 ms`、`588.961/180.743/769.704 ms`、`875.853/308.198/1184.051 ms`、`1595.644/572.131/2167.775 ms`；camera/preview/highlight P95 依次为 `0.966/0.635/0.579 ms`、`0.622/0.692/0.577 ms`、`0.537/0.512/0.506 ms`、`0.894/0.942/0.694 ms`、`0.725/0.698/0.692 ms`。五档均保持资源 `77 -> 77`、Edge `N -> N+1`，superseded/replacement 的 `SceneGeneration` 为 `1 -> 2`、`RenderRequestID` 为 `3 -> 4`，其他四个 token 分量稳定，两个 attempt 均为 1；`render-request` 与 `road-style` 的 1k 兼容回归也继续 PASS。完整自动化 896/896，双配置 build、Roslyn compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离 `--check-only` 均通过；MCP 确认正确项目、Godot 4.7、`MapTest` 未运行状态和 addon/server 版本匹配，minimal stderr/console 为空，editor 游标 1989 后无新增 error，既有 MCP/DAP transport 错误不计作本切片通过。六分量性能矩阵现完成三个子项，其余三分量及 renderer/aggregate 故障矩阵仍开放，因此 2.3 不关闭。

  - `GraphFacadeID` 分级性能矩阵进展（2026-08-22）：真实 Vulkan grid 1k/10k/25k/50k/100k 的同步取代/接管总耗时分别为 `59.790/304.579/822.712/1273.036/2345.394 ms`；该路径由 `SetGraph()` 同栈发布 replacement，故结构化结果中的 recovery 均为 `0`，总耗时不与 deferred 路径的拒绝阶段单独对比。camera/preview/highlight P95 依次为 `0.477/0.449/0.442 ms`、`0.469/1.953/0.460 ms`、`0.510/0.505/0.515 ms`、`0.581/0.586/0.567 ms`、`0.710/0.673/0.660 ms`。五档均保持资源 `77 -> 77`、Edge `N -> N+1`，`GraphFacadeID: 1 -> 3`、`GraphFacadeGeneration: 2 -> 3`、`RenderRequestID: 3 -> 4`，其他三个 token 分量稳定，两个 attempt 均为 1；前三种模式的 1k 兼容回归继续 PASS。完整自动化 896/896，双配置 build、Roslyn compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、最终隔离 `--check-only` 均通过；MCP 确认正确项目、Godot 4.7、`MapTest` 未运行状态和 addon/server 版本匹配，minimal stderr/console 为空，editor 游标 1989 后无新增 error，既有 MCP/DAP transport 错误不计作本切片通过。六分量性能矩阵现完成四个子项，其余两分量及 renderer/aggregate 故障矩阵仍开放，因此 2.3 不关闭。

  - `GraphFacadeGeneration` 分级性能矩阵进展（2026-08-22）：真实 Vulkan grid 1k/10k/25k/50k/100k 的提交前拒绝/恢复/合计分别为 `46.989/10.046/57.035 ms`、`226.285/59.298/285.583 ms`、`510.660/155.424/666.084 ms`、`811.079/284.769/1095.848 ms`、`1594.093/570.155/2164.248 ms`；camera/preview/highlight P95 依次为 `0.459/0.598/0.483 ms`、`0.490/0.490/0.457 ms`、`0.497/0.534/0.496 ms`、`0.576/0.562/0.553 ms`、`0.690/0.713/0.692 ms`。五档均保持资源 `77 -> 77`、Edge `N -> N+1`，`GraphFacadeGeneration: 2 -> 3`、`RenderRequestID: 3 -> 4`，其他四个 token 分量稳定，两个 attempt 均为 1；`graph-facade-id` 1k 同步兼容回归继续 PASS。完整自动化 896/896，双配置 build、Roslyn compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离 `--check-only` 均通过；MCP 确认正确项目、Godot 4.7、`MapTest` 未运行状态和 addon/server 版本匹配，minimal stderr/console 为空，editor 游标 1989 后无新增 error，既有 MCP/DAP transport 错误不计作本切片通过。六分量性能矩阵现完成五个子项，只余 `ChangeSequence` 及 renderer/aggregate 故障矩阵仍开放，因此 2.3 不关闭。

  - `ChangeSequence` 分级性能矩阵进展（2026-08-22）：真实 Vulkan grid 1k/10k/25k/50k/100k 的提交前拒绝/恢复/合计分别为 `64.741/6.024/70.765 ms`、`372.829/55.966/428.795 ms`、`910.134/172.078/1082.212 ms`、`1224.489/296.431/1520.920 ms`、`2537.144/587.846/3124.990 ms`；camera/preview/highlight P95 依次为 `0.459/0.507/0.463 ms`、`0.487/0.492/0.474 ms`、`0.519/0.522/0.525 ms`、`0.561/0.569/0.590 ms`、`0.662/0.681/0.651 ms`。五档均保持资源 `77 -> 77`、Edge `N -> N+2`，superseded 到 replacement 的 `ChangeSequence` 为 `2 -> 3`、`RenderRequestID` 为 `3 -> 4`，其他四个 token 分量稳定，两个 attempt 均为 1；pending 期两条道路都无 mixed-token hit，恢复后两处 hit 均绑定 replacement token，`graph-facade-generation` 1k 兼容回归继续 PASS。完整自动化 896/896，双配置 build、Roslyn compiler/analyzer、目标及工作区扫描可见的 3 个 GDScript diagnostics 与 `--check-only` 均通过；MCP 确认正确项目、Godot 4.7、`MapTest` 未运行状态和 addon/server 4.1.0 匹配，minimal stderr/console 为空，editor 游标 1989 后无新增 error。Debug DLL 包含 probe，`ExportRelease` 不包含，隔离 QA 根已移入回收站且原路径不存在。六分量性能矩阵已完成；其余 renderer/aggregate 故障矩阵仍开放，因此 2.3 不关闭。

  - 普通更新 token 纯 `Prepare` 故障矩阵进展（2026-08-22）：`v3-grid-rendering:2.2` 已在真实 ordinary mutation 的样式/revision snapshot 后、pure CLR `Prepare` 与 Resource Preflight 前建立第五个相邻故障点。attempt 1 在零 Resource 分配时 stalled，旧表现/cache 与 `ObjectResourceCount 77 -> 77` 保持，旧/未呈现 surface hit 均拒绝；同一 desired token 的 attempt 2 成功。完整自动化 897/897、生命周期与 QA export 聚焦 40/40、双配置 build、Roslyn/GDScript diagnostics、隔离 `--check-only` 与退出码 0 的 Vulkan 运行时门均通过；MCP 项目/addon/server、未运行状态和错误通道也已复核。该证据不替代其余 renderer 故障、其他真实 aggregate 组合或 Phase 8 组合矩阵，因此 2.3 保持开放。

  - 真实 aggregate worker-`Prepare` 故障矩阵进展（2026-08-23）：`v3-grid-rendering:2.2` 已在真实 `StartLoad()` worker 取得 `RoadGraphRevision` 后、pure CLR renderer `Prepare` 前建立零 Resource 分配故障点。失败 operation 保持 graph/tool/presentation/surface/token/hit/slot 与 placement/history，资源计数为 `77 -> 77`，紧邻的无故障 Load 成功重新接管；完整自动化 898/898、生命周期与 QA export 聚焦 41/41、双配置 build、Roslyn/GDScript diagnostics、隔离 `--check-only` 与退出码 0 的 Vulkan 运行时门均通过。该证据把真实 aggregate 相邻失败子场景扩为十个，不替代其余 renderer 故障、其他真实 aggregate 组合或 Phase 8 组合矩阵，因此 2.3 保持开放。

  - 真实 aggregate renderer admission 样式快照故障矩阵进展（2026-08-23）：`v3-grid-rendering:2.2` 已把真实 `StartLoad()` 的最早 renderer 故障点前移到 `BeginLoadAdmission()` 捕获生产样式快照；失败发生在 reservation/admission 发布和 worker 启动前，graph/tool admission 仍释放，四方状态与资源 `77 -> 77` 保持。恢复配置后紧邻 worker 故障和最终无故障 Load 依次证明 admission 可重新取得；完整自动化 899/899、生命周期与 QA export 聚焦 42/42、双配置 build、Roslyn/GDScript diagnostics、构建后的隔离 `--check-only` 与退出码 0 的 Vulkan 运行时门均通过。该证据把真实 aggregate 相邻失败子场景扩为十一个，不替代其余 renderer 故障、其他真实 aggregate 组合或 Phase 8 组合矩阵，因此 2.3 保持开放。

  - 真实 aggregate renderer admission 参数校验故障矩阵进展（2026-08-23）：`v3-grid-rendering:2.2` 已在生产样式快照之后、`ReserveLoad()` 之前覆盖 `RoadRendererLoadSettings.Validate()`；非法 `CurveDisplayTolerance` 使真实 `StartLoad()` 在 renderer admission 构造/发布和 worker 启动前失败，graph/tool admission 仍释放，四方状态与资源 `77 -> 77` 保持。恢复配置后紧邻 worker 故障和最终无故障 Load 依次证明 admission 可重新取得；完整自动化 900/900、生命周期与 QA export 聚焦 43/43、双配置 build、Roslyn/GDScript diagnostics、构建后的隔离 `--check-only` 与退出码 0 的 Vulkan 运行时门均通过。该证据把真实 aggregate 相邻失败子场景扩为十二个，不替代其余 renderer 故障、其他真实 aggregate 组合或 Phase 8 组合矩阵，因此 2.3 保持开放。

  - 真实 aggregate renderer admission 构造前故障矩阵进展（2026-08-23）：`v3-grid-rendering:2.2` 已在 `ReserveLoad()` 成功之后、`RoadRendererLoadAdmission` 构造/发布与 worker 启动之前建立一次性故障点。失败 operation 保持 graph/tool/presentation/surface/token/hit/slot 与 placement/history，已有 admission 正常释放且资源计数为 `77 -> 77`；紧邻 worker-Prepare probe 仍触发一次，最终无故障 Load 成功重新接管。完整自动化 901/901、生命周期与 QA export 聚焦 44/44、双配置 build、Roslyn/GDScript diagnostics、隔离目标脚本 `--check-only` 与退出码 0 的 Vulkan 运行时门均通过。该证据把真实 aggregate 相邻失败子场景扩为十三个，不替代其余 renderer 故障、其他真实 aggregate 组合或 Phase 8 组合矩阵，因此 2.3 保持开放。

  - 真实 aggregate renderer admission 发布前故障矩阵进展（2026-08-23）：`v3-grid-rendering:2.2` 已在 `RoadRendererLoadAdmission` 构造成功之后、`_loadAdmission` 发布与 worker 启动之前建立一次性故障点。失败 operation 保持 graph/tool/presentation/surface/token/hit/slot 与 placement/history，保存后的 graph payload 不变且资源计数为 `77 -> 77`；紧邻 worker-Prepare probe 仍取得并发布 admission、进入 worker 且触发一次，最终无故障 Load 成功重新接管。完整自动化 902/902、生命周期与 QA export 聚焦 45/45、双配置 build、Roslyn/GDScript diagnostics、隔离目标脚本 `--check-only` 与退出码 0 的 Vulkan 运行时门均通过。该证据把真实 aggregate 相邻失败子场景扩为十四个，不替代其余 renderer 故障、其他真实 aggregate 组合或 Phase 8 组合矩阵，因此 2.3 保持开放。

  - 真实 aggregate renderer admission 返回后故障矩阵进展（2026-08-23）：`v3-grid-rendering:2.2` 已在 `_loadAdmission` 发布、`BeginLoadAdmission()` 返回并赋给 orchestration 局部变量之后，于 participant capture、`Prepare` phase 推进和 worker 启动之前建立一次性故障点。失败 operation 保持 graph/tool/presentation/surface/token/hit/slot 与 placement/history，保存后的 graph payload 不变且资源计数为 `77 -> 77`；`finally` 释放已返回的 renderer admission，紧邻 worker-Prepare probe 仍触发一次，最终无故障 Load 成功重新接管。完整自动化 903/903、生命周期与 QA export 聚焦 46/46、双配置 build、Roslyn/GDScript diagnostics、隔离目标脚本 `--check-only` 与退出码 0 的 Vulkan 运行时门均通过。该证据把真实 aggregate 相邻失败子场景扩为十五个，不替代其余 renderer 故障、其他真实 aggregate 组合或 Phase 8 组合矩阵，因此 2.3 保持开放。

  - 真实 aggregate participant capture 后故障矩阵进展（2026-08-23）：`v3-grid-rendering:2.2` 已在真实 `StartLoad()` 成功捕获一个正式 load participant/reader 后、`Prepare` phase 推进与 worker 启动前建立一次性故障点。失败 operation 保持 graph/tool/presentation/surface/token/hit/slot 与 placement/history，保存后的 graph payload 不变且资源计数为 `77 -> 77`；`finally` 释放已取得的 admission，紧邻 worker-Prepare probe 仍触发一次，最终无故障 Load 成功重新接管。完整自动化 904/904、生命周期与 QA export 聚焦 47/47、双配置 build、Roslyn/GDScript diagnostics、隔离目标脚本 `--check-only` 与退出码 0 的 Vulkan 运行时门均通过。该证据把真实 aggregate 相邻失败子场景扩为十六个，不替代其余 renderer 故障、其他真实 aggregate 组合或 Phase 8 组合矩阵，因此 2.3 保持开放。

  - 真实 aggregate `Prepare` phase 推进后故障矩阵进展（2026-08-23）：`v3-grid-rendering:2.2` 已在真实 `StartLoad()` 成功推进 `lease` 至 phase 3 后、worker `Task.Run` 创建前建立一次性故障点。失败 operation 以 `finalPhase=3`、`Failed`、`committed=false` 且无 warning 返回，保持 graph/tool/presentation/surface/token/hit/slot 与 placement/history，保存后的 graph payload 不变；关闭无关 tooling autoload 后资源计数为 `65 -> 65`，`finally` 释放已取得的 admission，紧邻 worker-Prepare probe 仍触发一次，最终无故障 Load 成功重新接管。完整自动化 905/905、生命周期与 QA export 聚焦 48/48、双配置 build、Roslyn/GDScript diagnostics、隔离目标脚本 `--check-only` 与退出码 0 的 Vulkan 运行时门均通过。该证据把真实 aggregate 相邻失败子场景扩为十七个，不替代其余 renderer 故障、其他真实 aggregate 组合或 Phase 8 组合矩阵，因此 2.3 保持开放。

  - 真实 aggregate worker-entry 故障矩阵进展（2026-08-23）：`v3-grid-rendering:2.2` 已在 `Prepare` phase 推进后的下一条相邻边界证明 `Task.Run` worker 确实离开主线程，并在 slot preparation、renderer pure CLR `Prepare` 与 Godot Resource 分配前失败。operation 保持 graph/tool/presentation/surface/token/hit/slot 与 placement/history，资源计数为 `79 -> 79`；完整自动化 906/906、生命周期与 QA export 聚焦 49/49、双配置 build、Roslyn/GDScript diagnostics、隔离 `--check-only` 与退出码 0 的 Vulkan 运行时门均通过，CLI finalizer 退出崩溃也已收敛。该证据把真实 aggregate 相邻失败子场景扩为十八个，不替代其余 renderer 故障、其他真实 aggregate 组合或 Phase 8 组合矩阵，因此 2.3 保持开放。

  - 真实 aggregate slot preparation 后故障矩阵进展（2026-08-23）：`v3-grid-rendering:2.2` 已在 worker-entry 与 renderer worker-`Prepare` 之间新增相邻边界，证明 `CreateSlotStore().PrepareLoad(...)` 已产出 source slot 对应、含 1 个 participant 的 `PreparedSaveSlot`，但 `slot.GetPreparedState(context.Graph)` 尚未开始。失败 operation 保持 graph/tool/presentation/surface/token/hit/slot 与 placement/history，资源计数为 `77 -> 77`；完整自动化 907/907、生命周期与 QA export 聚焦 50/50、双配置 build、Roslyn/GDScript diagnostics、目标脚本 `--check-only` 与退出码 0 的 Vulkan 运行时门均通过。该证据把真实 aggregate 相邻失败子场景扩为十九个、早期边界扩为十个，不替代其余 renderer 故障、其他真实 aggregate 组合或 Phase 8 组合矩阵，因此 2.3 保持开放。

  - 真实 aggregate graph-state lookup 后故障矩阵进展（2026-08-23）：`v3-grid-rendering:2.2` 已在 slot preparation 后故障与 renderer worker-`Prepare` 故障之间新增相邻边界，证明 `slot.GetPreparedState(context.Graph)` 已返回状态类型 `RoadGraphRevision`，但生产 revision 类型确认尚未开始。失败 operation 保持 graph/tool/presentation/surface/token/hit/slot 与 placement/history，资源计数为 `77 -> 77`；完整自动化 908/908、生命周期与 QA export 聚焦 51/51、双配置 build、Roslyn/GDScript diagnostics、目标脚本 `--check-only` 与退出码 0 的 Vulkan 运行时门均通过。该证据把真实 aggregate 相邻失败子场景扩为二十个、早期边界扩为十一个，不替代其余 renderer 故障、其他真实 aggregate 组合或 Phase 8 组合矩阵，因此 2.3 保持开放。

  - 真实 aggregate renderer worker preparation 后故障矩阵进展（2026-08-23）：`v3-grid-rendering:2.2` 已在 renderer worker-`Prepare` 前故障与主线程 Resource Preflight 之间新增相邻边界，证明 `RoadRendererLoadPreparer.Prepare(...)` 已返回空 source slot 的完整纯 CLR prepared presentation，但 `PreparedLoadWork` 尚未返回且 Godot Resource 尚未分配。失败 operation 保持 graph/tool/presentation/surface/token/hit/slot 与 placement/history，资源计数为 `77 -> 77`；完整自动化 909/909、生命周期与 QA export 聚焦 52/52、双配置 build、Roslyn/GDScript diagnostics、目标脚本 `--check-only` 与退出码 0 的 Vulkan 运行时门均通过。该证据把真实 aggregate 相邻失败子场景扩为二十一个、早期边界扩为十二个，不替代其余 renderer 故障、其他真实 aggregate 组合或 Phase 8 组合矩阵，因此 2.3 保持开放。

  - 真实 aggregate prepared work 返回后故障矩阵进展（2026-08-23）：`v3-grid-rendering:2.2` 已在 renderer worker preparation 后故障与 `EnsureSceneRequestCurrent(sceneRequest)` 之间新增相邻边界，证明 `PreparedLoadWork` 已携带 source slot、1 个 participant 与空 presentation 返回主线程，但 scene/current-request、cancellation、Godot Resource Preflight 和 commit 尚未开始。失败 operation 保持 graph/tool/presentation/surface/token/hit/slot 与 placement/history，资源计数为 `77 -> 77`；完整自动化 910/910、生命周期与 QA export 聚焦 53/53、双配置 build、Roslyn/GDScript diagnostics、目标脚本 `--check-only` 与退出码 0 的 Vulkan 运行时门均通过。该证据把真实 aggregate 相邻失败子场景扩为二十二个、早期边界扩为十三个，不替代其余 renderer 故障、其他真实 aggregate 组合或 Phase 8 组合矩阵，因此 2.3 保持开放。

## 暂不执行

### 高级 RoadType 视觉

- 延期原因：V3 首版先验证四类道路的稳定名称、颜色、正有限宽度和混合路面拓扑；虚线、纹理、路肩及中央分隔带会扩大资源、几何和视觉验收范围。
- 保持现状：`RoadTypeStyle` 仅包含 2.1 明确的首版字段，派生视觉不得进入 RoadGraph 或存档事实。
- 重新开启条件：2.0～2.3 全部完成，并有明确产品样式、资源预算与生命周期约束及 V3 视觉验收要求。

### 分块道路 mesh

- 延期原因：现阶段没有 V3 同机指标证明单道路批次无法满足主线程离散门槛；提前分块会增加 surface owner、token 和原子接管复杂度。
- 保持现状：2.2 使用单道路 mesh，并把 snapshot、tessellation、commit 和后台总耗时分开测量。
- 重新开启条件：2.3 的 junction-dense 或 geometry-dense 10k 数据证明单批次方案无法满足已固定门槛，且瓶颈能由分块策略消除。

## 已解决基线

- [x] **基础 surface 查询已使用不可变空间索引。** 普通 snapshot 与 Load worker prepared payload 共用同一 AABB 层级；局部点/矩形查询不会随远隔 primitive 总数线性执行精确 triangle/disc 测试，Preflight 不重复复制或建树。Junction Patch 已进入同一索引，完整性能矩阵仍由开放的 2.3 负责。
- [x] **Degree-1 `TerminalCap` 已与道路样式及 surface 同源。** 普通与 Load 路径按相邻 `RoadTypeStyle` 生成同宽同色 marker 和解析 disc；A/B cap 携带稳定 Edge/Node/Endpoint 与 canonical 参数 `0/1`，并与 ribbon 共用同代 token 和 AABB 查询。其余工具消费和性能矩阵仍由开放的 2.2～2.3 负责。
- [x] **Degree-2 `SemanticJoin` 已与 ribbon mesh 及 surface 同源。** 不同 Edge/类型的非共线、同向和精确对向边界已有确定性分支；实际 join triangle 携带稳定 owner sector 与固定 canonical endpoint location，普通/Load 同代且不再绘制 degree-2 marker。其余工具消费和性能矩阵仍由开放的 2.2～2.3 负责。
- [x] **Degree≥3 `JunctionPatch` 已与 ribbon mesh 及 surface 同源。** 固定量化与 Clipper2 union/triangulation 为 T/X/锐角、self-loop 加支路和平行 Edge 生成有界 patch；确定 owner sector 携带稳定 Node/Edge/Endpoint 与 canonical location，普通/Load 同代且不再绘制 degree≥3 圆形 marker。其余工具消费和性能矩阵仍由开放的 2.2～2.3 负责。
- [x] **拆除选择已消费同代道路 surface。** hover、连续轨迹与矩形选择统一查询 frozen `RoadRenderToken` 对应的四类 surface；pending/stalled/superseded 会话清空选择并拒绝确认，提交还校验 graph facade 与 change sequence。完整工具矩阵和性能门禁仍由开放的 2.0、2.2～2.3 负责。
- [x] **RoadUpgrade 已消费同代道路 surface。** 独立改造会话冻结目标类型与完整 token，连续/矩形选择只保存稳定 canonical Edge ID；成功批次只产生一次事件/历史，旧 token、取消、NoChanges 与 full reset 均不留下可提交选择或 overlay。完整工具矩阵和性能门禁仍由开放的 2.0、2.2～2.3 负责。
- [x] **普通 mutation 表现失败已具备 provider 级 stalled/retry 门禁。** 失败绑定完整 desired token、attempt、异常类型和消息，保留上一代完整 mesh/surface/cache 但拒绝 hit；同 token 重试成功后才一次发布，更新请求会取代旧失败。其余工具命令 admission 与完整规模/扰动矩阵仍由开放的 2.2～2.3 负责。
- [x] **撤销与重做已服从 provider/graph 同代门禁。** 两个历史回放方向都在执行前校验 current token；pending、stalled 及回放自身引发的反向等待窗口不会清栈或修改图，matching presentation 发布后才恢复可用。
- [x] **Deferred 普通批次重建已具备独立 continuation generation。** 同帧 graph change 继续合并；Load admission、full reset、换图、退树和 aggregate commit 的同步 flush/reset 会推进 generation，旧 callable 无法消费后续新请求。
- [x] **未转交的表现资源已有确定性 cleanup。** 普通 rebuild、Load Preflight 与未提交 plan 分别持有并释放自己的 `ArrayMesh`/`MultiMesh`；资源工厂构造中途失败也不会把已创建 Resource 留给 GC，成功 transfer 后则由表现层继续持有。
- [x] **V2 六类原生几何共享只读显示采样。** `grid-rendering:1.1` 已验证 Edge、有效建造预览和拆除高亮复用 `RoadGeometryDisplaySampler`，显示容差不会反向修改权威控制参数。
- [x] **V2 大规模静态道路保持常数级渲染节点。** `grid-rendering:1.2` 已用道路 `ArrayMesh` 与节点 `MultiMesh` 把 10k/100k 的 `RoadRenderer` 静态子节点固定为 2。
- [x] **V2 规模性能证据已经记录。** `docs/performance/road-rendering-v2-baseline.md` 记录统一样式下 10k 三类交互满足 16.67 ms，以及 10k/100k 重建、draw calls、objects 和压测结果；V3 以同机同口径对照，不把这些结果当作分级表现已经完成。
- [x] **V2 RoadGraph 原生几何和显示边界已经验收。** `road-graph:2.5`～`2.6` 与 `grid-rendering:1.1` 是 V3 必须保留的基线，不转成 `v3-grid-rendering` 活动项。

## 完成标准

1. 2.0～2.3 全部通过各自自动化、真实 Godot/Vulkan 视觉和性能门禁；六类原生几何按 V3 新契约重新验证，并达到同机 junction-dense/geometry-dense 10k 门槛，不要求复用或兼容 V2 渲染 API、测试或实现。
2. canonical Edge、self-loop、平行 Edge、四类 RoadType、semantic boundary 和混合宽度 junction 形成无洞、可确定、可独立命中的同源 mesh/surface 表现，派生数据不反向修改 RoadGraph 或存档。
3. 普通 mutation 只在完整 token 匹配时一次发布 mesh/surface/presented token；失败保留旧表现、持续禁止道路交互并允许诊断重试，过期结果永不覆盖新图。
4. Load 在 Preflight 完成隐藏 Mesh/RID、surface snapshot、hit index 和不可抛 plan；关键失败只发生在 commit 前，成功与 graph、empty tool/overlay root、mesh/surface、token 和 `CurrentSlotID` 一次联合交换，提交后只有普通 observer warning，不存在关键表现失败或表现重试结果。
5. junction-dense 与 geometry-dense 10k 满足 Phase 0 固定主线程及 60 FPS 门槛；100k 使用相同口径记录完整压测但不阻塞 V3。
6. `v3-grid-rendering` 只负责本系统产出；第三代道路系统最终完成由 `v3-road-graph:8.6` 汇总 `v3-save-system`、`v3-tool-input`、`v3-ui` 和本路线图证据后判定。

# 第三代 RoadGraph 系统待办清单

> 系统 key：`v3-road-graph`
> 整理日期：2026-08-23
> 证据：当前工作区源码、RoadGraph 自动化测试、`docs/manuals/road-system-v2-gen.md` 附录 D 及 `docs/manuals/road-system-v3-gen.md`。
> 主导原则：负责第三代道路的数值与容量边界、连续拓扑存储、原生几何、自环/平行边、空间索引、RoadType、不可变事务以及最终跨系统集成验收；不负责交通模拟。

## 状态总览

| ID | 发现 | 当前状态 | 处置方式 |
|---|---|---|---|
| 8.0 | 数值、吸附/聚类、精确几何和 ID 分配没有统一边界 | 已完成 | 已建立 mutation 共用数值/容量基础、确定交点规划和 geometry canonicalizer；Load 接入由 `v3-save-system:2.1`～`2.2` 消费 |
| 8.1 | 邻接不能区分 self-loop 的 A/B incidence | 已完成 | 已引入端接角色、环路度数、六类原生反向、权威几何锚和 typed 方向 key |
| 8.2 | waypoint、原生段和 Group 仍碎片化 Edge | 已完成 | 已建立最大连续 Edge、半开 query fragment，并完整移除 RoadGroup |
| 8.3 | 闭合与自交路径没有规范提交格式 | 已完成 | 已建立 closed/full-turn 提交、incoming/incoming 交点规划、rooted seam 与原子重叠/歧义拒绝 |
| 8.4 | 规范 Edge 尚无稳定 RoadType 领域契约 | 已完成 | 已建立类型化建造、语义边界和拓扑替换继承 |
| 8.5 | 改造、不可变 root 与规范化没有统一事务身份 | 已完成 | 已建立结构共享 root、原子改造、统一 delta 事件与 lineage/revision/sequence 防护 |
| 8.6 | 第三代跨系统能力尚未组合验收 | 开放 | 负责 V3 道路存储、环路、类型、输入、UI、渲染和存档最终集成 |

### 设计覆盖矩阵

| 设计范围 | 当前事实 | 关联待办 |
|---|---|---|
| V3 规范存储、环路与道路分级 | 已完成 mutation 数值/容量、exact-sign line、endpoint-role incidence、六类原生方向、最大连续 Edge、删除后重归一化、半开 query fragment、RoadGroup 移除、公共闭合/自交提交、Edge 级 RoadType 及不可变 root/delta；严格 V3 format v1 reader/writer 已由 `v3-save-system:2.1` 接入，有界 token/恢复协议仍开放 | 8.6、`v3-save-system:2.2`～`2.3`、`v3-grid-rendering:2.0`～`2.3`、`v3-tool-input:2.1`～`2.4`、`v3-ui:1.1`～`1.4` |

## 执行顺序

### 阶段 8：第三代规范存储、环路、分级与集成

<a id="v3-road-graph8.0"></a>

- [x] **8.0 固化 RoadGraph 数值、容量与确定性基础**
  - 完成前问题：V2 运行时主要检查 `float.IsFinite`，但极大有限坐标仍可使距离平方、bounds、bucket 坐标和长度累计溢出；`_nextID++` 没有 checked reservation。交点候选按浮点参数/集合遍历处理，节点身份吸附和交点 epsilon 尚未隔离；line 合并若沿用普通 float cross/近似角度会吞掉 1 ULP 折点；圆弧也没有逐 bit full-turn 格式。近似相交代表坐标、`-0` 和重复 canonicalize 结果没有唯一基础契约。
  - 已实现：新增 `RoadNumericPolicy`、`RoadGraphCapacity` 和 checked `RoadGraphIDReservation`，限制坐标、六类原生控制参数、单 geometry/Edge/全图长度、实体/geometry/query fragment/bucket/ref 及单次 mutation work；距离和累计使用 finite double。`SubmitPolyline`、`SubmitPath`、交点规划和直接 subdivision 在 mutation 前完成 admission，Debug invariant 复核实际容量，结构化返回 `NumericOutOfRange`、`CapacityExceeded` 或 `AmbiguousIntersection`。`NodeSnap` 使用精确 double 距离且只在逐值同距时按最小 Node ID 破同值；intersection epsilon 与 `NodeSnapRadius` 分离。
  - 确定性基础：新增 binary32 exact-sign `Orient2D` / `DotSign` 和 geometry canonicalizer，只合并精确同向共线 line，保留 1 ULP 折点与回头，统一 `-0 => +0` 并保证第二次 canonicalize 无变化。交点 cluster 使用对称 witness、稳定 key、connected component、最大直径、既有 Node 优先和容量门禁；现阶段进入 mutation admission 并拒绝歧义。`CircularArc` 只把逐 bit `+/-Mathf.Tau` 识别为 full-turn，`End` / `GetPosition(1)` 直接复用 `Start`。
  - 后续归属：六类 geometry 原生反向和 typed direction key 属于 8.1；把 cluster 的单一代表坐标重锚到所有 split geometry、形成 canonical mutation plan 以及 query fragment locality 属于 8.2～8.3；不同历史下的 canonical graph 等价属于 8.2～8.3；V3 format v1 的有界逐值往返和 Load 复用本策略属于 `v3-save-system:2.1`～`2.2`。这些验收未删除，也不计入本基础项的完成证据。
  - 关联：`v3-save-system:2.2` 消费本项容量策略并负责磁盘字节与解析预算；这不是本项的前置依赖。
  - 集成负责人：`v3-road-graph`。
  - 验证证据（2026-08-13）：新增 6 个 V3 测试文件并扩充原生路径回归；`dotnet test tests/SimpleCities.RoadGraph.Tests/SimpleCities.RoadGraph.Tests.csproj --no-restore` 为 538/538 通过，`dotnet build SimpleCities.sln --no-restore` 为 0 警告、0 错误，Roslyn CodeLens 含 analyzer 为 0 diagnostics，`git diff --check` 通过。冻结运行 `Scenes/MapTest.tscn`：合法提交得到 3 Node、2 Edge、1 Group，renderer 接管 2 Edge/8 mesh 顶点；越界提交在 DAP `stdout` 明确打印 `NumericOutOfRange`，拒绝前后 payload SHA-256、`nextID = 6`、实体计数、renderer/mesh 和 undo 计数逐值不变；editor log 与 DAP `stderr` 无错误，临时槽、会话和进程均已清理。
  - 验收结果：本项负责的任何超限或歧义均在图/ID/事件变化前结构化拒绝；合法 mutation 不写入 NaN/Infinity/负 ID/`-0`，精确 full-turn seam 逐值闭合，1 ULP 折点不被近似吞并，附近无关 Node 不被 cluster 吸附，候选枚举扰动不改变 cluster 结果，geometry canonicalizer 第二次不产生变化。

<a id="v3-road-graph8.1"></a>

- [x] **8.1 建立 endpoint-role incidence 与 self-loop 图基础**
  - 完成前问题：`EdgeRef` 只有 Edge ID 和邻居 Node ID；`AddEdge`、恢复和不变式拒绝相同端点，无法区分 self-loop 在同一节点上的 A/B 端，detach 也只移除首个同 ID 引用。
  - 已实现：`GraphNode` 改为有序 `EdgeIncidence(EdgeID, Endpoint, NeighborNodeID)`；`IncidenceCount` / `Degree` 按端接计数，`IncidentEdgeCount` 单独统计不同 Edge，自环在同一 Node 注册 A/B 各一次。邻接 attach/detach/rebuild、删除、diagnostics、空间引用检查和 `RoadRenderer` 切线均按 endpoint role 处理；`PreparedRoadGraphTopology` / `RoadGraph.FromPreparedTopology` 提供先完整校验再发布的内部图边界，可表达 self-loop、平行 Edge 和普通边。非环 Edge 按 Node ID 升序定向，六类 `RoadGeometrySegment` 均实现不经显示采样的原生 `Reverse()`，self-loop 只比较当前链与逐段反向后的链，以版本化 primitive kind、规范周期角、canonical `-0` 和 binary32 typed token 确定方向，排除 ID、Group、JSON、长度、bounds、显示采样和空间引用。
  - 精确几何契约：圆弧持有逐 bit `Start` / `End` 锚及权威 `EndAngle`，clothoid 持有逐 bit `End` 锚及权威 `ReverseStartHeading`；split、reverse 和 geometry codec 交换或保留这些值，不用三角函数/积分重新猜测端点。方向 key 纳入这些权威字段，`GraphEdge` 在定向后再次验证相邻 primitive 逐 bit 连续，reverse twice 与 codec 往返均保持逐值稳定。旧 V2 geometry payload 仅在一整组新增锚字段全部缺失时兼容推导，部分提供则拒绝；V2 RoadGraph reader 继续拒绝 self-loop，V3 format v1 reader 仍由 `v3-save-system:2.1` 实现。
  - 依赖：`v3-road-graph:8.0`。
  - 验证证据（2026-08-13）：新增 `RoadGeometryDirectionV3Tests`、`RoadGraphIncidenceV3Tests` 并扩充圆弧、clothoid、codec、原生拆分和路径回归，覆盖六类及混合链反向、严格 reverse twice、full-turn seam/sweep、周期角、负零、ID/JSON 属性顺序扰动、self-loop、self-loop 加支路、parallel Edge、非环 ID 定向、逐 bit primitive join、codec 往返、incidence rebuild、重复 endpoint role 和缺失 endpoint 拒绝。最终 `dotnet test SimpleCities.sln --no-restore` 为 559/559，`dotnet build SimpleCities.sln --no-restore` 为 0 警告/0 错误，Roslyn CodeLens 含 analyzer 为 0 diagnostics，`git diff --check` 通过。
  - Godot Tier 3（2026-08-13）：冻结运行 `Scenes/MapTest.tscn`，用内部 prepared topology 构造 full-turn self-loop 与同端点对的 line/Bézier 平行 Edge，并绑定真实 `RoadRenderer`。结构化结果为 `pass = true`、3 Node、3 Edge、2 Group、6 incidence、seam roles `[A, B]`、`seamIsJunction = false`、附近平行 Edge `[5, 6]`、环查询命中 Edge 1、renderer 3 Edge/26 采样点/52 mesh 顶点；editor log 与 DAP `stderr` 均无错误。随后恢复原图、清空 exec holder、停止项目并删除临时 bridge/UID。
  - 验收结果：self-loop 已能作为一等 `GraphEdge` 存活于内部准备图、空间查询、渲染和完整不变式，平行 Edge 保持独立 incidence；当前拓扑代码不再用去重 neighbor 代替 degree。公共闭合提交、最大连续 Edge、Group 删除和 V3 reader 分别留在 8.2～8.3 与 `v3-save-system:2.1`，不计入本项。

<a id="v3-road-graph8.2"></a>

- [x] **8.2 形成最大连续 Edge 并移除 RoadGroup**
  - 完成前问题：`SubmitPolyline` 为非共线 waypoint 创建独立 Edge，`SubmitPathCore` 为每个 `NativePathPiece` 创建 Edge；`TryMergeAtNode` 只合并共线同 Group 边，导致输入采样、原生段边界和跨提交接缝都成为拓扑边界。索引按完整 geometry AABB 占桶，局部查询可能聚合 Edge 后再扫描整条 geometry，压缩后会随远端长度或段数退化。
  - 已实现：`RoadGeometryCanonicalizer` 以 8.0 exact-sign 规则只合并精确同向共线 line，其他原生 primitive 保持类型和参数；`RoadGraph.Canonicalization` 在提交、交叉拆分和删除后按受影响 Node ID 收敛所有非结构性二 incidence 节点，折角与混合 primitive 保持一条 Edge 的有序 geometry 链。拆分保留 A 侧原 Edge ID，合并保留最小 Edge ID；self-loop seam 是不跨越、不旋转的硬边界。六类 geometry 均通过 `ReanchorChain` 把 cluster/Node 的规范坐标类型化写回链端，定向后保持逐 bit 连续。
  - Group-free 与查询：已删除 `RoadGroup` 类型及 UID、`GraphEdge.GroupID`、Group 字典/查询/事件、提交 Group ID 和 Group 变更摘要；DebugPanel、renderer、输入、性能程序、Godot contract 与正常 fixture 只使用 Node/Edge/geometry。空间索引改为携带 `(EdgeID, GeometryIndex, ParameterStart, ParameterEnd)` 的 query fragment，fragment cut、primitive join、非环 B 端和 self-loop seam 使用半开所有权产生唯一 `RoadLocation`；closest/radius/rectangle/交点只对命中 fragment 做精确测试后再聚合 Edge，容量超限不回退全图扫描。
  - 依赖：`v3-road-graph:8.0`、`v3-road-graph:8.1`。
  - 集成负责人：`v3-road-graph`；V3 format v1 与旧持久化代码删除属于 `v3-save-system:2.1`，DebugPanel 属于 `v3-ui:1.3`。
  - 验证证据（2026-08-13）：`dotnet test SimpleCities.sln --no-restore` 为 572/572；`dotnet build SimpleCities.sln --no-restore` 为 0 警告/0 错误；Roslyn CodeLens compiler/analyzer 为 0 diagnostics；6 个修改过的 GDScript 逐文件为 0 diagnostics；`git diff --check` 通过，生产源码和正常 fixture 无 Group 残余。自动化覆盖同向 line 折叠、折角/混合链、跨提交延伸、交叉拆分、删除后重归一化、六类原生细分/重锚、ID 规则、事件摘要、半开边界唯一命中与索引容量。
  - 局部性与性能（2026-08-13）：单 Edge 的 line 长度由 4,096 扩到 65,536、geometry 数由 64 扩到 1,024 后，首/中/尾窗口均为 1 candidate fragment、1 exact test、1 aggregated Edge、0 full scan/visit；两次 Release 复跑的 P95 为 0.0003～0.0007 ms。10k junction-dense 全场景硬门槛均通过，多交叉提交 P95 为 8.281～9.118 ms、平均分配 6,881.2 KiB；100k 压测为 20.152～20.184 ms、46,878.7 KiB。批量交叉只执行 1 次 mutation admission pass，避免逐 Edge 重复扫描整图。
  - Godot Tier 3（2026-08-13）：冻结运行 `Scenes/MapTest.tscn`，通过真实 `RoadBuilder` 提交 16 条平行道路及一条贯穿道路；最终为 50 Node、49 Edge，`RoadRenderer` 接管 49 Edge/196 mesh 顶点/2 static render nodes。editor 增量错误和 DAP `stderr` 均为空，exec holder、测试运行和临时进程已清理。
  - 验收结果：无分支同向 line 形成 2 Node/1 Edge/1 line geometry，折角和复合曲线形成 1 Edge 与不可约原生链；成功 mutation 后不保留非结构性二 incidence Node，rooted self-loop 不跨 seam 归并。边界命中无漏失/重复，固定窗口精确访问不随同一 Edge 远端规模增长，Group 已从当前生产图和公共结果中完整移除。当前过渡期内部持久化已随 8.4 升为严格 `schemaVersion = 3` 并要求 `roadType`，但仍不是 V3 format v1；`v3-save-system:2.1` 保持开放。

<a id="v3-road-graph8.3"></a>

- [x] **8.3 支持闭合、自交路径及确定环路规范形**
  - 完成前问题：`ValidatePolyline` / `ValidateNativePath` 将首尾闭合判为重复点，交点规划只比较 incoming 与 existing；图不能形成简单环、棒棒糖、两路口环或八字形的稳定表示。
  - 已实现：公共折线/原生路径允许正长度首尾逐 bit 闭合和精确 `+/-Mathf.Tau` full-turn。交点规划统一 incoming/incoming、incoming/existing、离散交叉、相切与 overlap 边界，离散自交提升为 junction，连续自重叠返回 `SelfOverlap`。cluster 规范坐标在 mutation 前写入 incoming piece 与既有 subdivision；既有端点 provenance 仅在权威端点的 cluster 直径内继承，附近无关 Node 不参与二次吸附；同一 split parameter 的冲突规范坐标返回 `AmbiguousIntersection`。合法结果经 8.2 归一化形成 rooted self-loop、parallel Edge 或 junction 间 Edge，纯二度闭合分量保留最小 Node ID seam。
  - 依赖：`v3-road-graph:8.1`、`v3-road-graph:8.2`。
  - 集成负责人：`v3-road-graph`；玩家闭合草稿属于 `v3-tool-input:2.0`，闭合 ribbon 属于 `v3-grid-rendering:2.0`。
  - 验证证据（2026-08-13）：新增 `RoadGraphClosedPathV3Tests` 并扩充原生交点/重叠/细分回归，覆盖简单环、精确正反 full-turn、棒棒糖、端点与内部八字形、两路口环、多个离散交点、环与既有路交叉、覆盖接续、顺序稳定、无关 Node provenance、端点 solver 误差及冲突 split parameter。最终 `dotnet test SimpleCities.sln --no-restore` 为 586/586，`dotnet build SimpleCities.sln --no-restore` 为 0 警告/0 错误，Roslyn CodeLens 含 analyzer 为 0 diagnostics，6 个修改过的 GDScript 逐文件为 0 diagnostics，`git diff --check` 通过。
  - 性能与运行时（2026-08-13）：最终 Release 复跑的 10k 全场景均低于 16.67 ms，多交叉提交 P95 为 10.268 ms；100k 多交叉为 56.806 ms，仅作压力记录；固定窗口仍为 1 fragment、1 exact test、0 full scan/visit。冻结运行真实 `Scenes/MapTest.tscn`，四段闭环通过 `RoadBuilder` 一次确认，renderer 接管 1 Edge 并在两帧后生成 10 mesh 顶点；完全回走打印预期 `SelfOverlap` 且图、history 与 renderer 不变。editor 增量错误和 DAP `stderr` 均为空，exec holder、运行会话及测试槽均已清理。
  - 验收结果：所有声明的合法环路达到指南第 5 节规范格式；非法重叠或交点歧义在 Node/Edge/ID/事件变化前结构化拒绝；self-loop 与 parallel Edge 不再因端点相同被误判重复。闭合 ribbon 的 Phase 7 专属视觉/表面契约仍由 `v3-grid-rendering:2.0` 负责，不属于本项。

<a id="v3-road-graph8.4"></a>

- [x] **8.4 在规范 Edge 上建立 RoadType 与类型化提交**
  - 完成前问题：规范化 Edge、提交 API 和 merge key 没有道路等级；若先把类型塞进碎片化 Edge，后续存储重构会重复改写并产生不稳定语义边界。
  - 已实现：固定 `Dirt = 0`、`Street = 1`、`Arterial = 2`、`Highway = 3` 及严格小写存储 token；`GraphEdge`、prepared topology 和拆分结果都强制携带合法类型，不提供静默默认值。公共原生路径提交只保留 `SubmitPath(RoadBuildRequest)`，折线入口 `SubmitPolyline(RoadType, points)` 同样要求显式类型；新占据几何使用请求类型，既有拆分继承原类型，类型进入 merge key。异类型二 incidence 节点保留为 semantic boundary，同类型接续继续归一化；完全覆盖返回 `FullyCovered`，不改造既有 Edge 或消耗 ID。
  - 依赖：`v3-road-graph:8.2`、`v3-road-graph:8.3`。
  - 验证证据（2026-08-13）：新增 `RoadGraphRoadTypeV3Tests` 并迁移所有原生路径调用者到显式请求，覆盖构造/非法枚举、开放与闭合路径、六类几何、同/异类型接续与交叉、semantic boundary、完全覆盖、部分重叠、历史/过渡期 schema 3 往返和失败无副作用。最终 `dotnet test SimpleCities.sln --no-restore` 为 611/611，`dotnet build SimpleCities.sln --no-restore` 为 0 警告/0 错误，Roslyn CodeLens 含 analyzer 为 0 diagnostics，相关 GDScript 逐文件为 0 diagnostics，`git diff --check` 通过；旧 `SubmitPath(RoadType, RoadPath)` 引用为 0。
  - Godot Tier 3（2026-08-13）：冻结运行真实 `Scenes/MapTest.tscn`，8 次合法类型提交得到 10 Edge/16 Node，类型计数为 Dirt 3、Street 2、Arterial 2、Highway 3；异类型接缝保留 2 条 Street/Arterial incidence，交叉点的 4 条 incidence 分别保持既有 Highway 与新 Dirt。异类型完全覆盖返回 `FullyCovered` 且 payload、ID watermark 不变；两帧后 renderer 接管 10 Edge/40 mesh 顶点/2 static nodes，editor log 与 DAP 两个输出通道无错误，测试图和临时 bridge 均已清理。
  - 验收结果：每条活动 Edge 有且仅有一个合法类型；不同类型边界保留，同类型且无结构边界的相邻 Edge 合并。`v3-tool-input:2.1` 已让 `RoadBuilder` 以默认 `Street` 的 `SelectedRoadType` 建立会话并显式提交冻结类型；`v3-ui:1.1` 已在 Road/RoadUpgrade 上下文提供并验证玩家可见的四段式选择器。过渡期 `schemaVersion = 3` 只服务当前内部快照，不计作 V3 format v1。

<a id="v3-road-graph8.5"></a>

- [x] **8.5 实现改造后重归一化与统一事务摘要**
  - 完成前问题：图 API 只能增删 Edge；类型改造可能消除 semantic boundary 并触发 Edge/Node 合并，单纯 `UpdatedEdgeIDs` 或“ID 永远不变”无法表达真实结果。可变字典也无法让后台保存 O(1) 捕获稳定状态，revision、事件 sequence 和外部 load 后的图身份尚未分离。
  - 已实现：稳定 `RoadGraph` facade 以不可变 `RoadGraphRevision` 为唯一权威 root；Node/Edge 使用 immutable map，空间索引使用 copy-on-write bucket 页，root 同时保存 lineage、domain revision、change sequence、ID watermark、资源计数和总 geometry 长度。所有普通 mutation 先在未发布 working state 中完成 admission、规范化和不变式，再原子替换 root；未触碰 Entity、geometry 和 bucket 页跨 revision 按引用共享，旧 root 不由永久 revision 表保留。`CaptureRevision()` 为 O(1) 引用捕获，DebugPanel 从同一 root 读取 O(1) 计数。
  - 改造与事件：`ChangeRoadType` 对排序去重后的整批 Edge 做全量预检，合法批次改型后再次 canonicalize；所有 mutation 生成包含 Node/Edge 前后实体及 before/after content revision 的 `RoadGraphDelta`，只发布一次 `GraphChanged`。摘要包含排序去重的 created/removed/updated、`IsFullReset` 和单调 `ChangeSequence`；旧逐 Edge/`GraphCleared` 事件已删除。NoChanges 和失败不换 root，也不推进 ID、revision、sequence 或事件。
  - 身份与失败边界：`ApplyDelta` 用完整 `(LineageID, DomainRevisionID, ChangeSequence)` 校验方向、旧 sequence、重复 replay 和旧 lineage；undo/redo 恢复内容 revision，但 sequence、revision allocator 和 ID watermark 不回退。full reset 创建新 lineage、精确采用 prepared payload 的合法 `nextID`；事件发布期拒绝所有 mutation 入口重入，逐个隔离 observer 异常且不回滚已发布 root。RoadGraph 暴露完整 state token；异步表现结果的 consumer-specific generation/token 接管仍由 `v3-grid-rendering:2.2` 负责，不计入本领域项的完成证据。
  - 依赖：`v3-road-graph:8.2`～`8.4`。
  - 集成负责人：`v3-road-graph`；工具生命周期属于 `v3-tool-input:2.2`，历史 admission/容量属于 `v3-tool-input:2.3`。
  - 验证证据（2026-08-14）：`RoadGraphRoadTypeChangeV3Tests`、`RoadGraphTransactionV3Tests`、`RoadGraphMutationEventTests` 和 `RoadEditHistoryTests` 覆盖空集/重复/失效 ID、NoChanges、四类互转、semantic boundary 消失、rooted loop、事件内最终图不变式、所有普通 mutation 的 delta 正反应用、`R0/S0 -> R1/S1 -> R0/S2 -> R1/S3`、错误方向/旧 sequence/重复 replay、undo 后分叉、较小 watermark full reset、重入和首个/中间 observer 异常。最终 `dotnet test SimpleCities.sln --no-restore` 为 633/633；Debug build 为 0 警告/0 错误；Roslyn compiler/analyzer 为 0 diagnostics；`git diff --check` 通过。
  - 性能证据（2026-08-14，Release）：1k/10k/100k geometry 远端扩展下，固定局部类型改造分别分配 12.2/12.8/13.5 KiB，delta 固定为 0 Node/1 Edge，复制 2 个 Edge 对象与 2 个 bucket 页；未触碰 Entity 分别共享 2,999/29,999/299,999 个，100,000 次 root capture 均为 0 bytes，三档旧 root 均可释放。10k 全场景 P95 低于 16.67 ms，最坏多交叉为 7.997 ms；100k 结果完整记录但不作为硬门槛。
  - Godot Tier 3（2026-08-14）：冻结运行真实 `Scenes/MapTest.tscn`，真实 `RoadBuilder` 提交后为 1 rendered Edge/4 mesh 顶点/history 1；undo 后为 0 Edge/0 顶点、undo 0/redo 1；redo 后恢复 1 Edge/4 顶点、undo 1/redo 0。editor 增量日志与 DAP console/stdout/stderr 均为空，exec holder 为 0，项目已停止。
  - 验收结果：批次全有或全无，活动 root 一次替换且旧 root 不被修改；普通局部编辑复制与分配由受影响拓扑和 bucket 页决定，未随远端图线性增长。每次 commit sequence 唯一递增，错误 token 返回 `StaleGraphState` 且无副作用，同 lineage 不复用 revision/ID，full reset 换 lineage并采用 payload watermark；失败和 NoChanges 不污染 root、事件、allocator 或历史，observer 不能重入或回滚图，保存可 O(1) 捕获 root。

<a id="v3-road-graph8.6"></a>

- [ ] **8.6 完成第三代道路系统端到端评估**
  - 当前问题：领域测试不能证明规范存储、环路、类型选择、改造手势、混合渲染、V3 独立存档和命名槽在真实主场景共同成立。
  - 修改：按 `docs/manuals/road-system-v3-gen.md` Phase 8 建立组合契约，并把最终证据写回该指南附录 D。
  - 依赖：`v3-road-graph:8.0`～`8.5`、`v3-save-system:2.1`～`2.3`、`v3-grid-rendering:2.0`～`2.3`、`v3-tool-input:2.0`～`2.4`、`v3-ui:1.1`～`1.4`。
  - 集成负责人：`v3-road-graph`。
  - 验证：串行运行完整自动化和构建；执行真实 `MapTest` 的连续折线、跨提交延伸、精确 full-turn、简单环/棒棒糖/两路口环/八字形、支路删除重归一化、四类型建造与改造、token 防护 delta 撤销重做、不可变 root 结构共享/释放、V3 family/version format v1 有界往返、V2 根 canary 隔离、跨进程并发/自动存档、损坏/超限/ENOSPC 拒绝、publish/delete 中断恢复、成功/提交前失败/observer warning 且不存在提交后关键表现失败结果的 Load 生命周期、共享表面命中、混合 junction patch、presentation barrier、Vulkan 视觉、junction-dense/geometry-dense 存储与运行性能和 Windows 导出边界。
  - 验收：全部硬门禁有持久证据；TrafficGraph、A*、拥堵、高程道路和其他 V3 排除项不参与完成判定，也不得被宣称已实现。
  - 阶段证据（2026-08-14）：两路口环、八字形和删除支路后的 seam 重定位已进入领域/renderer 回归，真实 `MapTest` 的两路口环从 `4 Edge / 20 vertices / 4 markers` 收敛为 `2 Edge / 12 vertices / 2 markers`；完整自动化为 727/727，双配置构建及 Roslyn diagnostics 为 0。该证据只完成 Phase 7 的复杂 closed-ribbon 子矩阵；`v3-grid-rendering:2.0` 的缩放/重建与平行 Edge 表面命中、`2.1`～`2.3`、`v3-save-system:2.3`、`v3-tool-input:2.1`～`2.2`/`2.4` 和 `v3-ui:1.1`～`1.4` 尚未全部验收，因此 8.6 保持开放，附录 D 继续为空。
  - UI 集成进展（2026-08-20）：`v3-ui:1.1`～`1.2` 已完成 RoadType 四段式选择器与 Road/RoadUpgrade 双工具入口；相关 C# 契约 25/25、完整自动化 839/839、双配置 build 0 警告/0 错误，两项 Godot 运行时契约 PASS，并完成三档视口和 1600x900 OpenGL 截图复核。该证据只关闭所属 UI 条目；placement 的后续表现门禁由下一条单独记录，排队 continuation、其余命令 admission、完整表现/存档故障矩阵与 `v3-ui:1.3`～`1.4` 仍未完成，因此 8.6 保持开放。
  - Placement 门禁进展（2026-08-20）：类型化建造会话现冻结并持续复核完整 `RoadRenderToken`；pending、stalled、superseded 与成功 full reset 均不会留下可提交旧草稿或 preview。`RoadInputStrategyTests` 20/20、完整自动化 840/840、双配置 build 0 警告/0 错误，`road_render_token_runtime_contract.gd` 与 `road_input_strategy_runtime_contract.gd` 均 PASS。当前会话未暴露 Roslyn/Godot MCP 与 DAP，未刷新对应通道；排队 continuation、其余命令 admission、完整表现/存档故障矩阵与 `v3-ui:1.3`～`1.4` 仍未完成，因此 8.6 保持开放。
  - 历史回放门禁进展（2026-08-20）：undo/redo 的可用性查询与实际执行都要求 provider desired/presented/surface token 和当前 graph facade/change sequence 完全同代；pending、stalled 及回放自身产生的反向 pending 窗口均拒绝命令且保持图和两栈不变，matching presentation 发布后恢复。`RoadInputStrategyTests` 21/21、完整自动化 841/841、双配置 build 0 警告/0 错误，两个隔离道路运行时契约均 PASS。当前会话未暴露 Roslyn/Godot MCP 与 DAP，未刷新对应通道；排队 continuation、其余命令 admission、完整表现/存档故障矩阵与 `v3-ui:1.3`～`1.4` 仍未完成，因此 8.6 保持开放。
  - Deferred continuation 门禁进展（2026-08-20）：生产输入层源码复核未发现独立道路事务命令队列；renderer deferred 普通批次重建现捕获 continuation generation，并在 Load admission 同步 flush、full reset、换图、退树和 aggregate commit 时推进该代。pending mutation 后同栈 `StartLoad` 的隔离运行时契约证明 admission 先发布 current 代，Load 后额外两帧旧 callable 仍未覆盖新 lineage。renderer/sampler 聚焦组合 16/16、完整自动化 842/842、双配置 build 0 警告/0 错误，两个隔离道路运行时契约均 PASS。当前会话未暴露 Roslyn/Godot MCP 与 DAP；完整表现/存档故障矩阵与 `v3-ui:1.3`～`1.4` 仍未完成，因此 8.6 保持开放。
  - Preflight 资源生命周期进展（2026-08-20）：普通 renderer 构建、Load Preflight 与未提交 plan 已建立明确 ownership transfer；尚未挂载的 `ArrayMesh`/`MultiMesh` 在创建异常、过期/失败或 plan 放弃时确定性释放，成功 commit 后由表现层继续持有。renderer 生命周期契约 10/10、完整自动化 846/846、双配置 build 0 警告/0 错误，两个隔离道路运行时契约均 PASS；见 `save-system:BUG-14`。当前会话未暴露 Roslyn/Godot MCP 与 DAP，生命周期脚本的既有 disposed-object 输出未计作干净 console 门禁；真实 generation 失配、逐关键故障点、observer/cleanup 组合与其他开放矩阵仍未完成，因此 8.6 保持开放。
  - 真实 generation 失配进展（2026-08-20）：受控 `BoundaryInvalidatingLease` 在 `PreparedAggregateLoad` 进入 commit 时释放真实 `RoadGraph` admission；`PreparedAggregateLoadTests` 9/9 确认 `LoadPreflightInvalidException` 先于任何 `CommitReferences()`，graph revision、测试中的 presentation/slot 状态保持不变，未提交辅助计划各清理一次，admission 释放后 graph 可继续 mutation。完整 `dotnet test SimpleCities.sln --no-restore` 为 847/847，Debug 与 `ExportRelease` build 均为 0 警告、0 错误。xUnit 宿主无法初始化 Godot native Resource，因此不把该测试计作原生 Resource 生命周期证据；renderer/tool/slot 的真实 generation 失配、逐关键 Resource 故障和 observer/cleanup 联合组合仍保持开放。
  - Observer/cleanup 组合进展（2026-08-20）：新增 `ObserverAndCleanupFailures_AreIsolatedAcrossTheWholeAggregate`，以三个 fake participant 验证 observer 失败和多个 cleanup 失败不会回滚已交换 root，warning 按阶段聚合且后续 cleanup 继续执行；`PreparedAggregateLoadTests` 10/10，完整自动化 848/848，Debug 与 `ExportRelease` build 均为 0 警告、0 错误。该证据只属于 CLR aggregate 控制流；真实 renderer/tool/slot generation 失配、真实参与者 observer/cleanup 组合、逐关键 Godot Resource 故障仍开放，不能把它计作原生 `ArrayMesh`/`MultiMesh` 生命周期证据。
  - 真实 RoadGraph observer/cleanup 组合进展（2026-08-20）：`RealRoadGraphObserverFailure_IsolatedAlongsideOtherParticipantCleanupFailures` 让真实 `RoadGraph` 的 `GraphChanged` observer 抛错，同时让 fake `presentation`/`slot` cleanup 抛错；aggregate 仍先完成 graph、presentation、slot reference swap，再按 graph observer、presentation cleanup、slot cleanup 顺序聚合三个 warning，两个 companion cleanup 继续执行，新的 graph root 可查询且唯一 Edge 的 `RoadType` 为 `Highway`。`PreparedAggregateLoadTests` 11/11，完整自动化 849/849，Debug build 0 警告、0 错误。该切片覆盖真实 RoadGraph 的 CLR observer 路径与 fake companion cleanup，不证明 renderer/tool/slot 的真实 observer/cleanup 组合、generation 失配或逐关键 Godot Resource 故障；xUnit 宿主无法初始化 Godot native Resource，因此不扩展原生 `ArrayMesh`/`MultiMesh` 生命周期证据。
  - Load 二次复杂度修复收口复验（2026-08-20）：源码复核确认 `RoadGraph.AssertInvariants()` 通过批量 `UniformGrid.HasExactCoverage()` 单次扫描实际 bucket entries；完整自动化 849/849、Debug/`ExportRelease` build 0 警告/0 错误，Release C# 多交叉 10k/100k P95 为 7.575/6.339 ms。隔离 APPDATA 的真实 Vulkan fixture 在 10k/100k 均输出 `PASS`，camera/preview/highlight P95 为 0.549/0.541/0.572 ms 与 0.710/0.682/0.689 ms，Load/renderer rebuild 为 718.363/4617.638 ms，静态 renderer 节点为 2。该证据确认 BUG-21 当前未回归；12k～100k 逐级历史仍保留 2026-08-17 记录，junction-dense/geometry-dense 和 Phase 7 完整性能矩阵仍由 `v3-grid-rendering:2.3` 开放项负责。Roslyn/Godot MCP/DAP 当前未暴露，headless editor 的 UID/6550 环境错误不计作代码失败。
  - 追加 Load 复杂度收口复验（2026-08-21）：完整自动化更新为 864/864，Debug/`ExportRelease` build 仍为 0 警告/0 错误；Release `--enforce-budget` 多交叉 P95 为 7.247 ms（10k）与 7.431 ms（100k），10k 全场景低于 16.67 ms 硬门槛。该证据继续确认 `AssertInvariants()` 批量 coverage 修复未回归，8.6 仍等待真实 renderer/tool/slot generation 失配、逐关键 Resource 故障、完整 observer/cleanup 组合及 junction-dense/geometry-dense 矩阵。
  - Aggregate 提交边界失配补强（2026-08-21）：`CommitBoundaryGenerationMismatchForAnyParticipant_RejectsBeforeAnyReferenceSwap` 对 graph、tool、presentation、slot 四个 fake participant 逐一注入 commit boundary generation 失配；`PreparedAggregateLoadTests` 为 13/13，完整自动化为 865/865，任何位置均未发生引用交换或通知。该证据只扩大最终集成的 CLR 失败原子性边界，8.6 仍等待真实参与者、Godot Resource 和完整 Phase 7/8 故障矩阵。
  - 真实 Godot generation 失配协作进展（2026-08-21）：`road_load_generation_runtime_contract.gd` 在真实 `MapTest` 中以 10k Edge worker Prepare 分别移除但保留 `RoadRenderer` 实例，以及移除真实 `ToolManager`。前者使 renderer admission 在 Preflight 失败，后者通过 `UnregisterSceneParticipants()` 推进 scene generation 并在 Prepare 取消 Load；两者均为 `committed=false`，旧 RoadGraph payload、`CurrentSlotID`、renderer token/surface/mesh、`CurrentTool`、活动 placement 与 history 保持不变，节点恢复后可再次保存旧图。两个独立 APPDATA 运行均 PASS，完整自动化 865/865、双配置 build 0 警告/0 错误，GDScript/Roslyn、editor 与 DAP 门均通过。该证据扩大真实场景的失败原子性，但 slot-target commit-boundary、真实 observer/cleanup、逐关键 Resource 与完整 Phase 7/8 矩阵仍开放，因此 8.6 不关闭。
  - 真实 slot-target 提交边界协作进展（2026-08-21）：`SaveManager.SlotTargetLoadCommitPlan` 同时冻结 scene 与 `_currentSlotGeneration`；`RealSlotTargetGenerationMismatchAtCommitBoundary_RejectsEveryReferenceSwap` 在 `CrossCommitBoundary` 内只推进槽目标代际，aggregate 在任何 graph/tool/presentation/slot reference swap 或 notification 前拒绝，旧 companion 状态与 `CurrentSlotID` 保持。`PreparedAggregateLoadTests` 14/14、完整自动化 866/866、双配置 build 与 Roslyn diagnostics 为 0，隔离 APPDATA 的真实 Load 契约及编辑器 `MapTest` smoke 通过。真实 observer/cleanup、逐关键 Resource 与完整 Phase 7/8 矩阵仍开放，因此 8.6 不关闭。
  - 100k grid Vulkan Load 复验（2026-08-21）：隔离 APPDATA 的 `road_rendering_performance_contract.gd --dataset-kind=grid --dataset-size=100000 --enforce-budget` 退出码为 0 并输出 `PASS`；camera/preview/highlight P95 为 `0.635/0.664/0.669 ms`，Load/renderer rebuild 为 `4980.748 ms`，draw call 为 `4/5/4`、objects 为 `4/56/4`、静态 renderer 节点为 `2`。stderr 仅有既有 `ConstructionDock: ToolManager.Instance is missing` warning。该证据确认 BUG-21 的批量 coverage 修复在当前 100k grid 路径未回归，但不替代真实 renderer/tool/slot observer/cleanup、关键 Resource 故障和完整 junction/geometry/performance 矩阵；Roslyn CodeLens 与 Godot MCP/DAP 当前未暴露，8.6 保持开放。
  - Load 分段性能协作进展（2026-08-21）：成功 Load 现在发布带 token/slot 的 worker Prepare、主线程 Preflight、reference commit、aggregate commit 与端到端不可变快照；真实 Vulkan grid 1k/10k/100k 分段分别为 `113.216/9.839/0.713/1.949/126.841 ms`、`622.155/24.648/1.769/2.997/651.217 ms`、`4770.893/151.645/4.168/5.621/4929.850 ms`，外部观测 Load 为 `132.327/657.366/4938.626 ms`，三档均 PASS。完整自动化 868/868、双配置 build、Roslyn compiler/GDScript 目标诊断为 0，编辑器 `MapTest` 与 DAP 错误通道通过；唯一 Vulkan warning 为既有 `ConstructionDock` 缺少 `ToolManager.Instance`，analyzer 未获信任授权而未运行。该证据建立 Phase 7 的 Load 分段定位入口，但普通 mutation snapshot/presentation/full-reset 分段、完整 token 性能扰动、逐关键 Resource 与真实 observer/cleanup 矩阵仍开放，因此 8.6 不关闭。
  - 普通表现分段协作进展（2026-08-21）：`RoadRenderer` 将普通 graph mutation 的请求起点绑定完整 `RoadRenderToken`，并只在 matching presentation commit 后发布 snapshot capture、纯 CLR Prepare、Godot Resource Preflight、presentation commit、rebuild total 与 request-to-ready。真实 Vulkan grid 10k/100k 对 1/100/1000 Edge 的 upgrade/undo/redo 各输出九条 `PRESENTATION_PHASE_RESULT` 并全部 PASS；10k 的六段范围为 `0.033～0.043/42.916～67.472/3.368～4.845/0.023～0.035/47.465～72.010/48.042～72.638 ms`，100k 为 `0.030～0.090/498.725～561.087/36.216～44.828/0.040～0.052/537.191～606.009/539.680～606.375 ms`。完整自动化 869/869、双配置 build、compiler/GDScript diagnostics 为 0；编辑器与 DAP 只读门通过，唯一 Vulkan warning 为既有 `ConstructionDock` 缺少 `ToolManager.Instance`。该切片建立普通 mutation 的分段定位入口，但 non-aggregate full-reset barrier、完整 token 性能扰动、逐关键 Resource 与真实 observer/cleanup 矩阵仍开放，因此 8.6 不关闭。
  - Non-aggregate full-reset barrier 协作进展（2026-08-22）：Debug-only probe 以当前 O(1) graph snapshot 走既有单参与者 `CommitPreparedLoad()`，真实 `MapTest` 的同步 full reset 在方法返回前完成新 lineage、`ChangeSequence + 1`、facade generation/request ID 推进和 matching mesh/surface/presented token 发布，Edge/mesh/marker/surface 数保持。隔离 APPDATA 的 Vulkan Forward+ grid 1k/10k/100k 均以退出码 0 PASS，barrier 总时长为 `7.036/84.139/733.651 ms`，renderer request-to-ready 为 `5.493/71.468/647.488 ms`；10k camera/preview/highlight P95 为 `0.532/0.488/0.442 ms`，100k 为 `0.664/0.705/0.657 ms`。生命周期聚焦 12/12、完整自动化 870/870、双配置 build、Roslyn compiler 与 GDScript diagnostics 为 0；probe 只进入 Debug 且被 QA export 排除。该切片建立 Phase 7 的 non-aggregate barrier 基线，但不固定单轮阈值，也不替代完整 token 扰动、逐关键 Resource、真实 observer/cleanup 和 Phase 8 组合矩阵，因此 8.6 不关闭。
  - 真实 Load observer/cleanup 协作进展（2026-08-22）：Debug-only `RoadLoadObserverFailureProbe` 向真实 `RoadRenderer.PresentationReady` 安装一次性抛错 observer；第一次 Vulkan aggregate Load 仍先联合交换 graph/tool/presentation/slot，返回 committed `SucceededWithWarnings` 且 warning 精确归属 renderer，随后工具切换成功，第二次 Load 无 warning 恢复 1 Edge matching presentation。结构化结果为 `warning_result_kind=1`、`clean_result_kind=0`、`probe_trigger_count=1`、`rendered_edges=1`、`current_tool=2`；这证明真实 graph/tool/renderer admission 与 cleanup 已释放，slot target 也能再次联合接管。源码契约固定 renderer 逐 observer 隔离、tool 无 notification 且完成 builder/admission cleanup、slot notification/cleanup 为 no-op。生命周期聚焦 13/13、完整自动化 871/871、双配置 build、Roslyn compiler 与 GDScript diagnostics 为 0；两个 probe 只进入 Debug 并被 QA export 排除。编辑器冻结 `MapTest` ready/matching、静态节点 2，DAP stderr/console 与 editor 增量日志为空；唯一 CLI warning 为既有 `ConstructionDock` 缺少 `ToolManager.Instance`。该切片关闭真实 observer warning/cleanup 再接管协作缺口，但逐关键 Resource、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - `RoadSurfaceSnapshot` Preflight 故障协作进展（2026-08-22）：Debug-only partial probe 直接驱动真实 renderer admission/preparer/Preflight，在 4 个 road vertices 与 2 个 node markers 已创建 `ArrayMesh`/`MultiMesh` 后注入 null snapshot `PreparedData`。Vulkan Forward+ 契约得到 `ArgumentNullException` 与 `ObjectResourceCount 77 -> 77`，并逐值保持旧 RoadGraph payload、工具会话/history、mesh/node batch/surface/token/hit 和 `CurrentSlotID`；随后真实 Load 返回 `Succeeded`，证明 renderer admission 已释放。完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn compiler 与目标 GDScript diagnostics 为 0，三个 probe 仅进入 Debug 且被 QA export 排除；编辑器/DAP 门通过。该切片只关闭 snapshot 构造点，不冒充失败 aggregate Load；该切片完成时资源工厂、未提交 plan disposal、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - 未提交 Load plan disposal 协作进展（2026-08-22）：同一 Debug-only probe 以有效 4-vertex road mesh、2-marker node batch 与 surface snapshot 创建真实 `RoadRendererLoadCommitPlan`，确认 plan current 后连续执行两次 `Dispose()`；plan 变为 stale，`ObjectResourceCount` 为 `77 -> 77`，旧 graph/tool/presentation/token/hit/history/slot 状态保持，随后真实 Load 返回 `Succeeded`，证明 renderer admission 可重新取得。完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn compiler 与目标 GDScript diagnostics 为 0，probe 继续只进入 Debug 并被 QA export 排除；Vulkan Forward+ 契约 PASS，编辑器冻结 smoke ready/matching/2 static nodes 且游标 700 后无新增 error，唯一 CLI warning 为既有 `ConstructionDock` 缺少 `ToolManager.Instance`。DAP 因编辑器已有客户端达到 8 个上限而未接通。该切片关闭未提交 plan disposal；该切片完成时两个资源工厂内部异常、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - `CreateNodeBatch` 工厂故障协作进展（2026-08-22）：同一 Debug-only probe 直接调用真实工厂，使用 `Count=1` 且索引读取固定抛出 `InvalidOperationException` 的 marker 列表命中 `MultiMesh` 配置完成后的 marker loop。Vulkan Forward+ 契约得到 `ObjectResourceCount 77 -> 77`，旧 graph/tool/presentation/token/hit/history/slot 状态保持，随后真实 Load 返回 `Succeeded`；完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn compiler 与目标 GDScript diagnostics 为 0，probe 继续只进入 Debug 并被 QA export 排除。唯一 CLI warning 为既有 `ConstructionDock` 缺少 `ToolManager.Instance`，editor 游标 700 后无新增 error；DAP 仍受 8-client 上限阻塞。该切片关闭 node batch 工厂内部异常，但 `CreateRoadMesh`、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - `CreateRoadMesh` 所有权边界协作进展（2026-08-22）：`CreateRoadMesh()` 与 `CreateNodeBatch()` 以无捕获 static lambda 共用私有 `InitializeOwnedResource()`；源码契约固定两个 factory 的接线，Debug-only probe 对真实 `ArrayMesh` 注入进入初始化器后的 `InvalidOperationException`。Vulkan Forward+ 契约得到 `ObjectResourceCount 77 -> 77`，旧 graph/tool/presentation/token/hit/history/slot 状态保持，随后真实 Load 返回 `Succeeded`，其余三个资源点继续通过；该证据不声称 Godot 引擎 error 一定转换成托管异常。完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn compiler 与目标 GDScript diagnostics 为 0，probe 仅进入 Debug 并被 QA export 排除。10k grid Vulkan Load 为 `682.988 ms`，camera/preview/highlight P95 为 `0.508/1.679/0.822 ms`，静态节点 2；唯一 CLI warning 为既有 `ConstructionDock` 缺少 `ToolManager.Instance`。编辑器冻结 smoke ready/matching/2 static nodes，DAP stderr/console 与 editor 游标 700 后增量 error 均为空。该切片关闭已定位的 Preflight Resource 所有权点，但更新 token 的 renderer 故障、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - 普通更新 token Resource Preflight 故障协作进展（2026-08-22）：Debug-only `RoadRendererUpdateTokenFailureProbe` 在真实 renderer 的 `CreateRoadMesh()` / `CreateNodeBatch()` 完成后、surface 构造与表现提交前注入一次性异常。真实 Vulkan graph mutation 的 attempt 1 推进 desired 但保留旧 presented token 与完整旧表现，provider 拒绝旧 surface hit，未转交资源释放后 `ObjectResourceCount` 为 `77 -> 77`；attempt 2 以同一 desired token 重试成功并一次发布新 Edge/surface。结构化结果为 `trigger_count=1`、`stalled_attempt=1`、`recovered_attempt=2`；完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn compiler 与目标 GDScript diagnostics 为 0，probe 不进入 `ExportRelease`。编辑器冻结状态 ready/matching/2 static nodes，DAP stderr/console 与 editor 游标 710 后增量 error 均为空。该切片只关闭普通更新 token 的 Resource Preflight 故障点；其余 renderer 故障、失败 aggregate、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - 普通更新 token `CreateNodeBatch` 工厂故障协作进展（2026-08-22）：Debug-only `RoadRendererUpdateTokenFailureProbe` 在下一枚真实 graph mutation token 上替换 marker 输入，使 `CreateNodeBatch()` 已分配并配置 `MultiMesh` 后于 marker loop 首次索引读取抛出 `InvalidOperationException`。factory helper 与 ordinary rebuild `finally` 分别释放 node batch 和此前创建的 road mesh；attempt 1 推进 desired、保留旧 presented/Edge/mesh/marker/surface 并进入 stalled，provider 拒绝旧 hit，`ObjectResourceCount` 为 `77 -> 77`。attempt 2 以同一 desired token 成功发布新增 Edge/surface，结构化结果为 `trigger_count=1`、`marker_read_count=1`、`stalled_attempt=1`、`recovered_attempt=2`。`RoadRendererLifecycleContractTests` 20/20、生命周期与 QA export 聚焦 21/21、完整自动化 878/878，双配置 build、Roslyn compiler/analyzer 与目标 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约 PASS，Godot MCP 8 帧 smoke 读到道路系统与两类静态表现节点，minimal stderr/console 为空且 editor 游标 767 后无新增消息。`ExportRelease` 不含 probe 类型、node-batch arming API、marker wrapper 或注入消息，隔离 QA 根已清理。该切片关闭真实 ordinary mutation 的 `CreateNodeBatch` 工厂故障，但 `CreateRoadMesh` 的真实普通更新故障、其他 renderer 故障、失败 aggregate、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - 普通更新 token `CreateRoadMesh` 工厂故障协作进展（2026-08-22）：同一 Debug-only `RoadRendererUpdateTokenFailureProbe` 在下一枚真实 graph mutation token 上把 road index 输入替换为一次性抛错的 `IReadOnlyCollection<int>`；`CreateRoadMesh()` 先分配 `ArrayMesh`，再于 `InitializeOwnedResource()` 内首次枚举 indices 时抛出 `InvalidOperationException`。factory helper 立即释放尚未转交的 road mesh，node batch 尚未创建；attempt 1 推进 desired、保留旧 presented/Edge/mesh/marker/surface 并进入 stalled，provider 拒绝旧 hit，`ObjectResourceCount` 为 `77 -> 77`。attempt 2 以同一 desired token 成功发布新增 Edge/surface，结构化结果为 `UPDATE_TOKEN_ROAD_MESH_FAILURE_RESULT resource_before=77 resource_after=77 trigger_count=1 index_enumeration_count=1 stalled_attempt=1 recovered_attempt=2`。`RoadRendererLifecycleContractTests` 21/21、生命周期与 QA export 聚焦 22/22、完整自动化 879/879，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约输出三个 update-token 结果并 PASS，Godot MCP 8 帧 smoke 读到道路系统与两类静态表现节点，minimal stderr/console 为空且 editor 游标 767 后无新增消息。`ExportRelease` 不含 probe 类型、road-mesh arming API、indices wrapper 或注入消息，隔离 QA 根已清理。该切片关闭真实 ordinary mutation 的 `CreateRoadMesh` 工厂故障，但其他 renderer 故障、失败 aggregate、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - 真实 aggregate Load renderer Resource Preflight 故障协作进展（2026-08-22）：Debug-only `RoadLoadPreflightResourceFailureProbe` 在真实 `StartLoad()` 的 renderer Preflight 中，于 `CreateRoadMesh()` / `CreateNodeBatch()` 完成后、render token/surface/plan 创建前注入一次性异常。operation 返回 `Failed`、`committed=false`，活动 graph/tool/presentation/surface/token/hit/slot 逐值不变，未转交资源释放后 `ObjectResourceCount` 为 `77 -> 77`；随后同一源槽 Load 返回 `Succeeded` 并联合接管空 graph/tool/presentation/slot，证明所有 admission 与未提交计划均已释放。结构化结果为 `aggregate_failure_result_kind=2`、`aggregate_failure_committed=false`、`aggregate_failure_trigger_count=1`、`aggregate_resource_count_before=77`、`aggregate_resource_count_after=77`、后续 `load_result_kind=0`。生命周期源码契约固定 hook 顺序；完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn compiler/analyzer 与目标 GDScript diagnostics 为 0，probe 只进入 Debug 且被 QA export 排除。Vulkan Forward+ 契约 PASS，编辑器冻结 smoke ready/matching/2 static nodes，DAP stderr/console 为空，editor 游标 721 后无新增 error；唯一 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。该切片只关闭失败 aggregate 的 renderer Resource Preflight 子场景；其余 renderer 故障、其他 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - renderer plan 后续 aggregate Preflight fallback disposal 协作进展（2026-08-22）：同一 Debug-only probe 通过 `SaveManager` partial hook 在真实 `StartLoad()` 已将 renderer commit plan 加入 `preflightPlans` 后、slot-target plan 创建与 aggregate ownership 之前注入一次性异常。operation 返回 `Failed`、`committed=false`；`RunLoadAsync()` 的 `finally` fallback 逐个 `Dispose()` 已有 graph/tool/renderer plan，活动 RoadGraph、placement/history、tool、presentation/surface/token/hit 与 `CurrentSlotID` 逐值不变，`ObjectResourceCount` 为 `77 -> 77`。随后同槽 Save 逐字节确认旧图，最终来源槽 Load 返回 `Succeeded` 并联合接管空 graph/tool/presentation/slot。结构化结果为 `post_renderer_failure_result_kind=2`、`post_renderer_failure_committed=false`、`post_renderer_failure_trigger_count=1`、`post_renderer_resource_count_before=77`、`post_renderer_resource_count_after=77`、最终 `load_result_kind=0`。完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn production/test compiler/analyzer 与目标 GDScript diagnostics 为 0；`ExportRelease` 无 hook 实现、probe 类型或注入消息，QA export 继续排除 probe 源码。Vulkan Forward+ 契约 PASS，Godot 4.7 编辑器游标 721 后无新增 error，DAP stderr/console 为空；唯一 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。该切片只关闭 renderer plan 加入后的 fallback disposal 子场景；其余 renderer 故障、其他 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - slot plan 后续 aggregate Preflight fallback disposal 协作进展（2026-08-22）：同一 Debug-only probe 的第二个 `SaveManager` partial hook 在真实 `StartLoad()` 已将 `SlotTargetLoadCommitPlan` 加入 `preflightPlans` 后、aggregate ownership 之前注入一次性异常。operation 返回 `Failed`、`committed=false`；`RunLoadAsync()` 的 `finally` fallback 逐个 `Dispose()` 已有 graph/tool/renderer/slot plan，活动 RoadGraph、placement/history、tool、presentation/surface/token/hit 与 `CurrentSlotID` 逐值不变，`ObjectResourceCount` 为 `77 -> 77`。随后同槽 Save 逐字节确认旧图，最终来源槽 Load 返回 `Succeeded` 并联合接管空 graph/tool/presentation/slot。结构化结果为 `post_slot_failure_result_kind=2`、`post_slot_failure_committed=false`、`post_slot_failure_trigger_count=1`、`post_slot_resource_count_before=77`、`post_slot_resource_count_after=77`、最终 `load_result_kind=0`。完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn production/test compiler/analyzer 与目标 GDScript diagnostics 为 0；`ExportRelease` 不含 hook 实现、probe 类型或注入消息，QA export 继续排除 probe 源码。Vulkan Forward+ 契约以退出码 0 输出 PASS，Godot 4.7 编辑器游标 721 后无新增 error，DAP stderr/console 为空；唯一 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。该切片只关闭 slot plan 加入后的 fallback disposal 子场景；其余 renderer 故障、其他 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - aggregate ownership 后的 pre-commit disposal 协作进展（2026-08-22）：同一 Debug-only probe 的第三个 `SaveManager` partial hook 在真实 `StartLoad()` 的 `PreparedAggregateLoad` 已取得 graph/tool/renderer/slot plans 所有权后、`Commit()` 前注入一次性异常。operation 返回 `Failed`、`committed=false`；aggregate 离开 `using` scope 时逐个 `Dispose()` owned plans，`aggregateOwnsPlans=true` 阻止 `RunLoadAsync()` fallback 重复清理。活动 RoadGraph、placement/history、tool、presentation/surface/token/hit 与 `CurrentSlotID` 逐值不变，`ObjectResourceCount` 为 `77 -> 77`。随后同槽 Save 逐字节确认旧图，最终来源槽 Load 返回 `Succeeded` 并联合接管空 graph/tool/presentation/slot。结构化结果为 `post_ownership_failure_result_kind=2`、`post_ownership_failure_committed=false`、`post_ownership_failure_trigger_count=1`、`post_ownership_resource_count_before=77`、`post_ownership_resource_count_after=77`、最终 `load_result_kind=0`。完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn production/test compiler/analyzer 与目标 GDScript diagnostics 为 0；`ExportRelease` 不编译 probe 源码，release DLL 不含 probe 类型或注入消息。真实 Vulkan 1.4 Forward+ 契约以退出码 0 输出 PASS，Godot 4.7 编辑器 smoke 为 ready/matching，游标 721 后无新增 error，DAP stderr/console 为空；唯一 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。该切片只关闭 aggregate ownership 后、commit 前的 disposal 子场景；其余 renderer 故障、其他 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - 真实 renderer commit-boundary generation 失配协作进展（2026-08-22）：Debug-only `RoadLoadPreflightResourceFailureProbe` 以真实 `RoadRendererLoadCommitPlan` 和 graph/tool/slot 三个 `TrackingLoadCommitPlan` 组成 `PreparedAggregateLoad`；受控 lease 在 `CrossCommitBoundary` 内释放 renderer admission，第二次 generation 复核抛出 `LoadPreflightInvalidException`。四个 plan 均未交换引用，`MarkCommitted()` 为 0；aggregate disposal 释放隐藏 `ArrayMesh`/`MultiMesh`，三个 companion plans 各清理一次，旧 presentation/token 保持，`ObjectResourceCount` 为 `77 -> 77`，renderer admission 可重新取得。`RoadRendererLifecycleContractTests` 14/14、生命周期与 QA export 聚焦 15/15、完整自动化 872/872，双配置 build、Roslyn production/test compiler/analyzer 与目标 GDScript diagnostics 均为 0，Vulkan 1.4 Forward+ 契约 PASS，editor/DAP 错误通道为空；release DLL 不含 probe 类型、方法、lease 或注入 token。该切片证明真实 renderer plan 的 aggregate 原子拒绝与 cleanup，但 graph/tool/slot 是 `TrackingLoadCommitPlan` companions，不是完整真实 `StartLoad()` 四参与者 operation；其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - 真实 ToolManager commit-boundary generation 失配协作进展（2026-08-22）：同一 Debug-only probe 以真实 `ToolLoadCommitPlan` 和 graph/presentation/slot 三个 `ToolTrackingLoadCommitPlan` 组成 `PreparedAggregateLoad`；受控 lease 在 `CrossCommitBoundary` 内释放 tool admission，并同步释放其 builder admission，第二次 generation 复核抛出 `LoadPreflightInvalidException`。四个 plan 均未交换引用，`MarkCommitted()` 为 0，三个 companion plans 各清理一次；原 `CurrentTool=Road`、`SelectedRoadType=Arterial`、带一个固定拐点的 placement/draft 与 undo/redo 保持，tool admission 与独立 builder admission 均可重新取得。`RoadRendererLifecycleContractTests` 15/15、生命周期与 QA export 聚焦 16/16、完整自动化 873/873，双配置 build、Roslyn production/test compiler/analyzer 与目标 GDScript diagnostics 均为 0，Vulkan 1.4 Forward+ 契约 PASS，editor/DAP 错误通道为空；release DLL 不含 probe 类型、方法、lease、`ToolTrackingLoadCommitPlan` 或注入 token。该切片证明真实 tool plan 的 aggregate 原子拒绝与 cleanup，但 graph/presentation/slot 是 `ToolTrackingLoadCommitPlan` companions，不是完整真实 `StartLoad()` 四参与者 operation；其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - 真实 renderer observer 与 ToolManager cleanup 联合故障协作进展（2026-08-22）：同一次真实 graph/tool/presentation/slot `StartLoad()` 中，Debug-only probe 让 `RoadRenderer.PresentationReady` observer 与 `ToolLoadCommitPlan.CompleteCommit()` 尾部各抛一次；tool probe 位于 builder/tool admission 释放和 `_completed=true` 之后。operation 返回 `SucceededWithWarnings`、`committed=true`，两条 warning 精确标识 road presentation observer 与 `road-tools` cleanup；四方引用保持新值，后续 renderer/slot cleanup 继续，第二次 Load 无 warning 成功并恢复 1 Edge matching presentation。结构化结果为 `warning_result_kind=1`、`clean_result_kind=0`、`observer_trigger_count=1`、`tool_cleanup_trigger_count=1`、`rendered_edges=1`、`current_tool=2`。`RoadRendererLifecycleContractTests` 16/16、生命周期与 QA export 聚焦 17/17、串行完整自动化 874/874，双配置 build、Roslyn production/test compiler/analyzer 与目标 GDScript diagnostics 均为 0，Vulkan 1.4 Forward+ 契约 PASS，editor 游标 721 后无新增 error/warning，DAP stderr/console 为空；release DLL 不含 probe 类型、arming API、状态字段或注入消息，partial hook 声明名不作为排除断言。该切片关闭这一真实 observer/cleanup 联合子场景，但其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - 真实 renderer observer 与 renderer cleanup 联合故障协作进展（2026-08-22）：同一运行时契约新增第二个真实 graph/tool/presentation/slot `StartLoad()`，让 `RoadRenderer.PresentationReady` observer 与位于 renderer admission 释放、`_completed=true`、redraw 排队之后的 Debug-only cleanup probe 各抛一次。operation 返回 `SucceededWithWarnings`、`committed=true`，两条 warning 精确标识 road presentation observer 与 `road-presentation` cleanup；四方引用保持新值，随后无 warning Load 恢复 1 Edge matching presentation。两轮结构化结果为两个 warning/clean result 均为 `1/0`、observer `2` 次、tool cleanup `1` 次、renderer cleanup `1` 次。`RoadRendererLifecycleContractTests` 17/17、生命周期与 QA export 聚焦 18/18、串行完整自动化 875/875，双配置 build、Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0，Vulkan 1.4 Forward+ 契约与 Godot MCP 8 帧 `MapTest` smoke PASS，editor 游标 721 后无新增 error/warning，DAP stderr/console 为空；release DLL 不含 probe 类型、arming API、状态字段或注入消息。该切片关闭这一真实 observer/renderer cleanup 联合子场景，但其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - 真实 renderer observer 与 RoadGraph cleanup 联合故障协作进展（2026-08-22）：同一运行时契约通过真实 `RoadSystem` 将其 CLR graph 交给 Debug-only probe，并在第三个真实 graph/tool/presentation/slot `StartLoad()` 中让 `RoadRenderer.PresentationReady` observer 与 graph cleanup 各抛一次。graph partial 调用位于 admission 释放和 `_completed=true` 后；operation 返回 `SucceededWithWarnings`、`committed=true`，两条 warning 精确标识 road presentation observer 与 `road-graph` cleanup，四方引用保持新值，后续真实 tool/renderer/slot cleanup 继续，下一次无 warning Load 恢复 1 Edge matching presentation。累计结构化结果为三个 warning/clean result 均为 `1/0`、observer `3` 次、tool/renderer/graph cleanup 各 `1` 次。`RoadRendererLifecycleContractTests` 18/18、生命周期与 QA export 聚焦 19/19、串行完整自动化 876/876，双配置 build、Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0，Vulkan 1.4 Forward+ 契约与 Godot MCP 8 帧 `MapTest` smoke PASS，editor 游标 721 后无新增 error/warning，DAP stderr/console 为空；release DLL 不含 probe 类型关联、arming API、状态字段或注入消息。该切片关闭这一真实 observer/graph cleanup 联合子场景，但 slot cleanup、其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。
  - 真实 renderer observer 与 slot-target cleanup 联合故障协作进展（2026-08-22）：同一运行时契约在第四个真实 graph/tool/presentation/slot `StartLoad()` 前武装 Debug-only `SaveManager` partial probe；真实 `SlotTargetLoadCommitPlan.CommitReferences()` 已交换 `CurrentSlotID` 并设置 `_committed=true` 后，其 cleanup 回调与 `RoadRenderer.PresentationReady` observer 各抛一次。operation 返回 `SucceededWithWarnings`、`committed=true`，两条 warning 精确标识 road presentation observer 与 `slot-target` cleanup，四方引用保持新值，下一次无 warning Load 恢复 1 Edge matching presentation。累计四组 warning/clean result 均为 `1/0`，observer `4` 次，tool/renderer/graph/slot cleanup 各 `1` 次。`RoadRendererLifecycleContractTests` 19/19、生命周期与 QA export 聚焦 20/20、串行完整自动化 877/877，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0，Vulkan 1.4 Forward+ 契约与 Godot MCP 8 帧 `MapTest` smoke PASS，editor 游标 721 后无新增 error/warning，DAP stderr/console 为空；release DLL 不含 Debug probe 类型、arming API、状态字段或注入消息，隔离 QA 目录和日志已清理。该切片关闭这一真实 observer/slot cleanup 联合子场景，但其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实 renderer observer 与四参与者 cleanup 全故障协作进展（2026-08-22）：同一运行时契约在第五个真实 graph/tool/presentation/slot `StartLoad()` 前，同时武装 `RoadRenderer.PresentationReady` observer 与 RoadGraph、ToolManager、RoadRenderer、slot-target 四个既有 Debug-only cleanup probe。operation 返回 `SucceededWithWarnings`、`committed=true`，warning 字符串恰有五行并逐项标识 renderer observer 与四个 cleanup participant；graph cleanup 首先抛错后，后三个真实 cleanup 仍继续执行，四方引用和新 `CurrentSlotID` 保持。随后无 warning Load 恢复 1 Edge matching presentation。结构化结果为 `combined_warning_result_kind=1`、`combined_clean_result_kind=0`、observer `5` 次，tool/renderer/graph/slot cleanup 各 `2` 次。生命周期与 QA export 聚焦组合 20/20、完整自动化 877/877，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约 PASS，CLI stderr 只有既有 `ConstructionDock: ToolManager.Instance is missing` warning。Godot MCP 第二次无报告 `MapTest` smoke 推进 8 帧；editor 新增序号 722 的 `Max client limits reached` 工具桥接错误，minimal DAP 因既有 8-client 上限未接通。测试槽已删除，两个外层隔离 QA 目录因桌面安全策略拒绝递归清理而留在系统 Temp，未进入工作树。该切片关闭这一真实全 cleanup 组合，但其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实 RoadGraph observer 与四参与者 cleanup 全故障协作进展（2026-08-22）：同一运行时契约在第六个故障场景的真实 graph/tool/presentation/slot `StartLoad()` 前，为当前 `RoadGraph.GraphChanged` 安装一次性 Debug-only observer，并同时武装 RoadGraph、ToolManager、RoadRenderer 与 slot-target 四个 cleanup probe。四方引用完成交换后，真实 graph observer 与四个 cleanup 各抛一次；operation 仍返回 `SucceededWithWarnings`、`committed=true`，warning 恰有五行并精确标识 `RoadGraph observer` 与四个真实 cleanup participant。observer 在抛错前自行退订；graph cleanup 位于 cleanup 顺序首位，其失败未阻断后三个 cleanup，新的 graph root、`CurrentSlotID`、空 placement/history 与 matching presentation 保持。随后无 warning Load 恢复 1 Edge matching presentation。结构化结果为 `graph_observer_warning_result_kind=1`、`graph_observer_clean_result_kind=0`、`graph_observer_trigger_count=1`、renderer observer 累计 `5` 次，tool/renderer/graph/slot cleanup 各累计 `3` 次，最终 `rendered_edges=1`、`current_tool=2`。生命周期与 QA export 聚焦组合 20/20、完整自动化 877/877，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约 PASS。CLI 日志除预期 RoadGraph observer trace warning 外只有既有 `ConstructionDock` warning；Godot MCP 冻结 `MapTest` smoke 推进 8 帧并读到 `RoadSystem`、`RoadRenderer` 与两类静态表现节点，editor 游标 722 后无新增消息；minimal DAP 因既有 8-client 上限返回 `No active debug session`，不能声明 console 通过。`ExportRelease` 不含 probe 类型、graph observer arming/getter 或注入消息；本轮工作区内 QA 目录已清理。该切片关闭真实 RoadGraph observer/全 cleanup 组合，但其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 双真实 observer 与四参与者 cleanup 全故障协作进展（2026-08-22）：第七个真实 graph/tool/presentation/slot `StartLoad()` 同时武装 `RoadRenderer.PresentationReady`、当前 `RoadGraph.GraphChanged` 与四个真实 cleanup probe。两个 observer 和四个 cleanup 各抛一次，operation 仍返回 `SucceededWithWarnings`、`committed=true`，六条 warning 精确归属全部故障源；graph observer 自行退订，首位 graph cleanup 失败后其余 cleanup 继续，新的 graph root、`CurrentSlotID`、空 placement/history 与 matching presentation 保持。随后工具可切换，无 warning Load 恢复 1 Edge。结构化结果为 `dual_observer_warning_result_kind=1`、`dual_observer_clean_result_kind=0`、renderer observer 累计 `6` 次、RoadGraph observer 累计 `2` 次，四类 cleanup 各累计 `4` 次，最终 `rendered_edges=1`、`current_tool=2`。聚焦组合 20/20、完整自动化 877/877、双配置 build 与 Roslyn/GDScript diagnostics 均通过；Vulkan 1.4.341 Forward+ 契约两次 PASS，Godot MCP 8 帧 smoke 读到道路系统和两类静态表现节点，minimal stderr/console 为空。`ExportRelease` 不含四项 probe 标识，本轮测试槽与 QA 根已清理。该切片关闭双真实 observer/全 cleanup 组合，但其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 普通更新 token `RoadSurfaceSnapshot` 构造故障协作进展（2026-08-22）：同一 Debug-only `RoadRendererUpdateTokenFailureProbe` 在下一枚真实 graph mutation token 上把 snapshot `PreparedData` 替换为 null；hook 位于 road mesh、node batch 与统一 Resource Preflight 之后，真实构造函数抛出 `ArgumentNullException("prepared")`。两个未转交 Resource 由 ordinary rebuild 的 `finally` 同步释放；attempt 1 推进 desired、保留旧 presented/Edge/mesh/marker/surface 并 stalled，provider 拒绝旧 hit，`ObjectResourceCount` 为 `77 -> 77`。attempt 2 以同一 desired token 成功发布新增 Edge/surface，结构化结果为 `UPDATE_TOKEN_SURFACE_SNAPSHOT_FAILURE_RESULT resource_before=77 resource_after=77 trigger_count=1 stalled_attempt=1 recovered_attempt=2`。`RoadRendererLifecycleContractTests` 22/22、生命周期与 QA export 聚焦 23/23、完整自动化 880/880，双配置 build、Roslyn production/test compiler/analyzer、目标与 LSP workspace scan 可见的 2 个 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约输出四个 update-token 结果并 PASS，Godot MCP 分步推进 1+7 帧后运行树与错误通道正常。`ExportRelease` 不含 probe 类型或 snapshot API，隔离 QA 根已移入回收站。该切片关闭真实 ordinary mutation 的 `RoadSurfaceSnapshot` 构造故障，但其他 renderer 故障、失败 aggregate、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 普通更新 token pre-commit 取代协作进展（2026-08-22）：同一 Debug-only `RoadRendererUpdateTokenFailureProbe` 在 snapshot 构造完成、任何表现引用交换之前调用正式 `RequestRebuild()`，只将 `RenderRequestID` 推进一位。原 target attempt 1 命中现有 desired-token 复核后正常返回 false，不记录异常或 stalled；两个未转交 Resource 由 `finally` 释放，旧 presented/Edge/mesh/marker/surface 保持但 provider 在 pending 时拒绝旧 hit，`ObjectResourceCount` 为 `77 -> 77`。replacement token 与原 target 仅 `RenderRequestID` 不同，明确驱动后以自己的 attempt 1 成功发布新增 Edge/surface，结构化结果为 `UPDATE_TOKEN_PRECOMMIT_SUPERSESSION_RESULT resource_before=77 resource_after=77 trigger_count=1 superseded_attempt=1 replacement_attempt=1`。`RoadRendererLifecycleContractTests` 23/23、生命周期与 QA export 聚焦 24/24、完整自动化 881/881，双配置 build、Roslyn production/test compiler/analyzer、目标与 LSP workspace scan 可见的 2 个 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约 PASS。Godot MCP 首个单帧回执超时，随后 7+1 帧完成，运行树与错误通道正常；`ExportRelease` 不含 probe 类型或 supersession API，隔离 QA 根已移入回收站。该切片只关闭 `RenderRequestID` 的真实 ordinary pre-commit 取代；其他五个 token 分量的真实运行时取代、其余 renderer 故障、失败 aggregate、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 普通更新 token pre-commit `RoadStyleRevision` 取代协作进展（2026-08-22）：同一 Debug-only probe 复用 snapshot 后、任何表现引用交换前的 hook，以正式 `RequestStyleRefresh()` 在真实 graph mutation 的 target attempt 1 内推进样式修订。原 target 因 desired 已被 replacement 取代而正常返回 false，不产生异常或 stalled；两个未转交 Resource 由 `finally` 释放，旧 presented/Edge/mesh/marker/surface 保持但 provider 在 pending 时拒绝旧 hit，`ObjectResourceCount` 为 `77 -> 77`。replacement 与 target 的 scene/facade/facade generation/change sequence 相同，`RoadStyleRevision` 与必然同步变化的 `RenderRequestID` 各加一；replacement 以自己的 attempt 1 发布新增 Edge/surface。结构化结果为 `UPDATE_TOKEN_PRECOMMIT_STYLE_SUPERSESSION_RESULT resource_before=77 resource_after=77 trigger_count=1 superseded_attempt=1 replacement_attempt=1`。`RoadRendererLifecycleContractTests` 24/24、生命周期与 QA export 聚焦 25/25、完整自动化 882/882，双配置 build、Roslyn production/test compiler/analyzer、目标与 LSP workspace scan 可见的 2 个 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约输出两种 supersession 并 PASS。Godot MCP 8 帧 smoke 的运行树与 minimal/editor 错误通道正常；`ExportRelease` 不含 probe 类型或样式取代 API，隔离 QA 根已移入回收站。当前真实 pre-commit 覆盖为 `RenderRequestID` 与 `RoadStyleRevision`；其余四个 token 分量、其余 renderer 故障、失败 aggregate、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 普通更新 token pre-commit `SceneGeneration` 取代协作进展（2026-08-22）：Debug-only probe 将取代模式收敛为三值枚举，并在连续 Load 的全部断言之后通过正式 `SetSceneGeneration()` 为真实 graph mutation 的 target attempt 1 生成 replacement；注入代值只存活到随后的槽和场景清理，不被 Save/Load 再消费。原 target 因 desired 已被取代而正常返回 false，不产生异常或 stalled；两个未转交 Resource 由 `finally` 释放，旧 presented/Edge/mesh/marker/surface 保持但 provider 在 pending 时拒绝旧 hit，`ObjectResourceCount` 为 `77 -> 77`。replacement 与 target 的 facade/facade generation/change sequence/style 相同，`SceneGeneration` 与 `RenderRequestID` 各加一，并以自己的 attempt 1 发布新增 Edge/surface。结构化结果为 `UPDATE_TOKEN_PRECOMMIT_SCENE_SUPERSESSION_RESULT resource_before=77 resource_after=77 trigger_count=1 superseded_attempt=1 replacement_attempt=1`。`RoadRendererLifecycleContractTests` 25/25、生命周期与 QA export 聚焦 26/26、完整自动化 883/883，双配置 build、Roslyn production/test compiler/analyzer、目标与 LSP workspace scan 可见的 2 个 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约输出三种 supersession 并 PASS，唯一 CLI warning 为既有 `ConstructionDock` warning。Godot MCP 8 帧 smoke 运行树正常、minimal stderr/console 为空；editor 仅新增序号 771～772 的既有 MCP 单连接拒绝 warning。`ExportRelease` 不含 probe 类型或 scene arming/helper，隔离 QA 根已移入回收站。当前真实 pre-commit 覆盖三项；`GraphFacadeID`、`GraphFacadeGeneration`、`ChangeSequence`，以及其余 renderer 故障、失败 aggregate、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 普通更新 token pre-commit `GraphFacadeGeneration` 取代协作进展（2026-08-22）：同一 Debug-only probe 在 snapshot 与两个 Resource 准备完成后，先校验当前 `RoadGraph.FacadeID` 和 `CurrentStateToken.ChangeSequence` 与 target 一致，再通过正式 `RequestGraphChange(targetToken.ChangeSequence, isFullReset: true)` 生成 replacement。原 target 因 desired 已被取代而正常返回 false，不产生异常或 stalled；两个未转交 Resource 由 `finally` 释放，旧 presented/Edge/mesh/marker/surface 保持但 provider 在 pending 时拒绝旧 hit，`ObjectResourceCount` 为 `77 -> 77`。replacement 与 target 的 scene/facade ID/change sequence/style 相同，仅 `GraphFacadeGeneration` 与 `RenderRequestID` 各加一，并以自己的 attempt 1 发布新增 Edge/surface。结构化结果为 `UPDATE_TOKEN_PRECOMMIT_GRAPH_GENERATION_SUPERSESSION_RESULT resource_before=77 resource_after=77 trigger_count=1 superseded_attempt=1 replacement_attempt=1`。`RoadRendererLifecycleContractTests` 26/26、生命周期与 QA export 聚焦组合 27/27、完整自动化 884/884，双配置 build、Roslyn production/test compiler/analyzer、目标与 LSP workspace scan 可见的 2 个 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约输出四种 supersession 并 PASS，Godot MCP 冻结 `MapTest` 推进 8 帧且运行树与错误通道正常。`ExportRelease` 不含 probe 类型或 graph-generation arming/helper，隔离 QA 根已移入回收站。当前真实 pre-commit 覆盖四项；`GraphFacadeID`、`ChangeSequence`，以及其余 renderer 故障、失败 aggregate、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 普通更新 token pre-commit `ChangeSequence` 取代协作进展（2026-08-22）：同一 Debug-only probe 在 target 资源准备完成后，以真实 `RoadGraph.SubmitPolyline(RoadType.Street, ...)` 完成第二次普通 mutation；其 `GraphChanged` 通过正式 renderer 路径生成 replacement，probe 只失效自动 deferred continuation 以保留 pending 观察窗。原 target 因 desired 与 graph revision 已过期而正常返回 false，不产生异常或 stalled；两个未转交 Resource 由 `finally` 释放，旧 presented/Edge/mesh/marker/surface 保持且两条新路均不可命中，`ObjectResourceCount` 为 `77 -> 77`。replacement 与 target 的 scene/facade ID/facade generation/style 相同，仅 `ChangeSequence` 与 `RenderRequestID` 各加一，并以自己的 attempt 1 将两次 mutation 一次发布。结构化结果为 `UPDATE_TOKEN_PRECOMMIT_CHANGE_SEQUENCE_SUPERSESSION_RESULT resource_before=77 resource_after=77 trigger_count=1 superseded_attempt=1 replacement_attempt=1`。`RoadRendererLifecycleContractTests` 27/27、生命周期与 QA export 聚焦组合 28/28、完整自动化 885/885，双配置 build、Roslyn production/test compiler/analyzer、目标与 LSP workspace scan 可见的 2 个 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约输出五种 supersession 并 PASS，Godot MCP 冻结 `MapTest` 推进 8 帧且运行树正常。minimal DAP 因编辑器既有 8-client 上限未接通，editor 仅新增序号 783 的同源 warning；`ExportRelease` 不含 probe 类型或 change-sequence arming/helper，隔离 QA 根已移入回收站，项目已停止。当前真实 pre-commit 覆盖五项；`GraphFacadeID`，以及其余 renderer 故障、失败 aggregate、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 普通更新 token pre-commit `GraphFacadeID` 取代协作进展（2026-08-22）：同一 Debug-only probe 在 target 资源准备完成后，捕获当前 `RoadGraph` 快照，创建新 graph 并通过真实 `CommitPreparedLoad()` 对齐 `ChangeSequence`，再调用正式 `RoadRenderer.SetGraph()`。`SetGraph()` 在同一调用栈同步嵌套重建并发布 replacement；外层 target 因 graph 引用和 desired token 已变化而正常返回 false，不产生异常或 stalled，两个未转交 Resource 由 `finally` 释放，`ObjectResourceCount` 保持 `77 -> 77`。replacement 更换 `GraphFacadeID`，增加 `GraphFacadeGeneration + 1` 与 `RenderRequestID + 1`，保持 scene/change sequence/style，并以自己的 attempt 1 同步 ready；仍绑定旧 graph 的 `RoadBuilder` 因 facade ID 不匹配拒绝新命令。结构化结果为 `UPDATE_TOKEN_PRECOMMIT_GRAPH_FACADE_ID_SUPERSESSION_RESULT resource_before=77 resource_after=77 trigger_count=1 superseded_attempt=1 replacement_attempt=1`。`RoadRendererLifecycleContractTests` 28/28、生命周期与 QA export 聚焦组合 29/29、完整自动化 886/886，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区可见的 2 个 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约 PASS，Godot MCP 冻结 `MapTest` 推进 8 帧且运行树正常，editor 游标 783 后无新增 error/warning。minimal DAP 因编辑器既有 8-client 状态返回 `No active debug session`；`ExportRelease` 不含新 probe/API/helper，两个隔离 QA 根已移入回收站，项目已停止。至此六分量真实 ordinary pre-commit 取代全部覆盖；其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 普通更新 `PresentationStalled` observer 隔离协作进展（2026-08-22）：扩展后的 Debug-only probe 在真实 Resource Preflight 故障前依次订阅一次性抛错 observer 与后续计数 observer。attempt 1 进入 stalled 后，首个 observer 的 `InvalidOperationException` 被 `PublishPresentationStalled()` 捕获为预期 warning，后续 observer 仍执行一次；原构建异常、desired/stalled token 与旧 presented/mesh/marker/surface 保持，provider 拒绝旧和未呈现 surface，未转交 Resource 释放且 `ObjectResourceCount` 为 `77 -> 77`。同一 desired token 在 attempt 2 成功发布。结构化结果为 `UPDATE_TOKEN_STALLED_OBSERVER_FAILURE_RESULT resource_before=77 resource_after=77 failure_trigger_count=1 throwing_observer_count=1 continuing_observer_count=1 stalled_attempt=1 recovered_attempt=2`。`RoadRendererLifecycleContractTests` 29/29、生命周期与 QA export 聚焦组合 30/30、完整自动化 887/887，双配置 build、Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约与 Godot MCP 8 帧 smoke PASS，editor 游标 783 后无新增消息。minimal DAP 返回 `No active debug session`；`ExportRelease` 不含 probe API/handler/message，隔离 QA 根已移入回收站，项目已停止。该切片关闭普通 stalled observer 隔离与恢复子场景；其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实 aggregate Load `CreateNodeBatch` 工厂故障协作进展（2026-08-22）：Debug-only `RoadLoadPreflightResourceFailureProbe` 在含 1 Edge 的真实 `StartLoad()` renderer Preflight 中，于 road mesh 创建后把 marker 输入替换为 `Count=1` 的抛错列表。`CreateNodeBatch()` 分配并配置 `MultiMesh` 后在首个 marker 索引读取抛出 `InvalidOperationException`；factory helper 释放 node batch，外层 Preflight catch 释放 road mesh。operation 返回 `Failed`、`committed=false` 且无 warning，活动 RoadGraph、tool、placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 逐值不变，`ObjectResourceCount` 为 `77 -> 77`；后续 Preflight 与最终无故障 Load 均可重新接管。结构化结果为 `aggregate_node_batch_failure_result_kind=2 aggregate_node_batch_failure_committed=false aggregate_node_batch_failure_trigger_count=1 aggregate_node_batch_marker_read_count=1 aggregate_node_batch_resource_count_before=77 aggregate_node_batch_resource_count_after=77 load_result_kind=0`。`RoadRendererLifecycleContractTests` 30/30、生命周期与 QA export 聚焦组合 31/31、完整自动化 888/888，双配置 build、Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Godot 4.7 Vulkan Forward+ 契约与 Godot MCP 8 帧 smoke PASS，editor 游标 783 后无新增消息。minimal DAP 返回 `No active debug session`；`ExportRelease` 不含 probe 类型/API/wrapper/message，隔离 QA 根已移入回收站，项目已停止。该切片关闭真实 aggregate node-batch factory 子场景；其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实 aggregate Load `CreateRoadMesh` 工厂故障协作进展（2026-08-22）：同一 Debug-only probe 在含 1 Edge 的真实 `StartLoad()` renderer Preflight 中，把 `prepared.RoadIndices` 替换为 `Count=1` 的抛错集合。`CreateRoadMesh()` 完成 vertex/UV/color 数组准备并分配 `ArrayMesh` 后，在首次枚举 indices 时抛出 `InvalidOperationException`，尚未写入 surface；factory ownership helper 释放唯一已建 Resource，node batch 尚未创建。operation 返回 `Failed`、`committed=false` 且无 warning，活动 RoadGraph、tool、placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 逐值不变，`ObjectResourceCount` 为 `77 -> 77`；后续 Preflight 与最终无故障 Load 均可重新接管。结构化结果为 `aggregate_road_mesh_failure_result_kind=2 aggregate_road_mesh_failure_committed=false aggregate_road_mesh_failure_trigger_count=1 aggregate_road_mesh_index_enumeration_count=1 aggregate_road_mesh_resource_count_before=77 aggregate_road_mesh_resource_count_after=77 load_result_kind=0`。`RoadRendererLifecycleContractTests` 31/31、生命周期与 QA export 聚焦组合 32/32、完整自动化 889/889，双配置 build、Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Godot 4.7 Vulkan 1.4.341 Forward+ 契约与 Godot MCP 8 帧 smoke PASS，editor 游标 783 后无新增消息。minimal DAP 返回 `No active debug session`；`ExportRelease` 不含 probe 类型/API/indices wrapper/message，隔离 QA 根已移入回收站，项目已停止。该切片关闭真实 aggregate road-mesh factory 子场景；其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实 aggregate Load `RoadSurfaceSnapshot` 构造故障协作进展（2026-08-22）：同一 Debug-only probe 在含 1 Edge 的真实 `StartLoad()` renderer Preflight 中，于 road mesh、node batch、统一 Resource Preflight 与 reserved render token 完成后把 snapshot `PreparedData` 替换为 null。真实构造函数抛出 `ArgumentNullException("prepared")`，外层 Preflight catch 释放两个已创建但未转交的 Resource。operation 返回 `Failed`、`committed=false` 且无 warning，活动 RoadGraph、tool、placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 逐值不变，`ObjectResourceCount` 为 `77 -> 77`；后续 Preflight 与最终无故障 Load 均可重新接管。结构化结果为 `aggregate_snapshot_failure_result_kind=2 aggregate_snapshot_failure_committed=false aggregate_snapshot_failure_trigger_count=1 aggregate_snapshot_resource_count_before=77 aggregate_snapshot_resource_count_after=77 load_result_kind=0`。`RoadRendererLifecycleContractTests` 32/32、生命周期与 QA export 聚焦组合 33/33、完整自动化 890/890，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Godot 4.7 Vulkan 1.4.341 Forward+ 契约与 Godot MCP 8 帧 smoke PASS，editor 游标 783 后无新增消息。minimal DAP 返回 `No active debug session`；`ExportRelease` 不含 probe 类型或 snapshot arming/probe API，隔离 QA 根已移入回收站，项目已停止。该切片关闭真实 aggregate snapshot 构造子场景；其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实 aggregate Load reserved render token 创建故障协作进展（2026-08-22）：同一 Debug-only probe 在含 1 Edge 的真实 `StartLoad()` renderer Preflight 中，于 road mesh、node batch 与统一 Resource Preflight 完成后只把本次局部 `RoadRenderLoadReservation` 副本置为 default。真实 `_presentationTokens.CreateReservedLoadToken()` 抛出 `InvalidOperationException("Road render load reservation is stale.")`，tracker 与 admission reservation 保持不变，外层 Preflight catch 释放两个已创建但未转交的 Resource。operation 返回 `Failed`、`committed=false` 且无 warning，活动 RoadGraph、tool、placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 逐值不变，`ObjectResourceCount` 为 `77 -> 77`；后续 Preflight 与最终无故障 Load 均可重新接管。结构化结果为 `aggregate_reserved_token_failure_result_kind=2 aggregate_reserved_token_failure_committed=false aggregate_reserved_token_failure_trigger_count=1 aggregate_reserved_token_resource_count_before=77 aggregate_reserved_token_resource_count_after=77 load_result_kind=0`。`RoadRendererLifecycleContractTests` 33/33、生命周期与 QA export 聚焦组合 34/34、完整自动化 891/891，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Godot 4.7 Vulkan 1.4.341 Forward+ 契约以退出码 0 PASS，Godot MCP 冻结 `MapTest` 8 帧后运行树含两类静态表现节点，editor 游标 783 后无新增消息。minimal DAP 返回 `No active debug session`；`ExportRelease` 不含 probe 类型或 reserved-token arming/query API，两次隔离 QA 根已移入回收站，项目已停止。唯一 CLI warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。该切片关闭真实 aggregate reserved render token 创建子场景；其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实 aggregate Load `RoadRendererLoadCommitPlan` 构造故障协作进展（2026-08-22）：同一 Debug-only probe 在含 1 Edge 的真实 `StartLoad()` renderer Preflight 中，于 road mesh、node batch、reserved render token 与 `RoadSurfaceSnapshot` 完成后，在 plan constructor 已捕获全部字段但尚未返回 caller 时抛出一次性 `InvalidOperationException`。constructor 未完成，plan 所有权没有转交，外层 Preflight catch 同步释放两个 Godot Resource。operation 返回 `Failed`、`committed=false` 且无 warning，活动 RoadGraph、tool、placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 逐值不变，`ObjectResourceCount` 为 `77 -> 77`；后续 Preflight 与最终无故障 Load 均可重新接管。结构化结果为 `aggregate_commit_plan_failure_result_kind=2 aggregate_commit_plan_failure_committed=false aggregate_commit_plan_failure_trigger_count=1 aggregate_commit_plan_resource_count_before=77 aggregate_commit_plan_resource_count_after=77 load_result_kind=0`。`RoadRendererLifecycleContractTests` 34/34、生命周期与 QA export 聚焦组合 35/35、完整自动化 892/892，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Godot 4.7 Vulkan 1.4.341 Forward+ 契约以退出码 0 PASS。Godot MCP 冻结 `MapTest` 8 帧后运行树含两类静态表现节点，minimal stderr/console 为空；editor 增量窗口的最新 100 条均为既有 MCP/DAP 桥接基础设施噪声，停止后游标 1918 无新增消息。`ExportRelease` 不含 probe 类型或 commit-plan arming API，隔离 QA 根已移入回收站，项目已停止。唯一 CLI warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。该切片关闭真实 aggregate commit-plan constructor 子场景；其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实四参与者 `StartLoad()` renderer commit-boundary generation 失配协作进展（2026-08-22）：既有直接 aggregate probe 仍以真实 `RoadRendererLoadCommitPlan` 搭配 graph/tool/slot 三个 `TrackingLoadCommitPlan`；新增 Debug-only `SaveManager` partial 在 aggregate ownership 后、`Commit()` 前包装真实 `IStorageOperationLease`，并在 storage `CrossCommitBoundary()` 回调内释放 renderer admission。`PreparedAggregateLoad` 第二次 generation 复核在任何 graph/tool/presentation/slot `CommitReferences()` 前拒绝，operation 返回 `Failed`、`committed=false` 且无 warning，boundary 为 1、`MarkCommitted()` 为 0；活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，aggregate disposal 释放隐藏资源，`ObjectResourceCount` 为 `77 -> 77`，最终无故障 Load 成功重新接管。结构化结果为 `renderer_boundary_operation_failure_result_kind=2 renderer_boundary_operation_failure_committed=false renderer_boundary_operation_failure_trigger_count=1 renderer_boundary_operation_count=1 renderer_boundary_operation_mark_committed_count=0 load_result_kind=0`。`RoadRendererLifecycleContractTests` 35/35、生命周期与 QA export 聚焦组合 36/36、完整自动化 893/893，双配置 build、Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Godot 4.7 Vulkan 1.4.341 Forward+ 契约 PASS。Godot MCP 确认正确项目、版本、`MapTest`、Vulkan 与空 console/editor 增量错误，但运行树相关 RPC 持续超时，因此不声明该门通过。`ExportRelease` 不含 partial 实现、probe、lease wrapper 或测试 API，隔离 QA 根已移入回收站，项目已停止。该切片关闭完整真实 operation 的 renderer commit-boundary generation 失配子场景；其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实四参与者 `StartLoad()` ToolManager commit-boundary generation 失配协作进展（2026-08-22）：既有直接 aggregate probe 仍以真实 `ToolLoadCommitPlan` 搭配 graph/presentation/slot 三个 `ToolTrackingLoadCommitPlan`；新增 Debug-only `SaveManager` partial 在 renderer operation probe 后、`Commit()` 前再次包装真实 `IStorageOperationLease`，并在 storage `CrossCommitBoundary()` 回调内释放当前 `ToolLoadAdmission` 及其 builder admission。`PreparedAggregateLoad` 第二次 generation 复核在任何 graph/tool/presentation/slot `CommitReferences()` 前拒绝，operation 返回 `Failed`、`committed=false` 且无 warning，boundary 为 1、`MarkCommitted()` 为 0；活动 graph payload、`CurrentTool=Road`、`SelectedRoadType=Arterial`、placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，同槽 Save 后 payload 逐字节不变，aggregate disposal 释放隐藏资源，`ObjectResourceCount` 为 `77 -> 77`，最终无故障 Load 成功重新接管。结构化结果为 `tool_boundary_operation_failure_result_kind=2 tool_boundary_operation_failure_committed=false tool_boundary_operation_failure_trigger_count=1 tool_boundary_operation_count=1 tool_boundary_operation_mark_committed_count=0 tool_boundary_operation_resource_count_before=77 tool_boundary_operation_resource_count_after=77 load_result_kind=0`。`RoadRendererLifecycleContractTests` 36/36、生命周期与 QA export 聚焦组合 37/37、完整自动化 894/894，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区可见的 3 个 GDScript diagnostics 均为 0；隔离 `APPDATA` 的 Godot 4.7 Vulkan 1.4.341 Forward+ 契约 PASS，唯一 CLI warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。Godot MCP 确认正确项目、版本、`MapTest`、Vulkan 与空 stderr/console/editor 增量错误，但 `godot_game_time` 与 runtime-state RPC 超时，因此不声明运行树门通过。`ExportRelease` 不含 partial 实现、probe、lease wrapper 或测试 API，测试槽、隔离 QA 根与临时日志均已清理，项目已停止。该切片关闭完整真实 operation 的 ToolManager commit-boundary generation 失配子场景；其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实四参与者 `StartLoad()` RoadGraph commit-boundary generation 失配协作进展（2026-08-22）：既有直接 aggregate probe 仍以真实 `RoadGraphLoadCommitPlan` 搭配 presentation/slot 两个 `TrackingPlan`；新增 Debug-only `SaveManager` partial 在 renderer/tool operation probes 之前、aggregate ownership 后、`Commit()` 前包装真实 `IStorageOperationLease`，并在 storage `CrossCommitBoundary()` 回调内释放当前 `RoadGraphLoadAdmission`。`PreparedAggregateLoad` 第二次 generation 复核在任何 graph/tool/presentation/slot `CommitReferences()` 前拒绝，operation 返回 `Failed`、`committed=false` 且无 warning，boundary 为 1、`MarkCommitted()` 为 0；活动 graph payload、`CurrentTool=Road`、`SelectedRoadType=Arterial`、placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，同槽 Save 后 payload 逐字节不变，aggregate disposal 释放隐藏资源，`ObjectResourceCount` 为 `77 -> 77`，最终无故障 Load 成功重新接管。结构化结果为 `graph_boundary_operation_failure_result_kind=2 graph_boundary_operation_failure_committed=false graph_boundary_operation_failure_trigger_count=1 graph_boundary_operation_count=1 graph_boundary_operation_mark_committed_count=0 graph_boundary_operation_resource_count_before=77 graph_boundary_operation_resource_count_after=77 load_result_kind=0`。`RoadRendererLifecycleContractTests` 37/37、生命周期与 QA export 聚焦组合 38/38、完整自动化 895/895，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区可见的 3 个 GDScript diagnostics 均为 0；隔离 `APPDATA` 的 Godot 4.7 Vulkan 1.4.341 Forward+ 契约 PASS，唯一 CLI warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。Godot MCP 确认正确项目、Godot 4.7、`MapTest`、空 minimal stderr/console 与 editor 游标 1918 后无新增错误，但 `godot_game_time` 分帧 RPC 超时，因此不声明 MCP 运行树门通过。Debug DLL 包含 probe、lease wrapper 与测试 API，`ExportRelease` 均不包含；测试槽已删除，隔离 QA 根已移入回收站且原路径不存在，项目已停止。该切片关闭完整真实 operation 的 RoadGraph commit-boundary generation 失配子场景；其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实四参与者 `StartLoad()` slot-target commit-boundary generation 失配协作进展（2026-08-22）：既有 `RealSlotTargetGenerationMismatchAtCommitBoundary_RejectsEveryReferenceSwap` 只以真实 `SlotTargetLoadCommitPlan` 搭配 graph/tool/presentation fake companions；新增 Debug-only `SaveManager` partial 在 graph/renderer/tool operation probes 之后、aggregate ownership 后、`Commit()` 前包装真实 `IStorageOperationLease`，并在 storage `CrossCommitBoundary()` 回调内推进 `_currentSlotGeneration`。`PreparedAggregateLoad` 第二次 generation 复核在任何 graph/tool/presentation/slot `CommitReferences()` 前拒绝，operation 返回 `Failed`、`committed=false` 且无 warning，boundary 为 1、`MarkCommitted()` 为 0；活动 graph payload、`CurrentTool=Road`、`SelectedRoadType=Arterial`、placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，同槽 Save 后 payload 逐字节不变，aggregate disposal 释放隐藏资源，`ObjectResourceCount` 为 `77 -> 77`，最终无故障 Load 成功重新接管。结构化结果为 `slot_target_boundary_operation_failure_result_kind=2 slot_target_boundary_operation_failure_committed=false slot_target_boundary_operation_failure_trigger_count=1 slot_target_boundary_operation_count=1 slot_target_boundary_operation_mark_committed_count=0 slot_target_boundary_operation_resource_count_before=77 slot_target_boundary_operation_resource_count_after=77 load_result_kind=0`。`RoadRendererLifecycleContractTests` 38/38、生命周期与 QA export 聚焦组合 39/39、完整自动化 896/896，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区可见的 3 个 GDScript diagnostics 均为 0，隔离副本的 GDScript `--check-only` 退出码为 0；Godot 4.7 Vulkan 1.4.341 Forward+ 契约 PASS，唯一运行时 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。Godot MCP 确认正确项目、版本、`MapTest` 与未运行状态，minimal stderr/console 为空，editor 游标 1918 后无新增错误；Debug DLL 包含 slot-target probe/wrapper/API，`ExportRelease` 均不包含，测试槽与 QA 根已清理。该切片关闭完整真实 operation 的 slot-target commit-boundary generation 失配子场景；其余 renderer 故障、其他真实 aggregate 组合、完整 token 性能扰动与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - `RenderRequestID` 分级性能协作进展（2026-08-22）：真实 `MapTest` 的 `road_rendering_performance_contract.gd --measure-token-perturbation=render-request` 已逐级通过 grid 1k/10k/25k/50k/100k；提交前 superseded attempt 不交换引用并释放两个未转交 Resource，replacement 与 superseded 仅 `RenderRequestID: 3 -> 4`，replacement attempt 1 完整发布 `N+1` Edge，资源计数始终为 `77 -> 77`。五档拒绝/恢复/合计为 `45.680/9.888/55.568 ms`、`232.832/59.794/292.626 ms`、`532.356/155.228/687.584 ms`、`787.032/294.867/1081.899 ms`、`1612.186/579.119/2191.305 ms`；完整自动化 896/896、双配置 build、Roslyn/GDScript diagnostics、隔离 `--check-only` 与 Vulkan 门均通过。MCP 项目/Vulkan/game bridge/minimal console 门通过，但 game-time、运行树与 runtime-state RPC 超时，未计作通过。该切片只关闭六分量性能扰动矩阵中的 `RenderRequestID`；其余五分量、renderer 故障、其他 aggregate 组合与 Phase 8 仍开放，因此 8.6 不关闭。

  - `RoadStyleRevision` 分级性能协作进展（2026-08-22）：真实 `MapTest` 的同一契约新增 `--measure-token-perturbation=road-style` 并逐级通过 grid 1k/10k/25k/50k/100k；提交前 target attempt 1 不交换引用并释放两个未转交 Resource，replacement 保持 scene/facade/facade generation/change sequence，推进 `RoadStyleRevision: 1 -> 2` 与 `RenderRequestID: 3 -> 4`，再以自己的 attempt 1 完整发布 `N+1` Edge，资源计数始终为 `77 -> 77`。五档拒绝/恢复/合计为 `46.001/9.939/55.940 ms`、`221.569/58.811/280.380 ms`、`572.255/175.426/747.681 ms`、`830.529/298.035/1128.564 ms`、`1568.166/568.939/2137.105 ms`；完整自动化 896/896、双配置 build、Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics、隔离 `--check-only` 与 Vulkan 门均通过。MCP 项目/Vulkan/game bridge/minimal console 门通过，但 game-time 与 runtime-state RPC 超时，未计作通过。六分量性能矩阵现完成 `RenderRequestID` 与 `RoadStyleRevision`；其余四分量、renderer 故障、其他 aggregate 组合与 Phase 8 仍开放，因此 8.6 不关闭。

  - `SceneGeneration` 分级性能协作进展（2026-08-22）：真实 `MapTest` 的同一契约新增 `--measure-token-perturbation=scene-generation` 并逐级通过 grid 1k/10k/25k/50k/100k；retained 到 superseded 只推进 `ChangeSequence + 1` 与 `RenderRequestID + 1`，提交前 replacement 再保持 facade/facade generation/change sequence/style，仅推进 `SceneGeneration: 1 -> 2` 与 `RenderRequestID: 3 -> 4`。target attempt 1 不交换引用并释放两个未转交 Resource，replacement 以自己的 attempt 1 完整发布 `N+1` Edge，资源计数始终为 `77 -> 77`。五档拒绝/恢复/合计为 `47.371/12.581/59.952 ms`、`233.495/63.975/297.470 ms`、`588.961/180.743/769.704 ms`、`875.853/308.198/1184.051 ms`、`1595.644/572.131/2167.775 ms`；完整自动化 896/896，双配置 build、Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics、隔离 `--check-only` 与 Vulkan 门均通过，两个既有扰动模式的 1k 兼容回归继续 PASS。MCP 确认正确项目、Godot 4.7、`MapTest` 未运行状态和 addon/server 版本匹配，minimal stderr/console 为空，editor 游标 1989 后无新增 error；buffer 中既有 MCP/DAP transport 错误不计作本切片通过。六分量性能矩阵现完成 `RenderRequestID`、`RoadStyleRevision` 与 `SceneGeneration`；其余 `GraphFacadeID`、`GraphFacadeGeneration`、`ChangeSequence`、renderer 故障、其他 aggregate 组合与 Phase 8 仍开放，因此 8.6 不关闭。

  - `GraphFacadeID` 分级性能协作进展（2026-08-22）：真实 `MapTest` 的同一契约新增必须搭配单一 `--dataset-size` 的 `--measure-token-perturbation=graph-facade-id` 并逐级通过 grid 1k/10k/25k/50k/100k。target attempt 1 到达提交前边界后，正式 `SetGraph()` 同步发布新 facade 的 replacement attempt 1，因此 recovery 为 `0`；superseded 到 replacement 保持 scene/change sequence/style，切换 `GraphFacadeID: 1 -> 3`，推进 `GraphFacadeGeneration: 2 -> 3` 与 `RenderRequestID: 3 -> 4`。原 target 不交换自己的资源，两个未转交 Resource 同步释放，新 facade 完整发布 `N+1` Edge 与 matching surface hit，资源计数始终为 `77 -> 77`。五档同步取代/接管总耗时为 `59.790/304.579/822.712/1273.036/2345.394 ms`；完整自动化 896/896，双配置 build、Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics、最终隔离 `--check-only` 与 Vulkan 门均通过，前三种扰动模式的 1k 兼容回归继续 PASS。MCP 确认正确项目、Godot 4.7、`MapTest` 未运行状态和 addon/server 版本匹配，minimal stderr/console 为空，editor 游标 1989 后无新增 error；buffer 中既有 MCP/DAP transport 错误不计作本切片通过。六分量性能矩阵现完成四项；其余 `GraphFacadeGeneration`、`ChangeSequence`、renderer 故障、其他 aggregate 组合与 Phase 8 仍开放，因此 8.6 不关闭。

  - `GraphFacadeGeneration` 分级性能协作进展（2026-08-22）：真实 `MapTest` 的同一契约新增 `--measure-token-perturbation=graph-facade-generation` 并逐级通过 grid 1k/10k/25k/50k/100k；提交前 target attempt 1 不交换引用并释放两个未转交 Resource，replacement 保持 scene/facade/change sequence/style，推进 `GraphFacadeGeneration: 2 -> 3` 与 `RenderRequestID: 3 -> 4`，再以自己的 attempt 1 完整发布 `N+1` Edge，资源计数始终为 `77 -> 77`。五档拒绝/恢复/合计为 `46.989/10.046/57.035 ms`、`226.285/59.298/285.583 ms`、`510.660/155.424/666.084 ms`、`811.079/284.769/1095.848 ms`、`1594.093/570.155/2164.248 ms`；完整自动化 896/896，双配置 build、Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics、隔离 `--check-only` 与 Vulkan 门均通过，相邻 `graph-facade-id` 1k 兼容回归继续 PASS。MCP 确认正确项目、Godot 4.7、`MapTest` 未运行状态和 addon/server 版本匹配，minimal stderr/console 为空，editor 游标 1989 后无新增 error；buffer 中既有 MCP/DAP transport 错误不计作本切片通过。六分量性能矩阵现完成五项；只余 `ChangeSequence`、renderer 故障、其他 aggregate 组合与 Phase 8 仍开放，因此 8.6 不关闭。

  - `ChangeSequence` 分级性能协作进展（2026-08-22）：真实 `MapTest` 的同一契约新增 `--measure-token-perturbation=change-sequence` 并逐级通过 grid 1k/10k/25k/50k/100k；第一条真实 builder mutation 令 retained 到 superseded 的 `ChangeSequence` 与 `RenderRequestID` 各推进一位，提交前第二条真实 graph mutation 再令 superseded 到 replacement 的 `ChangeSequence: 2 -> 3` 与 `RenderRequestID: 3 -> 4` 各推进一位，其他四个 token 分量稳定。target attempt 1 不交换引用并释放两个未转交 Resource，pending 期两条道路都不可命中；replacement attempt 1 一次发布 `N+2` Edge 与两处 matching surface hit，资源计数始终为 `77 -> 77`。五档拒绝/恢复/合计为 `64.741/6.024/70.765 ms`、`372.829/55.966/428.795 ms`、`910.134/172.078/1082.212 ms`、`1224.489/296.431/1520.920 ms`、`2537.144/587.846/3124.990 ms`；完整自动化 896/896，双配置 build、Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics、`--check-only` 与 Vulkan 门均通过，相邻 `graph-facade-generation` 1k 兼容回归继续 PASS。MCP 确认正确项目、Godot 4.7、`MapTest` 未运行状态和 addon/server 4.1.0 匹配，minimal stderr/console 为空，editor 游标 1989 后无新增 error；Debug DLL 包含 probe，`ExportRelease` 不包含，隔离 QA 根已清理。六分量性能扰动矩阵已完成；renderer 故障、其他 aggregate 组合与 Phase 8 仍开放，因此 8.6 不关闭。

  - 普通更新 token 纯 `Prepare` 故障协作进展（2026-08-22）：`v3-grid-rendering:2.2` 已在真实 ordinary graph mutation 的样式/revision snapshot 后、`RoadRendererLoadPreparer.Prepare(...)` 与任何 Godot Resource 创建前注入一次性故障。attempt 1 推进 desired/stalled token 但保留旧 presented/mesh/surface/cache，provider 拒绝旧与未呈现 hit，`ObjectResourceCount` 为 `77 -> 77`；attempt 2 以同一 desired token 成功发布新增 Edge/surface。结构化结果为 `UPDATE_TOKEN_PREPARE_FAILURE_RESULT resource_before=77 resource_after=77 trigger_count=1 stalled_attempt=1 recovered_attempt=2`。`RoadRendererLifecycleContractTests` 39/39、生命周期与 QA export 聚焦 40/40、完整自动化 897/897，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离 `--check-only` 和正式 Vulkan 1.4.341 Forward+ 契约均通过；MCP 确认正确项目、addon/server 4.1.0 匹配、`MapTest` 未运行且错误通道无新增。Debug DLL 包含 probe/API，`ExportRelease` 不包含，隔离 QA 根已清理。该切片将普通更新相邻构建故障点由四个扩为五个；其余 renderer 故障、其他真实 aggregate 组合与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实 aggregate Load renderer worker-`Prepare` 故障协作进展（2026-08-23）：`v3-grid-rendering:2.2` 在完整四参与者 `StartLoad()` 的 worker 中，于 `PreparedSaveSlot` 取得 `RoadGraphRevision` 后、`RoadRendererLoadPreparer.Prepare(...)` 与任何 renderer Preflight/Resource 创建前注入一次性故障。operation 返回 `Failed`、`committed=false` 且无 warning，活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，`ObjectResourceCount` 为 `77 -> 77`；probe 解除后，下一次 Load 成功取得全部 admission 并联合接管。结构化结果为 `renderer_worker_prepare_failure_result_kind=2 renderer_worker_prepare_failure_committed=false renderer_worker_prepare_failure_trigger_count=1 renderer_worker_prepare_resource_count_before=77 renderer_worker_prepare_resource_count_after=77 load_result_kind=0`。生命周期与 QA export 聚焦组合 41/41、完整自动化 898/898，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离 `--check-only` 与正式 Vulkan 1.4.341 Forward+ 契约均通过；MCP 确认正确项目、addon/server 4.1.0 匹配、`MapTest` 未运行且游标 1989 后无新增 editor/minimal 错误。Debug DLL 包含 probe/API，`ExportRelease` 不包含，隔离 QA 根已清理。真实 aggregate 相邻失败子场景现由九个增至十个；其余 renderer 故障、其他真实 aggregate 组合与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实 aggregate Load renderer admission 样式快照故障协作进展（2026-08-23）：`v3-grid-rendering:2.2` 把完整 `StartLoad()` 的最早 renderer 故障点前移到 `RoadRenderer.BeginLoadAdmission()` 捕获生产 `RoadTypeStyleSnapshot`。共享 `RoadConfig` 暂缺一种样式时，真实校验在 render reservation、renderer admission 发布与 worker 启动前返回 `Failed`、`committed=false` 且无 warning；已经取得的 graph/tool admission 由 `RunLoadAsync()` 的 `finally` 释放，活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，`ObjectResourceCount` 为 `77 -> 77`。配置恢复后，紧邻 worker-Prepare probe 能进入 worker 并触发一次，最终无故障 Load 成功联合接管，证明没有 admission 滞留。结构化新增结果为 `renderer_admission_style_snapshot_failure_result_kind=2 renderer_admission_style_snapshot_failure_committed=false renderer_admission_style_snapshot_resource_count_before=77 renderer_admission_style_snapshot_resource_count_after=77`。生命周期与 QA export 聚焦组合 42/42、完整自动化 899/899，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、构建后的隔离 `--check-only` 与正式 Vulkan 1.4.341 Forward+ 契约均通过；MCP 确认正确项目、addon/server 4.1.0 匹配、`MapTest` 未运行且游标 1989 后无新增 editor/minimal 错误。本切片未新增生产 probe，四个隔离 QA 根已移入回收站且原路径不存在。真实 aggregate 相邻失败子场景现由十个增至十一个；其余 renderer 故障、其他真实 aggregate 组合与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实 aggregate Load renderer admission 参数校验故障协作进展（2026-08-23）：`v3-grid-rendering:2.2` 在生产 `RoadTypeStyleSnapshot` 成功后，以 `CurveDisplayTolerance` 构造 `RoadRendererLoadSettings` 并调用 `settings.Validate()`。共享生产配置的容差暂设为 `0.0` 时，真实校验以 `RoadRenderer curve tolerance is invalid.` 在 render reservation、renderer admission 构造/发布与 worker 启动前返回 `Failed`、`committed=false` 且无 warning；已经取得的 graph/tool admission 由 `RunLoadAsync()` 的 `finally` 释放，活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，`ObjectResourceCount` 为 `77 -> 77`。配置恢复后，紧邻 worker-Prepare probe 能进入 worker 并触发一次，最终无故障 Load 成功联合接管，证明没有 admission 滞留。结构化新增结果为 `renderer_admission_settings_failure_result_kind=2 renderer_admission_settings_failure_committed=false renderer_admission_settings_resource_count_before=77 renderer_admission_settings_resource_count_after=77`。生命周期与 QA export 聚焦组合 43/43、完整自动化 900/900，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、构建后的隔离 `--check-only` 与正式 Vulkan 1.4.341 Forward+ 契约均通过；MCP 确认正确项目、addon/server 4.1.0 匹配、`MapTest` 未运行且游标 1989 后无新增 editor/minimal 错误。本切片未新增生产 probe，两个隔离 QA 根已移入回收站且原路径不存在。真实 aggregate 相邻失败子场景现由十一个增至十二个；其余 renderer 故障、其他真实 aggregate 组合与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实 aggregate Load renderer admission 构造前故障协作进展（2026-08-23）：`v3-grid-rendering:2.2` 已在合法样式快照、参数校验与 `_presentationTokens.ReserveLoad()` 成功后，于 `RoadRendererLoadAdmission` 构造/发布和 worker 启动前注入一次性故障。operation 返回 `Failed`、`committed=false` 且无 warning；已有 graph/tool admission 由 `RunLoadAsync()` 的 `finally` 释放，render reservation 未交换 desired/presented，活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，`ObjectResourceCount` 为 `77 -> 77`。紧随其后的 worker-Prepare probe 仍取得新 admission 并触发一次，最终无故障 Load 成功联合接管。结构化新增结果为 `renderer_admission_construction_failure_result_kind=2 renderer_admission_construction_failure_committed=false renderer_admission_construction_failure_trigger_count=1 renderer_admission_construction_resource_count_before=77 renderer_admission_construction_resource_count_after=77`。生命周期与 QA export 聚焦组合 44/44、完整自动化 901/901，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离目标脚本 `--check-only` 与正式 Vulkan 1.4.341 Forward+ 契约均通过；Debug 反射可见 9 个相关 probe 入口，`ExportRelease` 中 probe 类型不存在且匹配入口为 0，MCP 项目/addon/server、未运行状态及 editor/minimal 错误通道也已复核。真实 aggregate 相邻失败子场景现由十二个增至十三个；其余 renderer 故障、其他真实 aggregate 组合与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实 aggregate Load renderer admission 发布前故障协作进展（2026-08-23）：`v3-grid-rendering:2.2` 已在局部 `RoadRendererLoadAdmission` 构造成功后、`_loadAdmission` 发布与 worker 启动前注入一次性故障。该构造函数只捕获 owner、generation、graph、reservation 与 pure CLR preparer；operation 以精确注入消息返回 `Failed`、`committed=false` 且无 warning，orchestration 没有接到 renderer admission 返回值，`_loadAdmission` 保持 null。已有 graph/tool admission 由 `RunLoadAsync()` 的 `finally` 释放，render reservation 未交换 desired/presented；活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，保存后的 payload 不变，`ObjectResourceCount` 为 `77 -> 77`。紧随其后的 worker-Prepare probe 仍取得并发布新 admission、进入 worker 且触发一次，最终无故障 Load 成功联合接管。结构化新增结果为 `renderer_admission_publication_failure_result_kind=2 renderer_admission_publication_failure_committed=false renderer_admission_publication_failure_trigger_count=1 renderer_admission_publication_resource_count_before=77 renderer_admission_publication_resource_count_after=77`。生命周期与 QA export 聚焦组合 45/45、完整自动化 902/902，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离目标脚本 `--check-only` 与正式 Vulkan 1.4.341 Forward+ 契约均通过；Debug 反射可见 10 个相关 probe 入口，`ExportRelease` 中 probe 类型不存在且匹配入口为 0，MCP 项目/addon/server、未运行状态及 editor/minimal 错误通道也已复核。两个隔离 QA 根均已清理且原路径不存在，其中运行时根已移入回收站。真实 aggregate 相邻失败子场景现由十三个增至十四个；其余 renderer 故障、其他真实 aggregate 组合与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实 aggregate Load renderer admission 返回后故障协作进展（2026-08-23）：`v3-grid-rendering:2.2` 已让完整四参与者 `StartLoad()` 从 `RoadRenderer.BeginLoadAdmission()` 取得已发布的 renderer admission，再于 `SaveSlotStore.CaptureLoadParticipants(...)`、`lease.AdvanceTo(Prepare)` 与 worker `Task.Run` 之前注入一次性故障。operation 以精确注入消息返回 `Failed`、`committed=false` 且无 warning；`RunLoadAsync()` 的 `finally` 按 renderer、tool、graph 顺序释放 admission，活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，保存后的 payload 逐字节不变，`ObjectResourceCount` 为 `77 -> 77`。紧随其后的 worker-Prepare probe 仍进入 worker 并触发一次，最终无故障 Load 成功联合接管。结构化新增结果为 `post_renderer_admission_failure_result_kind=2 post_renderer_admission_failure_committed=false post_renderer_admission_failure_trigger_count=1 post_renderer_admission_resource_count_before=77 post_renderer_admission_resource_count_after=77`。生命周期与 QA export 聚焦组合 46/46、完整自动化 903/903，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离目标脚本 `--check-only` 与正式 Vulkan 1.4.341 Forward+ 契约均通过；Debug 反射可见新 probe 的 10 个相关入口，`ExportRelease` 中 probe 类型不存在且匹配入口为 0，MCP 项目/addon/server、未运行状态及 editor/minimal 错误通道也已复核。两个隔离 QA 根均已清理且原路径不存在，其中运行时根已移入回收站。真实 aggregate 相邻失败子场景现由十四个增至十五个；其余 renderer 故障、其他真实 aggregate 组合与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实 aggregate Load participant capture 后故障协作进展（2026-08-23）：`v3-grid-rendering:2.2` 已让完整四参与者 `StartLoad()` 从 `RoadRenderer.BeginLoadAdmission()` 取得已发布的 admission，并由 `SaveSlotStore.CaptureLoadParticipants(GetRequiredSaveables())` 验证 participant 定义、捕获唯一正式 payload 的 load reader；随后 Debug-only partial probe 记录 participant 数为 `1`，再于 `lease.AdvanceTo(Prepare)` 与 worker `Task.Run` 前以精确消息拒绝。operation 返回 `Failed`、`committed=false` 且无 warning；`RunLoadAsync()` 的 `finally` 按 renderer、tool、graph 顺序释放 admission，活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，保存后的 payload 逐字节不变，`ObjectResourceCount` 为 `77 -> 77`。紧随其后的 worker-Prepare probe 仍进入 worker 并触发一次，最终无故障 Load 成功联合接管。结构化新增结果为 `post_participant_capture_failure_result_kind=2 post_participant_capture_failure_committed=false post_participant_capture_failure_trigger_count=1 post_participant_capture_participant_count=1 post_participant_capture_resource_count_before=77 post_participant_capture_resource_count_after=77`。生命周期与 QA export 聚焦组合 47/47、完整自动化 904/904，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离目标脚本 `--check-only` 与正式 Vulkan 1.4.341 Forward+ 契约均通过；Debug 反射可见新 probe 的 10 个相关方法，`ExportRelease` 中 probe 类型不存在且匹配方法为 0，MCP 项目/addon/server、未运行状态及 editor/minimal 错误通道也已复核。隔离项目根与 `APPDATA` profile 根均已移入回收站且原路径不存在。真实 aggregate 相邻失败子场景现由十五个增至十六个；其余 renderer 故障、其他真实 aggregate 组合与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实 aggregate Load `Prepare` phase 推进后故障协作进展（2026-08-23）：`v3-grid-rendering:2.2` 已让完整四参与者 `StartLoad()` 完成 participant capture，并由 `lease.AdvanceTo(SaveOperationPhase.Prepare)` 把 operation phase 发布为 `3`；随后 Debug-only partial probe 记录 observed phase `3`，再于 worker `Task.Run` 创建前以精确消息拒绝。operation 以 `finalPhase=3`、`Failed`、`committed=false` 且无 warning 返回；`RunLoadAsync()` 的 `finally` 按 renderer、tool、graph 顺序释放 admission，活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，保存后的 payload 逐字节不变。关闭隔离副本中与契约无关的 MCP/ImGui tooling autoload 后，`ObjectResourceCount` 为 `65 -> 65`；紧随其后的 worker-Prepare probe 仍进入 worker 并触发一次，最终无故障 Load 成功联合接管。结构化新增结果为 `post_prepare_phase_failure_result_kind=2 post_prepare_phase_failure_final_phase=3 post_prepare_phase_failure_committed=false post_prepare_phase_failure_trigger_count=1 post_prepare_phase_observed_phase=3 post_prepare_phase_resource_count_before=65 post_prepare_phase_resource_count_after=65`。生命周期与 QA export 聚焦组合 48/48、完整自动化 905/905，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离目标脚本 `--check-only` 与正式 Vulkan 1.4.341 Forward+ 契约均通过；Debug 反射可见新 probe 的 10 个相关方法，`ExportRelease` 中 probe 类型不存在且匹配方法为 0，MCP 项目/addon/server、未运行状态及 editor/minimal 错误通道也已复核。所有隔离根均已清理，5 个 profile 进入回收站，长路径项目根已永久删除。真实 aggregate 相邻失败子场景现由十六个增至十七个；其余 renderer 故障、其他真实 aggregate 组合与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

  - 真实 aggregate Load worker-entry 故障协作进展（2026-08-23）：`v3-grid-rendering:2.2` 已让完整四参与者 `StartLoad()` 在 `lease` 进入 phase 3 后实际进入 `Task.Run` worker，并由 Debug-only partial hook 在 `CreateSlotStore().PrepareLoad(...)` 与任何 slot/renderer preparation 前以精确消息拒绝。probe 确认离开主线程且只触发一次；operation 返回 `finalPhase=3`、`Failed`、`committed=false` 且无 warning，`RunLoadAsync()` 的 `finally` 释放 renderer/tool/graph admission。活动 graph payload、tool/placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，保存后的 payload 逐字节不变，`ObjectResourceCount` 为 `79 -> 79`；既有 renderer worker-Prepare probe 与最终无故障 Load 继续证明后续 admission 可重新取得并联合接管。结构化结果为 `worker_entry_failure_result_kind=2 worker_entry_failure_final_phase=3 worker_entry_failure_committed=false worker_entry_failure_trigger_count=1 worker_entry_failure_off_main_thread=true worker_entry_resource_count_before=79 worker_entry_resource_count_after=79`。生命周期与 QA export 聚焦组合 49/49、完整自动化 906/906，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 3 个 GDScript diagnostics、隔离目标脚本 `--check-only` 与正式 Vulkan 1.4.341 Forward+ 契约均通过；Debug 反射、`ExportRelease` 排除、MCP 项目状态及 editor/minimal 错误通道也已复核，CLI finalizer 退出崩溃已收敛。真实 aggregate 相邻失败子场景现由十七个增至十八个；其余 renderer 故障、其他真实 aggregate 组合与 Phase 8 组合矩阵仍开放，因此 8.6 不关闭。

## 暂不执行

### 交通模拟

- 延期原因：V3 先完成领域合法的 self-loop、parallel Edge、RoadType 和统一事务摘要；模拟层不能反向收窄这些契约。
- 保持现状：本路线图不实现 `TrafficGraph`、寻路、速度、容量或拥堵。
- 重新开启条件：`v3-road-graph:8.6` 完成后，由根层 `docs/todo/traffic-simulation.md` 定义模拟映射。

## 已解决基线

- [x] **V3 数值、容量与确定性基础已建立。** mutation 在写图前执行坐标、控制参数、长度、资源、work 与 ID admission；exact-sign line、确定交点 cluster、full-turn 识别和 canonical `-0` 已由 538 项自动化及真实 `MapTest` 拒绝场景验证。后续 canonical root、query fragment 和 V3 reader 必须消费该策略，不能另建不一致阈值。
- [x] **V3 endpoint-role incidence 与原生方向基础已建立。** self-loop 在同一 Node 拥有 A/B 两条 incidence，平行 Edge 保持独立；六类原生几何可逆，arc/clothoid 以权威 binary32 锚和反向参数保持 mixed-chain join，非环与 self-loop 均有确定存储方向。后续 canonical Edge、闭合提交、V3 reader 和 renderer 必须沿用这些字段与 endpoint role，不能从邻居去重或显示采样反推拓扑与方向。
- [x] **V3 最大连续 Edge、Group-free 图与 fragment locality 已建立。** 提交、拆分和删除后恢复规范二 incidence 形；折角/混合 primitive 留在一条 Edge，连续同向 line 精确折叠。query fragment 对 cut/join/B 端/seam 使用半开唯一所有权，远端 line/geometry 扩大 16 倍不增加固定窗口 exact test；后续闭合、自交、RoadType、不可变 root 与 V3 reader 必须沿用该 canonical Edge 和索引身份，不能恢复 Group 或整 Edge 局部扫描。
- [x] **V3 公共闭合、自交与环路规范形已建立。** closed/full-turn、incoming/incoming intersection、离散自交 junction、rooted self-loop 和 parallel Edge 已进入公共提交；连续重叠与 canonical split 歧义在 mutation 前结构化拒绝。后续 RoadType、V3 reader、renderer 和 delta 必须保留 seam、endpoint-role incidence、typed direction 及失败原子性。
- [x] **V3 Edge 级 RoadType 与类型化提交已建立。** 四个稳定领域值、严格 token、显式 `RoadBuildRequest`、拆分继承、typed merge key、semantic boundary 和覆盖不改造已由 611 项自动化及真实 `MapTest` 混合类型场景验证。后续改造、renderer、V3 reader 和工具选择必须消费 Edge 级类型，不能引入默认类型或把类型塞入几何草稿。
- [x] **V3 不可变 root、原子改造和统一 delta 身份已建立。** 普通 mutation 结构共享未触碰 Entity/geometry/bucket 页，`CaptureRevision()` 为 O(1)，`GraphChanged` 是唯一事务事件；lineage/revision/sequence token、ID watermark、observer 隔离和重入拒绝均有自动化、Release 远端扩展基准及真实 `MapTest` undo/redo 证据。后续 V3 writer、renderer token 和 full-reset aggregate 必须直接消费该 root/token，不能重新捕获可变图或恢复逐 Edge 事件。
- [x] **空间索引精确覆盖不变式已改为线性批量校验。** `AssertInvariants()` 先按引用 identity 汇总预期 bounds，再由 `UniformGrid.HasExactCoverage(...)` 单次扫描 bucket entries；缺失、额外、错桶、同桶重复和内部计数不符仍严格拒绝。2026-08-17 再次逐级完成 12k～100k V3 Load/renderer rebuild，100k 为 5129.679 ms，不再因 `reference × bucket` 二次扫描进入 AppHang；invariant + V3 persistence 为 33/33、完整自动化为 833/833。10k 硬门和 Windows QA 导出包 writable 存档契约也已通过，但当前会话未刷新 MCP/DAP，且完整 Phase 7 表现/故障矩阵仍未完成，因此不关闭 `v3-road-graph:8.6`。
- [x] **V2 道路数据层不依赖输入层方向或网格概念。** 任意角度直线、折线和结构化非法路径拒绝已有自动化保护。
- [x] **V2 原生曲线、二维交叉、查询、删除事务、渲染和存档已通过最终验收。** 这些是 V3 需要以新接口重新验证的玩家能力基线，不要求复用 V2 代码或读取 V2 数据。
- [x] **V2 规模基线已记录。** V3 性能门槛必须使用同机、同口径对照，不能把后台总耗时误报为主线程无卡顿。

## 完成标准

1. 8.0～8.5 的数值、容量、incidence、连续 Edge、环路、RoadType、不可变事务和 delta 契约均通过各自自动化及性能门禁。
2. V3 runtime、format v1、renderer、工具、UI 和事件消费者只使用一套新契约；旧 Group/API/事件/DTO/writer 已删除，不存在兼容适配器、双写或生产路径选择。
3. `v3-save-system`、`v3-grid-rendering`、`v3-tool-input` 与 `v3-ui` 的依赖项全部通过自身验收。
4. 8.6 在真实 `MapTest`、Vulkan、10k 门槛、100k 压测和 Windows 导出环境完成最终组合验证，证据写回 V3 指南附录 D。
5. `v3-road-graph:8.6` 是唯一最终集成负责人；交通模拟和其他明确排除项不阻塞 V3。

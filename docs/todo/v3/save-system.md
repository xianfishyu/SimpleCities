# 第三代存档系统待办清单

> 系统 key：`v3-save-system`
> 整理日期：2026-08-24
> 证据：当前工作区 `SaveManager`、`SaveSlotStore`、RoadGraph 持久化源码与存档自动化，V2 历史路线图 `docs/todo/save-system.md`，以及 `docs/manuals/road-system-v3-gen.md` 第 10 节。
> 主导原则：V3 建立唯一的新运行时存档契约、独立保存根和 `simple-cities-v3` format v1；不读取、迁移、转换、覆盖或删除 V2 存档，也不保留旧 DTO/接口适配器。严格版本、容量、原子发布、崩溃恢复和 aggregate Load 是 V3 自身的正确性边界。

## 状态总览

<a id="v3-save-system2"></a>

| ID | 发现 | 当前状态 | 处置方式 |
|---|---|---|---|
| 2.1 | V2 schema、保存根和 DTO 无法表达 V3 canonical Edge | 已完成 | 已建立隔离的 V3 format v1，并直接拒绝所有非 V3 格式 |
| 2.2 | 流式快照、严格 manifest、PNG 展示资产与目录事务需要统一的预算和恢复边界 | 已完成 | 有界 token reader、descriptor/digest 恢复、删除 tombstone、OS 根锁与故障矩阵均已验证 |
| 2.3 | 同步入口和顺序 prepared commit 曾缺少并发及原子会话协议 | 开放（部分实现） | async coordinator、等待取消防护、四参与者 aggregate、fake second saveable、worker-prepared 四类 surface/index/token、带 token/slot 的 Load 分段性能快照、与普通 mutation 分离的表现分段口径、现有道路事务 admission、deferred continuation 失效、真实 RoadGraph/renderer/tool/slot-target plans 的 commit-boundary generation，以及真实四参与者 `StartLoad()` 对同四种 participant 的 operation 级失配、真实 `RoadRenderer` admission、ToolManager scene-generation 失配、fake participant 与真实 `RoadGraph` observer/cleanup 隔离、真实 renderer observer warning 后的全参与者 cleanup 再接管、真实 renderer observer 的四个逐参与者 cleanup 联合故障、renderer/RoadGraph observer 各自及同时与四参与者 cleanup 全故障、连续 Load、未提交隐藏资源清理、四个已定位 Resource 所有权点、普通更新 token、真实 aggregate Load 的 renderer Resource Preflight 故障、renderer plan 加入后与 slot plan 加入后的后续 aggregate Preflight fallback disposal、aggregate ownership 后的 pre-commit disposal、Save As Prepare 取消/重复激活与 canonical publication 后取消拒绝，以及 Delete 越界前取消/重复激活与 tombstone 后同 token 取消拒绝已落地；仍需其余 renderer 故障与其他真实 aggregate 组合矩阵 |

### 设计覆盖矩阵

| 设计范围 | 当前事实 | 关联待办 |
|---|---|---|
| V3 格式与根隔离 | 生产运行已统一使用 `user://saves-v3`；manifest/payload 均为严格 `simple-cities-v3` format v1，RoadGraph reader/writer 可表达 canonical self-loop、parallel Edge、四类 `roadType` 和完整原生几何锚；V2/未知目录在 V3 根只分类为 `Foreign` | `v3-save-system:2.1`、`v3-road-graph:8.0`～`8.5` |
| 长连续 Edge | `IStreamingSaveable` 以 O(1) 捕获 immutable root；writer 直接输出无缩进 UTF-8，`V3JsonStreamReader` 在固定 buffer 上执行 byte/token/depth/lexeme 与 RoadGraph 容量预算，同一 payload 句柄完成长度/SHA-256/EOF 终检 | `v3-save-system:2.2`、`v3-road-graph:8.0`、`v3-road-graph:8.5` |
| 发布、恢复与删除 | operation-specific publish/delete descriptor 绑定 digest 与固定路径；五类 occupant、quarantine、删除 tombstone、跨进程 OS 根锁及 cleanup-pending/typed recovery-blocked 路径均已实现 | `v3-save-system:2.2` |
| Load 生命周期 | `SaveManager` 已把 RoadGraph root、空工具/history、基础 `ArrayMesh`/`MultiMesh`、带 canonical `RoadLocation` 和不可变 AABB 索引的同源 `EdgeRibbon` + `TerminalCap` + `SemanticJoin` + `JunctionPatch` `RoadSurfaceSnapshot`、matching 六分量 `RoadRenderToken` 与 `CurrentSlotID` 纳入一次 `PreparedAggregateLoad` 引用交换；surface triangle/disc 复制、Junction Patch 细分与建树在 worker Prepare 完成，未转交的隐藏 mesh/node batch 会在 Preflight 异常或 plan 放弃时确定性释放，renderer plan 或 slot plan 已加入但 aggregate 尚未接管时的后续异常由 `RunLoadAsync()` fallback 释放已有 plans，ownership transfer 后、commit 前的异常则由 aggregate `Dispose()` 释放 owned plans。placement、拆除、改造会话及 undo/redo 已按同一 token 边界失效，observer 与 post-commit cleanup 异常均逐参与者隔离为 warning，fake participant 的 observer/cleanup 组合、真实 `RoadGraph` observer/fake companion cleanup 组合、真实 renderer observer warning 后的 graph/tool/presentation/slot cleanup 再接管、真实 renderer observer 的四个逐参与者 cleanup 联合故障、renderer/RoadGraph observer 各自及同时与四参与者 cleanup 全故障均已验证；真实 `StartLoad()` 已分别覆盖 renderer Resource Preflight 失败、renderer plan 后续、slot plan 后续与 aggregate ownership 后的 Preflight 失败，确认 `committed=false`、活动四方状态不变、资源计数不增且随后 Save/Load 可成功。四个直接 aggregate 契约已分别覆盖真实 RoadGraph、renderer、tool 与 slot-target plan 的 commit-boundary generation 失配，但其余参与者仍为 tracking/fake companions；真实 `StartLoad()` 已进一步对 graph、renderer、tool 与 slot-target 四种 participant 逐一在同一 storage boundary 注入失配，并都在任何引用交换前原子拒绝。成功 Load 另保存带 operation token/target slot 的 worker Prepare、主线程 Preflight、引用交换、aggregate commit 与端到端分段快照 | `v3-save-system:2.3`、`v3-road-graph:8.5`、`v3-grid-rendering:2.2`、`v3-tool-input:2.4`、`v3-ui:1.4` |
| 操作权限 | `SaveManager` 已只公开 `StartSave/StartSaveAs/StartLoad/StartDeleteSlot/StartAutosave` token 入口，并发布结构化 phase/result；coordinator 实现进程内 gate、手动优先、pending autosave 合并、取消与退出收敛。Save As 在 Prepare、尚未进入真实 `SaveCaptured*()` 时可取消为未提交结果，重复激活保持唯一 token；越过 canonical publication 后同 token 取消返回 `false` 且必须以已提交结果完成 Cleanup。Delete 在 tombstone 前可取消为未提交结果，越过 tombstone 后同 token 取消返回 `false` 且必须以已提交结果完成 Cleanup | `v3-save-system:2.3`、`v3-ui:1.4` |

## 执行顺序

### 阶段 2：第三代道路 payload、容器与操作协议

<a id="v3-save-system2.1"></a>

- [x] **2.1 建立隔离的 V3 format v1 并删除旧格式入口**
  - 完成前问题：过渡期 `schemaVersion = 3` 没有 format family、独立根和 self-loop reader；编辑器/导出根分叉，旧 `ISaveable`/DTO/恢复入口仍可能形成兼容路径。
  - 已实现：生产环境统一使用 `user://saves-v3`，自动化可注入隔离根；manifest 与 RoadGraph payload 都严格写入 `formatFamily: "simple-cities-v3"` 和整数 `schemaVersion: 1`，payload 另要求 `payloadType: "road-network"`。`IStreamingSaveable` 从不可变 `RoadGraphRevision` 写无缩进 UTF-8；reader 只接受 canonical Node/Edge、四类 `roadType`、六类原生 geometry、正长度 self-loop、非覆盖 parallel Edge 和合法 `nextID`，成功 Load 创建新 runtime lineage。旧 `ISaveable`、旧 RoadGraph DTO/reader、Group 字段及兼容恢复入口均已删除。
  - 隔离边界：V3 的 List/Save/Save As/Load/Delete/autosave/恢复/启动清理只能从已验证的 V3 root capability 派生路径；不得枚举、打开、哈希、移动、转换、覆盖或删除 `res://saves`、`user://saves`。V2 槽不出现在 V3 UI；把 V2/未知目录手工复制到 V3 根只得到 `Foreign` 分类。
  - 依赖：`v3-road-graph:8.0`～`8.5`；V2 `save-system:0.4`、`0.5`、`5.3` 只作为历史事实，不是代码或格式依赖。
  - 集成负责人：`v3-save-system`；最终端到端判定由 `v3-road-graph:8.6` 负责。
  - 验证证据（2026-08-14）：V3 persistence 聚焦组、slot/manifest 契约及完整 solution 自动化最终为 637/637；开放 Edge、rooted self-loop、parallel Edge、full-turn、四种 RoadType、六类 geometry、watermark、确定往返和新 lineage 均通过。family/version/schema token、Group/groupID、未知/重复字段、非规范拓扑、内部交叉、损坏引用、长度/hash 不符均在提交前拒绝；V2 根 canary 和源码扫描证明 V3 操作不读取、改写或删除 V2 数据。
  - 运行证据（2026-08-14）：真实 `MapTest` 完成 V3 Save、修改、Load 恢复，Load 后 history 为 undo/redo `0/0`；命名槽、autosave、暂停菜单、曲线和最终组合契约均使用 V3 payload 并清理测试槽。该切片最初只有 Windows Debug QA 包的非 editor V3 根、manifest family/schema/长度/hash 和删除证据；同日后续 `v3-save-system:2.2` 已补齐 Windows Desktop QA 导出包的可写与只读 ACL 契约，原 Release 导出缺口不再开放。
  - 验收结果：只有精确 V3 format v1 能进入 prepared state；合法图逐值保留 incidence、rooted seam、parallel Edge、原生 geometry、RoadType 和 watermark，非法输入不改变活动图。生产运行只写独立 V3 根，V2/未知目录复制到该根只得到 `Foreign`，不触发迁移或兼容加载。

<a id="v3-save-system2.2"></a>

- [x] **2.2 为长连续 Edge 建立确定、有界且可恢复的存储管线**
  - 已实现：format v1 继续使用 geometry 内联 Edge 的无 BOM、无缩进 canonical UTF-8 JSON，不引入拓扑 chunk。writer 从不可变 `RoadGraphRevision` 直接写 operation-specific staging；duplicate-aware `V3JsonStreamReader` 从同一个受保护句柄执行属性原始字节、number lexeme、initial/declared/consumed length、EOF 与 SHA-256 校验，并在集合分配和图提交前执行 manifest、payload、整槽、token、深度、字符串、实体、geometry、坐标、长度、ID 与索引容量预算。thumbnail 作为独立 PNG 展示资产校验 signature、chunk/CRC、尺寸、解码扫描线和资源预算，缺失或损坏只产生占位 warning。
  - 发布与恢复：`SaveSlotStore` 在整个同步存储操作期间持有跨进程 `.save-root.lock`，并为每个请求使用 `.save-transactions/<slot>/<operation-id>/`。完整 staging 复核后先持久发布不可变 `publish.json`，绑定 slot、新旧 aggregate digest、固定 staging/backup 路径和 operation token，再允许 canonical move。五类 occupant、无 descriptor quarantine、new/old digest 恢复矩阵、typed `SavePublicationRecoveryException` 阻塞、`PublishedWithCleanupPending` 和旧槽回退均已实现；没有按时间戳猜测赢家。进程内 async gate、publish lease 和公开 operation state 仍严格属于 `v3-save-system:2.3`。
  - 删除：删除授权绑定明确目标、UI generation、operation token、occupant kind/digest 和确认摘要；`delete.json` 在 `slot -> tombstone` 前发布。越界后槽在逻辑上已删除，清理失败返回 `DeletedWithCleanupPending` 且恢复继续删除；canonical/tombstone 歧义通过 typed `SaveDeletionRecoveryException` 保留现场。`Foreign` / `Unsafe` 不能取得删除授权，`CorruptV3` 只能经当前列表 generation 的明确确认删除。
  - 依赖：`v3-road-graph:8.0`、`v3-road-graph:8.5`、`v3-save-system:2.1`；预算使用 Phase 0 的 junction-dense/geometry-dense 数据，V2 性能记录只作同机比较。
  - 集成负责人：`v3-save-system`；编辑历史内存属于 `v3-tool-input:2.3`，最终完成判定由 `v3-road-graph:8.6` 负责。
  - 验证证据（2026-08-14）：保存/manifest/PNG/persistence/export 聚焦组 118/118，`SaveManagerSlotContractTests` 45/45，完整 solution 698/698；Debug 与 `ExportRelease` build 均为 0 警告、0 错误。故障矩阵覆盖首次/覆盖 publish 的未越界、旧槽已移至 backup、canonical 已发布、cleanup pending 与 digest 歧义，delete descriptor 前后、tombstone cleanup 与后来替换槽冲突，无 descriptor staging、部分写入后 `ERROR_DISK_FULL`/ENOSPC、`.save-transactions` 文件占位、同进程/独立进程根锁、Windows 只读 ACL 和 V2 根逐字节/时间戳 canary。Windows QA 导出包在显示驱动下通过可写与只读 profile 契约，包内测试资源只保留 exported-save 契约及其 fixture。
  - 验收结果：合法长 Edge 保持一个 Edge ID 并在预算内往返，同向连续 line 只写一个 primitive；超限输入在大额业务分配和图提交前拒绝；恢复只处理 descriptor 明确拥有且 digest 可证明的路径，歧义现场完整保留；已复核发布或已越界删除不因 cleanup 失败被误报或反向恢复。当前只声明进程失败原子且 crash recoverable，不声明目录元数据 sudden-power-loss durable，事务 backup 也不冒充长期备份。

<a id="v3-save-system2.3"></a>

- [ ] **2.3 建立非阻塞、排他的保存、加载与删除协议**
  - 完成前问题：`SaveManager.Save/Load/SaveAutosave/ConfirmDeleteSlot` 和 Timer autosave 是同步 bool API；长图的 JSON/hash/I/O 及 prepared graph 构建发生在调用线程，且场景/应用退出没有收敛边界。底层 operation-specific 事务和跨进程 OS 根锁已经由 `2.2` 完成，但当时没有进程内 async coordinator、scene/participant generation、取消边界、publish lease、公开结构化 operation state 或 aggregate commit。
  - 修改：以新接口实现保存根级 `SaveOperationCoordinator`、不可变 operation state/result、进程内 async gate 和跨进程 lock；不保留同步 bool API 适配器。Save admission 在主线程 O(1) 捕获 immutable root 与 generation，后台 serialize/hash/I/O，再由 `PublishV3` token 取得一次性 publish lease。Load 和 Delete 各有独立 token、取消点与结果；Timer busy 时至多合并一个 pending autosave，手动操作优先；场景退出停止 admission、取消未越界 worker并等待已越界事务收敛。
  - Load 协议：每次 Load 固定经过 Admission、Prepare、Preflight、Non-yield commit/notification。Admission 冻结新道路命令但逐值保留 graph、草稿、选择、hover、overlay、历史和 `CurrentSlotID`；Prepare 从受保护句柄构造完整 immutable aggregate，预建 graph root、empty tool root、纯 CLR tessellation、surface snapshot 与 hit-index 数据；Preflight 重新验证全部 generation/capacity/token 并创建隐藏 Mesh/RID 和不可抛 commit plan。任何关键失败都发生在 Preflight，活动状态逐值不变。
  - Commit 边界：不可 yield 临界区只交换已验证的 graph root/lineage、empty tool/overlay root、hidden Mesh/RID、surface snapshot、hit index、presentation token、diagnostics 和 `CurrentSlotID`，随后发布一次 matching full reset 与 `PresentationReady`。普通 observer 与 post-commit cleanup 异常逐个隔离，结果提升为 `SucceededWithObserverWarnings`；不存在提交后关键 participant 失败或恢复页。Publish、Load、Delete 的 token、取消边界、结果和 UI 阶段不得合并。
  - 依赖：`v3-save-system:2.2`、`v3-road-graph:8.5`；工具 full reset 属于 `v3-tool-input:2.4`，隐藏资源与 surface 一次交换属于 `v3-grid-rendering:2.2`，busy/error/result 呈现属于 `v3-ui:1.4`。
  - 集成负责人：`v3-save-system`；真实场景端到端完成判定由 `v3-road-graph:8.6` 负责。
  - 验证：O(1) capture、后台 Godot Object 零访问、save I/O 中继续编辑；进程内/双进程 gate/lock、Timer 合并、手动优先和退出收敛；Publish/Load/Delete 分别产生唯一 token/result；fake graph/tool/presentation/第二 saveable aggregate、generation 失配、每个关键 Preflight 失败、commit 不抛、普通 observer 抛错、删除越界前后取消和 `CurrentSlotID` 结果。联合测试由协作系统覆盖真实隐藏资源及一次交换。
  - 验收：同一 V3 根跨进程最多一个目录事务；主线程不执行长 JSON/hash/I/O；Prepare/Preflight 失败逐值保留活动状态，non-yield commit 全有或全无且只通知一次；Load 只有成功、observer warning 或提交前失败/取消；autosave busy 有界；任何操作都不调用 V2 API 或触碰 V2 根。
  - 阶段进展（2026-08-14）：`SaveOperationCoordinator` 已实现进程内根 gate、结构化 token/state/result、手动请求优先、单 pending autosave、取消点和 shutdown；公开同步 bool 入口已删除。Save/Load/Delete 的长磁盘与 prepared 工作在 `Task.Run` 中执行，主线程 capture 只取得 immutable root。`SaveManager` 用 scene generation 跟踪任务，返回主菜单先 drain，窗口/菜单/主菜单退出统一等待未越界取消或已越界事务收敛；新请求在关闭期得到 typed rejection。
  - 等待取消修复（2026-08-14）：手动请求从 `_rootGate.WaitAsync()` 返回后、创建 `SaveOperationLease` 前，会在 coordinator 锁内重新检查外部 cancellation token；若取消与 gate 释放竞争，则立即释放已取得的 gate 并返回 Admission 阶段 `Canceled`。被取消的 waiter 不再成为无人终结的活动 lease，也不会让 scene drain 或 coordinator dispose 无限等待；见 `save-system:BUG-13`。
  - Aggregate 进展（2026-08-15）：Load 已按 Admission/Prepare/Preflight/Commit 运行，`PreparedAggregateLoad` 在同一 commit lease 中交换 RoadGraph 新 lineage、RoadBuilder 空 placement/removal/upgrade 与新 history、RoadRenderer 预建的基础 `ArrayMesh`/`MultiMesh`、含 `EdgeRibbon` + `TerminalCap` + `SemanticJoin` + `JunctionPatch` 的 `RoadSurfaceSnapshot`、matching desired/presented `RoadRenderToken`，以及 `CurrentSlotID`；明确的 `CurrentTool` 与 `SelectedRoadType` 保留。`SaveManager` 在 scene registration 时把 `SceneGeneration` 注入 renderer。render request/facade generation 在 admission 预留，generation 失配会在 commit 前拒绝；observer 逐个隔离为 warning。`RoadRendererLoadPreparer.Prepare()` 在 worker 中完成 ribbon/join/patch triangle、terminal disc、Junction Patch 的固定量化 Clipper2 细分、defensive copy 与统一 AABB 层级；Preflight 只用 reserved token 绑定 prepared 数据，不重复细分、复制或建树。
  - 仍缺（保持开放）：当前 renderer participant 已覆盖分级 open/closed ribbon、degree-1 terminal cap、degree-2 semantic join、degree≥3 junction patch、节点批次、带 canonical `RoadLocation` 和基础空间索引的同代 surface 与 matching 六分量 token；placement、拆除与 RoadUpgrade 会话及 undo/redo 均已消费并校验 provider token，full reset 保留 `CurrentTool` 与 `SelectedRoadType`。普通 mutation 的 deferred 表现 continuation 会由 generation 阻止旧 callable 跨 commit 消费新请求；未转交的 mesh/node batch 也会在普通失败、Load Preflight 异常或 plan 放弃时确定性释放，renderer plan 或 slot plan 已收集但 aggregate 尚未接管时由 orchestration fallback 释放已有 plans，aggregate 接管后、commit 前则由自身 `Dispose()` 释放 owned plans。aggregate 已锁定 fake participant observer/cleanup warning 聚合、真实 `RoadGraph` observer/fake companion cleanup、真实 renderer observer warning 后 graph/tool/presentation/slot cleanup 与第二次 Load 再接管、真实 renderer observer 的四个逐参与者 cleanup 联合故障与全 cleanup 联合故障，以及真实 RoadGraph observer 与全 cleanup 联合故障；真实 RoadGraph/renderer/tool/slot-target plans 的直接 commit-boundary generation 原子拒绝，以及真实四参与者 `StartLoad()` 对同四种 participant 的 operation 级失配、真实 `RoadRenderer` admission 与 ToolManager scene-generation/cancellation 失配均有证据。四个已定位 Resource 所有权点、普通更新 token、真实 aggregate Load 的 renderer Resource Preflight 故障、renderer plan 后续与 slot plan 后续 aggregate Preflight fallback disposal，以及 aggregate ownership 后的 pre-commit disposal 均已用真实 Godot Resource 覆盖。还需协同 `v3-grid-rendering:2.2`、`v3-tool-input:2.4` 完成其余 renderer 故障、其他真实 aggregate 组合与完整故障注入矩阵；若未来引入第二个正式业务 payload，还需为该真实参与者补齐相同矩阵，不能把当前 fake aggregate 或这些切片视为 `2.3` 完成。
  - 当前证据（2026-08-15）：完整 `dotnet test SimpleCities.sln` 为 833/833，RoadUpgrade 聚焦组合为 17/17，类型化建造聚焦组合为 60/60，拆除/history/surface/renderer 聚焦组合为 51/51；Debug 与 `ExportRelease` build 均为 0 错误，各有 1 条既有 `NU1900`（当前环境无法访问 NuGet 漏洞数据源），Roslyn compiler/analyzer 与 GDScript workspace scan 均为 0 diagnostics。`RoadJunctionTessellatorTests`、`RoadRendererLoadPrepareTests` 和生命周期契约证明 worker prepared Junction Patch 可与 ribbon/cap/join 一起绑定 matching token；既有 coordinator、pause menu、renderer lifecycle、closed ribbon 与 V3 综合运行时契约继续覆盖提交前失败和 aggregate。隔离的 `road_render_token_runtime_contract.gd` 与 `road_input_strategy_runtime_contract.gd` 均 PASS，后者验证四类建造、RoadUpgrade 目标冻结/选择/NoChanges/token 门禁，以及 active upgrade 上的 full-reset Load：`CurrentTool = RoadUpgrade`、`SelectedRoadType = Arterial`，session/preview/history 全空且 matching token ready。既有 current/旧 surface 的拆除与改造门禁及 pending 一次发布证据继续有效；它们不改变 Load 必须在 Preflight 失败、且没有提交后表现重试的边界。测试槽、动态探针、临时用户目录和日志均已清理，editor 与 DAP 错误通道为空；本轮未重跑 10k/100k。
  - Placement 协作证据（2026-08-20）：placement 在开始时冻结 matching presented token，所有后续草稿/确认入口复核 provider 与 graph 同代状态；Load admission 期间拒绝命令但逐值保留旧会话，成功 full-reset commit 才清空 session、preview 与冻结 token。完整自动化 840/840、双配置 build 0 警告/0 错误，两项隔离道路运行时契约均 PASS。当前会话未暴露 Roslyn/Godot MCP 与 DAP，未刷新对应通道；该证据只完成工具协作切片，不替代第二 saveable 或关键资源故障矩阵。
  - 历史回放协作证据（2026-08-20）：undo/redo 的可用性和执行入口现在同样要求 Load admission 为空且 provider/graph 同代；pending、stalled 以及回放自身产生的反向 pending 窗口均拒绝命令而不清栈或修改图。`RoadInputStrategyTests` 21/21、完整自动化 841/841、双配置 build 0 警告/0 错误，两项隔离道路运行时契约均 PASS。当前会话未暴露 Roslyn/Godot MCP 与 DAP，未刷新对应通道；该证据不替代第二 saveable 或关键资源故障矩阵。
  - Deferred continuation 协作证据（2026-08-20）：renderer deferred 普通重建现捕获独立 generation，Load admission 在冻结参与者前同步 flush current 工作，aggregate commit 再推进 generation；遗留 callable 不能覆盖新 lineage 或消费后续请求。renderer/sampler 聚焦组合 16/16、完整自动化 842/842、双配置 build 0 警告/0 错误；隔离 token 契约以 pending mutation 后同栈 `StartLoad` 验证 current presentation、最终 matching token 与额外两帧稳定性，输入契约继续 PASS。隔离目录已清理，原有 Godot 进程未受影响；当前会话未暴露 Roslyn/Godot MCP 与 DAP。
  - Aggregate cleanup 隔离证据（2026-08-20）：`PreparedAggregateLoadTests` 8/8 验证某一参与者的 `CompleteCommit()` 抛错后，已交换的所有引用保持新值，错误进入带 participant ID 的 warning，后续参与者仍执行 cleanup。完整自动化 843/843，Debug 与 `ExportRelease` build 均为 0 警告、0 错误；当前会话未暴露 Roslyn/Godot MCP，focused semantic diagnostics 未记为本轮通过。该证据只锁定逐参与者 cleanup 隔离，不替代真实参与者 generation 失配或 observer/cleanup 联合故障矩阵。
  - 连续 Load 协作证据（2026-08-20）：隔离 `APPDATA` 的 `road_render_token_runtime_contract.gd` 在同一真实 `MapTest` 和同一槽上连续完成两次 `StartLoad()`；两次 operation token 不同，第二次的 graph facade generation、change sequence 与 render request ID 均在第一次基础上精确加一，`EdgeRibbon`、`TerminalCap`、`SemanticJoin` 与 `JunctionPatch` 命中全部携带第二次 matching presented token。完整自动化保持 843/843，Debug 与 `ExportRelease` build 均为 0 警告、0 错误；隔离目录和日志已清理，原有 Godot PID 未受影响。当前会话未暴露 Roslyn/Godot MCP 与 DAP，未把相应诊断或 console 门禁记为通过。
  - 隐藏表现资源生命周期证据（2026-08-20）：普通 rebuild 在 mesh/node batch 未转交时由 `finally` 释放；`CreateRoadMesh()` / `CreateNodeBatch()` 在构造异常时自清理；Load Preflight 在 plan 接管前失败会释放已建资源，未 commit plan 的幂等 `Dispose()` 同时释放资源与 admission，成功 commit 后则保留已挂载资源。`RoadRendererLifecycleContractTests` 10/10、完整自动化 846/846、双配置 build 0 警告/0 错误；两个隔离道路运行时契约均 PASS。见 `save-system:BUG-14`。当前会话未暴露 Roslyn/Godot MCP 与 DAP；生命周期脚本主动移除 renderer 后的既有 disposed-object 输出未记作干净 console 门禁。本切片不替代每个关键资源故障点与 generation 失配的真实注入矩阵。
  - 真实 generation 失配协作证据（2026-08-20）：受控 lease 在 `PreparedAggregateLoad` 进入 commit 时使 admission 失效，真实 `RoadGraph` plan 返回 `LoadPreflightInvalidException`；graph revision、测试中的 presentation/slot 状态与所有 reference swap 保持不变，未提交的辅助计划各清理一次，admission 释放后 graph 可继续 mutation。`PreparedAggregateLoadTests` 9/9，完整 `dotnet test SimpleCities.sln --no-restore` 为 847/847；Debug 与 `ExportRelease` build 均为 0 警告、0 错误。xUnit 宿主无法初始化 Godot native Resource，本证据不扩展原生 `ArrayMesh`/`MultiMesh` 生命周期覆盖；renderer/tool/slot 的真实 generation 失配、逐关键 Resource 故障和 observer/cleanup 联合组合仍保持开放。
  - 全参与者 generation 失配补强（2026-08-21）：`AllParticipantGenerationMismatch_RejectsBeforeAnyReferenceSwap` 让 graph、tool、presentation、slot 四个 fake participant 中的 slot generation 失效，aggregate 在引用交换和通知前拒绝，四方旧状态及事件序列均不变。`PreparedAggregateLoadTests` 聚焦 12/12，完整 `dotnet test SimpleCities.sln --no-restore` 为 864/864；这是 CLR fake aggregate 的控制流证据，真实 renderer/tool/slot generation、Godot Resource 故障和 observer/cleanup 联合矩阵仍开放。
  - 提交边界逐参与者失配补强（2026-08-21）：`CommitBoundaryGenerationMismatchForAnyParticipant_RejectsBeforeAnyReferenceSwap` 依次让 graph、tool、presentation、slot 任一 fake participant 在 `CrossCommitBoundary` 内失效；每次均在任何 `CommitReferences()` 或 notification 前抛出 `LoadPreflightInvalidException`，四方状态、事件和引用交换计数保持为空。`PreparedAggregateLoadTests` 聚焦 13/13，完整自动化为 865/865；这是 aggregate 控制流证据，不替代真实 renderer/tool/slot generation、Godot Resource 故障或 observer/cleanup 联合矩阵。
  - 真实 Godot admission 失配证据（2026-08-21）：新增 `road_load_generation_runtime_contract.gd`，以 10k 条互不相交 Edge 延长 worker Prepare，并通过 `SaveOperationStateChanged` 在 Prepare 主线程通知内同步注入两类真实失效。移除但不释放 `RoadRenderer` 后，其真实 load admission 在 Preflight 返回 `Failed`，`committed=false`；移除 `ToolManager` 后，`UnregisterSceneParticipants()` 推进 scene generation 并取消同一 Load，结果为 Prepare 阶段 `Canceled`。两条路径均只注入一次，活动 RoadGraph payload、`CurrentSlotID`、renderer desired/presented token、surface/mesh/marker、`CurrentTool`、placement 和 undo/redo 保持旧值；节点重新入树后可再次保存并逐字节确认旧 graph。隔离 APPDATA 的契约连续两次输出 PASS，GDScript diagnostics 与 `--check-only` 为 0，完整自动化 865/865，Debug/`ExportRelease` build 0 警告/0 错误，Roslyn 测试项目 diagnostics 为 0，Godot editor 无新增 error 且 DAP stderr/console 为空。该切片不直接使 `SlotTargetLoadCommitPlan` 在 commit boundary 失效，也不覆盖真实 observer/cleanup 或逐关键 Resource 故障，2.3 继续开放。
  - 真实 slot-target 提交边界证据（2026-08-21）：`RunLoadAsync()` 构造 `SlotTargetLoadCommitPlan` 时冻结 scene request 与 `_currentSlotGeneration`，并在 aggregate 进入 commit 前复核。`RealSlotTargetGenerationMismatchAtCommitBoundary_RejectsEveryReferenceSwap` 在 `CrossCommitBoundary` 内只推进槽目标代际，验证 graph/tool/presentation/slot 均无 reference swap 或 notification，`CurrentSlotID` 保持旧值。`PreparedAggregateLoadTests` 14/14、完整自动化 866/866、双配置 build 与 Roslyn diagnostics 为 0；隔离 APPDATA 的真实 Load 契约再次 PASS，`MapTest` smoke、editor 与 DAP 错误通道通过。该计划级 CLR 证据不覆盖真实 observer/cleanup 或逐关键 Resource 故障，2.3 继续开放。
   - Observer/cleanup 组合协作证据（2026-08-20）：`ObserverAndCleanupFailures_AreIsolatedAcrossTheWholeAggregate` 用三个 fake participant 同时注入一个 observer 异常和两个 cleanup 异常；三方引用先全部交换，warning 按 observer、participant cleanup 顺序聚合，最后一个 participant 仍完成 cleanup。`PreparedAggregateLoadTests` 10/10，完整自动化 848/848，Debug 与 `ExportRelease` build 均为 0 警告、0 错误。该证据只覆盖 CLR aggregate 控制流；真实 renderer/tool/slot participant 的 observer/cleanup 组合、逐关键 Godot Resource 故障与 generation 失配仍开放，不能视为原生 `ArrayMesh`/`MultiMesh` 生命周期证明。
   - 真实 RoadGraph observer/cleanup 组合协作证据（2026-08-20）：`RealRoadGraphObserverFailure_IsolatedAlongsideOtherParticipantCleanupFailures` 让真实 `RoadGraph` 的 `GraphChanged` observer 抛错，同时让 fake `presentation`/`slot` cleanup 抛错；aggregate 仍先完成三方 reference swap，再按 graph observer、presentation cleanup、slot cleanup 顺序聚合三个 warning，两个 fake companion cleanup 都继续执行，新的 graph root 可查询且唯一 Edge 的 `RoadType` 为 `Highway`。`PreparedAggregateLoadTests` 达到 11/11，完整自动化达到 849/849，Debug build 为 0 警告、0 错误。该切片覆盖真实 RoadGraph 的 CLR observer 路径与 fake companion cleanup，不证明 renderer/tool/slot 的真实 observer/cleanup 组合、generation 失配或逐关键 Godot Resource 故障；xUnit 宿主无法初始化 Godot native Resource，因此不扩展原生 `ArrayMesh`/`MultiMesh` 生命周期证据。
  - Fake second saveable aggregate 协作证据（2026-08-21）：`SecondSaveable_UsesTheSameAggregateCommitAndCleanupBoundary` 将 fake second saveable 与 graph、tool、presentation、slot 一起放入 `PreparedAggregateLoad`，断言五方先完成引用交换，再按参与者顺序发布 notification，最后逐一完成 cleanup；没有任何 warning 或提前提交。`PreparedAggregateLoadTests` 聚焦 15/15，完整 `dotnet test SimpleCities.sln --no-restore` 为 867/867，Debug build 为 0 警告、0 错误。该证据只固化第二参与者的 aggregate 协议，不引入第二个正式业务 payload，也不替代真实 renderer/tool/slot observer/cleanup 或逐关键 Godot Resource 故障矩阵。
  - Load 分段性能可观测性（2026-08-21）：`PreparedAggregateLoad.ReferenceCommitDuration` 只包围 commit boundary 内的 generation 复核与全部 `CommitReferences()`；`SaveManager.GetLastLoadPerformanceMetrics()` 只在成功 Load 后发布带 `operationToken`/`targetSlotID` 的 worker Prepare、主线程 Preflight、reference commit、aggregate commit 和端到端不可变快照。性能契约改为直接等待 `StartLoad()`，拒绝旧 token/slot、负值及不满足 `referenceCommitMs <= aggregateCommitMs <= totalMs` 的快照。隔离 APPDATA 的真实 Vulkan grid 1k/10k/100k 均输出 `PASS`；分段分别为 `113.216/9.839/0.713/1.949/126.841 ms`、`622.155/24.648/1.769/2.997/651.217 ms`、`4770.893/151.645/4.168/5.621/4929.850 ms`，外部观测 Load 为 `132.327/657.366/4938.626 ms`。聚焦 `PreparedAggregateLoadTests` 16/16、完整自动化 868/868、双配置 build 与 Roslyn compiler/GDScript 单文件 diagnostics 为 0；编辑器内 `MapTest`、editor 增量 error 与 DAP stderr/console 通过，唯一 Vulkan warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。该切片建立观测口径但不固定新门槛，也不替代真实 observer/cleanup 与逐关键 Resource 故障矩阵；analyzer 未获信任授权，未运行。
  - 普通表现与 Load 分段边界复核（2026-08-21）：普通 `GraphChanged` 现在由 `RoadRenderer` 以 matching `RoadRenderToken` 独立记录 snapshot、Prepare、Resource Preflight、presentation commit、rebuild total 和 request-to-ready；aggregate Load 仍只走上述 `PreparedAggregateLoad`/`SaveManager` 专用口径，同一次 Load full reset 因 graph token 消重不会被误计为普通 rebuild。隔离 APPDATA 的 Vulkan grid 1k/10k/100k 均 PASS，10k/100k 各九次 upgrade/undo/redo 指标都通过 token、非 full-reset 和时间包含关系；完整自动化 869/869、双配置 build 与 compiler/GDScript diagnostics 为 0。该证据只澄清跨系统性能所有权，不替代 2.3 仍开放的真实 renderer/tool/slot observer/cleanup 与逐关键 Resource 故障矩阵。
  - 真实 aggregate Load renderer Resource Preflight 故障证据（2026-08-22）：Debug-only `RoadLoadPreflightResourceFailureProbe` 在真实 `StartLoad()` 的 renderer Preflight 中，于 `CreateRoadMesh()` / `CreateNodeBatch()` 完成后、render token/surface/plan 创建前注入一次性异常。operation 返回 `Failed`、`committed=false` 与精确错误，活动 graph/tool/presentation/surface/token/hit/slot 逐值不变，未转交资源释放后 `ObjectResourceCount` 为 `77 -> 77`；随后同一源槽 Load 返回 `Succeeded` 并联合接管空 graph/tool/presentation/slot，证明所有 admission 与未提交计划均已释放。结构化结果为 `aggregate_failure_result_kind=2`、`aggregate_failure_committed=false`、`aggregate_failure_trigger_count=1`、`aggregate_resource_count_before=77`、`aggregate_resource_count_after=77`、后续 `load_result_kind=0`。生命周期源码契约固定 hook 顺序；完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、双配置 build、Roslyn compiler/analyzer 与目标 GDScript diagnostics 为 0，probe 只进入 Debug 且被 QA export 排除。Vulkan Forward+ 契约 PASS，编辑器冻结 smoke ready/matching/2 static nodes，DAP stderr/console 为空，editor 游标 721 后无新增 error；唯一 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。该证据只关闭 renderer Resource Preflight 这一失败 aggregate 子场景，其余 renderer 故障、其他 aggregate 组合与完整矩阵仍开放，2.3 不关闭。
  - renderer plan 后续 aggregate Preflight fallback disposal 证据（2026-08-22）：同一 Debug-only probe 通过 `SaveManager` partial hook 在真实 `StartLoad()` 已创建 renderer commit plan 并加入 `preflightPlans` 后、slot-target plan 创建与 `PreparedAggregateLoad` ownership 之前注入一次性异常。operation 返回 `Failed`、`committed=false` 与精确错误；因为 aggregate 尚未接管，`RunLoadAsync()` 的 `finally` fallback 逐个 `Dispose()` 已有 graph/tool/renderer plan，释放隐藏资源与 admission。活动 graph/tool/presentation/surface/token/hit/slot 逐值不变，结构化结果为 `post_renderer_failure_result_kind=2`、`post_renderer_failure_committed=false`、`post_renderer_failure_trigger_count=1`、`post_renderer_resource_count_before=77`、`post_renderer_resource_count_after=77`；随后同槽 Save 逐字节确认旧图，最终来源槽 Load 返回 `Succeeded`。完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、Debug/`ExportRelease` build 0 警告/0 错误、Roslyn production/test compiler/analyzer 与目标 GDScript diagnostics 为 0；`ExportRelease` 无 hook 实现、probe 类型或注入消息，QA export 继续排除 probe 源码。Vulkan Forward+ 契约 PASS，Godot 4.7 编辑器游标 721 后无新增 error，DAP stderr/console 为空；唯一 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。该证据只关闭 renderer plan 加入后的 fallback disposal 子场景，其余 renderer 故障、其他 aggregate 组合与完整矩阵仍开放，2.3 不关闭。
  - slot plan 后续 aggregate Preflight fallback disposal 证据（2026-08-22）：同一 Debug-only probe 的第二个 `SaveManager` partial hook 在真实 `StartLoad()` 已创建 `SlotTargetLoadCommitPlan` 并加入 `preflightPlans` 后、`PreparedAggregateLoad` ownership 之前注入一次性异常。operation 返回 `Failed`、`committed=false` 与精确错误；因为 aggregate 尚未接管，`RunLoadAsync()` 的 `finally` fallback 逐个 `Dispose()` 已有 graph/tool/renderer/slot plan，释放隐藏资源与 admission。活动 graph/tool/presentation/surface/token/hit/slot 逐值不变，结构化结果为 `post_slot_failure_result_kind=2`、`post_slot_failure_committed=false`、`post_slot_failure_trigger_count=1`、`post_slot_resource_count_before=77`、`post_slot_resource_count_after=77`、最终 `load_result_kind=0`；随后同槽 Save 逐字节确认旧图，最终来源槽 Load 返回 `Succeeded`。完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、Debug/`ExportRelease` build 0 警告/0 错误、Roslyn production/test compiler/analyzer 与目标 GDScript diagnostics 为 0；`ExportRelease` 不含 hook 实现、probe 类型或注入消息，QA export 继续排除 probe 源码。Vulkan Forward+ 契约以退出码 0 输出 PASS，Godot 4.7 编辑器游标 721 后无新增 error，DAP stderr/console 为空；唯一 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。该证据只关闭 slot plan 加入后的 fallback disposal 子场景，其余 renderer 故障、其他 aggregate 组合与完整矩阵仍开放，2.3 不关闭。
  - aggregate ownership 后的 pre-commit disposal 证据（2026-08-22）：同一 Debug-only probe 的第三个 `SaveManager` partial hook 在真实 `StartLoad()` 的 `PreparedAggregateLoad` 已取得 graph/tool/renderer/slot plans 所有权后、`Commit()` 前注入一次性异常。operation 返回 `Failed`、`committed=false` 与精确错误；离开 `using` scope 时 aggregate 自身逐个 `Dispose()` owned plans，`aggregateOwnsPlans=true` 阻止 `RunLoadAsync()` fallback 重复清理。活动 graph/tool/presentation/surface/token/hit/slot 逐值不变，结构化结果为 `post_ownership_failure_result_kind=2`、`post_ownership_failure_committed=false`、`post_ownership_failure_trigger_count=1`、`post_ownership_resource_count_before=77`、`post_ownership_resource_count_after=77`、最终 `load_result_kind=0`；随后同槽 Save 逐字节确认旧图，最终来源槽 Load 返回 `Succeeded`。完整自动化 871/871、生命周期与 QA export 聚焦组合 14/14、Debug/`ExportRelease` build 0 警告/0 错误、Roslyn production/test compiler/analyzer 与目标 GDScript diagnostics 为 0；`ExportRelease` 不编译 probe 源码，release DLL 不含 probe 类型或注入消息。真实 Vulkan 1.4 Forward+ 契约以退出码 0 输出 PASS，Godot 4.7 编辑器 smoke 为 ready/matching，游标 721 后无新增 error，DAP stderr/console 为空；唯一 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。该证据只关闭 aggregate ownership 后、commit 前的 disposal 子场景，其余 renderer 故障、其他 aggregate 组合与完整矩阵仍开放，2.3 不关闭。
  - 真实 renderer commit-boundary generation 失配证据（2026-08-22）：同一 Debug-only probe 以真实 renderer admission/preparer/Preflight 构造 `RoadRendererLoadCommitPlan`，并与 graph/tool/slot 三个 `TrackingLoadCommitPlan` 组成 `PreparedAggregateLoad`。`BoundaryInvalidatingLease` 在 `CrossCommitBoundary` 内释放 renderer admission，aggregate 的第二次 generation 复核抛出 `LoadPreflightInvalidException`；四方引用交换和 `MarkCommitted()` 均为 0，aggregate disposal 释放 renderer 隐藏资源，三个 companion plans 各清理一次。旧 presentation 引用及 token 保持，`ObjectResourceCount` 为 `77 -> 77`，renderer admission 可重新取得。结构化结果为 `renderer_boundary_exception_type=LoadPreflightInvalidException`、`renderer_boundary_count=1`、`renderer_boundary_mark_committed_count=0`。`RoadRendererLifecycleContractTests` 14/14、生命周期与 QA export 聚焦 15/15、完整自动化 872/872，Debug/`ExportRelease` build、Roslyn production/test compiler/analyzer 与目标 GDScript diagnostics 为 0；Vulkan 1.4 Forward+ 契约 PASS，editor/DAP 错误通道为空。release DLL 不含 probe 类型、方法、lease 或注入 token。该证据只覆盖真实 renderer plan 与 `TrackingLoadCommitPlan` companions 的直接 aggregate，不是完整真实 `StartLoad()` 四参与者 operation；其余 renderer 故障、其他真实 aggregate 组合与完整矩阵仍开放，2.3 不关闭。
  - 真实 ToolManager commit-boundary generation 失配证据（2026-08-22）：同一 Debug-only probe 以真实 tool admission 与 `PreflightFullReset()` 构造包含 `RoadBuilderLoadCommitPlan` 的 `ToolLoadCommitPlan`，并与 graph/presentation/slot 三个 `ToolTrackingLoadCommitPlan` 组成 `PreparedAggregateLoad`。`ToolBoundaryInvalidatingLease` 在 `CrossCommitBoundary` 内释放 tool 与 builder admissions，aggregate 的第二次 generation 复核抛出 `LoadPreflightInvalidException`；四方引用交换和 `MarkCommitted()` 均为 0，三个 companion plans 各清理一次。原 `CurrentTool`、`SelectedRoadType`、带一个固定拐点的 placement/draft 与 undo/redo 保持，tool admission 与独立 builder admission 随后均可重新取得。结构化结果为 `tool_boundary_exception_type=LoadPreflightInvalidException`、`tool_boundary_count=1`、`tool_boundary_mark_committed_count=0`，两个 admission reacquired 均为 `true`。`RoadRendererLifecycleContractTests` 15/15、生命周期与 QA export 聚焦 16/16、完整自动化 873/873，Debug/`ExportRelease` build、Roslyn production/test compiler/analyzer 与目标 GDScript diagnostics 为 0；Vulkan 1.4 Forward+ 契约 PASS，editor/DAP 错误通道为空。release DLL 不含 probe 类型、方法、lease、`ToolTrackingLoadCommitPlan` 或注入 token。该证据只覆盖真实 tool plan 与 `ToolTrackingLoadCommitPlan` companions 的直接 aggregate，不是完整真实 `StartLoad()` 四参与者 operation；其余 renderer 故障、其他真实 aggregate 组合与完整矩阵仍开放，2.3 不关闭。
  - 真实 renderer observer 与 ToolManager cleanup 联合故障证据（2026-08-22）：现有 `RoadLoadObserverFailureProbe` 增加一次性 ToolManager cleanup probe，生产 partial 调用位于真实 builder/tool admission 释放和 `_completed=true` 之后；同一次真实四参与者 `StartLoad()` 中，`RoadRenderer.PresentationReady` observer 与 tool cleanup 各抛一次。operation 返回 `SucceededWithWarnings`、`committed=true`，两条 warning 分别标识 road presentation observer 与 `road-tools` cleanup；graph/tool/presentation/slot 已联合接管，后续 renderer/slot cleanup 继续，第二次 Load 无 warning 成功并恢复 1 Edge matching presentation。结构化结果为 `warning_result_kind=1`、`clean_result_kind=0`、`observer_trigger_count=1`、`tool_cleanup_trigger_count=1`、`rendered_edges=1`、`current_tool=2`。`RoadRendererLifecycleContractTests` 16/16、生命周期与 QA export 聚焦 17/17、串行完整自动化 874/874，双配置 build、Roslyn production/test compiler/analyzer 与目标 GDScript diagnostics 均为 0；Vulkan 1.4 Forward+ 契约 PASS，editor/DAP 增量错误通道为空，唯一 CLI warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。与 `ExportRelease` 并行的首次完整套件因既有 worker/caller thread ID 断言同为 14 而为 873/874；该单测隔离重跑及随后串行完整套件均通过。release DLL 不含 probe 类型、arming API、状态字段或注入消息；partial hook 声明名不作为排除断言。该切片关闭这一真实 observer/cleanup 联合子场景，但其余 renderer 故障、其他真实 aggregate 组合与完整矩阵仍开放，2.3 不关闭。
  - 真实 renderer observer 与 renderer cleanup 联合故障证据（2026-08-22）：同一 `road_load_observer_cleanup_runtime_contract.gd` 在前一联合场景完成无 warning 再接管后，重新布置 `RoadRenderer.PresentationReady` observer 与一次性 renderer cleanup probe；production partial 调用位于 renderer admission 释放、`_completed=true` 和 `QueueRedraw()` 之后。第二个真实四参与者 `StartLoad()` 仍返回 `SucceededWithWarnings`、`committed=true`，两条 warning 分别标识 road presentation observer 与 `road-presentation` cleanup；graph/tool/presentation/slot 引用均保持新值，随后无 warning Load 恢复 1 Edge matching presentation。两轮结构化结果为 `warning_result_kind=1`、`clean_result_kind=0`、`renderer_warning_result_kind=1`、`renderer_clean_result_kind=0`、`observer_trigger_count=2`、`tool_cleanup_trigger_count=1`、`renderer_cleanup_trigger_count=1`、`rendered_edges=1`、`current_tool=2`。`RoadRendererLifecycleContractTests` 17/17、生命周期与 QA export 聚焦 18/18、串行完整自动化 875/875，双配置 build 为 0 警告/0 错误，Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Vulkan 1.4 Forward+ 契约 PASS，Godot MCP `MapTest` smoke 推进 8 帧，editor 游标 721 后无新增 error/warning，DAP stderr/console 为空。唯一 CLI warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。release DLL 不含 probe 类型、arming API、状态字段或注入消息，partial hook 声明名不作为排除断言。该切片只关闭这一真实 observer/renderer cleanup 联合子场景，其余 renderer 故障、其他真实 aggregate 组合与完整矩阵仍开放，2.3 不关闭。
  - 真实 renderer observer 与 RoadGraph cleanup 联合故障证据（2026-08-22）：同一 `road_load_observer_cleanup_runtime_contract.gd` 在前两轮故障及各自无 warning 再接管后，通过接收真实 `RoadSystem` 的 Debug-only probe 武装其 CLR `RoadGraph`。production partial 调用位于 graph admission 释放和 `_completed=true` 之后；第三个真实四参与者 `StartLoad()` 同时触发 `RoadRenderer.PresentationReady` observer 与 graph cleanup 异常，仍返回 `SucceededWithWarnings`、`committed=true`，两条 warning 分别标识 road presentation observer 与 `road-graph` cleanup。graph cleanup 位于首个 cleanup 位置，异常隔离后后续真实参与者继续收尾，随后无 warning Load 恢复 1 Edge matching presentation。三轮结构化结果新增 `graph_warning_result_kind=1`、`graph_clean_result_kind=0`、`graph_cleanup_trigger_count=1`，累计 observer `3` 次，tool/renderer/graph cleanup 各 `1` 次。`RoadRendererLifecycleContractTests` 18/18、生命周期与 QA export 聚焦 19/19、串行完整自动化 876/876，双配置 build、Roslyn compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Vulkan 1.4 Forward+ 契约 PASS，Godot MCP `MapTest` smoke 推进 8 帧，editor 游标 721 后无新增 error/warning，DAP stderr/console 为空。唯一 CLI warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。release DLL 不含 probe 类型关联、arming API、状态字段或注入消息，partial hook 声明名不作为排除断言。该切片只关闭这一真实 observer/graph cleanup 联合子场景，slot cleanup、其余 renderer 故障、其他真实 aggregate 组合与完整矩阵仍开放，2.3 不关闭。
  - 真实 renderer observer 与 slot-target cleanup 联合故障证据（2026-08-22）：同一运行时契约在前三轮故障及各自无 warning 再接管后，为真实 `SlotTargetLoadCommitPlan` 增加只在完成阶段调用的回调；`CommitReferences()` 已调用 `SetCurrentSlot()` 并设置 `_committed=true` 后，Debug-only `SaveManager` partial probe 才抛错。第四个真实四参与者 `StartLoad()` 同时触发 `RoadRenderer.PresentationReady` observer 与 slot-target cleanup 异常，仍返回 `SucceededWithWarnings`、`committed=true`，两条 warning 分别标识 road presentation observer 与 `slot-target` cleanup；四方引用和新的 `CurrentSlotID` 保持，随后无 warning Load 恢复 1 Edge matching presentation。四轮 warning/clean result 均为 `1/0`，累计 observer `4` 次，tool/renderer/graph/slot cleanup 各 `1` 次。`RoadRendererLifecycleContractTests` 19/19、生命周期与 QA export 聚焦 20/20、串行完整自动化 877/877，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Vulkan 1.4 Forward+ 契约与 Godot MCP 8 帧 `MapTest` smoke PASS，editor 游标 721 后无新增 error/warning，DAP stderr/console 为空。唯一 CLI warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。release DLL 不含 Debug probe 类型、arming API、状态字段或注入消息，隔离 QA 目录和日志已清理。该切片关闭这一真实 observer/slot cleanup 联合子场景，但其余 renderer 故障、其他真实 aggregate 组合与完整矩阵仍开放，2.3 不关闭。

  - 真实 renderer observer 与四参与者 cleanup 全故障证据（2026-08-22）：同一运行时契约在四个逐参与者场景及各自无 warning 再接管后，于第五个真实四参与者 `StartLoad()` 前同时武装 `RoadRenderer.PresentationReady` observer 与 RoadGraph、ToolManager、RoadRenderer、slot-target 四个既有 Debug-only cleanup probe。operation 返回 `SucceededWithWarnings`、`committed=true`，warning 字符串恰有五行并逐项标识 road presentation observer 与四个 cleanup participant；graph cleanup 首先抛错后，tool/renderer/slot cleanup 仍继续执行，四方引用和新的 `CurrentSlotID` 保持。随后无 warning Load 恢复 1 Edge matching presentation。结构化结果为 `combined_warning_result_kind=1`、`combined_clean_result_kind=0`、observer `5` 次，tool/renderer/graph/slot cleanup 各 `2` 次。生命周期与 QA export 聚焦组合 20/20、完整自动化 877/877，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约 PASS，CLI stderr 只有既有 `ConstructionDock: ToolManager.Instance is missing` warning。Godot MCP 第二次无报告 `MapTest` smoke 推进 8 帧；editor 新增序号 722 的 `Max client limits reached` 工具桥接错误，minimal DAP 因既有 8-client 上限未接通。测试槽已删除，两个外层隔离 QA 目录因桌面安全策略拒绝递归清理而留在系统 Temp，未进入工作树。该切片只关闭真实四 cleanup 同次失败组合；其余 renderer 故障、其他真实 aggregate 组合与完整矩阵仍开放，2.3 不关闭。

  - 真实 RoadGraph observer 与四参与者 cleanup 全故障证据（2026-08-22）：同一运行时契约在第六个故障场景的真实四参与者 `StartLoad()` 前，为 `road_system.Graph.GraphChanged` 安装一次性 Debug-only observer，并同时武装 RoadGraph、ToolManager、RoadRenderer 与 slot-target 四个 cleanup probe。四方引用完成交换后，RoadGraph observer 与四个 cleanup 各抛一次；operation 仍返回 `SucceededWithWarnings`、`committed=true`，warning 恰有五行并精确标识 `RoadGraph observer` 与四个真实 cleanup participant。observer 自行退订，graph cleanup 失败后其余 cleanup 继续，新的 `CurrentSlotID`、空 placement/history 与 matching presentation 保持；随后无 warning Load 恢复 1 Edge matching presentation。结构化结果为 `graph_observer_warning_result_kind=1`、`graph_observer_clean_result_kind=0`、`graph_observer_trigger_count=1`、renderer observer 累计 `5` 次，tool/renderer/graph/slot cleanup 各累计 `3` 次，最终 `rendered_edges=1`、`current_tool=2`。生命周期与 QA export 聚焦组合 20/20、完整自动化 877/877，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约 PASS。CLI 日志除预期的 RoadGraph observer trace warning 外只有既有 `ConstructionDock` warning；Godot MCP 冻结 `MapTest` smoke 推进 8 帧且 editor 游标 722 后无新增消息，minimal DAP 因既有 8-client 上限返回 `No active debug session`，不能声明 console 通过。`ExportRelease` 不含 probe 类型、graph observer arming/getter 或注入消息；本轮工作区内 QA 目录已清理。该切片关闭真实 RoadGraph observer/全 cleanup 组合，但其余 renderer 故障、其他真实 aggregate 组合与完整矩阵仍开放，2.3 不关闭。

  - 双真实 observer 与四参与者 cleanup 全故障证据（2026-08-22）：第七个真实四参与者 `StartLoad()` 同时武装 `RoadRenderer.PresentationReady`、当前 `RoadGraph.GraphChanged` 与 graph/tool/renderer/slot cleanup。两个 observer 和四个 cleanup 各抛一次，operation 仍返回 `SucceededWithWarnings`、`committed=true`，六条 warning 逐项保留；四方引用、新 `CurrentSlotID`、空工具/history 与 matching presentation 不回滚，后续无 warning Load 恢复 1 Edge。结构化结果为 `dual_observer_warning_result_kind=1`、`dual_observer_clean_result_kind=0`、renderer observer 累计 `6` 次、RoadGraph observer 累计 `2` 次，四类 cleanup 各累计 `4` 次，最终 `rendered_edges=1`、`current_tool=2`。生命周期与 QA export 聚焦组合 20/20、完整自动化 877/877，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区 GDScript diagnostics 均为 0；Vulkan 1.4.341 Forward+ 契约两次 PASS。Godot MCP 冻结 `MapTest` 推进 8 帧，minimal stderr/console 为空；editor 游标 758～767 只新增既有 MCP/DAP 工具桥接消息。Debug DLL 命中四项 probe 标识，`ExportRelease` 均不命中；本轮测试槽与 QA 根已清理。2.3 仍因其余 renderer 故障、其他真实 aggregate 组合与完整矩阵开放。

  - 真实四参与者 `StartLoad()` slot-target commit-boundary generation 失配证据（2026-08-22）：既有 `RealSlotTargetGenerationMismatchAtCommitBoundary_RejectsEveryReferenceSwap` 只以真实 `SlotTargetLoadCommitPlan` 搭配 graph/tool/presentation fake companions；新增 Debug-only `SaveManager` partial 在 graph/renderer/tool operation probes 之后、aggregate ownership 后、`Commit()` 前包装真实 storage lease，并在其 `CrossCommitBoundary()` 回调内以 `NextGeneration()` 推进 `_currentSlotGeneration`。第二次 generation 复核在任何 graph/tool/presentation/slot `CommitReferences()` 前拒绝，operation 返回 `Failed`、`committed=false` 且无 warning，boundary 为 1、storage `MarkCommitted()` 为 0；活动 graph payload、RoadType、placement/history、presentation/surface/token/hit 与 `CurrentSlotID` 四方零交换，同槽 Save 后 payload 逐字节相等，aggregate disposal 释放隐藏 renderer 资源，`ObjectResourceCount` 为 `77 -> 77`，最终无故障 Load 可重新接管。结构化结果为 `slot_target_boundary_operation_failure_result_kind=2 slot_target_boundary_operation_failure_committed=false slot_target_boundary_operation_failure_trigger_count=1 slot_target_boundary_operation_count=1 slot_target_boundary_operation_mark_committed_count=0 slot_target_boundary_operation_resource_count_before=77 slot_target_boundary_operation_resource_count_after=77 load_result_kind=0`。`RoadRendererLifecycleContractTests` 38/38、生命周期与 QA export 聚焦组合 39/39、完整自动化 896/896，双配置 build、Roslyn production/test compiler/analyzer、目标与工作区可见的 3 个 GDScript diagnostics 均为 0，隔离副本的 GDScript `--check-only` 退出码为 0；Godot 4.7 Vulkan 1.4.341 Forward+ 契约 PASS，唯一运行时 warning 为既有 `ConstructionDock: ToolManager.Instance is missing`。Godot MCP 项目/编辑器门、minimal stderr/console 与 editor 游标 1918 后增量错误门均通过；Debug DLL 包含 probe/wrapper/API，`ExportRelease` 均不包含，测试槽与 QA 根已清理。该证据补齐四种 participant 的完整真实 operation 级 commit-boundary generation 失配矩阵，但不替代其余 renderer 故障、其他真实 aggregate 组合与完整故障注入矩阵，因此 2.3 保持开放。

  - 真实 aggregate Load ownership 后、commit 前故障补强证据（2026-08-24）：既有 `ProbeAggregateLoadPostOwnershipPreCommitFailure` 现把真实四参与者 `StartLoad()` 的 ownership transfer 固化为连续第二十一个早期边界。`SaveManager` 已按 `road-graph → road-tools → road-presentation → slot-target` 顺序收集四个 plan，构造 `PreparedAggregateLoad` 并设置 `aggregateOwnsPlans=true`；随后 Debug-only hook 在 preflight duration 收尾、commit 计时、四个 commit-boundary generation probe 与 `aggregate.Commit(...)` 前以 `Injected aggregate Load failure after ownership transfer and before commit.` 精确失败。probe 确认主线程、`Preflight=4`、四 participant 顺序、空 target graph/source presentation 计数和 source slot；operation 返回 `Failed`、`committed=false` 且无 warning。离开 `using` scope 时 aggregate 自身 dispose 四个 owned plans，外层 fallback 因 ownership flag 跳过，从而不重复清理；全部 admission 随后释放，graph/tool/presentation/surface/token/slot 与 `SelectedRoadType=Arterial`、placement/history 保持，payload 逐字节不变且资源 `77 -> 77`，最终无故障 Load 成功再接管。结构化结果新增 `post_ownership_failure_final_phase=4 post_ownership_failure_on_main_thread=true post_ownership_plan_count=4 post_ownership_first_participant_id=road-graph post_ownership_second_participant_id=road-tools post_ownership_third_participant_id=road-presentation post_ownership_fourth_participant_id=slot-target post_ownership_target_edge_count=0 post_ownership_source_participant_count=1 post_ownership_road_vertex_count=0 post_ownership_surface_primitive_count=0 post_ownership_node_marker_count=0`，并保持既有 result/committed/trigger/resource 字段与 `load_result_kind=0`。完整自动化 918/918、生命周期与 QA export 聚焦 61/61、双配置 build、Roslyn/GDScript、`--check-only`、正式 Vulkan 运行时、Debug/ExportRelease 隔离与 MCP smoke 均通过。aggregate 相邻失败总数仍为二十七个，连续早期边界由二十个扩为二十一个；其余 renderer 故障、其他真实 aggregate 组合与最终矩阵仍开放，因此 2.3 不关闭。

  - 真实四参与者 `StartLoad()` RoadGraph `Commit=5` 边界补强证据（2026-08-24）：既有 RoadGraph commit-boundary generation 失配现进一步记录真实 operation 的 phase、四 plan 顺序与 prepared 内容。`PreparedAggregateLoad.Commit()` 调用 `AcquireCommitLease()` 后 operation 进入 `Commit=5`，storage lease 的 `CrossCommitBoundary()` 再释放 RoadGraph admission；第二次 generation 复核在 graph/tool/presentation/slot-target 任一 `CommitReferences()` 前拒绝，storage `MarkCommitted()` 为 `0`。probe 在主线程确认 `road-graph → road-tools → road-presentation → slot-target`，并因本场景加载活动槽而观察到 target `1 Edge`、source participant `1`、prepared `4 road vertices / 4 surface primitives / 2 node markers` 与匹配的 slot ID。operation 返回 `Failed`、`committed=false` 且无 warning，活动 graph/tool/presentation/slot 四方引用、`SelectedRoadType=Arterial`、placement/history、surface/token/hit 均保持，aggregate disposal 释放隐藏表现资源，`ObjectResourceCount` 为 `77 -> 77`，最终无故障 Load 以 `load_result_kind=0` 成功再接管。结构化结果为 `graph_boundary_operation_failure_result_kind=2 graph_boundary_operation_failure_final_phase=5 graph_boundary_operation_failure_committed=false graph_boundary_operation_failure_trigger_count=1 graph_boundary_operation_probe_on_main_thread=true graph_boundary_operation_plan_count=4 graph_boundary_operation_first_participant_id=road-graph graph_boundary_operation_second_participant_id=road-tools graph_boundary_operation_third_participant_id=road-presentation graph_boundary_operation_fourth_participant_id=slot-target graph_boundary_operation_target_edge_count=1 graph_boundary_operation_source_participant_count=1 graph_boundary_operation_road_vertex_count=4 graph_boundary_operation_surface_primitive_count=4 graph_boundary_operation_node_marker_count=2 graph_boundary_operation_count=1 graph_boundary_operation_mark_committed_count=0 graph_boundary_operation_resource_count_before=77 graph_boundary_operation_resource_count_after=77 load_result_kind=0`，observed slot 字段与活动槽一致。`RoadRendererLifecycleContractTests` 61/61、生命周期与 QA export 聚焦组合 62/62、完整自动化 919/919，双配置 build、Roslyn compiler/analyzer、目标与工作区 4 个 GDScript diagnostics、`--check-only`、正式 Vulkan 1.4.341 Forward+、Debug/`ExportRelease` 隔离与 MCP smoke 均通过。该切片首次结构化覆盖 Commit phase，不增加既有二十七个 aggregate 相邻失败或二十一个连续 Preflight 早期边界；其余 renderer 故障、其他真实 aggregate 组合与完整故障注入矩阵仍开放，因此 2.3 不关闭。

  - 真实四参与者 `StartLoad()` RoadRenderer `Commit=5` 边界补强证据（2026-08-24）：既有 renderer commit-boundary generation 失配现进一步记录真实 operation 的 phase、四 plan 顺序与 prepared 内容。aggregate 取得 commit lease 后 operation 进入 `Commit=5`，renderer storage wrapper 在 `CrossCommitBoundary()` 内释放当前 `RoadRendererLoadAdmission`；第二次 generation 复核在 graph/tool/presentation/slot-target 任一 `CommitReferences()` 前拒绝，storage `MarkCommitted()` 为 `0`。probe 在主线程确认 `road-graph → road-tools → road-presentation → slot-target`，并因本场景加载活动槽而观察到 target `1 Edge`、source participant `1`、prepared `4 road vertices / 4 surface primitives / 2 node markers` 与匹配的 slot ID。operation 返回 `Failed`、`committed=false` 且无 warning，活动 graph/tool/presentation/slot 四方引用、`SelectedRoadType=Arterial`、placement/history、surface/token/hit 均保持，aggregate disposal 释放隐藏表现资源，`ObjectResourceCount` 为 `77 -> 77`，最终无故障 Load 以 `load_result_kind=0` 成功再接管。结构化结果为 `renderer_boundary_operation_failure_result_kind=2 renderer_boundary_operation_failure_final_phase=5 renderer_boundary_operation_failure_committed=false renderer_boundary_operation_failure_trigger_count=1 renderer_boundary_operation_probe_on_main_thread=true renderer_boundary_operation_plan_count=4 renderer_boundary_operation_first_participant_id=road-graph renderer_boundary_operation_second_participant_id=road-tools renderer_boundary_operation_third_participant_id=road-presentation renderer_boundary_operation_fourth_participant_id=slot-target renderer_boundary_operation_target_edge_count=1 renderer_boundary_operation_source_participant_count=1 renderer_boundary_operation_road_vertex_count=4 renderer_boundary_operation_surface_primitive_count=4 renderer_boundary_operation_node_marker_count=2 renderer_boundary_operation_count=1 renderer_boundary_operation_mark_committed_count=0 renderer_boundary_operation_resource_count_before=77 renderer_boundary_operation_resource_count_after=77 load_result_kind=0`，observed slot 字段与活动槽一致。`RoadRendererLifecycleContractTests` 62/62、生命周期与 QA export 聚焦组合 63/63、完整自动化 920/920，双配置 build、Roslyn compiler/analyzer、目标与工作区 4 个 GDScript diagnostics、`--check-only`、正式 Vulkan 1.4.341 Forward+、Debug/`ExportRelease` 隔离与 MCP smoke 均通过。该切片是第二个结构化 Commit phase 边界，不增加既有二十七个 aggregate 相邻失败或二十一个连续 Preflight 早期边界；其余 renderer 故障、其他真实 aggregate 组合与完整故障注入矩阵仍开放，因此 2.3 不关闭。

  - 真实四参与者 `StartLoad()` ToolManager `Commit=5` 边界补强证据（2026-08-24）：既有 tool commit-boundary generation 失配现进一步记录真实 operation 的 phase、四 plan 顺序与 prepared 内容。aggregate 取得 commit lease 后 operation 进入 `Commit=5`，tool storage wrapper 在 `CrossCommitBoundary()` 内同时释放当前 `ToolLoadAdmission` 与 builder admission；第二次 generation 复核在 graph/tool/presentation/slot-target 任一 `CommitReferences()` 前拒绝，storage `MarkCommitted()` 为 `0`。probe 在主线程确认固定 participant 顺序，活动槽 target `1 Edge`、source participant `1`、prepared `4 road vertices / 4 surface primitives / 2 node markers` 与匹配的 slot ID。operation 返回 `Failed`、`committed=false` 且无 warning，活动 graph/tool/presentation/slot 四方引用、`CurrentTool=Road`、`SelectedRoadType=Arterial`、placement/history、surface/token/hit 均保持，aggregate disposal 释放隐藏表现资源，`ObjectResourceCount` 为 `77 -> 77`，最终无故障 Load 以 `load_result_kind=0` 成功再接管。结构化结果为 `tool_boundary_operation_failure_result_kind=2 tool_boundary_operation_failure_final_phase=5 tool_boundary_operation_failure_committed=false tool_boundary_operation_failure_trigger_count=1 tool_boundary_operation_probe_on_main_thread=true tool_boundary_operation_plan_count=4 tool_boundary_operation_first_participant_id=road-graph tool_boundary_operation_second_participant_id=road-tools tool_boundary_operation_third_participant_id=road-presentation tool_boundary_operation_fourth_participant_id=slot-target tool_boundary_operation_target_edge_count=1 tool_boundary_operation_source_participant_count=1 tool_boundary_operation_road_vertex_count=4 tool_boundary_operation_surface_primitive_count=4 tool_boundary_operation_node_marker_count=2 tool_boundary_operation_count=1 tool_boundary_operation_mark_committed_count=0 tool_boundary_operation_resource_count_before=77 tool_boundary_operation_resource_count_after=77 load_result_kind=0`，observed slot 字段与活动槽一致。`RoadRendererLifecycleContractTests` 63/63、生命周期与 QA export 聚焦组合 64/64、完整自动化 921/921，双配置 build、Roslyn compiler/analyzer、目标与工作区 4 个 GDScript diagnostics、`--check-only`、正式 Vulkan 1.4.341 Forward+、Debug/`ExportRelease` 隔离与 MCP smoke 均通过。该切片是第三个结构化 Commit phase 边界，不增加既有二十七个 aggregate 相邻失败或二十一个连续 Preflight 早期边界；其余 renderer 故障、其他真实 aggregate 组合与完整故障注入矩阵仍开放，因此 2.3 不关闭。

  - 真实四参与者 `StartLoad()` slot-target `Commit=5` 边界补强证据（2026-08-24）：既有 slot-target commit-boundary generation 失配现进一步记录真实 operation phase、四 plan 顺序与 prepared 内容。aggregate 取得 commit lease 后 operation 进入 `Commit=5`，slot-target storage wrapper 在 `CrossCommitBoundary()` 回调内推进 `_currentSlotGeneration`；第二次 generation 复核在 graph/tool/presentation/slot-target 任一 `CommitReferences()` 前拒绝，storage `MarkCommitted()` 为 `0`。probe 在主线程确认 `road-graph → road-tools → road-presentation → slot-target`，活动槽 target `1 Edge`、source participant `1`、prepared `4 road vertices / 4 surface primitives / 2 node markers` 与匹配 slot ID。operation 返回 `Failed`、`committed=false` 且无 warning，活动 graph/tool/presentation/slot 四方引用、`CurrentTool=Road`、`SelectedRoadType=Arterial`、placement/history、surface/token/hit 均保持，aggregate disposal 释放隐藏表现资源，`ObjectResourceCount` 为 `77 -> 77`，最终无故障 Load 以 `load_result_kind=0` 成功再接管。结构化结果为 `slot_target_boundary_operation_failure_result_kind=2 slot_target_boundary_operation_failure_final_phase=5 slot_target_boundary_operation_failure_committed=false slot_target_boundary_operation_failure_trigger_count=1 slot_target_boundary_operation_probe_on_main_thread=true slot_target_boundary_operation_plan_count=4 slot_target_boundary_operation_first_participant_id=road-graph slot_target_boundary_operation_second_participant_id=road-tools slot_target_boundary_operation_third_participant_id=road-presentation slot_target_boundary_operation_fourth_participant_id=slot-target slot_target_boundary_operation_target_edge_count=1 slot_target_boundary_operation_source_participant_count=1 slot_target_boundary_operation_road_vertex_count=4 slot_target_boundary_operation_surface_primitive_count=4 slot_target_boundary_operation_node_marker_count=2 slot_target_boundary_operation_count=1 slot_target_boundary_operation_mark_committed_count=0 slot_target_boundary_operation_resource_count_before=77 slot_target_boundary_operation_resource_count_after=77 load_result_kind=0`，observed slot 字段与活动槽一致。`RoadRendererLifecycleContractTests` 64/64、生命周期与 QA export 聚焦组合 65/65、完整自动化 922/922，双配置 build、Roslyn compiler/analyzer、目标与工作区 4 个 GDScript diagnostics、`--check-only`、正式 Vulkan 1.4.341 Forward+、Debug/`ExportRelease` 隔离与 MCP 结构 smoke 均通过；minimal DAP 因既有 8-client 上限阻塞，editor cursor 2 后无新增 error。该切片是第四个结构化 Commit phase 边界，不增加既有二十七个 aggregate 相邻失败或二十一个连续 Preflight 早期边界；其余 renderer 故障、其他真实 aggregate 组合与完整故障注入矩阵仍开放，因此 2.3 不关闭。

  - 真实四参与者 `StartLoad()` renderer `CreateRoadMesh()` `Preflight=4` 边界补强证据（2026-08-24）：既有 road-mesh factory failure 现由 `SaveManager` 在调用 renderer Preflight 前结构化记录真实 operation 输入。此时 graph/tool 两个 plan 已按 `road-graph → road-tools` 加入 fallback-owned 列表；Debug-only observation hook 仅在既有 renderer failure probe 已武装时记录主线程、phase、target revision、source slot 与 prepared presentation，不改变异常来源。真实 `CreateRoadMesh()` 分配 `ArrayMesh` 后在首次枚举注入 indices 时抛出 `InvalidOperationException`，尚未发布 surface 或创建 node batch；factory helper 释放该 Resource，orchestration fallback dispose graph/tool plans，全部 admission 随后释放。operation 返回 `Failed`、`finalPhase=4`、`committed=false` 且无 warning，活动 graph/tool/placement/history/presentation/surface/token/hit/slot 保持，payload 逐字节不变，资源 `77 -> 77`，最终无故障 Load 成功再接管。结构化结果包含 `aggregate_road_mesh_failure_result_kind=2 aggregate_road_mesh_failure_final_phase=4 aggregate_road_mesh_preflight_observation_count=1 aggregate_road_mesh_preflight_observation_on_main_thread=true aggregate_road_mesh_preflight_observed_phase=4 aggregate_road_mesh_preflight_plan_count=2 aggregate_road_mesh_preflight_first_participant_id=road-graph aggregate_road_mesh_preflight_second_participant_id=road-tools aggregate_road_mesh_preflight_target_edge_count=1 aggregate_road_mesh_preflight_source_participant_count=1 aggregate_road_mesh_preflight_road_vertex_count=4 aggregate_road_mesh_preflight_surface_primitive_count=4 aggregate_road_mesh_preflight_node_marker_count=2 load_result_kind=0` 与既有 trigger/index/resource 字段，observed slot 与活动槽一致。`RoadRendererLifecycleContractTests` 65/65、生命周期与 QA export 聚焦 66/66、完整自动化 923/923，双配置 build、Roslyn/GDScript diagnostics、`--check-only`、正式 Vulkan 1.4.341 Forward+、Debug/`ExportRelease` 隔离与 MCP 结构 smoke 均通过；minimal DAP 因既有 8-client 上限阻塞，editor cursor 2 后无新增 error。该切片补强既有失败，不增加二十七个 aggregate 相邻失败或二十一个连续 Preflight 早期边界；其余逐关键 Preflight Resource 故障、renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.3 保持开放。

  - 真实四参与者 `StartLoad()` renderer `CreateNodeBatch()` `Preflight=4` 边界补强证据（2026-08-24）：既有 node-batch factory failure 现由泛化后的 renderer-factory observation hook 记录真实 operation 输入。此时 graph/tool 两个 plan 已按 `road-graph → road-tools` 加入 fallback-owned 列表；Debug-only hook 只在对应 renderer failure probe 已武装时记录主线程、phase、target revision、source slot 与 prepared presentation，road-mesh/node-batch 各自持有独立 state、owner 与 count，不改变真实异常来源。renderer Preflight 成功创建 road mesh 后，`CreateNodeBatch()` 分配并配置 `MultiMesh`，再于 marker 首次 index read 抛出 `InvalidOperationException`；factory helper 释放 node batch，外层 Preflight catch 释放 road mesh，orchestration fallback dispose graph/tool plans，全部 admission 随后释放。operation 返回 `Failed`、`finalPhase=4`、`committed=false` 且无 warning，活动 graph/tool/placement/history/presentation/surface/token/hit/slot 保持，payload 逐字节不变，资源 `77 -> 77`，最终无故障 Load 成功再接管。结构化结果包含 `aggregate_node_batch_failure_result_kind=2 aggregate_node_batch_failure_final_phase=4 aggregate_node_batch_failure_trigger_count=1 aggregate_node_batch_marker_read_count=1 aggregate_node_batch_preflight_observation_count=1 aggregate_node_batch_preflight_observation_on_main_thread=true aggregate_node_batch_preflight_observed_phase=4 aggregate_node_batch_preflight_plan_count=2 aggregate_node_batch_preflight_first_participant_id=road-graph aggregate_node_batch_preflight_second_participant_id=road-tools aggregate_node_batch_preflight_target_edge_count=1 aggregate_node_batch_preflight_source_participant_count=1 aggregate_node_batch_preflight_road_vertex_count=4 aggregate_node_batch_preflight_surface_primitive_count=4 aggregate_node_batch_preflight_node_marker_count=2 load_result_kind=0` 与既有 committed/resource 字段，observed slot 与活动槽一致。`RoadRendererLifecycleContractTests` 66/66、生命周期与 QA export 聚焦 67/67、完整自动化 924/924，双配置 build、Roslyn/GDScript diagnostics、`--check-only`、正式 Vulkan、Debug/`ExportRelease` 隔离与 MCP 结构 smoke 均通过；minimal DAP 因既有 8-client 上限阻塞。该切片补强既有失败，不增加二十七个 aggregate 相邻失败或二十一个连续 Preflight 早期边界；其余逐关键 Preflight Resource 故障、renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.3 保持开放。

  - 真实四参与者 `StartLoad()` renderer Resource Preflight `Preflight=4` 边界补强证据（2026-08-24）：既有 resource-preflight failure 现由泛化后的 renderer preflight observation hook 记录真实 operation 输入。此时 graph/tool 两个 plan 已按 `road-graph → road-tools` 加入 fallback-owned 列表；Debug-only hook 只在对应 renderer failure probe 已武装时记录主线程、phase、target revision、source slot 与 prepared presentation，road-mesh factory、node-batch factory 与 resource preflight 各自持有独立 state、owner 与 count，不改变真实异常来源。renderer Preflight 成功创建 road mesh 与 node batch 后，在 reserved render token、`RoadSurfaceSnapshot` 与 `RoadRendererLoadCommitPlan` 构造前抛出 `InvalidOperationException`；外层 catch 释放两个 Godot Resource，orchestration fallback dispose graph/tool plans，全部 admission 随后释放。operation 返回 `Failed`、`finalPhase=4`、`committed=false` 且无 warning，活动 graph/tool/placement/history/presentation/surface/token/hit/slot 保持，payload 逐字节不变，资源 `77 -> 77`，最终无故障 Load 成功再接管。结构化结果包含 `aggregate_failure_result_kind=2 aggregate_failure_final_phase=4 aggregate_failure_committed=false aggregate_failure_trigger_count=1 aggregate_resource_preflight_observation_count=1 aggregate_resource_preflight_observation_on_main_thread=true aggregate_resource_preflight_observed_phase=4 aggregate_resource_preflight_plan_count=2 aggregate_resource_preflight_first_participant_id=road-graph aggregate_resource_preflight_second_participant_id=road-tools aggregate_resource_preflight_target_edge_count=1 aggregate_resource_preflight_source_participant_count=1 aggregate_resource_preflight_road_vertex_count=4 aggregate_resource_preflight_surface_primitive_count=4 aggregate_resource_preflight_node_marker_count=2 aggregate_resource_count_before=77 aggregate_resource_count_after=77 load_result_kind=0`，observed slot 与活动槽一致。`RoadRendererLifecycleContractTests` 67/67、完整自动化 925/925，双配置 build、Roslyn/GDScript diagnostics、`--check-only`、正式 Vulkan、Debug/`ExportRelease` 隔离、MCP 结构 smoke、DAP 与 editor 日志门均通过。该切片补强既有失败，不增加二十七个 aggregate 相邻失败或二十一个连续 Preflight 早期边界；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.3 保持开放。

  - 真实四参与者 `StartLoad()` renderer reserved render-token `Preflight=4` 边界补强证据（2026-08-24）：既有 reserved-token creation failure 现由共享 renderer preflight observation hook 记录真实 operation 输入。此时 graph/tool 两个 plan 已按 `road-graph → road-tools` 加入 fallback-owned 列表；Debug-only hook 只在对应 renderer failure probe 已武装时记录主线程、phase、target revision、source slot 与 prepared presentation，reserved-token observation 与前三个 renderer Preflight 点分别持有独立 state、owner 与 count，不改变真实异常来源。renderer Preflight 成功创建 road mesh/node batch 并通过统一 Resource Preflight 后，一次性 probe 将 admission reservation 的局部副本置为 default；`CreateReservedLoadToken()` 以真实 stale-reservation 校验失败，surface snapshot 与 commit plan 均尚未建立。外层 catch 释放两个 Godot Resource，orchestration fallback dispose graph/tool plans，全部 admission 随后释放。operation 返回 `Failed`、`finalPhase=4`、`committed=false` 且无 warning，活动 graph/tool/placement/history/presentation/surface/token/hit/slot 保持，payload 逐字节不变，资源 `77 -> 77`，最终无故障 Load 成功再接管。结构化结果包含 `aggregate_reserved_token_failure_result_kind=2 aggregate_reserved_token_failure_final_phase=4 aggregate_reserved_token_failure_committed=false aggregate_reserved_token_failure_trigger_count=1 aggregate_reserved_token_preflight_observation_count=1 aggregate_reserved_token_preflight_observation_on_main_thread=true aggregate_reserved_token_preflight_observed_phase=4 aggregate_reserved_token_preflight_plan_count=2 aggregate_reserved_token_preflight_first_participant_id=road-graph aggregate_reserved_token_preflight_second_participant_id=road-tools aggregate_reserved_token_preflight_target_edge_count=1 aggregate_reserved_token_preflight_source_participant_count=1 aggregate_reserved_token_preflight_road_vertex_count=4 aggregate_reserved_token_preflight_surface_primitive_count=4 aggregate_reserved_token_preflight_node_marker_count=2 aggregate_reserved_token_resource_count_before=77 aggregate_reserved_token_resource_count_after=77 load_result_kind=0`，observed slot 与活动槽一致。`RoadRendererLifecycleContractTests` 68/68、完整自动化 926/926，双配置 build、Roslyn/GDScript diagnostics、`--check-only`、正式 Vulkan、Debug/`ExportRelease` 隔离、MCP 结构 smoke、DAP 与 editor 日志门均通过。该切片补强既有失败，不增加二十七个 aggregate 相邻失败或二十一个连续 Preflight 早期边界；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.3 保持开放。

  - 真实四参与者 `StartLoad()` renderer `RoadSurfaceSnapshot` `Preflight=4` 边界补强证据（2026-08-24）：既有 snapshot construction failure 现由共享 renderer preflight observation hook 记录真实 operation 输入。此时 graph/tool 两个 plan 已按 `road-graph → road-tools` 加入 fallback-owned 列表；Debug-only hook 只在对应 renderer failure probe 已武装时记录主线程、phase、target revision、source slot 与 prepared presentation，snapshot observation 与前四个 renderer Preflight 点分别持有独立 state、owner 与 count，不改变真实异常来源。renderer Preflight 成功创建 road mesh/node batch 与 reserved render token 并通过统一 Resource Preflight 后，一次性 probe 将 local `PreparedData` 置为 null；真实 `RoadSurfaceSnapshot` 构造函数按参数校验失败，commit plan 尚未建立。外层 catch 释放两个 Godot Resource，orchestration fallback dispose graph/tool plans，全部 admission 随后释放。operation 返回 `Failed`、`finalPhase=4`、`committed=false` 且无 warning，活动 graph/tool/placement/history/presentation/surface/token/hit/slot 保持，payload 逐字节不变，资源 `77 -> 77`，最终无故障 Load 成功再接管。结构化结果包含 `aggregate_snapshot_failure_result_kind=2 aggregate_snapshot_failure_final_phase=4 aggregate_snapshot_failure_committed=false aggregate_snapshot_failure_trigger_count=1 aggregate_snapshot_preflight_observation_count=1 aggregate_snapshot_preflight_observation_on_main_thread=true aggregate_snapshot_preflight_observed_phase=4 aggregate_snapshot_preflight_plan_count=2 aggregate_snapshot_preflight_first_participant_id=road-graph aggregate_snapshot_preflight_second_participant_id=road-tools aggregate_snapshot_preflight_target_edge_count=1 aggregate_snapshot_preflight_source_participant_count=1 aggregate_snapshot_preflight_road_vertex_count=4 aggregate_snapshot_preflight_surface_primitive_count=4 aggregate_snapshot_preflight_node_marker_count=2 aggregate_snapshot_resource_count_before=77 aggregate_snapshot_resource_count_after=77 load_result_kind=0`，observed slot 与活动槽一致。`RoadRendererLifecycleContractTests` 69/69、完整自动化 927/927，双配置 build、Roslyn/GDScript diagnostics、`--check-only`、正式 Vulkan、Debug/`ExportRelease` 隔离、MCP 结构 smoke、DAP 与 editor 日志门均通过。该切片补强既有失败，不增加二十七个 aggregate 相邻失败或二十一个连续 Preflight 早期边界；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.3 保持开放。

  - 真实四参与者 `StartLoad()` renderer `RoadRendererLoadCommitPlan` `Preflight=4` 构造边界补强证据（2026-08-24）：既有 commit-plan construction failure 现由共享 renderer preflight observation hook 记录真实 operation 输入。此时 graph/tool 两个 plan 已按 `road-graph → road-tools` 加入 fallback-owned 列表；Debug-only hook 只在对应 renderer failure probe 已武装时记录主线程、phase、target revision、source slot 与 prepared presentation，commit-plan observation 与前五个 renderer Preflight 点分别持有独立 state、owner 与 count，不改变真实异常来源。renderer Preflight 成功创建 road mesh/node batch、reserved render token 与 `RoadSurfaceSnapshot` 并通过统一 Resource Preflight 后，`RoadRendererLoadCommitPlan` constructor 完成 owner/admission/prepared/resource/token/snapshot 字段赋值，再由一次性 probe 抛出 `InvalidOperationException("Injected aggregate Load road presentation commit plan construction failure.")`；constructor 未返回，renderer plan 尚未进入 orchestration 列表。外层 catch 释放两个 Godot Resource，orchestration fallback dispose graph/tool plans，全部 admission 随后释放。operation 返回 `Failed`、`finalPhase=4`、`committed=false` 且无 warning，活动 graph/tool/placement/history/presentation/surface/token/hit/slot 保持，payload 逐字节不变，资源 `77 -> 77`，最终无故障 Load 成功再接管。结构化结果包含 `aggregate_commit_plan_failure_result_kind=2 aggregate_commit_plan_failure_final_phase=4 aggregate_commit_plan_failure_committed=false aggregate_commit_plan_failure_trigger_count=1 aggregate_commit_plan_preflight_observation_count=1 aggregate_commit_plan_preflight_observation_on_main_thread=true aggregate_commit_plan_preflight_observed_phase=4 aggregate_commit_plan_preflight_plan_count=2 aggregate_commit_plan_preflight_first_participant_id=road-graph aggregate_commit_plan_preflight_second_participant_id=road-tools aggregate_commit_plan_preflight_target_edge_count=1 aggregate_commit_plan_preflight_source_participant_count=1 aggregate_commit_plan_preflight_road_vertex_count=4 aggregate_commit_plan_preflight_surface_primitive_count=4 aggregate_commit_plan_preflight_node_marker_count=2 aggregate_commit_plan_resource_count_before=77 aggregate_commit_plan_resource_count_after=77 load_result_kind=0`，observed slot 与活动槽一致。`RoadRendererLifecycleContractTests` 70/70、完整自动化 928/928，双配置 build、Roslyn/GDScript diagnostics、`--check-only`、正式 Vulkan、Debug/`ExportRelease` 隔离、MCP 结构 smoke、DAP 与 editor 日志门均通过。该切片补强既有失败，不增加二十七个 aggregate 相邻失败或二十一个连续 Preflight 早期边界；其余 renderer 故障与真实 aggregate 组合矩阵仍未完成，因此 2.3 保持开放。

  - aggregate Load 提交后辅助异常的成功语义证据（2026-08-24）：`RunLoadAsync()` 原先让同一通用 `catch` 同时覆盖 `aggregate.Commit()` 与后续槽位列表/性能辅助工作，真实故障注入因而在四参与者已经联合提交后仍返回 `Failed`、`finalPhase=5`、`committed=true` 且 warning 为空。`save-system:BUG-15` 把 commit 后辅助工作放入独立 `try/catch`；异常现在追加 `Load post-commit work failed: ...` 并以 `SucceededWithWarnings` 完成 lease，提交前异常仍保留原有 `Failed` 路径。Debug-only probe 与连续真实 Load 证明 warning 结果保留新的 graph/tool/presentation/slot 状态、释放全部 admission，下一次无故障 Load 返回 `Succeeded`。`RoadRendererLifecycleContractTests` 73/73、完整自动化 948/948，Debug 与 `ExportRelease` build 均为 0 警告/0 错误，Roslyn compiler/analyzer 与目标 GDScript `--check-only` 为 0；Godot 4.7 CLI 输出 `post_commit_warning_result_kind=1`、`post_commit_clean_result_kind=0`、`post_commit_trigger_count=1` 与 PASS，Debug/`ExportRelease` 隔离确认 probe 不进入发布程序集。Godot editor MCP 未连接，LSP/DAP 门未刷新。该切片只关闭 aggregate 已提交后的非关键辅助结果边界；其余 renderer 故障、其他真实 aggregate 组合与完整矩阵仍开放，因此 2.3 不关闭。

  - aggregate post-commit 与全部真实 warning 来源联合证据（2026-08-24）：同一次真实四参与者 `StartLoad()` 同时武装 `RoadGraph.GraphChanged`、`RoadRenderer.PresentationReady` 两个 observer，RoadGraph、ToolManager、RoadRenderer、slot-target 四个 cleanup，以及 `SaveManager` post-commit 辅助异常。operation 仍返回 `SucceededWithWarnings`、`committed=true`，并严格按 graph observer、renderer observer、四 participant cleanup、SaveManager post-commit 的顺序保留七条 warning；新的 graph/tool/presentation/slot 状态不回滚，全部一次性 probe 解除，工具可继续切换。紧接的无故障 Load 返回 `Succeeded`、warning 为空并恢复 1 Edge matching presentation，证明所有 admission 可再取得。结构化结果为 `post_commit_combined_warning_result_kind=1`、`post_commit_combined_clean_result_kind=0`、renderer/graph observer 累计 `7/3` 次、四类 cleanup 各 `5` 次、post-commit 累计 `2` 次。Godot `--check-only` 与真实 4.7 CLI 契约均以退出码 0 通过并输出 PASS；完整自动化 948/948、Debug 与 `ExportRelease` build 0 警告/0 错误、Roslyn production/test compiler/analyzer 0 diagnostics。Godot editor MCP 未连接，LSP 与 DAP 未运行；隔离 QA 根已送入回收站。提交 `1406f92` 只扩展运行时回归，不修改生产逻辑；该切片关闭 warning 列表组合保留这一格，但其余 renderer 故障、其他真实 aggregate 组合与完整矩阵仍开放，因此 2.3 不关闭。

  - aggregate Load 提交后取消拒绝证据（2026-08-24）：Debug-only `RoadLoadObserverFailureProbe` 在真实四参与者 `StartLoad()` 已进入 `PresentationReady` observer 后读取当前 `ActiveOperationToken`，记录 token 与触发次数，先解除一次性 handler，再对同一 operation 调用 `CancelOperation()`。请求精确返回 `false`；Load 仍为 `Succeeded`、`committed=true`、warning/error 为空，空 placement/history、目标 `CurrentSlotID` 与 matching presentation 保持已提交状态。工具随后可切换，紧接的无故障 Load 再以 `Succeeded`、空 warning 恢复 1 Edge matching presentation，证明 point-of-no-return 后的取消既不改写成功结果，也不遗留 admission。结构化结果为 `post_commit_cancel_result_kind=0`、`post_commit_cancel_clean_result_kind=0`、`post_commit_cancel_request_accepted=false`、`post_commit_cancel_trigger_count=1`。`RoadRendererLifecycleContractTests` 74/74、完整自动化 949/949，Debug 与 `ExportRelease` build 0 警告/0 错误，Roslyn production/test compiler/analyzer 0 diagnostics；Godot 4.7 `--check-only` 与隔离 APPDATA 的真实 CLI 契约均通过并输出 PASS，日志没有 ERROR/FATAL/Unhandled/AccessViolation/Crash。Godot editor MCP 未连接，LSP 与 DAP 未运行，未把这些门记为通过；隔离 QA 根已送入回收站。提交 `72546be` 只增加测试探针、源码契约与真实运行时回归，不修改生产逻辑；该切片关闭提交后同 token 取消拒绝这一格，但其余 renderer 故障、其他真实 aggregate 组合与完整矩阵仍开放，因此 2.3 不关闭。

  - aggregate 提交后取消拒绝与七项 warning 联合证据（2026-08-24）：同一次真实四参与者 `StartLoad()` 同时武装 `RoadGraph.GraphChanged`、`RoadRenderer.PresentationReady` 两个 observer，RoadGraph、ToolManager、RoadRenderer、slot-target 四个 cleanup，`SaveManager` post-commit 辅助异常，以及 `PresentationReady` 内针对当前 operation token 的取消请求。取消精确返回 `false`，operation 仍为 `SucceededWithWarnings`、`committed=true`、error 为空；warning 列表继续严格按两个 observer、四个 participant cleanup、post-commit 辅助工作的既定顺序保留七行，没有加入伪取消 warning。空 placement/history、目标 slot 与 matching presentation 保持已提交状态，全部一次性 probe 解除；紧接的无故障 Load 以 `Succeeded`、空 warning 恢复 1 Edge matching presentation。结构化结果为 `post_commit_cancel_combined_warning_result_kind=1`、`post_commit_cancel_combined_clean_result_kind=0`、`post_commit_cancel_combined_request_accepted=false`、取消累计 `2` 次；renderer/graph observer 累计 `8/4` 次，四类 cleanup 各 `6` 次，post-commit 辅助故障累计 `3` 次。完整自动化 949/949，Debug 与 `ExportRelease` build 0 警告/0 错误；Godot 4.7 `--check-only` 与隔离 APPDATA 的真实 CLI 契约均以退出码 0 通过并输出 PASS，stderr 只有既有 `ConstructionDock` warning 和故障注入产生的预期 RoadGraph observer warning。Godot editor MCP、LSP 与 DAP 门未刷新，未把这些门记为通过；隔离 QA 根已送入回收站且没有残留 Godot 进程。提交 `c31a8b8` 只扩展真实运行时组合回归，不修改生产逻辑；该切片关闭取消 point-of-no-return 与全部现有 warning 来源的联合格，但其余 renderer 故障、其他真实 aggregate 组合与完整矩阵仍开放，因此 2.3 不关闭。

  - aggregate 首个 observer 的提交后取消拒绝证据（2026-08-24）：`PreparedAggregateLoad` 在交换 graph/tool/presentation/slot 全部引用并调用 `MarkCommitted()` 后，才按参与者顺序发布通知；Debug-only `RoadLoadObserverFailureProbe` 因而在首个 `RoadGraph.GraphChanged` observer 中读取当前 `ActiveOperationToken`，先记录并解除 handler，再调用 `CancelOperation()`。请求精确返回 `false`，operation 仍为 `Succeeded`、`committed=true`、warning/error 为空；后续 renderer notification 与四参与者 cleanup 全部完成，空 placement/history、目标 `CurrentSlotID` 与 matching presentation 保持已提交状态。工具可立即切换，紧接的无故障 Load 再以 `Succeeded`、空 warning 恢复 1 Edge matching presentation。结构化结果为 `post_commit_graph_cancel_result_kind=0`、`post_commit_graph_cancel_clean_result_kind=0`、`post_commit_graph_cancel_request_accepted=false`、`post_commit_graph_cancel_trigger_count=1`。`RoadRendererLifecycleContractTests` 75/75、完整自动化 950/950，Debug 与 `ExportRelease` build 均为 0 警告、0 错误，Roslyn production/test compiler/analyzer 为 0 diagnostics；Godot 4.7 `--check-only` 与隔离 APPDATA 的真实 CLI 契约均通过并输出 PASS，stderr 只有既有 `ConstructionDock` warning 和故障注入产生的预期 RoadGraph observer warning。新增探针符号在 Debug/`ExportRelease` DLL 中分别命中 `1/0` 次；Godot editor MCP、LSP 与 DAP 门未刷新，未把这些门记为通过。隔离 QA 根已送入回收站且没有残留 Godot 进程。提交 `0ae88f0` 只增加测试探针、源码契约与真实运行时回归，不修改生产逻辑；该切片关闭首个 aggregate observer 内的取消 point-of-no-return 格，但其余 renderer 故障、其他真实 aggregate 组合与完整矩阵仍开放，因此 2.3 不关闭。

  - Delete 越界前后取消与收敛证据（2026-08-24）：Debug-only `SaveDeleteOperationProbe` 先在真实 `RunDeleteAsync()` 的 Recover 阶段、`SaveSlotStore.Delete()` 前锁定可取消窗口；PauseMenu 双确认保持同一个 token，双 Escape 只进入一次 `CancelOperation()`，结果为 `Canceled`、`finalPhase=Recover`、`committed=false`，磁盘与 `CurrentSlotID` 不变。第二个门只在 `SavePublicationPhase.DeletionTombstoned` 进入；源码顺序固定 `CrossCommitBoundary(slot -> tombstone)`、`MarkCommitted()`、tombstone observer，因此该窗口已越过 point-of-no-return。PauseMenu 收到 crossed-boundary state 后不再转发 Escape；对相同 token 直接调用 `CancelOperation()` 精确返回 `false`，Delete 仍为 `Succeeded`、`finalPhase=Cleanup`、`committed=true`，目标保持逻辑删除、当前槽切回 autosave，后续 cleanup-pending 与其他存档操作继续收敛。提交 `a604f64` 与 `257b9bb` 均只扩展 Debug 测试缝、源码契约、导出隔离与真实运行时回归，不修改发布行为。最新 PauseMenu/ExportPreset 聚焦契约为 8/8，完整自动化 954/954，双配置 build 0 警告/0 错误，Roslyn compiler/analyzer 与目标 GDScript LSP 为 0 diagnostics；Godot 4.7 `--check-only` 与隔离 APPDATA 契约均退出码 0，后者输出 PASS，正确项目冻结 5 帧 `MapTest` 的 editor error 与 DAP 双通道为空。新门相关六项标识在 Debug/`ExportRelease` DLL 中命中 8/0；隔离 QA 根已送入回收站，隐藏 editor 已关闭且没有残留 Godot 进程。该切片关闭 2.3 验证中的 Delete 越界前后取消格，但其余 renderer 故障、其他真实 aggregate 组合与完整矩阵仍开放，因此 2.3 不关闭。

  - Save As Prepare 取消与重复激活证据（2026-08-24）：Debug-only `SavePublishOperationProbe` 在真实 `RunAdmittedPublishAsync()` 已进入 Prepare、构造 store 后且调用 `SaveCaptured*()` 前锁定一次性窗口，并通过独立 partial hook 记录 `CancelOperation()`。PauseMenu 连续激活两次 Save As 仍只有同一个 token，且门只触发一次；双 Escape 只转发一次取消，释放门后 storage lease 在实际写入前观察 cancellation，operation 返回 `Canceled`、`finalPhase=Prepare`、`committed=false`。隔离运行证明没有同名槽发布，`CurrentSlotID` 与名称保持，manager/UI 恢复 idle，后续正常 Save As/Overwrite/Load/Delete 继续收敛。提交 `cac8d5e` 只增加 Debug 测试缝、源码契约、导出隔离与真实运行时回归，不改变 `ExportRelease` 行为。PauseMenu/ExportPreset 聚焦契约为 9/9，完整自动化 955/955，双配置 build 0 警告/0 错误，Roslyn compiler/analyzer 与目标 GDScript LSP 为 0 diagnostics；Godot 4.7 `--check-only` 与隔离 APPDATA 契约均退出码 0，后者输出 PASS。八项探针标识在 Debug/`ExportRelease` DLL 中命中 8/0，生成 UID 后的 QA 导出资源契约为 1/1；正确项目冻结 5 帧 `MapTest` 的 editor error 与 DAP 双通道为空，隔离 QA 根已送入回收站，隐藏 editor 已关闭且没有残留 Godot 进程。该切片关闭 Publish Prepare 的一个取消/重复激活格，但其余 renderer 故障、其他真实 aggregate 组合与完整矩阵仍开放，因此 2.3 不关闭。

  - Save As canonical publication 后取消拒绝证据（2026-08-24）：扩展 Debug-only `SavePublishOperationProbe`，在 observer-backed `SaveSlotStore` 收到 `CanonicalPublished` 后锁定一次性门；源码顺序固定 canonical move 的 `CrossCommitBoundary()`、`MarkCommitted()`、canonical observer，因而该窗口已越过 point-of-no-return。PauseMenu 收到 crossed-boundary 的 `Publish=6` state 后不再转发 Escape；对相同 token 直接调用 `CancelOperation()` 精确返回 `false` 且只记录一次，Save As 仍以 `Succeeded`、`finalPhase=Cleanup`、`committed=true` 收敛，新槽保持发布并成为 `CurrentSlotID`，manager/UI 恢复 idle，后续重复名称与其他存档操作继续成功。提交 `0e695bb` 只扩展 Debug 测试探针、源码契约与真实运行时回归，不改变生产逻辑或 `ExportRelease`。PauseMenu/ExportPreset 聚焦契约为 10/10，完整自动化 956/956，Debug 串行复跑与 `ExportRelease` build 均为 0 警告、0 错误，Roslyn compiler/analyzer 与目标 GDScript LSP 为 0 diagnostics；Godot 4.7 `--check-only` 与隔离 APPDATA 契约均退出码 0，后者输出 PASS。八项 post-commit 标识在 Debug/`ExportRelease` DLL 中命中 8/0，QA 导出资源契约继续为 1/1；正确项目冻结 5 帧 `MapTest` 的 editor error 与 DAP 双通道为空，隔离 QA 根已送入回收站，隐藏 editor 已关闭且没有残留 Godot 进程。该切片关闭 canonical publication 后取消拒绝格，但其余 renderer 故障、其他真实 aggregate 组合与完整矩阵仍开放，因此 2.3 不关闭。

## 暂不执行

### 二进制、压缩与持久化 geometry chunk

- 延期原因：当前没有证据证明 JSON 解析 CPU 或磁盘体积是主要瓶颈；引入新容器会扩大 reader 和调试成本。
- 保持现状：V3 format v1 使用内联 geometry 的 UTF-8 JSON；I/O buffer、渲染批次和 query fragment 没有持久领域身份，也不能制造伪 Node/Edge。
- 重新开启条件：geometry-dense 基准证明体积或 I/O 未达门槛后，先定义显式 codec、编码/解码长度和压缩比预算，并提升 V3 manifest 版本。

### 敌对写入认证、防回滚与长期介质恢复

- 延期原因：保存根锁和 SHA-256 只覆盖协作实例、意外损坏与中断恢复，不是敌对本机进程隔离、来源认证或版本新鲜度证明；事务 backup 也不是长期备份。
- 保持现状：准确声明目录级 TOCTOU、无密钥 hash 和临时 backup 的边界，不把 crash recovery 写成 durable 或 bit-rot 自动恢复。
- 重新开启条件：产品明确要求敌对环境或长期代际恢复时，另行设计受保护密钥/签名、可信单调版本、目录隔离和保留代际，并提升容器协议。

### 第二个正式持久化业务系统

- 延期原因：V3 当前业务 payload 仍只有 RoadGraph；本路线图先用 fake second saveable 固化 prepared aggregate 协议。
- 保持现状：保留每系统独立 payload 扩展边界，禁止回退为逐系统可抛 `RestoreState`。
- 重新开启条件：第二个业务系统进入产品范围时，在其所属系统路线图新增工作项，并以 `v3-save-system:2.3` 的 aggregate Preflight/commit 故障注入作为集成前置条件。

## 已解决基线

- [x] **V2 已证明严格版本门禁是必要的。** `save-system:0.4` 的历史证据用于要求 V3 精确校验 family/version；V3 不复用 V2 reader 或版本号语义。
- [x] **V2 RoadGraph 已先完整准备再提交。** `save-system:0.5` 与 `save-system:0.11` 的失败保护是行为基线；V3 以新接口扩展为 graph/tool/renderer/slot-target prepared aggregate。
- [x] **V2 已验证命名槽、自动槽和 staging/backup 的用户语义。** V3 重新实现这些能力，并用 descriptor、独立根和跨进程协调替代旧内部架构。
- [x] **V2 保存范围已限定为 RoadGraph。** `save-system:1.4` 是历史产品边界；V3 的 fake aggregate 不代表第二个业务 payload 已进入存档。
- [x] **V2/过渡期 payload 与保存根只作历史证据。** 历史 Node/Edge/Group、当前 Group-free 且含 `roadType` 的严格 `schemaVersion = 3`、`res://saves` 和 `user://saves` 均不是 V3 format v1 输入或实现依赖。
- [x] **V3 format v1 与独立根已成为唯一生产存档入口。** `IStreamingSaveable`、严格 manifest/payload family、`user://saves-v3`、canonical RoadGraph reader/writer、新 lineage Load 和 V2 根隔离均已有自动化、真实 `MapTest` 与 Debug 导出运行证据；后续 2.2/2.3 只能扩展有界 I/O、恢复与并发协议，不能恢复旧格式或同步兼容接口。
- [x] **V3 同步存储原语已经确定、有界且可恢复。** `V3JsonStreamReader`、同句柄 length/hash/EOF、PNG 展示资产预算、operation-specific publish/delete descriptor、五类 occupant、OS 根锁、quarantine/tombstone 与 digest 恢复矩阵已有自动化和 Windows 导出/ACL 证据；`2.3` 只能在其上增加异步 coordinator、publish lease 和 aggregate Load，不能绕过这些磁盘不变量。
- [x] **Load 隐藏表现资源已有明确所有权边界。** 普通构建、Load Preflight 与未提交 plan 会确定性释放尚未转交的 `ArrayMesh`/`MultiMesh`；成功 commit 后资源归表现层持有，重复 plan cleanup 不会销毁已挂载资源。关键故障逐点注入仍由开放的 `2.3` 负责。

## 完成标准

1. `v3-save-system:2.1`～`2.3` 的新格式、根隔离、容量、流式 I/O、发布/删除恢复、并发、取消与 prepared aggregate 自动化全部通过。
2. V3 writer 只生成 `simple-cities-v3` format v1；reader 只接受精确 family/version，并完整表达 canonical Edge、self-loop、parallel Edge、RoadType 和 `nextID`。
3. 五类 occupant、publish/delete descriptor、删除 tombstone、`PublicationRecoveryBlocked`、`DeletionRecoveryBlocked`、`PublishedWithCleanupPending` 和 `DeletedWithCleanupPending` 通过全矩阵与中断点验证。
4. V2 两个保存根在 V3 List/Save/Save As/Load/Delete/autosave/恢复/启动清理中均零访问；不存在迁移、导入、只读加载、兼容 DTO 或双写入口。
5. Publish、Load 与 Delete 在 API、operation token、取消点、结果和 UI 呈现上分离；Load 的关键失败全部发生在 Preflight，commit 一次交换全部引用。
6. 主线程 capture 保持 O(1)，长 JSON/hash/I/O 在后台执行；同一 V3 根跨进程排他，autosave busy 有界，场景退出和各不可取消点均可收敛。
7. `v3-road-graph:8.6` 联合 `v3-grid-rendering:2.2`、`v3-tool-input:2.4` 和 `v3-ui:1.4` 在真实 `MapTest`、Windows 导出及 junction-dense/geometry-dense 场景完成最终集成；未取得目录元数据耐久证据时不得宣称 durable。

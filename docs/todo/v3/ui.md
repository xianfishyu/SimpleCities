# 第三代 UI 系统待办清单

> 系统 key：`v3-ui`
> 整理日期：2026-08-24
> 证据：`Scripts/UI/ConstructionDock.cs`、`Scripts/UI/ToolContextPanel.cs`、`Scripts/UI/GameHUD.cs`、`Scripts/UI/PauseMenu.cs`、`Scripts/UI/DebugPanel.cs`、`Scripts/Tools/ToolManager.cs`、`Scripts/Road/RoadGraphDiagnosticsSnapshot.cs`、`Scripts/Road/RoadGraph.Diagnostics.cs`、`Scenes/UI/GameHUD.tscn`、`tests/SimpleCities.RoadGraph.Tests/RoadTypeSelectorContractTests.cs`、`tests/SimpleCities.RoadGraph.Tests/RoadGraphDiagnosticsSnapshotTests.cs`、`tests/SimpleCities.RoadGraph.Performance/Program.cs`、`tests/godot/command_center_runtime_contract.gd`、`tests/godot/pause_menu_runtime_contract.gd` 与 `docs/manuals/road-system-v3-gen.md`。
> 主导原则：UI 只呈现和编辑工具/操作状态，不直接修改 RoadGraph 或磁盘；桌面、窄屏、键盘焦点和场景重复进入必须共享同一行为契约。

## 状态总览

| ID | 发现 | 当前状态 | 处置方式 |
|---|---|---|---|
| 1.1 | 道路上下文没有 RoadType 选择控件 | 已完成 | 四段式名称与颜色 swatch 选择器写入共享 tool state |
| 1.2 | ConstructionDock 没有道路改造工具呈现 | 已完成 | Road/RoadUpgrade 资源化双工具、T 动作、选中态和稳定焦点链已验证 |
| 1.3 | DebugPanel 仍把 RoadGroup 数量作为路网指标 | 已完成 | 以不可变 diagnostics snapshot 展示 canonical Node/Edge/geometry/query/self-loop 结构量，并通过可见性与 sequence 门禁避免逐帧全图读取 |
| 1.4 | 暂停菜单没有异步 Save/Load/Delete 的独占状态机 | 开放（部分实现） | operation/render token、generation、busy、Escape、退出收敛、indexed 四类 surface、现有道路事务 admission、deferred continuation 失效、七项 post-commit warning UI、renderer Resource Preflight、Save As 磁盘失败、Prepare/Staged 取消/重复激活与 canonical publication 后取消拒绝、Overwrite cleanup-pending，以及 Delete 失效授权、Recover 取消/重复激活、tombstone 后取消拒绝与 cleanup-pending 恢复已接入；补齐其余结果矩阵 |

### 设计覆盖矩阵

| 设计范围 | 当前事实 | 关联待办 |
|---|---|---|
| 命令中心基线 | ConstructionDock、ToolContextPanel、DebugPanel 和 PauseMenu 已有响应式布局、焦点链和运行时契约 | 已解决基线 |
| V3 类型建造 | `RoadBuilder` 提供默认 `Street` 的 `SelectedRoadType` 与会话冻结；ToolContextPanel 已在 Road/RoadUpgrade 上下文显示四段式样式选择器，通过 ToolManager 委托写回共享状态 | `v3-tool-input:2.1`、1.1～1.2（均已完成） |
| V3 既有道路改造 | `ToolType.RoadUpgrade` 与独立输入生命周期已实现；Roads catalog 以独立图标和 T 动作呈现 RoadUpgrade，ConstructionDock 与 ToolManager 双向同步选中态和上下文 | 1.2、`v3-tool-input:2.2`（均已完成） |
| V3 规范存储诊断 | `RoadGraphDiagnosticsSnapshot` 已发布 Node、canonical Edge、原生 geometry segment、query fragment 与 self-loop；DebugPanel 只在展开且 `ChangeSequence` 改变时刷新 | 1.3、`v3-road-graph:8.2`～`8.5`（1.3 已完成） |
| V3 异步存档体验 | PauseMenu 已消费结构化 operation state/result，按 token、menu/scene generation 过滤 continuation，busy 时禁用冲突入口并让 Escape 只请求一次取消；退出流程会 drain/shutdown。Load 已联合发布基础 mesh、带 canonical `RoadLocation` 和不可变空间索引的 `EdgeRibbon` + `TerminalCap` + `SemanticJoin` + `JunctionPatch` surface 与 matching `RoadRenderToken` acknowledgment；真实 UI 已按既定顺序显示 graph/renderer observer、四参与者 cleanup 与 post-commit 辅助工作的七项 warning，并在下一次干净 Load 清除旧 warning。renderer Resource Preflight 的真实失败会保留菜单暂停、目标选择、CurrentSlotID、工具与 presentation，恢复操作按钮，并允许紧接的 Load 接管；Save As 的磁盘事务根阻断同样保留名称、CurrentSlotID 和输入状态，解除阻断后可立即创建存档。Save As 在 worker 的 Prepare、尚未进入 `SaveCaptured*()` 时，重复激活保持同一个 operation token，连续 Escape 只请求一次取消；未提交取消不发布槽、不切换 `CurrentSlotID` 或清空名称，完成后恢复输入与按钮。完整 staging 与 descriptor 已写入、但尚未取得 commit lease 时，同样保持 Prepare/crossed-boundary=false；重复激活仍只有一个 token，双 Escape 只请求一次取消，取消后清理 `.save-transactions` 且不发布目标槽。越过 canonical publication 后，PauseMenu 已观察到 `Publish` 与 crossed-boundary 状态，Escape 不再转发取消，同 token 的直接取消返回 `false`；operation 仍以 committed Cleanup 成功收敛并发布目标槽。Overwrite 越过 canonical publication 后的 cleanup failure 会显示为“已覆盖”的成功 warning，保留 CurrentSlotID 与目标选择，列表刷新完成事务恢复，紧接的干净覆盖不会保留旧 warning。Delete 确认后的 occupant digest 若失效，旧授权会在 tombstone 前拒绝；PauseMenu 保留目标槽与 CurrentSlotID、恢复选择和操作按钮，并可用刷新后的授权立即重试。Delete Recover 期间连续两次确认保持同一个 operation token 且只进入一次 worker 门，连续两次 Escape 只请求一次取消；未提交的取消不改磁盘、`CurrentSlotID` 或目标选择，完成后恢复操作按钮。Delete 越过 tombstone 后，PauseMenu 已观察到 Commit 与 crossed-boundary 状态，连续 Escape 不再转发取消；同 token 的直接取消返回 `false`，operation 仍以 committed Cleanup 成功收敛，目标保持逻辑删除并恢复按钮。越界后的 cleanup failure 则显示为成功 warning，目标不回列表，删除当前槽会切回 autosave，下一次列表读取完成事务恢复。普通 mutation 的 renderer provider 在 pending/stalled 时拒绝 hit，placement、拆除、RoadUpgrade 与 undo/redo 已覆盖当前玩家道路事务入口。deferred 普通表现回调也会在 Load admission 同步 flush，并以 generation 拒绝旧 callable | 1.4、`v3-save-system:2.3`、`v3-tool-input:2.4`、`v3-grid-rendering:2.2` |

## 执行顺序

### 阶段 7：第三代道路控件、诊断与存档交互

<a id="v3-ui1.1"></a>

- [x] **1.1 在道路上下文中提供可访问的 RoadType 选择器**
  - 当前问题：`ToolContextPanel` 只能展示当前工具说明；玩家无法选择 `Dirt`、`Street`、`Arterial` 或 `Highway`，也无法确认预览将使用哪个类型。
  - 修改：在道路建造和改造上下文中加入四段式单选控件，每项显示来自 `RoadTypeStyle` 的名称与颜色 swatch；控件只更新 `SelectedRoadType`，不调用 RoadGraph。默认选择 `Street`，运行期间切换工具保留选择，重新进入城市场景恢复默认。
  - 依赖：`v3-road-graph:8.4`、`v3-grid-rendering:2.1`、`v3-tool-input:2.1`。
  - 集成负责人：`v3-ui`；端到端完成判定由 `v3-road-graph:8.6` 负责。
  - 验证：四项唯一选择、鼠标、键盘/手柄焦点、无效样式降级、道路/改造上下文共享状态、切换分类、暂停返回、场景重复进入，以及 1600x900、640x480、435x480 布局。
  - 验收：选中类型始终可见且与 tool state 一致；窄屏不与 ConstructionDock、DebugPanel 或 PauseMenu 重叠；控件失效时不会提交错误类型。
  - 完成证据（2026-08-20）：`ToolContextPanel` 使用 `ButtonGroup` 注册 Dirt/Street/Arterial/Highway 四项，读取 `RoadTypeStyle.DisplayName/Color`，无效 `RoadConfig` 使用后备 swatch 并禁用所有按钮；GameHUD 通过 ToolManager 委托读写 `RoadBuilder.SelectedRoadType`，不直接调用 RoadGraph。`RoadTypeSelectorContractTests` 为 3/3；`dotnet test SimpleCities.sln --no-build --no-restore` 为 836/836；`godot --headless --path . --script tests/godot/command_center_runtime_contract.gd` 输出 `PASS command center runtime contract`，覆盖鼠标、`ui_focus_next`、Road/RoadUpgrade 共享状态、无效样式、暂停返回、同场景 HUD 重入和新 MapTest 恢复 Street；同一契约在 1600x900、640x480、435x480 检查边界与不重叠；`road_type_style_runtime_contract.gd` 输出 PASS。Debug/ExportRelease build 均 0 警告、0 错误。
  - 验证限制：本会话未暴露 Roslyn/Godot MCP 工具，focused semantic diagnostics 与 MCP/DAP 增量读取未能刷新；headless editor 仍有已知 main-scene UID 提前解析、MCP 6550 已占用和 ImGui 插件环境噪声，未发现由本项新增的运行时错误。

<a id="v3-ui1.2"></a>

- [x] **1.2 在道路分类中呈现并同步 RoadUpgrade 工具**
  - 完成前问题：输入层已有稳定的 `ToolType.RoadUpgrade = 3` 和独立改造生命周期，但 `ConstructionDock.RenderRoadsMenu` 只接受 `ToolType.Road`，内置展示只覆盖 Select 和 RoadRemove，无法资源化呈现改造工具或同步选中态。
  - 修改：扩展道路 catalog 和 dock 渲染规则，使“城市道路”和“道路改造”作为两个稳定工具项显示；改造工具使用独立图标、tooltip、焦点节点和 `ToolType.RoadUpgrade`，与 GameHUD / ToolManager 实际状态双向同步。切出工具时 UI 只触发输入层取消，不自行提交改造。
  - 依赖：`v3-tool-input:2.2`、`v3-ui:1.1`。
  - 集成负责人：`v3-ui`；端到端完成判定由 `v3-road-graph:8.6` 负责。
  - 验证：catalog 唯一 ID、排序、图标资源、选中态、分类展开/折叠、焦点循环、快捷动作显示、工具切换取消预览、场景生命周期和三档视口运行时契约。
  - 验收：玩家可从道路分类进入建造或改造并明确看到目标类型；工具显示、ToolManager 状态和输入会话始终一致。
  - 完成证据（2026-08-20）：`RoadsConstructionCategory.tres` 按 `SortOrder` 依次资源化 `city-road` 与 `road-upgrade`，后者使用独立 `construction-road-upgrade.svg`、中文标签、说明和 `ToolType.RoadUpgrade`。catalog 运行时校验拒绝重复 ID、ToolType 和 SortOrder；`ConstructionDock` 生成 `RoadToolButton` / `RoadUpgradeToolButton`，保持互斥选中态及“景观分类 -> 城市道路 -> 道路改造 -> 工具上下文”的稳定焦点链。按钮和 HUD 只设置 `ToolManager.CurrentTool`，切出铺路工具由既有输入生命周期取消未提交预览。
  - 输入兼容（2026-08-20）：`InputBindingManager` 与 `project.godot` 新增默认 T 的 `tool_upgrade`。加载旧 `input_bindings.cfg` 时先保留所有已有绑定；T 未占用才补默认值，若旧动作已占用 T，则保留旧动作并让 RoadUpgrade 暂时显示“未绑定”，不会覆盖玩家配置。
  - 验证证据（2026-08-20）：相关 C# 契约 25/25、完整 solution 自动化 839/839；Debug 与 `ExportRelease` build 均为 0 警告、0 错误。`roads_construction_category_contract.gd` 和 `command_center_runtime_contract.gd` 均输出 PASS；后者覆盖双工具按钮、鼠标/T 动作切换、选中态、上下文、取消铺路预览、HUD 重入、分类展开/折叠、焦点链，以及 1600x900、640x480、435x480 的边界与不重叠。1600x900 OpenGL 实际截图复核确认双工具居中、互不重叠，独立图标、中文标签、RoadUpgrade 选中态、T 提示和 RoadType 上下文同时可见。
  - 验证限制：本会话未暴露 Roslyn/Godot MCP 与 DAP 工具，未把这些门禁记为本轮通过；headless editor 仍有既有 main-scene UID 提前解析和 MCP 6550 端口占用环境噪声。

<a id="v3-ui1.3"></a>

- [x] **1.3 将 DebugPanel 改用 canonical RoadGraph 指标**
  - 完成前问题：`DebugPanel` 和 `command_center_runtime_contract.gd` 通过 `GetAllGroups()` 显示/断言 RoadGroup 数；V3 移除 Group 后该指标既无法编译，也不能说明连续存储是否生效。若直接在 `_Process` 中调用 `GetAllNodes/Edges` 统计新指标，会每帧复制全图并让 geometry-dense 长 Edge 产生额外扫描/分配。
  - 修改：删除 RoadGroup 行和相关场景节点/引用，改为显示 Node、canonical Edge、原生 geometry segment、query fragment 和 self-loop 数；标签明确区分拓扑量、权威几何量与派生索引量，不把 parallel Edge 按邻居去重。只读取 `v3-road-graph:8.5` 随事务维护的不可变 diagnostics snapshot；面板可见且 sequence 改变时刷新文本，隐藏时不轮询/复制全图。
  - 依赖：`v3-road-graph:8.2`、`v3-road-graph:8.3`、`v3-road-graph:8.5`。
  - 集成负责人：`v3-ui`；端到端完成判定由 `v3-road-graph:8.6` 负责。
  - 验证：直路、非共线多段、简单环、两路口环、交叉和删除重归一化后的指标；面板隐藏/显示、普通 delta/full reset、sequence 连跳、10k/100k 和单 Edge N geometry 下每帧分配；命令中心宽/窄屏和场景重复进入契约。
  - 验收：N 段无分支道路显示 2 Node / 1 Edge / N geometry segment；简单环显示 1 Node / 1 Edge / 1 self-loop；面板无失效 Group 文案或引用，隐藏或图未变化时无逐帧全图枚举/分配。
  - 完成证据（2026-08-21）：新增不可变 `RoadGraphDiagnosticsSnapshot`，以 `StateToken` 绑定提交状态并发布 Node、canonical Edge、geometry segment、query fragment、self-loop 五项指标；构造、普通 mutation、delta/undo/redo、prepared topology 和 full-reset Load commit 均在提交边界发布快照，`CaptureDiagnosticsSnapshot()` 对同一图状态返回同一引用。`DebugPanel` 折叠时不访问 RoadGraph，展开后仅在 `ChangeSequence` 改变时刷新五项文本；场景和命令中心契约已移除 `GetAllGroups()`/RoadGroup 断言。
  - 验证证据（2026-08-21）：聚焦 `RoadGraphDiagnosticsSnapshotTests` + `GameHUDCompositionContractTests` 为 11/11；完整 `dotnet test SimpleCities.sln --no-restore` 为 863/863；Roslyn compiler/analyzer 为 0 diagnostics；Debug 与 `ExportRelease` build 均为 0 警告、0 错误。Release 性能程序的 1k/10k/100k diagnostics capture 读取均为 0 bytes 分配（约 0.018/0.019/0.019 ms）；单 Edge 4,096 geometry 的 100,000 次读取同样为 0 bytes。Godot MCP 主场景真实提交后显示 `Node=2 / Edge=1 / Geometry=1 / Query=2 / Self-loop=0`，editor 新增 error 为 0，DAP `stderr` 与 `console` 为空。

<a id="v3-ui1.4"></a>

- [ ] **1.4 呈现并约束异步保存、加载与删除操作**
  - 完成前问题：PauseMenu 以同步 bool 结果驱动槽列表和关闭行为；重复按钮、Enter、Escape 或旧 continuation 可能产生重复请求、错误覆盖/删除提示或提前关闭菜单。V3 还必须只展示独立根中的 `CompleteV3` / `CorruptV3` 槽，不能让 V2/`Foreign` 内容进入普通操作。
  - 修改：消费 coordinator 的不可变 operation state/result，显示操作类型、目标存档名及 Save 的 Capture/Prepare/Publish、Load 的 Admission/Prepare/Preflight/Commit、Delete 的 Recover/Commit/Cleanup 阶段；busy 时禁用冲突按钮并防止重复提交。每个 continuation 同时校验 `SceneGeneration + MenuOpenGeneration + OperationToken`。手动 Load 从 admission 到 commit 始终保持菜单打开和场景暂停：Admission/Prepare/Preflight 期间 Escape 只发送一次取消请求并继续消费输入；进入短 non-yield commit 后只消费 Escape，不能关闭菜单、恢复游戏或再取消。Load 的关键 graph/tool/mesh/surface 失败必须在 Preflight，成功 commit 同时发布 matching `PresentationReady`；提交后只有普通 observer warning，可显示 `SucceededWithObserverWarnings`，不存在表现重试页。覆盖与删除二次确认必须显示精确 display name、slot ID 和 occupant 状态；`CorruptV3` 只允许确认删除，`Foreign` / `Unsafe` 不显示为可操作槽。删除越过 tombstone move 后即显示逻辑删除，cleanup pending 作为 warning，不把槽重新加入列表。autosave `SkippedBusy` 只更新诊断，不弹错误。
  - 依赖：`v3-save-system:2.2`～`2.3`、`v3-tool-input:2.4`、`v3-grid-rendering:2.2`。
  - 集成负责人：`v3-ui`；端到端完成判定由 `v3-road-graph:8.6` 负责。
  - 验证：保存/另存/覆盖/加载/删除各阶段，鼠标/键盘重复激活；Admission/Prepare/Preflight 连按 Escape、commit 按 Escape；成功、observer warning、cleanup pending、提交前失败/取消；scene/menu generation 和旧 continuation；五类 occupant 的列表/按钮策略、精确确认、默认焦点、V2/Foreign 不可见；pending autosave 合并/跳过，菜单关闭重开、场景退出重入，以及三档视口。
  - 验收：每次命令只对应一个 operation token；旧 generation/continuation 无法改变当前菜单或磁盘；冲突按钮、Enter 和 Escape 不重复发起或提前恢复游戏。Publish、Load、Delete 结果不混淆，Load 不改盘；失败/取消不关闭菜单或误切 `CurrentSlotID`，observer/cleanup warning 不误报失败；成功 Load 只在所有根和 matching presentation token 一次交换后恢复游戏，V2 槽从不出现在 V3 UI，autosave busy 不产生错误噪音。
  - 阶段进展（2026-08-14）：PauseMenu 已改用 `StartSave/StartSaveAs/StartLoad/StartDeleteSlot`，订阅不可变 state/result，并以 `OperationToken + MenuOpenGeneration + SceneGeneration` 拒绝旧 continuation。操作期间保存控件、确认入口和视图切换被禁用；Escape 在 commit 前只发送一次取消，越界后只消费输入。Load 只有 matching 成功结果才关闭菜单，失败/取消保留原菜单；Delete 继续绑定 UI generation、occupant digest 与确认 token。`AutosaveController` 独立统计 success/failure/canceled/skipped-busy。
  - 退出与焦点进展（2026-08-14）：返回主菜单先进入 exit-convergence 状态、等待当前 scene operation drain，再切换场景；窗口关闭、暂停菜单和 MainMenu 退出统一由 `SaveManager` shutdown。所有 deferred focus 通过执行时有效性门禁，旧菜单离树后不会操作失效控件。对应已验证修复记录见 `save-system:BUG-12` 与 `ui:BUG-16`。
  - 当前证据（2026-08-15）：`PauseMenuContractTests`、`AutosaveContractTests`、`SaveOperationCoordinatorTests`、renderer token/surface/preparer 契约、RoadUpgrade 聚焦组合 17/17、拆除/history 聚焦组合 51/51、类型化建造聚焦组合 60/60 与完整 833/833 自动化通过；scene-style drain 回归继续覆盖等待 gate 的请求与外部取消竞争。四类 surface 与 matching token 可由 Load worker 预建；`road_input_strategy_runtime_contract.gd` 还验证改造目标冻结、连续/矩形选择、NoChanges、旧 token、semantic boundary 合并和 active upgrade 上的 full-reset Load。Debug/`ExportRelease` build 均为 0 错误，各有 1 条既有 `NU1900`；Roslyn/GDScript diagnostics 为 0，editor 无新增错误且 DAP `stderr`/`console` 为空。测试槽、隔离目录和日志已清理，本轮未重跑 10k/100k。
  - 工具门禁进展（2026-08-20）：类型化建造现与拆除、RoadUpgrade 一样冻结完整表现 token；pending、stalled 或 superseded 时不会开始/继续/提交旧 placement，Load admission 期间也只拒绝新命令而不提前破坏当前 UI/工具状态。完整自动化 840/840、双配置 build 0 警告/0 错误，两项隔离道路运行时契约均 PASS；当前会话未暴露 Roslyn/Godot MCP 与 DAP，未刷新对应通道。
  - 历史命令门禁进展（2026-08-20）：HUD 查询的 undo/redo 可用性与实际回放入口均要求 provider desired/presented/surface token 和 graph current；普通 mutation pending/stalled 与回放后的反向 pending 窗口会保持按钮命令不可用，不清栈或修改图，matching presentation 发布后恢复。`RoadInputStrategyTests` 21/21、完整自动化 841/841、双配置 build 0 警告/0 错误，两项隔离道路运行时契约均 PASS；当前会话未暴露 Roslyn/Godot MCP 与 DAP，未刷新对应通道。
  - Deferred continuation 协作进展（2026-08-20）：当前生产输入层没有独立道路事务命令队列；renderer 的真实 `CallDeferred` 普通重建已捕获 continuation generation。pending mutation 后同调用栈 `StartLoad` 会先同步发布 current presentation，Load commit 使旧 callable 失效；额外两帧后 UI 可观察的 graph/mesh/surface/token 仍为新 lineage。聚焦组合 16/16、完整自动化 842/842、双配置 build 0 警告/0 错误，两个隔离道路运行时契约均 PASS；当前会话未暴露 Roslyn/Godot MCP 与 DAP。
  - Load warning UI 进展（2026-08-24）：`pause_menu_runtime_contract.gd` 通过 PauseMenu 的真实 Load 按钮同时注入 RoadGraph/RoadRenderer observer、graph/tool/renderer/slot-target cleanup 与 SaveManager post-commit 辅助异常；结果仍按成功路径关闭菜单并恢复游戏，状态文本严格按既定顺序包含七项 warning。随后从重开的菜单对同一槽执行无故障 Load，旧 warning 文本被清除，全部一次性探针保持各触发一次，菜单与 admission 可再次使用。为让干净 `APPDATA` 契约不依赖历史状态，Road 重绑改用未占用的 G，返回 MainMenu 后通过真实 `AutosaveController` 创建首个 autosave；对应原子提交为 `75a1f4f`、`5fad322` 与 `15210da`。Godot 4.7 `--check-only` 及隔离 APPDATA 的完整契约均以退出码 0 通过并输出 `PASS pause menu runtime contract`；完整自动化 950/950，Debug 与 `ExportRelease` build 均为 0 警告、0 错误，Roslyn compiler/analyzer 与目标 GDScript LSP 均为 0 diagnostics。Godot editor MCP 连接正确项目，冻结 `MapTest` 推进 5 帧后 editor error、DAP `stderr` 与 `console` 均为空；隔离 QA 根已送入回收站且没有残留 Godot 进程。
  - Load 关键失败 UI 进展（2026-08-24）：同一 `pause_menu_runtime_contract.gd` 通过真实 Load 确认流程注入 renderer Resource Preflight 异常，并要求 PauseMenu 显示精确“加载存档失败”诊断、退出确认页但保持菜单和场景暂停。失败前后的 `CurrentSlotID`、`ToolManager.CurrentTool` 与 renderer presentation 完全一致，目标槽仍被选中，覆盖/加载/删除按钮重新启用；一次性 failure/observation probe 各触发一次并解除。紧接的七项 warning Load 与再下一次干净 Load 均成功，证明 admission 和 UI 状态可恢复。提交 `0f04f61` 只增加该真实运行时回归，不修改生产逻辑；Godot 4.7 `--check-only` 与隔离 APPDATA 契约均以退出码 0 通过，完整自动化 950/950，双配置 build 0 警告/0 错误，Roslyn compiler/analyzer 与 GDScript LSP 为 0 diagnostics；正确项目的 Godot editor MCP、冻结 5 帧 `MapTest`、editor error 与 DAP 双通道门均通过，隔离 QA 根已送入回收站且没有残留 Godot 进程。
  - Save As 磁盘失败 UI 进展（2026-08-24）：`pause_menu_runtime_contract.gd` 在隔离 V3 根以文件占用 `.save-transactions`，通过真实 Save As 按钮触发确定性磁盘失败。PauseMenu 保持 Save Management 可见和场景暂停，显示“新建存档失败”，保留原 `CurrentSlotID` 与输入名称，不发布同名槽，并恢复名称输入和 Save As 按钮；移除 marker 后同一菜单立即成功创建存档，后续覆盖、Load、Delete 和场景重入契约继续通过。故障注入运行的 `RefreshSaveSlots` 预期记录“Publication transaction root is occupied by a file”错误，未把该通道宣称为干净；无注入的 Godot editor/DAP smoke 仍为零错误。提交 `2b42981` 只增加该运行时回归，不修改生产逻辑；Godot 4.7 `--check-only` 与隔离 APPDATA 契约退出码均为 0，完整自动化 950/950，双配置 build 0 警告/0 错误，Roslyn compiler/analyzer 与 GDScript LSP 为 0 diagnostics。两个 QA 根均已送入回收站且没有残留 Godot 进程。
  - Delete 失效授权 UI 进展（2026-08-24）：`pause_menu_runtime_contract.gd` 在真实 Delete 确认页打开后，仅向目标槽的合法 manifest 追加 JSON 空白，使 occupant 内容保持 `CompleteV3` 但 digest 与确认时授权不再一致。旧授权在 tombstone move 前以精确 `Deletion authority occupant digest is stale.` 失败；PauseMenu 退出确认页但保持 Save Management 可见和场景暂停，`CurrentSlotID` 与目标槽均不变，原目标继续选中，覆盖/加载/删除按钮恢复。随后不关闭菜单，直接再次点击 Delete；刷新后的 summary 产生新授权，确认摘要仍指向同一 display name，第二次操作成功逻辑删除目标。提交 `b2804c3` 只增加该真实运行时回归，不修改生产逻辑。Godot 4.7 `--check-only` 与隔离 APPDATA 契约均以退出码 0 通过并输出 `PASS pause menu runtime contract`；完整自动化 950/950，Debug 与 `ExportRelease` build 均为 0 警告、0 错误，Roslyn compiler/analyzer 与目标 GDScript LSP 为 0 diagnostics。正确项目的 Godot editor MCP、冻结 5 帧 `MapTest`、editor error 与 DAP 双通道门均通过；隔离 QA 根已送入回收站，测试游戏与隐藏 editor 已关闭且没有残留 Godot 进程。
  - Delete cleanup-pending UI 进展（2026-08-24）：新增 Debug-only `SaveDeleteCleanupFailureProbe`，只在 `RunDeleteAsync()` 为本次 Delete 构造 store 后替换测试实例，并在 `SavePublicationPhase.DeletionTombstoned` 精确抛出一次 `IOException`；`ExportRelease` 中该 partial 调用与探针符号均被消除，Windows Desktop QA 也显式排除探针源码/UID。真实 PauseMenu 创建并删除当前手动槽时，结果显示“已删除”及精确 cleanup warning，目标不会回到磁盘或列表，`CurrentSlotID` 切回 autosave，失败后的列表刷新清除事务恢复根，名称输入和 Save As 恢复可用；随后创建/删除损坏槽及场景重入继续成功。提交 `d2ada7c` 包含最小 Debug 注入、源码契约、导出隔离与运行时回归，不改变发布行为。Godot 4.7 `--check-only` 和两次隔离 APPDATA 契约均退出码 0 并输出 `PASS pause menu runtime contract`；完整自动化 951/951，Debug 与 `ExportRelease` build 均为 0 警告、0 错误，Roslyn compiler/analyzer 和最终 GDScript LSP 为 0 diagnostics。Debug/`ExportRelease` DLL 的探针符号命中数分别为 6/0，生成 UID 后的 QA 导出资源契约为 1/1；正确项目冻结 5 帧 `MapTest` 的 editor error 与 DAP 双通道均为空。两个隔离 QA 根已送入回收站，测试游戏与隐藏 editor 均已关闭且没有残留 Godot 进程。
  - Overwrite cleanup-pending UI 进展（2026-08-24）：新增 Debug-only `SavePublishCleanupFailureProbe`，只在 `RunAdmittedPublishAsync()` 为本次发布构造 store 后替换测试实例，并在 `SavePublicationPhase.CanonicalPublished` 精确抛出一次 `IOException`；`ExportRelease` 中该 partial 调用与探针符号均被消除，Windows Desktop QA 也显式排除探针源码/UID。真实 PauseMenu 对选中手动槽执行覆盖时，结果显示“已覆盖”及精确 cleanup warning，`CurrentSlotID` 与目标选择保持不变，操作后的列表刷新清除 `.save-transactions`；紧接的无故障覆盖成功且不保留旧 warning。提交 `4509641` 包含最小 Debug 注入、源码契约、导出隔离与运行时回归，不改变发布行为。聚焦 PauseMenu/ExportPreset 契约为 6/6，完整自动化 952/952；Godot 4.7 `--check-only` 与隔离 APPDATA 契约均退出码 0 并输出 `PASS pause menu runtime contract`，Debug 与 `ExportRelease` build 均为 0 警告、0 错误，Roslyn compiler/analyzer 与 GDScript LSP 为 0 diagnostics。Debug/`ExportRelease` DLL 的探针符号命中数分别为 6/0，生成 UID 后的 QA 导出资源契约为 1/1；正确项目冻结 5 帧 `MapTest` 的 editor error 与 DAP 双通道均为空，隔离 QA 根已送入回收站且没有残留 Godot 进程。
  - Delete Recover 取消/重复激活 UI 进展（2026-08-24）：新增 Debug-only `SaveDeleteOperationProbe`，在 `RunDeleteAsync()` 进入 Recover、构造真实 store 后且调用 `SaveSlotStore.Delete()` 前提供确定性门，并记录通过 PauseMenu 发出的取消请求。真实 Delete 确认页连续激活两次时，活动 operation token 保持一致且 Recover 门只触发一次；门内连续按两次 Escape 只调用一次 `CancelOperation()`，busy 期间确认、返回与存档操作按钮持续禁用，菜单和场景保持暂停。释放门后结果为 `Canceled`、`finalPhase=Recover`、`committed=false`，磁盘目标、`CurrentSlotID` 与目标选择均不变，状态显示“操作已取消”并恢复操作按钮；随后的 stale-authority Delete 及刷新授权后的重试链继续通过。提交 `a604f64` 包含最小 Debug 测试缝、源码契约、导出隔离与真实 UI 回归，不改变发布行为。聚焦 PauseMenu/ExportPreset 契约为 7/7，最终独占完整自动化为 953/953；Godot 4.7 `--check-only` 与隔离 APPDATA 契约均退出码 0 并输出 `PASS pause menu runtime contract`，Debug 与 `ExportRelease` build 均为 0 警告、0 错误，Roslyn compiler/analyzer 与 GDScript LSP 为 0 diagnostics。六项关键探针符号在 `ExportRelease` 全部为 0，组合命中 Debug/`ExportRelease` 为 15/0，生成 UID 后的 QA 导出资源契约为 1/1；正确项目冻结 5 帧 `MapTest` 的 editor error 与 DAP 双通道均为空，四个隔离 QA 根已送入回收站且没有残留 Godot 进程。故障注入契约仍含既有预期 Save As 文件占位错误、observer warning 与损坏槽 warning，未把该通道宣称为干净。
  - Delete Commit/Cleanup 取消拒绝 UI 进展（2026-08-24）：扩展 Debug-only `SaveDeleteOperationProbe`，只在 armed 时用 observer-backed store 锁定 `SavePublicationPhase.DeletionTombstoned`；源码顺序证明此时 `slot -> tombstone` 的 `CrossCommitBoundary()` 与 `MarkCommitted()` 均已完成。真实 PauseMenu 收到 `Commit` 与 crossed-boundary state 后，连续两次 Escape 不调用 `CancelOperation()`；对相同 operation token 直接请求取消则精确返回 `false` 并只记录一次，后续 Escape 仍不转发。门内菜单与场景保持暂停，确认、返回与所有存档操作按钮持续禁用；释放后结果为 `Succeeded`、`finalPhase=Cleanup`、`committed=true`，目标不回磁盘或列表，`CurrentSlotID` 切回 autosave，状态显示“已删除”且操作按钮恢复，随后 cleanup-pending、损坏槽删除和场景重入链继续通过。提交 `257b9bb` 只扩展测试探针、源码契约与真实 UI 回归，不修改生产逻辑。PauseMenu/ExportPreset 聚焦契约为 8/8，完整自动化 954/954；Godot 4.7 `--check-only` 与隔离 APPDATA 契约均退出码 0，后者输出 `PASS pause menu runtime contract`，Debug 与 `ExportRelease` build 均为 0 警告、0 错误，Roslyn compiler/analyzer 与目标 GDScript LSP 为 0 diagnostics。新门相关六项标识在 Debug/`ExportRelease` DLL 中命中 8/0；正确项目冻结 5 帧 `MapTest` 的 editor error、DAP `stderr` 与 `console` 均为空。隔离 QA 根已送入回收站，隐藏 editor 已关闭且没有残留 Godot 进程；故障注入契约仍含既有预期 Save As 文件占位错误、observer warning 与损坏槽 warning，未把该通道宣称为干净。
  - Save As Prepare 取消/重复激活 UI 进展（2026-08-24）：新增 Debug-only `SavePublishOperationProbe`，在 `RunAdmittedPublishAsync()` 进入 Prepare、构造真实 store 后且调用 `SaveCaptured*()` 前提供一次性门，并独立记录 PauseMenu 发出的取消请求。真实 Save As 按钮连续激活两次时，活动 operation token 保持一致且 Prepare 门只触发一次；门内连续按两次 Escape 只调用一次 `CancelOperation()`，名称输入、Save As、覆盖、加载、删除和返回入口持续禁用，菜单和场景保持暂停，玩家输入名称不变。释放门后结果为 `Canceled`、`finalPhase=Prepare`、`committed=false`，没有同名槽发布，`CurrentSlotID` 与名称保持，状态显示“操作已取消”，manager、输入与按钮恢复；后续 Save As/Overwrite/Load/Delete 链继续通过。提交 `cac8d5e` 只增加 Debug 测试缝、源码契约、导出隔离与真实 UI 回归，不改变 `ExportRelease` 行为。PauseMenu/ExportPreset 聚焦契约为 9/9，完整自动化 955/955；Godot 4.7 `--check-only` 与隔离 APPDATA 契约均退出码 0，后者输出 `PASS pause menu runtime contract`，Debug 与 `ExportRelease` build 均为 0 警告、0 错误，Roslyn compiler/analyzer 与目标 GDScript LSP 为 0 diagnostics。八项新探针标识在 Debug/`ExportRelease` DLL 中命中 8/0，生成 UID 后的 QA 导出资源契约为 1/1；正确项目冻结 5 帧 `MapTest` 的 editor error、DAP `stderr` 与 `console` 均为空。隔离 QA 根已送入回收站，隐藏 editor 已关闭且没有残留 Godot 进程；故障注入契约仍含既有预期 Save As 文件占位错误、observer warning 与损坏槽 warning，未把该通道宣称为干净。
  - Save As Staged 取消/重复激活 UI 进展（2026-08-24）：扩展 Debug-only `SavePublishOperationProbe`，在 staged gate armed 时为真实 `SaveSlotStore` 接入 observer，并只在 `SavePublicationPhase.Staged` 锁定一次性门。源码顺序固定 participant、manifest、digest 与 `publish.json` 已完整写入，随后依次执行 Staged observer、`ThrowIfCancellationRequested()` 与 `AcquireCommitLease()`；因此门内仍为 `Prepare`、`crossed-boundary=false`，但 `.save-transactions` 已存在。真实 PauseMenu 连续激活两次 Save As 保持同一个非空 token，门只进入一次；双 Escape 只转发一次同 token 取消，名称和所有存档入口保持禁用，菜单和场景持续暂停。释放门后 operation 返回 `Canceled`、`finalPhase=Prepare`、`committed=false`，目标手动槽未发布，`CurrentSlotID` 与名称保持，事务根清理，状态显示“操作已取消”且输入/按钮恢复；后续 canonical publication、Overwrite/Load/Delete 与场景重入链继续通过。提交 `0198637` 只扩展 Debug 测试探针、源码契约与真实 UI 回归，不改变生产逻辑或 `ExportRelease`。PauseMenu/ExportPreset 聚焦契约为 11/11，完整自动化 957/957；Godot 4.7 `--check-only` 与隔离 APPDATA 契约均退出码 0，后者输出 `PASS pause menu runtime contract`，Debug 串行复跑与 `ExportRelease` build 均为 0 警告、0 错误，Roslyn production/test compiler/analyzer 与目标 GDScript LSP 为 0 diagnostics。八项 staged 标识在 Debug/`ExportRelease` DLL 中命中 8/0，QA 导出资源契约继续为 1/1；正确项目冻结 5 帧 `MapTest` 后 editor error、DAP `stderr` 与 `console` 均为空，隔离 QA 根已送入回收站，隐藏 editor 已关闭且没有残留 Godot 进程。故障注入契约仍含既有预期 ConstructionDock 缺依赖 warning、Save As 文件占位错误、observer warning 与损坏槽 warning，未把该通道宣称为干净。
  - Save As Publish/Cleanup 取消拒绝 UI 进展（2026-08-24）：扩展 Debug-only `SavePublishOperationProbe`，只在 armed 时用 observer-backed store 锁定 `SavePublicationPhase.CanonicalPublished`；`SaveSlotStore.SaveCore()` 的源码顺序证明此时 canonical move 的 `CrossCommitBoundary()` 与 `MarkCommitted()` 均已完成。真实 PauseMenu 收到 `Publish` 与 crossed-boundary state 后，连续两次 Escape 不调用 `CancelOperation()`；对相同 operation token 直接请求取消则精确返回 `false` 并只记录一次，后续 Escape 仍不转发。门内名称和所有存档操作入口持续禁用，菜单和场景保持暂停；释放后结果为 `Succeeded`、`finalPhase=Cleanup`、`committed=true`，新槽保留在磁盘和列表并成为 `CurrentSlotID`，状态显示“已创建”、名称清空且输入/按钮恢复，随后重复名称、Overwrite/Load/Delete 和场景重入链继续通过。提交 `0e695bb` 只扩展 Debug 测试探针、源码契约与真实 UI 回归，不改变生产逻辑或 `ExportRelease`。PauseMenu/ExportPreset 聚焦契约为 10/10，完整自动化 956/956；Godot 4.7 `--check-only` 与隔离 APPDATA 契约均退出码 0，后者输出 `PASS pause menu runtime contract`，Debug 串行复跑与 `ExportRelease` build 均为 0 警告、0 错误，Roslyn compiler/analyzer 与目标 GDScript LSP 为 0 diagnostics。八项 post-commit 标识在 Debug/`ExportRelease` DLL 中命中 8/0，QA 导出资源契约继续为 1/1；正确项目冻结 5 帧 `MapTest` 的 editor error、DAP `stderr` 与 `console` 均为空。隔离 QA 根已送入回收站，隐藏 editor 已关闭且没有残留 Godot 进程；故障注入契约仍含既有预期 Save As 文件占位错误、observer warning 与损坏槽 warning，未把该通道宣称为干净。
  - 仍缺（保持开放）：renderer 已具备四类 surface、provider 级 stalled/retry 门禁和 deferred continuation 代际失效，placement、拆除、RoadUpgrade 与 undo/redo 也已执行完整 token/graph admission，RoadType 选择器和 RoadUpgrade 双工具入口已有共享状态 UI，PauseMenu 已覆盖 Load 七项 warning 联合显示、干净恢复、一个真实 renderer Resource Preflight 失败格、Save As 磁盘失败恢复、Prepare/Staged 取消/重复激活与 canonical publication 后取消拒绝、Overwrite cleanup-pending，以及 Delete 失效授权失败/重试、越界前取消/重复激活、越界后取消拒绝与 cleanup-pending 格；但 Save/Save As 的其余 Capture 与取得 publish lease 后、越过 boundary 前的取消/重复激活，Load 各阶段重复激活/取消、逐项 warning 和其余真实关键资源故障矩阵仍未全部验收，因此尚不能关闭最终的 graph/tool/mesh/surface 一次接管语义。

## 暂不执行

### 道路类型详情与模拟数据

- 延期原因：V3 不实现速度、容量、维护费、建筑接入和拥堵，UI 没有可信数据可展示。
- 保持现状：类型选择器只显示名称和颜色，不伪造玩法数值或优劣排序。
- 重新开启条件：相应 owning system 已实现并提供稳定只读查询契约。

## 已解决基线

- [x] **ConstructionDock 已使用资源化道路 catalog 生成工具按钮。** 分类、工具选中态、焦点和响应式布局已有 C# 与 Godot 契约。
- [x] **ToolContextPanel 已支持宽屏和 760px 以下折叠/展开。** 新控件必须复用现有布局边界并保持内容可滚动。
- [x] **GameHUD 统一路由工具、暂停和撤销重做动作。** 子控件不得绕过 ToolManager 或 RoadEditHistory 写图。
- [x] **命令中心已验证 1600x900、640x480 和 435x480。** 新 RoadType 控件和第二个道路工具必须保留这些视口门禁。
- [x] **RoadType 选择器已接入共享工具状态。** `ToolContextPanel` 只通过 `ToolManager.GetSelectedRoadType/SetSelectedRoadType` 读写 `RoadBuilder.SelectedRoadType`；Road/RoadUpgrade 共用选择，切换工具保留选择，新 `MapTest` 实例恢复 `Street`，无效样式不会提交类型。
- [x] **RoadUpgrade 已作为第二个资源化道路工具接入命令中心。** 独立图标、默认 T 动作、旧配置迁移、互斥选中态、catalog 上下文和稳定焦点链均有 C# 与 Godot 契约；UI 不直接提交道路改造。
- [x] **DebugPanel 已改用 canonical diagnostics snapshot。** 面板展示 Node、canonical Edge、原生 geometry、query fragment 和 self-loop；折叠时不读取图，展开后按 `ChangeSequence` 增量刷新，五项读取不复制节点、边或索引。

## 完成标准

1. 1.1～1.4 通过 C# 结构契约和真实 Godot 主场景交互。
2. 四类选择和两个道路工具在桌面、窄屏、鼠标与键盘焦点下均可操作且不重叠。
3. UI 只修改 tool/operation state；类型化建造、改造、磁盘事务和图提交仍由 owning system 执行。
4. 场景退出/重入、暂停和工具切换不会保留失效引用、重复信号或未提交选择。
5. Load 的成功一次交换 graph/tool/mesh/surface/token/`CurrentSlotID`，关键失败仅发生在 Preflight；Publish、Load、Delete 的授权和提交边界分别可见，V2/Foreign 内容不可操作。
